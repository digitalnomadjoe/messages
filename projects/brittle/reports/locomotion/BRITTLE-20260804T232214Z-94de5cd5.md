---
id: BRITTLE-20260804T232214Z-94de5cd5
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S14D
created_at: "2026-08-04T23:22:14.241208Z"
source_commit: b50d756
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S14D_STARTSTOP_FORENSIC_20260804T150500Z.md
local_source_sha256: cea050d68dd04172eed84d7fdf61d819eeaba54b15a317e463de057fd7e2d734
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S14D_STARTSTOP_FORENSIC_20260804T150500Z
mirror_bytes: 8247
---
# UNIFIED_LOCOMOTION_PHASE12UC11S14D_STARTSTOP_FORENSIC_20260804T150500Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S14D_STARTSTOP_FORENSIC_20260804T150500Z.md` |
| sha256 | `cea050d68dd04172eed84d7fdf61d819eeaba54b15a317e463de057fd7e2d734` |
| brittle commit | `b50d756` |
| lane | locomotion |
| unit | 12U-C11S14D |
| mirrored at | 2026-08-04T23:22:14.241208Z |

<!-- brittle:mirrored-content -->

# 12U-C11S14D — Prior Start/Stop Forensic: the Mechanism Was the F8 Stand-Prior, and It Still Works

**date:** 2026-08-04 · **unit:** 12U-C11S14D · **lane:** locomotion · read-only + bounded supervised probes; no PPO, no reward change, no adoption, no Guard mutation
**constitution:** C1, C3, C4, C7, C12 · **potential violations:** none · **amendment proposed:** no
**status:** attribution SOLVED with receipts on both eras. Phase 4's DS-stop came from **explicit fixed-point pinning — the F8 stand-prior in PPO** (`--f8-stand-prior-w 0.5`, a supervised zero-action loss on commanded-stand minibatch samples, **already in the bound trainer, set to 0.0 on our cards**) and its imitation twin, the 4D hold-anchored BC (λ=0.746). The trainer's own comment records our exact failure: *"pure PPO income could not break the trunk's walking limit cycle (f8t1s0b/c: holds still walked at 12M)."* We re-derived a solved problem. **Both prior heads still stop perfectly cross-era on today's plant+law+book** (demo head: 32/32 DS, hold action RMS 0.019 ≈ the implanted zero-action fixed point, p_cap 0, track 10.1 — the C11S14C book already PAYS the intended stop). **Decision: ONE path — reuse the prior successful transition mechanism: enable the stand-prior on the existing C11S14C card and rerun the bounded continuation.** STOPPED for Joe.
**recommend:** flip `--f8-stand-prior-w 0.5` (the demo head's own proven value) on the card; rebind (fingerprint moves — stand_prior_w is in the resolved f8 block) + 4 sentences; retry 3 × ≤8,388,608 from the s1 head under the same gates.
**decision:** Joe — adopt the returned path (sentences to follow on your word)
**next:** await ruling

---

## 1. Mechanism attribution (ticket options 1–5)

**(5) another measured mechanism — explicit supervised fixed-point pinning — with (2) as its imitation-layer twin.** The receipts:

- **PPO side (the origin of the skill):** `f8_startstop_demo_head` (2026-07-20) trained with **`stand_prior_w: 0.5`** (its resolved_config) — on commanded-stand minibatch samples (obs52=obs53=0), loss `+ w·‖actor_mean‖²` pulls toward zero action = q_des at the standing anchor. The code comment documents that reward income alone had already failed twice at 12M (f8t1s0b/c) before this was added — the July arc hit exactly our C11S14/C11S14C wall and closed it with the prior.
- **Imitation side:** Phase 4C localized the see-saw (command-diverse walking BC data monotonically erodes holds: 0.006 → 0.02–0.06 → 0.31–0.38 across three mixtures); Phase 4D closed it with the **hold-anchored BC loss** `L = L_loco + 0.746·L_hold` (λ gradient-calibrated; 21,408 certified teacher hold samples) → holds 0.001–0.004, zero chatter, 48/48 — "the conflict was in the OBJECTIVE, not the capacity."
- **(1) teacher/reference:** supplies the path (authored exit taper → DS pads; the SAME `build_f8_exit_arc_family` generator as our member) — necessary, not sufficient: our validated member carries the identical exit and the perch ignores it.
- **(3) command obs:** necessary substrate (the prior keys on cols 52/53 == 0) — not the differentiator.
- **(4) reward shaping:** **documented insufficient in BOTH eras** — July's 12M walk-through-holds, our 24M perch acquisition, our 8.4M×3 tax continuation. Three independent replications.

## 2. Matched walk→stop→hold comparison (all on TODAY'S plant v2 + law v2 + C11S14C book; prior heads are cross-era subjects, labeled)

| subject (init/training) | stop→DS | dt | hold lift | hold speed | hold action RMS | contact seq | p_cap | track | falls |
|---|---|---|---|---|---|---|---|---|---|
| **f8_startstop_demo_head** (PPO + stand-prior 0.5; era member c1p000) | **32/32** | **0.56 s** | **0.0 mm** | 0.032 mm/s | **0.019** | decel tds → both-loaded, stays planted | **0.0** | 10.1 | 0 |
| **unified_locomotion_v1** (BC/DAgger + 4D hold-anchor; era member) | **32/32** | 0.38 s | 0.0 mm | 0.113 mm/s | 0.061 | same | 0.0 | 10.7 | 0 |
| C11S14 s1 head (PPO, prior=0, our member) | 0/24 | — | 44 mm | ~27 mm/s | walking-scale | one foot never re-loads | −7.9 (C11S14C) | 9.4 | 0 |
| perch continuation ckpts (PPO + tax, prior=0) | 0/24 ×32 ckpts | — | 39–43 mm | 24–49 mm/s | — | same | −7.9 | — | 0 |
| `ccref_vx031_c0p752` reference (kinematic) | DS by construction | taper 165 fr | 0 | 0 | — | contacts→DS below A=0.45, pads DS | 0 (exempt) | 16.0 identity | — |

Command obs identical across subjects (cols 52/53 → 0 at stop; same walk54_v60 layout). The demo head's hold action RMS 0.019 **is the stand-prior's implanted fixed point, visible six weeks later on a different plant** — and the current book pays it full DS income (p_cap 0 via the C11S14C exemption; fence −0.0008).

## 3. Reuse tests (all bounded, no PPO)

1. **Teacher reference retimed to 1.34 s preserving DS — ALREADY DONE:** our member IS the same-generator exit at stride 1.34 with DS pads, validated end-to-end (member_validation.json ALL PASS; DFL preamble PASS). The reference transferred in C11S14B; it is not the gap.
2. **Transition samples vs the 54/60 contract — COMPATIBLE:** 122k hold-basin samples are (·,54)-obs/(·,12)-act with today's exact column semantics (52=vx, 53=cad/2; 52.4% commanded-stand rows with both ≡ 0; stand-action RMS 0.127). Caveats: pre-plant-v2 base-50 statistics; era cadence col 0.55 vs our 0.376 on walking rows.
3. **BC on stop windows only — NO (4C replicated on our lineage):** 300 Adam steps of stop-window-only cloning on the s1/widened heads converge stands (RMS 0.27→0.014) but **destroy walking** (drift 0.51; 16/16 anchors fall; starts 0%). A 4D-style mixed loss (hold anchor + walk self-distillation) retains stride 1.36 and starts 100% and pulls the perch foot **44→22.6 mm**, but one DAgger round leaves a half-hover + 6/16 walking falls. Single-shot BC is not the clean carrier; the full 4D recipe (certified labels, iterated DAgger, calibrated λ) would be real distillation work.
4. **Teacher-guided initialization makes DS locally reachable under the C11S14C book — YES:** the supervised gradient demonstrably moves the foot down (44→22.6 mm in 600 steps), and the book already pays the destination (demo head: p_cap 0, track 10.1, DS income ≈ the intended 15.7/step ordering). The clean, PROVEN carrier of exactly this supervision is the **stand-prior inside PPO**: it applies the anchor to the student's own visited stand states every update (a built-in DAgger loop) with the PPO locomotion gradient as the walk-retention term — the 4D mixed objective, auto-balanced, already implemented, already validated by the demo head.

## 4. Decision (exactly one path)

**Reuse/distill the prior successful transition — via its mechanism: enable the F8 stand-prior (`--f8-stand-prior-w 0.5`) on the existing C11S14C card and rerun the bounded continuation from the s1 head.** Not "adapt its reference only" (the reference already transferred and the perch ignores it); not "dense reward shaping" (three era-independent replications say income cannot break the limit cycle; the C11S14C book is correct and stays UNCHANGED — the prior is a training loss, not a reward). Fallback if the prior underdelivers: full 4D distillation with the demo head as relabeling teacher (it still executes perfect stops on today's plant).

On your word: card `--f8-stand-prior-w 0.5` → dry-run fingerprint re-pin (stand_prior_w sits in the resolved f8 block) → 4 sentences (transition/mutation[trainer? no — launcher + settings + fingerprint + construction]/transition/ppo ≤8,388,608 per seed) → probes → 3 seeds from s1 head → same gates (start ≥90%, stop→DS ≥90%, stride 1.34±0.07), first-qualifying stop, inspector open.

## 5. Artifacts

`rgl/artifacts/c11s14b_startstop_20260804T150500Z/c11s14d/` — demo-head + v1 forensic probes (per-world rows), naive + mixed BC tests (curves, gate evals, mixed weights), scripts. Sources read: `UNIFIED_LOCOMOTION_PHASE4C/4D` reports, `f8_startstop_demo_head/resolved_config.json`, `unified_locomotion_v1/{runtime_manifest,canonical_stamp}.json`, trainer `:5250` stand-prior block, hold-basin/DAgger datasets. Nothing mutated; canonical stacks untouched.
