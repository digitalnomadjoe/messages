---
id: BRITTLE-20260804T170143Z-36dc0583
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S14B1
created_at: "2026-08-04T17:01:43.887301Z"
source_commit: e6059a6
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S14B1_LAUNCH_CONSTRUCTION_FREEZE_20260804T150500Z.md
local_source_sha256: f5ec597e46b7e567c2d046dbf219612b48484517d8de27fc2fcb98007b3fd13a
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S14B1_LAUNCH_CONSTRUCTION_FREEZE_20260804T150500Z
mirror_bytes: 7716
---
# UNIFIED_LOCOMOTION_PHASE12UC11S14B1_LAUNCH_CONSTRUCTION_FREEZE_20260804T150500Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S14B1_LAUNCH_CONSTRUCTION_FREEZE_20260804T150500Z.md` |
| sha256 | `f5ec597e46b7e567c2d046dbf219612b48484517d8de27fc2fcb98007b3fd13a` |
| brittle commit | `e6059a6` |
| lane | locomotion |
| unit | 12U-C11S14B1 |
| mirrored at | 2026-08-04T17:01:43.887301Z |

<!-- brittle:mirrored-content -->

# 12U-C11S14B1 — Start/Stop Launch Construction Freeze

**date:** 2026-08-04 · **unit:** 12U-C11S14B1 · **lane:** locomotion · no PPO, no adoption, no registration, no Guard mutation
**constitution:** C1, C2, C3, C6, C12 · **potential violations:** none · **amendment proposed:** no
**status:** BOTH GAPS CLOSED + **the mandated dry-run caught three real launch defects (all repaired + re-proven)** + **the stand-income recheck found and closed a genuine 12.0/step armed-stand r_fwd leak.** Final fingerprint **`3af20a25d0004d8e`** pinned (out-dir/seed-invariant; baseline `59d49206` reproduced off-lane). One immutable payload + 4 sentences returned. **STOPPED for Joe.**
**recommend:** issue the §6 sentences; the rebind chain in the payload executes verbatim.
**decision:** Joe — §6 sentences
**next:** await sentences

---

## 1. Correction 1 — `init-widened-c11s14` is now an enforced member

`ppo_18874368_cmdphase54.zip` (`63869b6193d537fa`) is pinned **inside the card** (no longer a caller argument): the card resolves the path itself, verifies sha256_16 fail-closed before anything runs, and assertion **D1b** re-checks it in the preflight block; `plant_id.json` (81d4e6d8) staged beside it so **D2** proves plant lineage; **D3** now asserts the walk54_v60 shape (54/60). The payload registers it as construction member `init-widened-c11s14` inside `bound-construction-c11s4-walking` — D-11 re-hashes it at every guarded launch.

## 2. Correction 2 — the exact card was dry-run NOW, and it caught three real defects

Derived dry-run script = the card ± two tokens (REPO depth + `--dry-run`; diff in the unit tree). Iterating to a clean construction exposed:

1. **Missing manifold pins (launch-fatal):** the C11S14B card lacked `--vx-set 0.31` (manifold defaulted to vx 0.29 → "missing member" abort), `--vx-cmd-obs 0.31`, `--target-fwdrate 0.31`, and `--tip-weight rp_step_period=1.3298` — all fail-closed trainer requirements.
2. **Unfaithful parent derivation (book-fatal):** my C11S14B card had dropped `--obs-phase` (policy built 50/56 — the widened init could not even load) **and the entire crown `$TW` tip-weight book** — its resolved book (`9c00fef4…`) was not the registered book at all. The card is now a **faithful copy of `launch_c11s4_walking.sh`** (TW verbatim, obs-phase, `--cmd-profile`, full A/B/C/D assertion block) with six marked deltas (C14B-1…6).
3. **Fingerprint instability (pin-fatal):** the manifold lane recorded the per-run `$OUT/ref_concat.npz` path into the fingerprinted config — two dry-runs gave two fingerprints; per-seed launches could never match a single D-11 pin. Repaired: the resolved record carries the stable family path + per-member content sha256_16.

**Final dry-run: exit 0, all 15 card assertions PASS, DFL preamble PASS, manifold+f8 wired, widened init loads into the 54/60 policy, `constructed OK`.** Evidence archived in `dryrun/` (resolved_config, dry_run.json, log).

## 3. Correction 3 — final resolved fingerprint, pinned

**`3af20a25d0004d8e`** — proven **identical across two dry-runs with different out dirs AND different seeds**. Book delta vs the registered baseline is exactly: `rp_step_period 0.66→1.3298`, + the f8_sched block, manifold member pins, and `walking_r_fwd.f8_stand_scoped` (§5). Every other weight bit-identical (reward_hash line-item diff in `dryrun/`). **The plain frozen card under the repaired trainer still resolves `59d492060bdd3fe1` exactly** — both trainer edits are inert off-lane; the baseline value is recorded in the payload for any future plain-lane re-pin.

## 4. Correction 4 — one immutable payload

`registration_payload_c11s14b1.json`: all 19 member IDs + hashes (9 mutated/new + 10 unchanged with their registered values), construction hash `55cd5a42bc2910c9` (recipe stated), final fingerprint + invariance proof, **full resolved launcher argv** (TW expanded, 313 tokens), walk54_v60 contract dims/semantics (registered as a NEW settled entry — the existing `cmdphase54` registry entry is `runtime_semantics_ambiguous` from the uncommitted-code F8 run and stays untouched), init hash + provenance, and the verbatim rebind steps. Candidates to land verbatim at rebind: `canonical_v8_settings_CANDIDATE.json` (`e64ad97f6eb82ae1`, fingerprint pin) and `contract_registry_CANDIDATE.json` (`059733db6039f732`, +walk54_v60 `a65255d2…`). Canonical files untouched this unit.

## 5. Stand-income clarification — leak FOUND and CLOSED

**The reported ≈0.001 / 0.036 are transition/non-compliance income** (real decel touchdowns + windows closing while the command-blind walker is fenced −4.70/step) — **nothing persists into a true hold**: completed stands measure **exactly 0.0** on cadence / alternation / airtime / single-support / ss-hold / r_fwd, zero touchdown events (8 hold worlds × 13 s + settled stop worlds; `f8_standincome_recheck.json`).

**But the recheck's armed-latch probe exposed a real defect the C11S14B battery missed:** a world that arms the latch (two alternating steps) and then obeys a stop command collected **r_fwd = 12.0/step, unbounded, while motionless** — the f8 stand ET-refresh rewrites `_last_real_td_t` every stand step, the altwin event detector reads that as an alternating-event stream, and the stand-frozen phase prices standing at identity (v=0 == v_ref=0). Fully organic (arm → stop → camp) and it would have dominated the start/stop PPO.

**Repair (authorized by the ticket's "fix if persistent"):** altwin_ref now scopes commanded stands — no window events, level income 0, window re-anchors at restart — gated on `cmd_phase` so the plain lane is untouched (first discriminator attempt used the cadence array and leaked into the plain fingerprint; caught by the baseline reproduction check and corrected). Declared in the resolved contract (`f8_stand_scoped`). **Re-proven:** armed-hold r_fwd 31,200 → **0.0 exactly** with the latch still armed; anchors walking r_fwd 4.9356 (unchanged to 4 decimals); walking-through-stand now collects 0 on r_fwd while still paying the fence; starts/keep-alive/latch-reset regressions identical. Stale-latch bound: the cadence validity band (0.30–2.0 s) pays exactly 0 for any post-hold touchdown ≥2 s; airtime pays once for a real restart swing only.

## 6. Owner sentences (proposed verbatim — trainer added to the mutation scope)

1. `I authorize transition to 12U-C11S12 for the lifecycle advance from ACQUISITION to POST_ACQUISITION_AUDIT.`
2. `I authorize canonical mutation for 12U-C11S12 scoped to: trainer-source-c11s4, launcher-c11s14-startstop, reference-startstop-c11s14, init-widened-c11s14, contract-walk54-v60, contract-registry-c11s4, canonical-settings-c11s4, reward-fingerprint-c11s12, bound-construction-c11s4-walking.`
3. `I authorize transition to 12U-C11S14 from 12U-C11S12 for the lifecycle advance from POST_ACQUISITION_AUDIT to ACQUISITION.`
4. `I authorize PPO for 12U-C11S14 scoped to bound-construction-c11s4-walking, with a 24,117,248-step absolute ceiling per seed.`

Run plan, gates, stopping rules, and the first-qualifying-checkpoint visual stop are unchanged from C11S14B §5.

## 7. Artifacts

`rgl/artifacts/c11s14b_startstop_20260804T150500Z/` — registration_payload_c11s14b1.json · candidate_hashes.txt (final table) · both CANDIDATE files · rebuilt launcher card · dryrun/ (resolved_config `3af20a25`, dry_run.json 54/60, log) · f8_standincome_recheck.json · f8_preflight_postfix_c11s14b1.json (pre-fix §3 values preserved in the C11S14B report) · scripts/ (derived dry-run card, recheck harness, payload builder). Trainer now `683a68b40137d5fe` (rebind-pending; guarded runs correctly DENY until the sentences land).
