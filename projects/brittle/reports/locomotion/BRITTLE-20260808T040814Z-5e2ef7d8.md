---
id: BRITTLE-20260808T040814Z-5e2ef7d8
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15CY
created_at: "2026-08-08T04:08:14.874781Z"
source_commit: df6a8ff
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15CY_FINAL_FREEZE_20260808T041500Z.md
local_source_sha256: 9578c5e955766b1fe6d7181a4eea2e1d3b112abfb416cabbc0fe1599b5947e86
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15CY_FINAL_FREEZE_20260808T041500Z
mirror_bytes: 9588
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15CY_FINAL_FREEZE_20260808T041500Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15CY_FINAL_FREEZE_20260808T041500Z.md` |
| sha256 | `9578c5e955766b1fe6d7181a4eea2e1d3b112abfb416cabbc0fe1599b5947e86` |
| brittle commit | `df6a8ff` |
| lane | locomotion |
| unit | 12U-C11S15CY |
| mirrored at | 2026-08-08T04:08:14.874781Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15CY — Post-Incident Final Card Freeze: **SPEED/CADENCE FINAL CARD FROZEN**

**date:** 2026-08-08 · **unit:** 12U-C11S15CY · **lane:** locomotion · **NO PPO · NO Guard mutation · NO sed-built variants**
**constitution:** C1, C2, C3, C5, C6, C7, C9 · **potential violations:** none · **amendment proposed:** no
**return:** **`SPEED/CADENCE FINAL CARD FROZEN`**
**changelog:** row 98 · post-append csv `sha256_16` **`a0df20e1839893bf`**

## Final immutable identities

| artifact | sha256_16 |
|---|---|
| trainer `rl/mjw/train_tip_reftrack.py` | **`b3d1329765f15675`** |
| estimator `rl/bridge/path_speed_v1.py` | **`4127331df42baa8e`** |
| member manifest | **`dca42548ae633d3e`** |
| launch card `launch_acquisition.sh` | **`fbd8cd76841b208a`** |
| `Mobs_v1.npy` | **`976c327503a092c3`** |
| `Mact.npy` | **`23289b73d9b23faa`** |

No further trainer or card edits were made after the proofs below.

---

## 0. Incident quarantine — confirmed

`UNAUTHORIZED_PPO_RUN_QUARANTINE_20260808T002500Z/` is preserved intact with its incident receipt.
Its checkpoint was **not** evaluated, compared, replayed, banked, promoted, cited or reused by this
unit — the only thing done with that directory was leaving it alone. No rollback was needed; the
banked actor (`13fc030e59ddfc65`), `latest.txt`, canonical pointers and Guard state were all
already verified unchanged. **No further PPO was run.** Every proof below is non-training.

## 1. Dry verification through the real card — `NO OPTIMIZER STEP`

**No `sed` variant exists.** `TC_MODE` is a first-class parameter of the frozen card
(`run` | `dry` | `supervision-test`), so the thing verified is the thing that will run.

**Exact command:**
```
TC_MODE=dry bash rgl/artifacts/c11s15tc_contract_20260807T230000Z/launch_acquisition.sh \
     rgl/artifacts/c11s15tc_contract_20260807T230000Z/out_dry
```

**Proof printed BEFORE anything expensive** — the card greps its own pinned trainer and shows the
early return, no execution required:
```
[card] DRY-PROOF MODE ACTIVE.
[card]   --dry-proof-out will be passed; train() RETURNS immediately after the first
[card]   rollout batch, before the optimizer is stepped. Static proof from the trainer source:
[card]     5483:        if getattr(args, "dry_proof_out", None):
[card]     .../train_tip_reftrack.py:5556:            return
[card]   NO OPTIMIZER STEP will be taken in this mode.
```

**Receipt, exit 0:**
```
[card] identity checks PASS (manifest, trainer, banked ckpt, Mobs_v1, Mact)
[card] inspector preflight OK: matrices installed, viewer initialised, manifest validated
[card] ===== NO OPTIMIZER STEP =====
[card]   [reftrack] DRY PROOF written -> .../out_dry/tc_dryproof.json (no optimizer step taken)
[card]   no ppo_*.zip written; dry verification complete
```
The card **fails closed at exit 8** if the trainer's own no-optimizer-step line is absent or if any
`ppo_*.zip` appears — both asserted, not assumed.

⚠ **A defect of the same class as the incident recurred and was caught here.** The first dry run
exited 8: my card *generator* had stripped the line-continuation before `> "$OUT/train.log"`, so
the redirect detached from the trainer command and `train.log` was empty. Nothing harmful happened
— dry mode still ran and wrote no checkpoint — but in `run` mode `TRAINER_PID` would have been
wrong and the supervision loop broken. Fixed by restoring the backslash; the card now captures 126
lines of trainer output. **The same shortcut, the same failure mode, twice in one arc.**

### 100 % cyclic sampling receipt

```
CYCLIC FRACTION: 100.0000 %   (147456 / 147456 samples)   members below 100 %: NONE
anchor ccref_vx015_c1p000_kP200: obs45 [+0.3060, +0.3060]
anchor ccref_vx015_c1p000_kM200: obs45 [-0.3060, -0.3060]
post-reset err obs45 / obs52 / obs53 = 0.0 / 3.8e-09 / 1.2e-08
```
All 20 members at 100.00 %. The ±2 anchors expose the full commanded curvature where they
previously showed identically 0.0000.

## 2. Matrix tamper-DENY

Against corrupted **copies**; the banked originals are `chmod 444` and were never written.

| corrupted copy | card exit | reason |
|---|---|---|
| `Mobs_v1.npy` → `5ba3411eeca321c3` | **3** | `BLOCKER: Mobs_v1.npy sha256_16 5ba3411eeca321c3 != pinned 976c327503a092c3` |
| `Mact.npy` → `e39ed57908b03ccb` | **3** | `BLOCKER: Mact.npy sha256_16 e39ed57908b03ccb != pinned 23289b73d9b23faa` |

Both refused **before** the identity-pass line and before any trainer launch. Originals re-verified
after the test: `976c327503a092c3` / `23289b73d9b23faa`, permissions `-r--r--r--`.

⚠ **The first attempt at this proof was a no-op and I am recording it rather than deleting it:**
the copies inherited `chmod 444`, `cp` failed, the matrices stayed pristine, and the card "passed"
for the wrong reason. A passing test whose setup silently failed is worse than a failing one.
Redone with writable copies and the corruption verified by hash before each run.

## 3. Supervised spectator — positive and both negatives

**Positive**, through the real card's preflight on a **real staged checkpoint** (no empty-watch-loop
receipt was accepted anywhere):
```
[spectator] EXACT-SYMMETRY matrices Mobs_v1.npy / Mact.npy
[spectator] RELOADED_CHECKPOINT ppo_0.zip (step 0)
[spectator] MANIFEST PARITY OK  law=kp20kd2ap1/ASO1.0/ry20kd1/tau12.5/acd10  scales=[0.2, 0.28, 0.96, 0.99, 0.96, 0.28]...
[spectator] episode start  cmd=+0.00 rad/s  vx=0.15 m/s  ckpt=ppo_0.zip
[card] inspector preflight OK: matrices installed, viewer initialised, manifest validated
```

**Negatives**, exercising the card's single `_supervise` definition — the same function the run
uses:

| test | card exit | receipt |
|---|---|---|
| `TC_SUPTEST=death` (spectator killed) | **5** | `BLOCKER: inspector died during PPO — terminating the trainer` |
| `TC_SUPTEST=blocker` (`BLOCKER_` injected into its log) | **5** | `BLOCKER: inspector reported a failure during PPO — terminating the trainer` |

Exact-PID shutdown succeeded in both. ⚠ **Scoped honestly:** in the negative tests the trainer is a
sleep stub — only the *failure path* is under test; the loop code is the real one. Law parity in
`run` mode compares the manifest against the **trainer's own resolved config**, never against
itself.

## 4. Guard OLD-state negative control — **double-DENY, no record**

Executed while Guard is unchanged, against the final launcher:
```
{"cmd": "run guarded", "exit": 3, "verdict": "REFUSED", "state_commit": null, "reasons": [
  "bound-construction validation failed (D-11 gate, construction bound-construction-c11s4-walking):",
  "member trainer-source-c11s4: rl/mjw/train_tip_reftrack.py bytes b3d1329765f15675 != pin
   5c17a4338e21913f — the bound construction was altered",
  "launched script '.../launch_acquisition.sh' is not a file-bearing member of
   bound-construction-c11s4-walking — refusing to execute an unbound script (D-11)"]}
```
**Both required reasons named explicitly.** No run record opened: the live guard-state directory is
**byte-identical** before and after (`diff -r`), the guard-state working tree shows 0 changes, and
the out-dir was never created.

## 5. Reward-book parity receipt

`proofs/item5_reward_parity.json` — machine-readable, final card resolved config vs the **Q8C**
source resolved config.

| | |
|---|---|
| fields compared | 149 |
| fields equal | 139 |
| declared deltas | 10 (all covered by 6 enumerated reasons) |
| **undeclared deltas** | **0** |
| reward-book fields | 122 |
| reward-book fields differing | **2**, both declared |
| all inherited walking reward/objective fields equal | **yes** |
| plant/control fields equal (`kp_mult`, `kd_mult`, `tau_limit`, `acd_hz`, `action_scale`, `plant_sha1`) | **yes** |

The two reward deltas are forced, not chosen: `tip_weight.rp_step_period` = 1.176471 = 1/mean(cadence)
over {0.70, 0.85, 1.00} — the sparse resolver **refuses** any other value, and Q8C's 1.0 was correct
for its single-cadence family — and `target_fwdrate` = −1, the multi-vx sentinel the resolver
requires. Nothing was silently inherited or dropped.

## 6. Guard staging — prepared, **not applied**

`proofs/item6_guard_staging_request.txt` holds the three records verbatim.

| record | target | value |
|---|---|---|
| 1/3 | `trainer-source-c11s4` | re-pin → **`b3d1329765f15675`** |
| 2/3 | `launcher-c11s15tc-acquisition` | new target → **`fbd8cd76841b208a`** |
| 3/3 | `bound-construction-c11s4-walking` | + `launcher-c11s15tc-acquisition` (46 → 47 members) |

⚠ **All previous re-pin requests are VOID** — `5c17a4338e21913f` and `81966301c23929ae` must not be
authorized. ACQUISITION forbids `canonical_mutation`, so the chain is `transition` →
POST_ACQUISITION_AUDIT → the three records → `transition` back → probe / tamper-DENY / re-ALLOW.
**The owner authorization must name all three target ids.** After the rebind the positive probe must
ALLOW for exactly the two reasons the OLD-state control denied for.

## 7. Artifacts

`rgl/artifacts/c11s15tc_contract_20260807T230000Z/proofs/` — `item1_dry.log`, `item2_tamper.log`,
`item3_supervision.log`, `item4_guard_negctl.log`, `item4_norecord.log`,
`item5_reward_parity.json`, `item6_guard_staging_request.txt`.
`out_dry/` (dry verification: `tc_dryproof.json`, `resolved_config.json`, `train.log`, no checkpoint).
`UNAUTHORIZED_PPO_RUN_QUARANTINE_20260808T002500Z/` preserved untouched.

**No PPO. No Guard mutation. Nothing promoted, banked or made canonical. DFL thresholds, the banked
actor, `latest.txt` and the historical reference families are untouched.**
