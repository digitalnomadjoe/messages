---
id: BRITTLE-20260803T175340Z-3c17885d
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S4H
created_at: "2026-08-03T17:53:40.186534Z"
source_commit: ca38ba2
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S4H_IMITATION_SEMANTICS_REPAIR_20260803T174454Z.md
local_source_sha256: 21f6902700fcf5633be0cc55546f547d14d23988ec687f78cd13bb22500eb852
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S4H_IMITATION_SEMANTICS_REPAIR_20260803T174454Z
mirror_bytes: 10335
---
# UNIFIED_LOCOMOTION_PHASE12UC11S4H_IMITATION_SEMANTICS_REPAIR_20260803T174454Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S4H_IMITATION_SEMANTICS_REPAIR_20260803T174454Z.md` |
| sha256 | `21f6902700fcf5633be0cc55546f547d14d23988ec687f78cd13bb22500eb852` |
| brittle commit | `ca38ba2` |
| lane | locomotion |
| unit | 12U-C11S4H |
| mirrored at | 2026-08-03T17:53:40.186534Z |

<!-- brittle:mirrored-content -->

# 12U-C11S4H — Restore Imitation Semantics (D-27 + D-28) and Re-probe C3

**unit:** 12U-C11S4H · **timestamp:** 20260803T174454Z
**constitution:** C1, C2, C3, C6, C12 · **potential violations:** none · **amendment proposed:** no
**scope:** exactly the owner ruling of 2026-08-03: D-27 and D-28 corrected in `rl/mjw/train_tip_reftrack.py`, nothing else. No R-C, cadence, reward-weight, stance-cap, or control-law change; no PPO; no Guard mutation; no promotion/policy-card/latest.txt; no motion claim. The ruling explicitly supersedes the frozen C11S4 construction binding for these two corrections only.

**status:** Both repairs LANDED and validated through the REAL repaired code. Exact-c344 execution: foot error ≤ 4.2e-09, `track` = 16.000 of 16, phase advance exactly 1.000 fr/step with **zero** lead, truncation at 632/634 (the clip's own end — the 601-frame wall is gone). **Repaired C3 re-probe: PASS** — intended 21.85–21.98/step vs candidate repriced 18.27/step; margins +3.57/step and +354 discounted; disjoint on all four bases.
**recommend:** proceed to the PPO-formulation discussion Joe gated on this PASS (fresh-start only — no warm-start is authorized across the phase-semantics boundary; prerequisites D-11 + trainer-file pinning remain).
**decision:** D-27 CLOSED, D-28 CLOSED (this unit). The C3 FAIL recorded by 12U-C11S4G is superseded by this PASS **for the repaired book**; the defective book's FAIL stands as history.
**next:** owner review; changelog row appended; PPO discussion may open per the ruling's own gate.

---

## 1. The owner ruling, honored item by item

| ruling requirement | disposition |
|---|---|
| correct D-27 and D-28 only | ✓ one file, 50+/6− lines, five hunks (§2); `git diff --stat` = `rl/mjw/train_tip_reftrack.py` only |
| supersession scoped to the two corrections | ✓ launcher NOT touched (its Guard-registered hash `c4986396` intact; its :41-46 "kept AS-IS" comment is now historical — noted, not edited) |
| pin D-27 frame convention to the robot's pelvis yaw | ✓ rotation uses `rpy[:, 2]` from the actual pelvis quaternion (`Rz(−ψ_robot)` applied to world foot offsets) |
| quarantine notices at all dormant sibling foot-error sites | ✓ three: profile_mix feet branch (same defect class, dormant), a4 `r_ee` (same class, dormant, `pose_form='product'` on this card), anchor-frame branch (reviewed: frame-consistent by design — note added) |
| validate exact-reference execution | ✓ §3 |
| rerun every C11S4G scenario, reprice intended + candidate, all four bases | ✓ §4 |
| declare the phase-observation semantic change | ✓ in-code declaration at the ref_xnet build: obs cols 50–51 keep width, change dynamics (~1.076→1.000 fr/step for an on-reference walker); **no warm-start PPO across the boundary** |
| append LOCOMOTION_CHANGELOG.csv row | ✓ `rgl/LOCOMOTION_CHANGELOG.csv` created (file did not exist) with header + the C11S4H row, both trees |
| stop on FAIL/INDETERMINATE | not triggered — verdict PASS |

## 2. The edits (all in `rl/mjw/train_tip_reftrack.py`)

1. **`_ref_xnet_arc(xy)`** — new module-level helper: cumulative planar `hypot(dx,dy)` + the pre-existing `1e-9·arange` strictly-increasing tie-break. Single source for both build sites (C2). Holds remain monotone plateaus (hold handling preserved).
2. **Initial `ref_xnet` build** (was `maximum.accumulate(world-X)`) → `_ref_xnet_arc(tpos[:,:2])`, with the phase-semantics declaration comment.
3. **Per-window rebuild** (the effective builder) → same helper per window. The stale "left as-was — income only here" comment replaced with a closure note. Runtime `xnet` accumulation untouched (the arc-RSI seed reads `self.ref_xnet` live and is now consistent with runtime arc — a beneficial side effect, no code change there).
4. **`_pose_err` feet term (live plain branch)**: `frb/flb = Rz(−ψ_robot)·(foot_xy − pelvis_xy)` compared against `rf_rel/lf_rel` (both body-frame now). D-27 provenance comment in place.
5. **Quarantines**: profile_mix branch and a4 `r_ee` marked as unrepaired same-class siblings ("repair before any use of that mode"); anchor-frame branch marked reviewed-consistent.

## 3. Validation — real code, pre/post (harness `c11s4h_validate.py`)

The harness calls the actual `ReftrackAdapter._pose_err` and the actual `_ref_xnet_arc` (no reimplementation). **Pre-edit baseline first**: it reproduced every C11S4G ground-truth number under the defective formulas (executor advance 1.0764, truncation 577, track_live 0.1749, track_sync 2.0697; S2 closure vs the recorded `term_track` max dev 1.27e-04; S2 track 0.20218 / r_zt 1.76618 / 1400 saturated steps — byte-for-byte with C11S4F). The instrument is therefore tied to the recording before it measures the repair.

| criterion (ticket) | post-repair measurement |
|---|---|
| exact c344 execution: foot error ≈ 0 | e_feet+all-terms at sync: mean **1.17e-10**, max **4.21e-09** |
| `track` ≈ 16 | **15.999999998** (live map ≡ sync map) |
| synchronized progress through the usable clip | mean advance **1.000000** fr/step; max \|phase − wall\| = **0.0 frames** over all 632 steps |
| no premature phase exhaustion | truncation at **632/634** = the `ref_truncate` n−2 rule at the true clip end (was 577; the 58-frame unreachable tail is gone) |
| zero error at nonzero yaw incl. +90° | feet term exactly 0 at ψ ∈ {0°, 37°, 90°, −120°}; at ψ=+90° (matching the ref yaw) total e = **1.3e-30** → track = 16.0 exact; residuals at other ψ decompose exactly to the legitimate rpy orientation error ((ψ−90°)²·i_rpy to 3 decimals) |
| repair is live | the repaired formula now *diverges* from the defective-era recording (max 6.17) — expected and confirming |

## 4. Repaired-book C3 re-probe (`c11s4h_reprobe.py` — every C11S4G scenario)

Static repricing: trajectories held fixed, re-scored through the repaired path (track/r_zt via the real `_pose_err` + arc `ref_xnet`; all other terms formula-unchanged, C11S4G-validated implementations). Behavioral adaptation is out of scope (no PPO authorized). S2 anchors fitted with the OLD formula against the OLD recording (formula-matched; anchors are physical spawn offsets).

| scenario | per-step | per-sec | full-episode | discounted γ=0.99 |
|---|---:|---:|---:|---:|
| **S1 intended (repaired live construction ≡ synchronized)** | **21.848–21.978** | 1092–1099 | 13,808–13,890 (n=632) | **2,181–2,194** |
| **S2 candidate `ppo_13107200` repriced** | **18.274** | 913.7 | 9,018 (ep 493.5) | **1,827** (median; capped-660 identical) |
| S6 smooth long-stride (synthetic, held assumptions + repriced uplift) | 16.75–17.99 | 837–900 | 11,053–11,875 | 1,673–1,797 |
| S5 aggressive failure (reference attempt, fall at 4.5 s) | 24.05 (early-clip window) | — | 5,362 | 2,147 |
| S4 fall at 1 s (exploit gait) | 18.27 | — | 864 | 691 |
| S4b immediate death | 18.27 | — | 41 | 41 |
| S3 stand-still (track now 16.0 at spawn; `p_cap` still ruinous) | −39.45 (window-30: **20.0**) | — | −8,877 | −3,534 |

S2 repricing detail: track 0.20218 → **0.9714** (valid-39; all-64 **1.1674** — the excluded stiff worlds track *better* under the repaired map), r_zt 1.76618 → 1.76046, saturated steps 1400 → 802 (the sway no longer buys free phase — the 11% over-consumption channel is closed for the policy too). S1 book: r_fwd 2.283, r_zt 2.000, track 16.000, r_lift 4.587, p_cap −2.517, p_slip −0.415.

**C3 decision: PASS.** The intended gait robustly outranks the exploit on **all four bases with disjoint bounds**: per-step margin **+3.574** (lower-bound S1 21.848 vs S2 18.274), discounted **+353.8**, full-episode +4,790, per-second +178. Anchor sensitivity: under every anchor treatment (unrefined / formula-matched / even the biased repaired-formula fit) S2 total ∈ [18.02, 18.27] and the margin stays ≥ +3.57. The canonical window-local C3 chain now holds: **intended 21.9 > safe non-compliance 20.0 (first 0.6 s, before `p_cap`) > exploit 18.3**, and both ≫ aggressive/death on full-episode and discounted bases (S5's high per-step is the reference's own early-clip rate; S5 < S1 on every basis).

## 5. Caveats

- **Static repricing only.** What a policy would *learn* under the repaired book is a PPO question, explicitly out of scope. The S2 row is the recorded behavior re-scored, not a prediction; its closed-loop obs (phase) would differ under the new map.
- S6 keeps its C11S4G construction (recorded `ds_prog`, synthetic duty raster) with the repriced track/r_zt uplift applied as a held assumption — synthetic, not dynamically realizable, as before.
- S2 training-ledger anchor cannot be repriced (no recorded training states); eval basis declared (valid-39 primary, all-64 quoted; DR ensemble per D-23).
- The S4/S4b/S5 horizons remain declared assumptions carried over from C11S4G.
- The launcher's :41-46 comment describing the arc mismatch as "kept AS-IS" is now historical; editing it would change the Guard-registered hash and was not authorized. Rebind/refresh is a control-lane item for the next Guard maintenance window.
- The trainer file is still not Guard-hash-bound (handoff-audit condition): **before any PPO**, D-11 closure must extend fail-closed pins to `rl/mjw/train_tip_reftrack.py`, and this run's commit (below) is the reference point.
- Quarantined siblings (profile_mix feet, a4 `r_ee`) remain defective by design of the minimum delta — repair before enabling those modes.
- D-30 (ankle-roll kd 1.0 + `arc_rsi` on this lane) is untouched by this unit and still awaits its owner disposition before any training that spawns on reference frames.

## 6. Artifacts and landing

`rgl/artifacts/c11s4h_imitation_repair_20260803T174454Z/` (lane tree): `c11s4h_validate.py`, `validate_pre.json`, `validate_post.json`, `c11s4h_reprobe.py`, `reprobe.json`. Source change: `rl/mjw/train_tip_reftrack.py` (50+/6−, five hunks, syntax-checked, committed with this report and the changelog — commit hash in the git log; the pre-edit behavior remains reproducible from the parent commit). `rgl/LOCOMOTION_CHANGELOG.csv` created with the C11S4H row (both trees). Interpreter: the lane venv (`~/code/brittle/.venv`). No PPO, no rollout, no Guard state touched, no promotion, no motion claim, no visual acceptance recorded.
