---
id: BRITTLE-20260808T193807Z-e5e06f61
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15CY
created_at: "2026-08-08T19:38:07.084128Z"
source_commit: 266ef12
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15CY_FINAL_EXECUTION_FREEZE_20260808T220000Z.md
local_source_sha256: 1b8ca22fc7260985f9b8e42f97891755a1c12f9d93bd687ae85cefad19cd3831
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15CY_FINAL_EXECUTION_FREEZE_20260808T220000Z
mirror_bytes: 7691
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15CY_FINAL_EXECUTION_FREEZE_20260808T220000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15CY_FINAL_EXECUTION_FREEZE_20260808T220000Z.md` |
| sha256 | `1b8ca22fc7260985f9b8e42f97891755a1c12f9d93bd687ae85cefad19cd3831` |
| brittle commit | `266ef12` |
| lane | locomotion |
| unit | 12U-C11S15CY |
| mirrored at | 2026-08-08T19:38:07.084128Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15CY — Final Execution Freeze: **`CY FINAL EXECUTION PACKAGE FROZEN`**

**date:** 2026-08-08 · **unit:** 12U-C11S15CY · **lane:** locomotion (packaging + runbook only)
**constitution:** C1, C2, C6 · **potential violations:** none · **amendment proposed:** no
**prohibitions honoured:** NO PPO · NO Guard mutation · NO campaign bootstrap · NO control-plane/trainer/locomotion/reward/reference change · no `bash -c`/`--rcfile` hardening added · no decision resolved
**changelog:** row 105 · post-append csv `sha256_16` **`49bb63aadf2fcfd5`**

| artifact | sha256_16 |
|---|---|
| **owner ruling** `OWNER_RULING_C11S15CY.json` | **`9636f8b5bafb7a79`** |
| **authority package** `proofs/item9_authority_package.txt` | **`cbd2f61f7e255a10`** |
| **registration payload** `proofs/item2_targets_registration_payload.json` | **`5bef963d011a9643`** |
| **construction membership digest** (51 members) | **`da7b18b13af274a6`** |

**Counts:** **7** target records · **8** canonical-mutation subjects · **51** construction members.
Ruling lineage: `13f6ed10d95f37f4` → `d3fa61f3e7fb6b92` → `3f54936461ea251f` → **`9636f8b5bafb7a79`**.

---

## 1. Runbook repaired

**STEP 1** was not runnable as written. `campaign-bootstrap-commit` **requires `--citation`**
(`brittlectl.py:650`, *"the owner sentence, verbatim"*) and the old text omitted it; `--commit`
requires a **full 40-hex** id (`:646`) and the old text carried the placeholder
`R=<commit that contains …>`. Both fixed:

```
R=83a1c5e81028561820c3d9836f60fd7769170fda
python3 -m rl.guard.brittlectl campaign-bootstrap-commit --state-dir "$S" \
    --repo "$T" --commit "$R" --source-ref "$R" \
    --source-path rgl/artifacts/c11s15tc_contract_20260807T230000Z/OWNER_RULING_C11S15CY.json \
    --citation "I authorize transition to 12U-C11S15CY as the active acquisition unit, advancing from 12U-C11S15." \
    --unit 12U-C11S15CY
```

`R` is the telemetry commit that contains the **final** ruling `9636f8b5bafb7a79` — not HEAD at
read time. `ledger._check_source_document` reads the ruling from the **tracked blob at that
commit**, so citing an earlier commit would authenticate the *wrong sentence*.

**STEP 4** now carries **all eight** current subjects and the final wording, plus the two
failure modes an operator would otherwise hit late:

* the verdict text **must be byte-identical** to the ruling — authority is re-derived *from the
  text*, so one character of drift refuses the decision;
* **all eight** subjects are required — seven would not fail here, it would fail silently at
  **STEP 5**.

**Verified, not asserted:** all five ruling sentences appear **byte-identical** in the runbook
(checked programmatically against the ruling JSON, 5/5).

## 2. One authoritative sequence

`proofs/item1_cy_authority_sequence.txt` → `OBSOLETE_STAGING/item1_cy_authority_sequence_SUPERSEDED.txt`,
marked **VOID** with its reason. It was a second runbook for the same eight steps, predating the
51-member construction, the 7-record payload, the 8 subjects, the `--citation` requirement and the
final wording.

> Two runbooks for one procedure is the same defect class as two authorization engines: **the
> stale one is the one an operator finds under pressure.**

`cy_freeze_50.py` was renamed to `cy_freeze_final.py` for the same reason — one authoritative
freeze script.

## 3. Estimator bound — premise verified first

The ticket's premise was checked before acting on it:

```
train_tip_reftrack.py:3764   from rl.bridge import path_speed_v1      (inside cadence_integrity_gate)
train_tip_reftrack.py:3769+  raise SystemExit("BLOCKER cadence-gate ...")   ×5
```

It is load-bearing and **can terminate the acquisition**. A gate that can stop the run, whose
measuring instrument is unpinned, is not a gate.

New member `estimator-path-speed-v1-c11s15cy` → `rl/bridge/path_speed_v1.py` `4127331df42baa8e`
(re-hashed from disk, matched).

**Derived, not assumed:**

| quantity | value |
|---|---|
| construction members | 50 → **51** — zero removals, exactly four additions vs the 47 base |
| target records | 6 → **7** |
| canonical-mutation subjects | 7 → **8** (`targets.py:106` + `authority.py:294-300`) |
| membership digest | **`da7b18b13af274a6`** |

⚠ The 51-member digest is **derived**, and deliberately not "verified": **no prior value was
published for 51**, so there is nothing to check against and inventing an expectation would be
circular. What *is* verified is that the **50-member sub-digest `cc407b8d5e213640` still
reproduces** — which is what actually proves the base set is unchanged.

Ruling C `subject_refs` 7 → 8. **All five sentences byte-identical** — *"register the acquisition
targets"* already covers a larger target set, which is exactly why that wording was chosen.

## 4. Re-freeze verification

| check | result |
|---|---|
| all five owner sentences ADMISSIBLE | **5/5**, exit 0 |
| registration payload schema-valid | **VALID** |
| merged target store schema-valid | **VALID** — 52 → **57** entries |
| D-11 over all **51** members | **ALLOW** → `bound-construction-c11s4-walking` |
| **corrupted estimator COPY → D-11 DENY** | **DENY** (below) |
| script/argv production form | **ACCEPT** `{form: interpreted, index: 1}` |
| old C11S15 PPO grant under simulated CY | **REFUSED** — still GRANTS under its own unit |
| repository clean | **0 tracked-modified**, both trees |
| Guard state change | **none** — 241 decisions / 66 transitions / 52 targets, untouched |

**Estimator DENY proof:**

```
victim   : rl/bridge/path_speed_v1.py
original 4127331df42baa8e   corrupted copy ffdd9943f78e688d   CHANGED=True
REAL file on disk unchanged: True
▸ member estimator-path-speed-v1-c11s15cy: rl/bridge/path_speed_v1.py
  bytes ffdd9943f78e688d != pin 4127331df42baa8e — the bound construction was altered
```

Only that member is flagged. The corruption is applied to a **copy** inside a symlink shadow repo,
and the test asserts both that the copy's hash moved **and** that the real file did not.

⚠ Fixed in passing: the negative test's own header still read *"corrupt ONE battery runtime COPY"*
while the victim was the estimator. **A receipt must not mislabel its own victim.**

## 5. The authoritative eight-step owner sequence

Full commands in `proofs/item9_authority_package.txt` (`cbd2f61f7e255a10`) — the **only**
authoritative sequence. **Nothing below has been run.**

1. **`campaign-bootstrap-commit`** with `--citation` (ruling A verbatim) and
   `R=83a1c5e81028561820c3d9836f60fd7769170fda` — ⚠ itself a Guard mutation that **moves the
   trust root** every existing decision is verified against.
2. **A** — `transition` grant, then record `12U-C11S15` → `12U-C11S15CY`.
3. **B** — `transition` CY `ACQUISITION` → `POST_ACQUISITION_AUDIT`.
4. **C** — `canonical_mutation` over the **eight** subjects, verdict text byte-identical to the ruling.
5. **REGISTER** — `target register --unit 12U-C11S15CY --targets-file <7-record payload
   5bef963d011a9643>`; live store 52 → 57 entries.
6. **D** — `transition` CY back to `ACQUISITION`.
7. **E** — `ppo` naming `launcher-c11s15tc-acquisition`. **Do not reuse `D-ed52647cd278`.**
8. **One `run guarded … -- bash "$LAUNCHER" "$OUT"`**, with `TC_MODE` / `TC_RUN_ACK` in the
   environment of the `brittlectl` call.

Unchanged and re-measured: launcher `4f354fc205ca93aa`, trainer `b3d1329765f15675`, control plane
`runs.py f1c09c7f86d60d4c` / `wrapper.py cbdefe90990a70f5`, `Mobs_v1 976c327503a092c3`,
`Mact 23289b73d9b23faa`, manifest `dca42548ae633d3e`.

**No PPO. No Guard mutation. No campaign bootstrap. No decision requested or resolved.**
