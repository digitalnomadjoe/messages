---
id: BRITTLE-20260804T001742Z-15d3dcdc
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S6
created_at: "2026-08-04T00:17:42.649935Z"
source_commit: ca38ba2
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S6_WALKING_ACCOUNTING_QUALITY_20260804T001601Z.md
local_source_sha256: bea56e8134ebab0e5913bb5c9da68a7b64552c3b4e0bdc0606a79df550df81a6
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S6_WALKING_ACCOUNTING_QUALITY_20260804T001601Z
mirror_bytes: 10966
---
# UNIFIED_LOCOMOTION_PHASE12UC11S6_WALKING_ACCOUNTING_QUALITY_20260804T001601Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S6_WALKING_ACCOUNTING_QUALITY_20260804T001601Z.md` |
| sha256 | `bea56e8134ebab0e5913bb5c9da68a7b64552c3b4e0bdc0606a79df550df81a6` |
| brittle commit | `ca38ba2` |
| lane | locomotion |
| unit | 12U-C11S6 |
| mirrored at | 2026-08-04T00:17:42.649935Z |

<!-- brittle:mirrored-content -->

# 12U-C11S6 — Correct Walking Accounting and Select Quality Shaping

**date:** 2026-08-04 · **unit:** 12U-C11S6 · **lane:** locomotion (brittle-telemetry)
**constitution:** C1, C2, C3, C6, C7, C12 · **potential violations:** none · **amendment proposed:** no
**status:** Phase A LANDED + regression-proven (stance gate 0.30, truthful c344 manifests, force-channel declaration). Phase B sweep COMPLETE — **NO unique minimum quality configuration passes the selection rule → STOPPED for Joe** (per the ticket's stop clause). Nothing beyond Phase A was landed. No PPO.
**recommend:** Joe rules on the two measured blockers: (1) the cadence channel needs `sp_w` wiring AND a weight decision (at w=1.5 it is ≤0.2% of the book — widths alone cannot create preference); (2) every lateral-velocity penalty family measured (raw ceiling AND EMA-residual) prefers C11S5 over the reference — only a reference-phase-locked/imitation form can discriminate.
**decision:** Joe — quality-formulation redesign parameters (below)
**next:** owner ruling; inspector CLOSED after this report (done)

---

## 1. Phase A — factual corrections (landed, commit `4fe4a5b5dc8ef163`)

| item | landing | proof |
|---|---|---|
| `rp_stance_min_t` 0.35 → **0.30** | trainer `train_tip_reftrack.py`, appended after the launch card's book flags (last-wins merge; the C11S4J unconditional-in-trainer pattern) — frozen launcher `c4986396` and the crown book artifact untouched by construction | full-chain dry-run through the REAL launcher argv: resolved_config diff vs s2 production = **exactly** {`rp_stance_min_t` 0.35→0.30, `reward_hash`, `fingerprint`}; kd/kp/tau/action-scale/acd **SAME** |
| force-channel declaration | comment block at the kernel `step_ok` gate (`kernels_tip.py`), stating ft_air/ft_last_stance_dur/ft_air_maxclr are FORCE-CONTACT windows, 20–40 ms shorter than z-height at the lightly-loaded edges, and that these thresholds are force-calibrated | behavior-neutral (comments); C11S5A measurement cited in-code |
| D-31: truthful `profile_ref` | `write_runtime_manifest.py` derives `profile_ref` from the resolved `ref_npz` (repo-relative; `--profile-ref` override; legacy constant only for preset callers, with a warning) | s0/s1/s2 manifests regenerated → all three now declare `carriage_sweep_20260716T211854Z/curved_walk_c344.npz` |

**Regression battery (all through the real code):**

- **C11S5 walking qualifies:** s2-final GPU cohort under the corrected resolution — **0/64 stalls over the full 24 s horizon** (was 64/64 at med 13.7 s); real-step clock resets 72/world at **3.00/s = the visual cadence**; 0 falls.
- **Standing still rejected:** zero-action cohort — 0 clock resets, stall fires at exactly step 225 in 64/64 worlds (the ET still terminates non-steppers).
- **Slide / no-lift chatter still rejected:** 0 qualified steps at 0.30 (C11S5A synthetic battery: slide 0, standing 0, 60 ms chatter 0; clearance 21 mm / swing 0.15 s / travel gates unchanged).
- **Inspector loads the actual c344 profile:** spectator restarted against the regenerated s2 manifest — MANIFEST PARITY OK, clean episode starts; the spectator adopts the manifest's `profile_ref` (its mismatch blocker only fires on an explicit conflicting flag).
- **No law/plant/reference/obs/action/spawn change:** law id effective `7c05558aacc0f0c8` (verified live); plant v2/v3 asserts pass; scales/dims/spawn identical in the resolved diff.
- **Provenance notes:** trainer sha256_16 now `4fe4a5b5dc8ef163`, superseding the bound pin `dd73337a63aae6fa` — the armed construction correctly DENIES guarded runs until an owner re-registration (no PPO authorized this unit, so nothing is blocked today). Proposed **D-35**: the trainer prints `walking_control_law_id` *before* `set_kp_mult`/`set_tau_limit_vec` run, so the printed id (`768577d8…`) misstates the effective law (`7c05558a…`) — print-ordering display defect only; production C11S5 logs carry the same misleading line.

## 2. Phase B — measurements first

**Cadence channel was DEAD all C11S5 (measured, not inferred):** kernel comp `r_cadence` = **+0.000000/step in every recorded subject** (64-world cohorts, corrected book). Mechanism: with `sp_w = 0` (never wired in the plain c344 path — only the Stage-B1 manifold branch wires it) the cadence gate falls back to `dir_factor`, which is exactly 0 at zero yaw command ([[dir-factor zero gate]], fourth instance). Any cadence economics therefore *presupposes* wiring `sp_w = 1/cadence_cmd` in the plain path (Stage-B1 mirror, `check_per_world_tempo`-guarded).

**Exact-c344 reference pelvis lateral velocity (mandated first measurement):**
RMS **152.8 mm/s**, med 92.2, P95 285.3, peak 303.8 mm/s; lateral position p2p **94.3 mm** — the reference's LIPM weight shift at 1.34 s stride is large and slow.

**Subjects** (64-world GPU cohorts under the corrected book; crown = CPU previous-plant diagnostic; c344 = reference kinematics):

| subject | same-foot period med | qualified events | pelvis \|vy\| RMS | EMA-residual RMS (τ=0.4 s) |
|---|---|---|---|---|
| exact c344 | **1.34 s** | 13 | **152.8 mm/s** | 131.4 mm/s |
| s2-final | 0.64 s | 4482 | 96.3 | 91.0 |
| s0 @39.3M | 0.60 s | 4690 | 103.1 | 97.5 |
| s1 @22.0M | 0.58 s | 4116 | 139.5 | 131.4 |
| s2 @18.4M | 0.58 s | 3427 | 161.5 | 147.3 |
| crown (diag) | 0.78 s | 18 | 60.0 | 54.4 |
| standing (zero-action) | — | 0 | 2.1 | 1.9 |

Every C11S5 checkpoint recorded **walks** (thousands of qualified events under the corrected gate); training had been slowing cadence (0.58→0.64 s) and quieting lateral motion (161→96 mm/s) on its own.

## 3. Cadence-width sweep (`rp_step_period_sig`, target = same-foot 1.3298 s, w = 1.5)

Per-SECOND channel income at the ticket periods (alternating → same-foot ×2):

| σ | ref 0.66 s alt | crown-like 0.40 s | C11S5 0.32 s | ref/C11S5 ratio |
|---|---|---|---|---|
| 0.12 | 1.490/s | 0.000 | 0.000 | ~1.5e12× |
| 0.15 | 1.494 | 0.000 | 0.000 | ~1.5e9× |
| 0.18 | 1.496 | 0.000 | 0.000 | ~2.4e6× |
| 0.22 | 1.497 | 0.005 | 0.000 | 18 559× |
| 0.28 | 1.498 | 0.042 | 0.003 | 432× |
| 0.35 | 1.499 | 0.152 | 0.031 | 49× |

Measured per-subject income (ledger units/step): exact-c344 earns **0.020–0.021/step at every σ**; s2-final ≤ 0.038/step even at σ 0.35.

**The decisive fact: at w = 1.5 the whole channel is ≤ 0.2% of an ~18–22/step book.** The σ-ratios are enormous, but the *absolute* margin the channel can move (≤ 0.04/step) is noise against the 3.574/step S1–S2 base margin and against the base-book gradients that actually set the double-time attractor (r_fwd ≈ 9.6/step). **No σ can "robustly prefer" the reference cadence economically; and σ ≤ 0.22 additionally zeroes income at the C11S5 gait (violating "positive, learnable income at the C11S5 gait")**. The authorized lever (width) cannot bind without a weight decision (~50–100× or a restructure) — which is outside this ticket's authorization.

## 4. Lateral-velocity penalty — every measured family fails criterion 4

- **Raw ceiling (quiet-pelvis style): INADMISSIBLE.** Reference RMS 152.8 > s2-final 96.3 > crown 60.0 mm/s. Any w > 0 prefers C11S5's lateral motion over exact c344 — the *opposite* of "prefers exact c344 pelvis behavior over C11S5 lateral motion".
- **EMA-residual (precedented emaroll/emapitch form, τ = 0.40 s, bounded cap): ALSO INVERTED.** The reference's 0.375 Hz weight-shift passes through the EMA imperfectly: residual RMS ref 131.4 vs s2-final 91.0 mm/s → at every weight the penalty charges the reference ~1.8× more than C11S5 (e.g. w=1: Δref −0.0148 vs Δs2 −0.0083/step).
- What *does* separate them is **phase-locked deviation from the reference's own lateral trajectory** — i.e. an imitation-channel form (the existing `track` term's jurisdiction), not a magnitude/band penalty. Designing that is a formulation decision reserved to Joe (C7).

## 5. C3 ordering status

- **Factual corrections (Phase A): PASS-preserved.** `rp_stance_min_t` changes no income formula; it extends *walking* horizons (stall 225 → truncation) while standing still stalls at 225 and the constructed falls are unchanged (S5-aggressive executes reference stepping and ends by fall at 225 *by construction* — verified no unblocking). Full-episode and discounted bases therefore move **in walking's favor**; every H-reprobe ordering (S1 21.85–21.98/step > S2 18.274; margins +3.574/step, +353.8 disc) is preserved.
- **Swept quality configs: orderings unchanged by ALL of them** — max |Δ| per step (cadence ≤ +0.038, lat-vel ≤ −0.059) is bounded far below every base margin; S1 and S5 shift together (same reference events). Pre-existing razor-thin S1-vs-S5 discounted margin (+34) noted again as a book condition, unaffected.
- **Selection rule: NO unique minimum configuration passes** — cadence criterion unreachable at the authorized lever, lat-vel criterion unreachable in every measured family. **STOP FOR JOE** (ticket clause honored; nothing landed beyond Phase A).
- Full probe rerun: not triggered (conditioned on landing a configuration).

## 6. Warm-start suitability of s2-final (ticket output)

Mechanically **suitable**: walk52_v58 (52/58), current plant v2, law v2, walks with clean hard gates under the corrected clock (τ P99 8.2, cap-occ ≤6e-4, clip 0, falls 0/64, unbroken alternation). Behaviorally **conditional**: its gait sits at the 0.64 s same-foot attractor (2.1× too fast, 172 mm steps); C11S5's own training trend was moving the right way (cadence slowing, lateral quieting), but no live economy currently prices cadence at all (§2). Warm-starting before a binding cadence economy exists risks re-converging to the same attractor; a from-scratch acquisition under a corrected economy is the conservative alternative. **Joe's call, tied to the weight decision above.**

## 7. What Joe needs to rule on (exactly)

1. **Wire `sp_w` in the plain path** (Stage-B1 mirror; prerequisite for any cadence economics) — yes/no.
2. **Cadence weight**: keep 1.5 (channel stays advisory/telemetry) or raise to a binding level (I can propose a measured value once authorized to sweep *weights*).
3. **Lateral-motion quality term**: drop (rely on track) / authorize a reference-phase-locked design pass.
4. **Warm-start vs from-scratch** for the next acquisition (with 1–2 settled).
5. Re-registration of the bound construction over trainer `4fe4a5b5dc8ef163` when the next PPO unit is authorized.

## 8. Artifacts

`rgl/artifacts/c11s6_quality_formulation_20260804T001601Z/` — recorder/sweep scripts, launcher argv,
dry-run resolved config (the Phase-A resolution proof), five 64-world recordings with full income
decomposition (37 kernel comps + adapter terms), `sweep_raw.json` (all tables). Phase-A code:
commit `4fe4a5b5dc8ef163…` (trainer, kernels comment, manifest writer). Inspector closed after this
report per ticket. Changelog row appended (both trees).
