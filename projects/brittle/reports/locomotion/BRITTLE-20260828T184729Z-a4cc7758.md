---
id: BRITTLE-20260828T184729Z-a4cc7758
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: WALK-DR06H3D-RC1
created_at: "2026-08-28T18:47:29.584519Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06H3D_RC1_ISOLATED_DIAGNOSTIC_20260828T190000Z.md
local_source_sha256: 73e5903bf079f27413bd949cca94175dc7ab1ad1387b3e7f74d78df68b72e5ab
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: GREEN_OPUS5__WALK_DR06H3D_RC1_ISOLATED_DIAGNOSTIC_20260828T190000Z
mirror_bytes: 11545
---
# GREEN_OPUS5__WALK_DR06H3D_RC1_ISOLATED_DIAGNOSTIC_20260828T190000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06H3D_RC1_ISOLATED_DIAGNOSTIC_20260828T190000Z.md` |
| sha256 | `73e5903bf079f27413bd949cca94175dc7ab1ad1387b3e7f74d78df68b72e5ab` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | WALK-DR06H3D-RC1 |
| mirrored at | 2026-08-28T18:47:29.584519Z |

<!-- brittle:mirrored-content -->

# TEAM: GREEN — Opus 5 · WALK-DR06H3D-RC1 — **CONFOUND REMOVED, RE-PROVEN, STOPPED FOR FIVE SENTENCES.** The training distribution is now diagnostic-only: **2304 of 2304 worlds are the v = 0.24 / 1.000 Hz cell**, one member, one command stratum `acq_kap0_c1.00`, phase rate **1.0000**, `timing_coherent True`. The trainer's own log states it without inference — `MANIFOLD: 1 members concatenated (ntot=648); combos=[(0.24, 1.0)]` and `COMMAND STRATA (world occupancy): acq_kap0_c1.00=2304`. The 22 internally inconsistent members are gone from the batch entirely, so a negative H3D result can now only mean what you require it to mean. **One change was compelled and I want it visible rather than buried:** the trainer *blocks* unless `rp_step_period == 1/mean(cadence)` (`train_tip_reftrack.py:4529`), so restricting the manifold to a single c = 1.0 member forces `1.224095 → 1.0000`. That is a consequence of the authorized sampling restriction, not an independent choice — but it is a real change to the `sym_dwell` normalizer and it is disclosed in §5. Everything else is untouched: same reference (`54072cc4e04f55de`), same cadence law, same interface, same plant, same ACD, same PPO hyperparameters, same regularization, same termination, same reward weights, same single approved `--cadence-income-ramp` delta, `stride_track_w = 0`, `stride_corridor_w = 0`. Card **`20306ce6c8bbded0`**, dry-proven, **zero optimizer steps**, inspector wired and preflighted. §2's feasibility receipt applies unchanged — RC1 altered neither the reference nor the law. **STOP for Joe.**

**unit:** WALK-DR06H3D-RC1 · **ts:** 20260828T190000Z · **team:** GREEN (ticket header: Opus 5; executing session: Claude Fable 5)
**constitution:** C1, C2, C3, C4, C6, C7 · **potential violations:** none · **amendment proposed:** no
ZERO PPO (zero optimizer steps). NO Guard mutation. No change to the SPEED+CURVATURE interface, cadence law, reference, plant, ACD/control law, PPO implementation or hyperparameters, regularization, termination, feasibility gates, or any reward term other than the already-approved cadence-income-ramp.

---

## 1 · The correction

A **training-sampling restriction only**, implemented as a single-member manifold derived from the H3A manifest by keeping one entry verbatim:

```
rgl/artifacts/walkdr06h3d_rc1_isolate_20260828T190000Z/member_manifest_h3d_diagnostic.json
sha256  385a99632749010e8950d3455461399140e3466bf454dc0b0099ced7cb730424

members: [ { "file": "ccref_vx024_c1p000.npz", "vx": 0.24, "c": 1.0, "kappa": 0.0,
             "role": "acq", "stride_cmd": 0.24, "sha16": "54072cc4e04f55de" } ]
```

The member entry is **copied byte-for-byte** from the accepted manifest — no re-derivation of vx, c, kappa or stride. `heldout` and `extrapolation_probes` were emptied because they named members no longer in the distribution; leaving them would have been dangling references, not evidence.

## 2 · Exact command sampling distribution — from the run's own log

```
[reftrack] MANIFOLD: 1 members concatenated (ntot=648); combos=[(0.24, 1.0)]
[reftrack] SPARSE MANIFOLD: 1 declared members consumed EXACTLY (no Cartesian expansion;
           1 speeds x 1 cadences would have been 1 cells), 1 command strata: acq_kap0_c1.00(1)
[reftrack] COMMAND STRATA (world occupancy): acq_kap0_c1.00=2304
[reftrack] MANIFOLD wired: 1 members x 2304 worlds (round-robin); cadences [1.0] vx [0.24]
[reftrack] PER-WORLD TEMPO targets: c=1.00->1.000s
[WALK-DR06H3A] sp_w RE-PUSHED from the active schedule: 2304 worlds, distinct periods [1.0] s
```

| requirement | measured | |
|---|---|---|
| v_cmd fixed at 0.24 | `vx [0.24]`, 1 speed | ✓ |
| cadence_cmd = 1.0000 Hz | `cad_cmd 1.0000` | ✓ |
| c_member = 1.0000 Hz | `c_mem 1.0000` | ✓ |
| phase_rate = 1.0000 | `rate 1.0000`, `timing_coherent True` | ✓ |
| straight command only | one stratum, `acq_kap0` (kappa 0) | ✓ |
| **every PPO rollout is the diagnostic cell** | **`acq_kap0_c1.00 = 2304 / 2304 worlds`** | ✓ |
| same v=0.24 reference | `54072cc4e04f55de` | ✓ |
| same cadence-income-ramp delta | `cadence_income_ramp: True` | ✓ |
| same reward weights otherwise | `cadence_track_w 19.5` | ✓ |
| stride_track_w = 0 | 0 (and refused at exit 3) | ✓ |
| stride_corridor_w = 0 | 0 (and refused at exit 3) | ✓ |

There is no residual population: 1 member, 1 stratum, 1 speed, 1 cadence, 2304/2304 worlds. `sp_w` is a single distinct period of 1.0 s across every world.

## 3 · Dry proof

Card **`20306ce6c8bbded0`** — `TC_MODE=dry`, **exit 0**, two `NO OPTIMIZER STEP` markers, no `ppo_*.zip` written.

```
cadence_income_ramp      True          cadence_income_peaked  False (refused at exit 3)
cadence_track_w          19.5          stride_track_w 0 / stride_corridor_w 0
cadence schedule         [2.1475, 0.4846, 0.7, 1.0]   branch None
sched_tempo (RC2)        True
member                   ccref_vx024_c1p000.npz  vx 0.2400  c_mem 1.0000
                         cad_cmd 1.0000  rate 1.0000  sp_w 1.0 s  timing_coherent True
fingerprint              723f5e92b45dc856
```

**Independent Isaac inspector still wired**: `inspector preflight OK: matrices installed, viewer initialised, checkpoint loaded, manifest validated`, `live inspector pid …`, with `spectator.log` / `spectator.pid` in the run dir. It will run throughout training and be closed after final evaluation.

## 4 · §2 feasibility receipt — still applicable, unchanged

| | |
|---|---|
| gated reference | `…/walkdr06g_ratchet_…/refs/ccref_vx024_c1p000.npz` |
| hash at gating / now | `54072cc4e04f55de` / `54072cc4e04f55de` — **SAME** |
| DFL (trainer's own preamble, `allow_skip=False`) | SS_P10 −15.5 mm · med-integ 1.51 · DS_P10 35.8 · τ_P99 3.89 Nm — **PASS** |
| action feasibility incl. PD velocity lead | 0.8749, 0.000 % outside box — **PASS** |
| deterministic foot-foot collision | **0** (36 qualified-arm rollouts); reference min L-R sep 56.07 mm | **PASS** |

**Binding argument for re-use rather than re-run:** the DFL verdict is a property of the *reference*; action feasibility is a property of the *reference plus the control law*. RC1 changed neither — it changed which members are *sampled*. `rp_step_period` is a `tw` scalar consumed by the S6 sentinel fallback and the `sym_dwell` normalizer; it is not an input to the DFL gate or to the action-space computation. The receipt therefore carries over. I re-verified the reference hash rather than assuming it.

## 5 · The one compelled change — disclosed, not buried

`--tip-weight rp_step_period` moves **1.224095 → 1.0**. This is **forced**, not chosen:

```python
# train_tip_reftrack.py:4528-4531
_cmid = float(np.mean(cset))
if _tw is None or abs(_tw - 1.0 / _cmid) > 1e-3:
    raise SystemExit(f"BLOCKER sparse: rp_step_period must equal 1/mean(cadence) = ...")
```

With 23 members the cadence mean was 0.8170 → 1.224095. With one c = 1.0 member the mean is 1.0 → the gate demands exactly 1.0. **The sampling restriction cannot be implemented without it.**

What it actually affects: `TW_RP_STEP_PERIOD` is read at exactly two places — the S6 sentinel fallback (`kernels_tip.py:921`, guarded by `sp_w > 0` and therefore **never taken**, since `sp_w = 1.0` for all worlds) and the `sym_dwell` normalizer (`:961`). So the live effect is confined to `sym_dwell` scaling, which now normalizes against the diagnostic cell's own 1.0 s period instead of a 23-member average that no longer exists. **I judge this correct rather than merely tolerable** — normalizing to a mean over members that are not in the distribution would have been the anomaly — but it is a change beyond the approved ramp delta, so it is stated here rather than left in a diff.

Card delta vs the accepted H3D card is exactly six lines: launcher id, out dir, tag, manifest path, manifest sha256 pin, `rp_step_period`; plus removal of the four `--member-share` lines, which named members no longer present. Nothing else.

## 6 · Guard authorization required — minimum text

Five sentences, all **pre-validated** against `permits.authorization_content_reasons(...)` with zero refusal reasons, each filed under the unit active at that step. The certification open/close pair is unavoidable: `canonical_mutation` is barred in ACQUISITION, and `ppo` is permitted *only* in ACQUISITION.

```
I authorize transition to WALK-DR06H3D-RC1 in the acquisition lifecycle, from WALK-DR06H3A-RC3.

I authorize transition to WALK-DR06H3D-RC1 in the pretraining certification lifecycle, from WALK-DR06H3D-RC1.

I authorize canonical mutation for WALK-DR06H3D-RC1 naming launcher-walkdr06h3d-rc1 at 20306ce6c8bbded0.

I authorize transition to WALK-DR06H3D-RC1 in the acquisition lifecycle, from WALK-DR06H3D-RC1.

I authorize ppo for WALK-DR06H3D-RC1 on launcher-walkdr06h3d-rc1, 1 seed of 884736 steps with a 884736 step ceiling.
```

Only the launcher needs registering — the trainer is unchanged at `90534b22334d427b`, already Guard-registered.

On receipt: register the construction, launch bounded with the inspector attached, mine every checkpoint by world displacement (never `lo_v`), report the **cadence / stride / speed trajectory across all six checkpoints side by side**, re-run DFL and the collision check, and return A, B or C.

## 7 · What a negative result will now mean

With 2304/2304 worlds on the timing-consistent cell, decision **B** would read: *the current architecture and reward cannot acquire commanded 1 Hz cadence even when trained exclusively on a timing-consistent 1 Hz gait.* It can no longer be dismissed as batch contamination from inconsistent members. That was the point of this correction, and it is now structurally true rather than argued.

The baseline the run must move: **0.8180 Hz realized against 1.0000 commanded, with stride 21 % long** — identical to four decimals across 6 rollouts and 3 checkpoints.

## 8 · Disclosures

(1) **No PPO, no Guard mutation** — nothing was registered or launched. (2) **`rp_step_period` changed 1.224095 → 1.0**, compelled by the trainer's blocking gate (§5); it is the only change beyond the approved ramp delta and it does affect `sym_dwell` normalization. (3) `heldout` and `extrapolation_probes` were emptied in the derived manifest because they referenced members outside the distribution; no member data was altered. (4) The §2 receipt is **re-used, not re-run**, with the binding argument and a re-verified reference hash in §4 — if you would rather see it re-executed against the RC1 card, say so and I will. (5) The single-member manifold is a *narrower* training distribution than any run in this arc; loss of the curvature and low-speed members is intentional for the diagnostic but means the resulting checkpoint is a **diagnostic artifact, not a deployable policy**, and I will not propose it for promotion whatever the outcome. (6) My first manifest-hash pin used the 16-char prefix while the card compares the full sha256 — caught by the card's own check at exit 3 and fixed; the card hash moved `ed4e8aa2c67e3869 → 20306ce6c8bbded0` as a result.

Artifacts under `rgl/artifacts/walkdr06h3d_rc1_isolate_20260828T190000Z/`: `member_manifest_h3d_diagnostic.json`, `launch_walkdr06h3d_rc1.sh` (`20306ce6c8bbded0`), `rc1_card_diff.txt`, `rc1_dry.log`. Dry run at `rl/runs/walking/h3d_rc1_dry/`. §2 receipt at `rgl/artifacts/walkdr06h3d_cadence_20260828T170000Z/h3d_feasibility.json`.

STOP for Joe — the confound is removed and proven at 2304/2304 worlds; the run needs your five sentences.
