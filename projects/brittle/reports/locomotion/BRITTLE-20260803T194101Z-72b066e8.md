---
id: BRITTLE-20260803T194101Z-72b066e8
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S4J
created_at: "2026-08-03T19:41:01.794637Z"
source_commit: ca38ba2
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S4J_WALKING_LAW_ANKLE_ROLL_KD_20260803T193003Z.md
local_source_sha256: a658e3bb5b614abf02e5a1dcc0bc024e9d3beed8f322e9dd429532840da33cc9
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S4J_WALKING_LAW_ANKLE_ROLL_KD_20260803T193003Z
mirror_bytes: 9336
---
# UNIFIED_LOCOMOTION_PHASE12UC11S4J_WALKING_LAW_ANKLE_ROLL_KD_20260803T193003Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S4J_WALKING_LAW_ANKLE_ROLL_KD_20260803T193003Z.md` |
| sha256 | `a658e3bb5b614abf02e5a1dcc0bc024e9d3beed8f322e9dd429532840da33cc9` |
| brittle commit | `ca38ba2` |
| lane | locomotion |
| unit | 12U-C11S4J |
| mirrored at | 2026-08-03T19:41:01.794637Z |

<!-- brittle:mirrored-content -->

# 12U-C11S4J — Walking-Lane Ankle-Roll Kd Repair (Control-Law v2) and Qualification

**unit:** 12U-C11S4J · **timestamp:** 20260803T193003Z
**constitution:** C1, C2, C6, C7, C10, C12 · **potential violations:** none · **amendment proposed:** no
**scope:** exactly the owner ruling of 2026-08-03: ankle-roll Kd 1.0 → 0.2 on both ankle-roll joints in the active walking lane, as a NEW walking-lane control-law version, qualified under the repaired C11S4H trainer. No PPO, no reward edit, no spawn removal, no plant/reference/contract change, no promotion, no policy card, no `latest.txt`, no motion claim, no Guard mutation. The scout lane's accumulator and all other control-law elements were NOT imported.

**status:** **LANDED and QUALIFIED — all acceptance criteria PASS.** Walking-lane control-law **v2** minted from live effective values: **`7c05558aacc0f0c8`** (v1 = `8d515655b0af4178`). Standing spawns: **0/128 falls in every cell**. Reference spawns under v2: **10/128 (NOM) and 11/128 (DR)** — at/below the measured Kd-0.2 baseline (11/128) — with **decaying** ankle-roll oscillation (growth 0.68/0.74), torque P99 ≤ 6.9 N·m and cap occupancy ≤ 3.4e-4, against the v1 control's 83% falls, 57 rad/s oscillation and **0.38 cap occupancy**. Repaired C3 semantics regression-checked: the C11S4H validation reproduces byte-identically under the edited module.
**recommend:** control-lane Guard binding of the exact hashes below, then the fresh-PPO formulation ticket.
**decision:** the walking lane's law resolution now applies ankle-roll kd_mult 0.2 **unconditionally** (`apply_walking_law_v2`, called by `train()`); the law identity is derived from LIVE built-sim values (`walking_control_law_id`) and registered in `canonical_v8_settings.json → walking_control_law_version`. D-30 disposition executed. All pre-C11S4J walking checkpoints (incl. `ppo_13107200`) become law-v1 diagnostics — moot for training since C11S4H already mandates fresh PPO.
**next:** Guard binding (control lane, Joe-gated) → fresh-PPO formulation.

---

## 1. Provenance pins

| item | value |
|---|---|
| C11S4H commit / C11S4I commit | `029fe8a98a2fd8e6d…` / `7e6e13f` |
| trainer sha256 BEFORE this unit (H state) | `4ffc57245384b8e9a9740ef99cc8919a7067598962b71e5c4983adc418812c25` |
| trainer sha256 AFTER (law v2 landed, for Guard binding) | recorded in §6 / changelog row (the committed file) |
| checkpoint (frozen subject) | `ppo_13107200.zip` sha1 `20ab67fd3cd529b390bc921b3e947fe68ea30f4a` |
| reference | c344 sha1 `c602133cff865db1c377b72a8502f1939eb92c57` |
| plant | v2 `81d4e6d8226ed12b` + v3 `c44ad73fbd8b6f45`, asserted per built arm |
| **old resolved law fingerprint (v1)** | **`8d515655b0af4178`** — computed on a LIVE v1 build via the same derivation |
| **new resolved law fingerprint (v2)** | **`7c05558aacc0f0c8`** |
| launcher (UNCHANGED) | `launch_c11s4_walking.sh` sha256₁₆ `c4986396051cb271` — re-verified post-edit; Guard target intact |
| reward book (UNCHANGED) | `reward_hash 6b48d6e32e006e44` / contract c8j.1 — `straight_profile.py` untouched (diff scope = 1 file, +44 lines) |
| D-27/D-28 semantics (UNCHANGED) | C11S4H validation re-run post-edit: advance 1.000, track 15.999999998, e_sync max 4.2e-09, S2 closure figures byte-identical |

## 2. Implementation (one file, +44 lines)

`rl/mjw/train_tip_reftrack.py`:
- `WALKING_LAW_ANKLE_ROLL_KD_MULT = 0.2` + **`apply_walking_law_v2(sim)`** — sets kd_mult[5] and kd_mult[11] to 0.2, everything else untouched; full provenance docstring (C11E instability, C11S4I falsifier numbers, C11J value parity). Called **unconditionally** in `train()`'s law resolution immediately after the `--law-kd` block — the same launcher flags now resolve to the v2 law, so the Guard-bound launcher hash is unchanged by construction.
- **`walking_control_law_id(sim, acd_hz)`** — sha256₁₆ over the LIVE effective law read from the built sim (kp_mult, kd_mult, tau_limit_vec, acd_hz, action_scale + base scalars/rates; schema mirrors C11J's `control_law_id_inputs`). Used by `train()` (prints the id at every launch) and by the qualification harness — one derivation path (C2).
- `resolved_config.json` continues to record the live `kd_mult`; its sha1 fingerprint therefore mints the new resolved-contract identity automatically — no relabeling of the legacy law anywhere.
- Registry: `rl/configs/canonical_v8_settings.json` gains a `walking_control_law_version` block (v2 id, v1 previous, event, classification CONTROL-CONTRACT-not-plant, checkpoint impact, derivation pointers) — the walking lane's own version chain, parallel to (not merged with) the scout lane's `control_law_version` chain.

Preserved verbatim: Kp (20 via mult), all other Kd (hip-pitch/knee ×2, rest ×1), ASO/action transform, ACD 10 Hz, 50/500/500 rates, τ cap 12.5, plant + contact construction, repaired D-27/D-28, walk52_v58 contract, spawn semantics.

## 3. Qualification (subject = law v2 via the REAL resolution; 2 seeds × 64 worlds × 400 steps per cell)

Reference states injected at t=0 through the adapter's own primitives (C11S4I method); injection frames identical across arms; **pairing gates (subject-ref vs control-ref injected qpos): max |Δ| = 0.0 exactly, both strata, both seeds.** Effective kd_mult asserted on every build ([1,1,2,2,1,**0.2**]×2 subject; […,1]×2 control).

| cell (law · spawn · stratum) | fell (95% CI) | stall | cens @8s | t_fall med | ankle \|qd\| pk med | τ peak / P99 / P99.9 | cap occ | step_ok | stance med |
|---|---|---|---|---|---|---|---|---|---|
| **v2 · standing · NOM** | **0/128** [0, .029] | 0 | 128 | — | 3.06 | 6.98 / 6.63 / 6.83 | **0.00000** | 1353/3072 (44%) | 0.34 s |
| **v2 · standing · DR** | **0/128** [0, .029] | 0 | 128 | — | 3.31 | 7.70 / 6.82 / 7.31 | **0.00000** | 1723/2926 (59%) | 0.36 s |
| **v2 · reference · NOM** | **10/128 = .078** [.043, .138] | 88 | 30 | 0.95 s | 3.09 | 12.50 / 6.69 / 9.09 | 0.00034 | 350/2179 | 0.30 s |
| **v2 · reference · DR** | **11/128 = .086** [.049, .147] | 51 | 66 | 0.98 s | 3.04 | 12.50 / 6.88 / 8.63 | 0.00022 | 895/2421 | 0.34 s |
| v1 control · reference · NOM | 106/128 = .828 [.753, .884] | 21 | 1 | 0.71 s | **56.67** | 12.50 / **12.50** / 12.50 | **0.38425** | 4/392 | 0.28 s |
| v1 control · reference · DR | 107/128 = .836 [.762, .890] | 16 | 5 | 0.68 s | **56.70** | 12.50 / **12.50** / 12.50 | **0.36924** | 60/387 | 0.32 s |

## 4. Acceptance — ALL PASS

| criterion | measured | verdict |
|---|---|---|
| zero standing-spawn falls | 0/128 + 0/128 | ✅ |
| reference fall rate ≤ measured Kd-0.2 baseline (11/128) | 10/128 and 11/128 (CIs match I's A4) | ✅ |
| no ankle-roll oscillation growth | growth medians 0.68 / 0.74 (< 1, decaying); peaks ~3 rad/s vs control's 57 | ✅ |
| no torque rail / material cap occupancy | subject P99 ≤ 6.88 (cap 12.5); cap-touch occupancy ≤ 3.4e-4 (isolated to the ~8% dying worlds) vs control 0.38 | ✅ |
| no regression of repaired C3 semantics | C11S4H validation byte-identical post-edit; reward path untouched | ✅ |
| exact hashes ready for Guard binding | law v2 `7c05558aacc0f0c8`, committed trainer file, launcher `c4986396` unchanged | ✅ |

Bonus channels (recorded, not gated): under v2 the **real-step qualification rate** rises dramatically (standing cells 44%/59% vs the v1 lineage's ~2%; stance medians 0.34–0.36 s at the 0.35 s gate boundary) and standing worlds stop stalling entirely — the D-13 step-gate behavior is law-sensitive, consistent with C11S4I's secondary finding. Relevant context for the coming reward/gating formulation; no claim beyond the measurement (frozen v1-trained policy running on the v2 law).

## 5. Caveats

- The subject policy was trained under law v1; all v2 rollouts are out-of-training-law characterization. Capability claims are not made and not needed — C11S4H already mandates fresh PPO (phase-semantics boundary), and this unit adds the law boundary to the same fresh-start requirement.
- Deterministic `actor_mean`; stochastic-action behavior unmeasured (as in C11S4I).
- `step_ok` is computed from contact rasters + foot z with the kernel's gate constants (0.35 s / 0.15 s / 21 mm); it is the same estimator C11S4E cross-validated against the kernel's own channel.
- Cap-occupancy is a per-control-step `tau_max ≥ 12.4999` proxy (per-joint-substep occupancy is not exposed); declared.
- CPU-env parity for the walking lane under v2 (per-joint kd on the CPU `JointCommand` path) is an open follow-up for the Guard-binding step — the CPU env still carries scalar KD; flagged, out of this unit's scope.
- The scout lane's `control_law_version` chain (v3, accumulator) is untouched and remains a separate lineage.

## 6. Landing

Committed in the lane tree with this report: `rl/mjw/train_tip_reftrack.py` (law v2, +44 lines over the H state), `rl/configs/canonical_v8_settings.json` (`walking_control_law_version` registry block), the qualification artifact dir `rgl/artifacts/c11s4j_law_qualification_20260803T193003Z/` (`c11s4j_qualify.py`, `qualify.json`), and the `rgl/LOCOMOTION_CHANGELOG.csv` row. The committed trainer's sha256 is the Guard-binding value and is recorded in the changelog row (computed at commit time). Bus mirror published. GPU checked idle before each battery; nothing left running.
