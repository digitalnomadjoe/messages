---
id: BRITTLE-20260808T185023Z-25afd876
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15CY
created_at: "2026-08-08T18:50:23.545627Z"
source_commit: ad1446c
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15CY_50MEMBER_FREEZE_20260808T200000Z.md
local_source_sha256: 109bca4d17823ff3f83c3b242db6ecd4e30f0e57baf678d023719d0ff8165fc5
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15CY_50MEMBER_FREEZE_20260808T200000Z
mirror_bytes: 8830
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15CY_50MEMBER_FREEZE_20260808T200000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15CY_50MEMBER_FREEZE_20260808T200000Z.md` |
| sha256 | `109bca4d17823ff3f83c3b242db6ecd4e30f0e57baf678d023719d0ff8165fc5` |
| brittle commit | `ad1446c` |
| lane | locomotion |
| unit | 12U-C11S15CY |
| mirrored at | 2026-08-08T18:50:23.545627Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15CY — 50-Member Guard Package: **`CY 50-MEMBER AUTHORITY PACKAGE FROZEN`**

**date:** 2026-08-08 · **unit:** 12U-C11S15CY · **lane:** locomotion (packaging only)
**constitution:** C1, C2, C6 · **potential violations:** none · **amendment proposed:** no
**owner ruling:** use the 50-member construction; the three g_v1 pre-run battery dependencies are load-bearing runtime files and must be explicit D-11 members
**prohibitions honoured:** NO PPO · NO Guard mutation · NO campaign bootstrap · NO trainer/locomotion/reward/reference/control-plane change · no decision resolved
**changelog:** row 103 · post-append csv `sha256_16` **`fe97e74cecefc117`**

---

## 1. Final immutable hashes

| artifact | sha256_16 |
|---|---|
| **registration payload** `proofs/item2_targets_registration_payload.json` | **`eba4fd1aa2b61a2f`** (NEW, 6 records) |
| **construction membership digest** (50 members) | **`cc407b8d5e213640`** — expectation **VERIFIED** |
| **owner ruling** `OWNER_RULING_C11S15CY.json` | `13f6ed10d95f37f4` → **`d3fa61f3e7fb6b92`** |
| authority package `proofs/item9_authority_package.txt` | `7c7d0af40c933471` |
| launcher `launch_acquisition.sh` | `4f354fc205ca93aa` — **UNCHANGED** |
| trainer `rl/mjw/train_tip_reftrack.py` | `b3d1329765f15675` — **UNCHANGED** |
| estimator / manifest / `Mobs_v1` / `Mact` | `4127331df42baa8e` / `dca42548ae633d3e` / `976c327503a092c3` / `23289b73d9b23faa` — **UNCHANGED** |
| control plane `runs.py` / `wrapper.py` (commit `cd4d704`) | `f1c09c7f86d60d4c` / `cbdefe90990a70f5` — **UNCHANGED** |

## 2. The three new members — measured against the launcher's pins

| target id | path | sha256_16 |
|---|---|---|
| `gv1-battery-grader-c11s15cy` | `…/c11s15tc_contract_20260807T230000Z/proofs/gv1_battery_grade.py` | `91b225bc82e4d7cc` |
| `gv1-battery-oracle-c11s15cy` | `…/redteam_prepppo_acceptance_20260807T232932Z/c2_gv1_oracle.py` | `1b6099cd040d1f9a` |
| `gv1-battery-frozen-c11s15cy` | `…/redteam_prepppo_acceptance_20260807T232932Z/c2_gv1_frozen_battery.json` | `e9b8730a724b81cc` |

All three re-hashed from disk and matched the launcher's independent pins. Role `instrument`
for all three: the grader and the oracle are measurement code, and the frozen battery is the
measurement's fixed input — *"a verdict is only as fixed as its inputs."*

## 3. Construction: 47 → exactly 50, zero removals

```
live construction         : 46 members
prepared (launcher only)  : 47 members   digest 1a7680e360eca76e
frozen here               : 50 members   digest cc407b8d5e213640
removals vs the 47-set    : NONE
additions vs the 47-set   : gv1-battery-frozen-c11s15cy,
                            gv1-battery-grader-c11s15cy,
                            gv1-battery-oracle-c11s15cy
duplicates                : none
```

**The digest expectation was verified, not trusted** — and verifying it took real work.
`cc407b8d5e213640` is a digest over member **IDs**, so it is only reproducible if the ID set is
recorded next to it. The prior log recorded the value but **not the ids**, so I recovered the
exact triple the earlier computation used from the session transcript rather than guessing three
plausible names and reporting a mismatch. With those ids the digest reproduces exactly.

⚠ Worth fixing in future packaging: *an informational digest published without the input set
it was computed over is not checkable.* The frozen payload now carries the ids and the scheme
together.

The digest remains **informational** — D-11 iterates `members` and re-hashes each member *file*
against *its own* pin; occurrences of the construction's own `hash` in `runs.py`: **0**.

## 4. Record count: 3 → **6**, and subjects 4 → **7**

`register_targets(..., replace=True)` **upserts by `target_id`** (`targets.py:114-122`), so the
payload carries only new or changed records — not the whole store.

| # | target id | role | note |
|---|---|---|---|
| 1 | `trainer-source-c11s4` | instrument | **re-pin** — the live store still carries the stale `5c17a4338e21913f` |
| 2 | `launcher-c11s15tc-acquisition` | instrument | new registration |
| 3 | `gv1-battery-grader-c11s15cy` | instrument | new |
| 4 | `gv1-battery-oracle-c11s15cy` | instrument | new |
| 5 | `gv1-battery-frozen-c11s15cy` | instrument | new |
| 6 | `bound-construction-c11s4-walking` | construction | 50 members |

**Canonical-mutation subject set — seven, derived from the code, not chosen:**

```
targets.py:106      _require(state_dir, [unit] + [t["target_id"] for t in targets])
authority.py:294-300  a grant is REFUSED when the request names subjects its
                      subject_refs do not cover
```

`12U-C11S15CY` · `trainer-source-c11s4` · `launcher-c11s15tc-acquisition` ·
`gv1-battery-grader-c11s15cy` · `gv1-battery-oracle-c11s15cy` ·
`gv1-battery-frozen-c11s15cy` · `bound-construction-c11s4-walking`

Schema-validated against the real `targets.schema.json` **both alone and merged** into a copy of
the live store: **VALID** in both. Live entries **52 → 56**.

## 5. The one forced ruling change

The ticket said not to touch the ruling unless the schema *mechanically* requires it. It does:
`authority.py:294-300` refuses a request naming subjects the grant does not cover, so ruling C
had to declare all seven.

**Only `C_cy_canonical_mutation.subject_refs` changed (4 → 7). Every `verdict_verbatim` is
byte-identical**, and all five sentences re-validate **ADMISSIBLE** against the shipped
`permits.authorization_content_reasons` (exit 0). Ruling `13f6ed10d95f37f4` → `d3fa61f3e7fb6b92`.

⚠ **Left for Joe, deliberately not changed:** sentence C still reads *"…to register the
acquisition **triple**"* while the payload is now **six** records. The validator never reads that
phrase, so changing it is not mechanically required. If you want the prose to match, the drop-in
replacement is:

> *"I authorize canonical mutation for 12U-C11S15CY to register the acquisition targets."*

## 6. D-11 proofs — read-only, against a **copy** of the live store

| # | check | outcome |
|---|---|---|
| 6a | all 50 members re-hashed from the real tree; launcher script-bound | **ALLOW** → `bound-construction-c11s4-walking` |
| 6b | **corrupt one battery-runtime COPY** (shadow repo of symlinks) | **DENY** — `member gv1-battery-frozen-c11s15cy: … bytes 76a417f029f6e122 != pin e9b8730a724b81cc`; only that member flagged |
| 6c | argv/script identity on `bash "$LAUNCHER" "$OUT"` | **ACCEPT** `{form: interpreted, index: 1}` |
| 6d | `D-ed52647cd278` under simulated CY | **REFUSED** — *"authority does not carry across units"*; still **GRANTS** under its own unit |

The negative test corrupts a **copy** inside a symlink shadow repo and asserts the copy's hash
actually moved (`e9b8730a724b81cc` → `76a417f029f6e122`) **and** that the real file is unchanged
— a read-only copy would have failed the write silently and passed for the wrong reason.

## 7. Housekeeping

`proofs/item2_targets_three_records.json` (3 records / 47 members) moved to
`OBSOLETE_STAGING/item2_targets_three_records_47member.json` and marked **VOID** with its reason.
It is kept because `proofs/cy_freeze_50.py` reads it as the input of record; that script was
repointed and re-runs clean. The launcher's `adjudication_note` was corrected — it still
described `TC_RUN_TOKEN` / `TC_GRANT_ID`, mechanisms the launcher no longer has. **No hash pin
was changed by either edit.**

## 8. Exact next Red audit inputs

| input | value |
|---|---|
| registration payload | `proofs/item2_targets_registration_payload.json` `eba4fd1aa2b61a2f` |
| owner ruling | `OWNER_RULING_C11S15CY.json` `d3fa61f3e7fb6b92` |
| authority package (8 steps) | `proofs/item9_authority_package.txt` `7c7d0af40c933471` |
| freeze proof + log | `proofs/cy_freeze_50.py`, `proofs/item_freeze50.log` |
| re-validation log | `proofs/item6_revalidation.log` (5/5 ADMISSIBLE, exit 0) |
| launcher negatives | `proofs/item4_launcher_negatives.log` (exit 2/2/2/3) |
| construction | `bound-construction-c11s4-walking`, 50 members, digest `cc407b8d5e213640` |
| canonical-mutation subjects | the seven listed in §4 |
| control-plane patch | `cd4d704` — `runs.py f1c09c7f86d60d4c`, `wrapper.py cbdefe90990a70f5`, suite 510/0 |
| live store (unmutated) | 52 entries, 46-member construction, 241 decisions, 66 transitions |

Suggested Red angles: whether `instrument` is the right role for the frozen battery **data**;
whether re-pinning `trainer-source-c11s4` inside the same mutation as the new registrations
hides a trainer change (it does not — `b3d1329765f15675` is unchanged since C11S15TC); whether
six records in one canonical mutation is too coarse a grant; and the "triple" wording above.

**No PPO. No Guard mutation. No campaign bootstrap. No decision requested or resolved.**
