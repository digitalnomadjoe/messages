---
id: BRITTLE-20260804T215706Z-6adf3178
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S14C
created_at: "2026-08-04T21:57:06.661970Z"
source_commit: 87aafa0
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S14C_CONTINUATION_VERDICT_20260804T214500Z.md
local_source_sha256: 6e362aeb7066f0129bf493a2ffe8fe6bff2a9080a3c82ba6defd0c0b38f8caf2
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S14C_CONTINUATION_VERDICT_20260804T214500Z
mirror_bytes: 6199
---
# UNIFIED_LOCOMOTION_PHASE12UC11S14C_CONTINUATION_VERDICT_20260804T214500Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S14C_CONTINUATION_VERDICT_20260804T214500Z.md` |
| sha256 | `6e362aeb7066f0129bf493a2ffe8fe6bff2a9080a3c82ba6defd0c0b38f8caf2` |
| brittle commit | `87aafa0` |
| lane | locomotion |
| unit | 12U-C11S14C |
| mirrored at | 2026-08-04T21:57:06.661970Z |

<!-- brittle:mirrored-content -->

# 12U-C11S14C — Continuation Verdict: Correct Tax, Unreachable Stand — the Perch Survives

**date:** 2026-08-04 · **unit:** 12U-C11S14C (continuation) · **lane:** locomotion
**constitution:** C1, C2, C3, C4, C7, C9, C12 · **potential violations:** none · **amendment proposed:** no
**status:** rebind + probes + 3 receipted continuation seeds executed cleanly under your four sentences — **NO checkpoint qualifies. The perch persists at 96–99% through all 32 evaluated checkpoints across 3 seeds despite paying −7.9/step.** The repair re-ranked the basin (C3-verified) but created no *local* gradient out of it: PPO lowered its returns (2.55 → 0.64) without changing behavior, and training falls rose 15× as taxed exploration wobbled without ever landing a clean double-support. **STOPPED for Joe** — repair options in §4; no promotion; review head unchanged (C11S14 s1/ppo_18874368).
**recommend:** option (a)+(b) — dense-shape the exemption (graded relief ∝ streak) + stand-scoped σ_foot 35 (imitation pulls the foot down continuously); one bounded book iteration, per-mechanism probes, then retry the continuation.
**decision:** Joe — §4 repair path
**next:** await ruling; viewer CLOSED after this report per the ticket

---

## 1. Execution (all clean)

Ruling commit `79b5723` → bootstrap → **D-308c953fd8c4**/TR-208d2e30c107 (ACQ→POST_ACQ) → **D-35221dfde6f4** (mutation, 6 ids + unit token) → registration ALL MATCH (trainer `71899f69`, launcher `a8a87794`, init `ec31841d` = s1/ppo_18874368, settings `cf0dfd14`, fingerprint `dc90bdd95e57cb53`, construction `55cd5a42` unchanged) → **D-5a831af62bc3**/TR-6400ad36a508 (→ACQUISITION) → **D-021fb6cadc3f** (PPO, 8,388,608 ceiling). Probes: ALLOW `R-61b257a27cea`/`RCPT-36a057f85f45b175` (resolved fingerprint == pin at launch) → trainer tamper **DENY** (nothing executed) → byte-exact restore → re-ALLOW `R-b6680e5c0620`/`RCPT-ab24d929502120c0`. Seeds × 8,388,608 warm from s1/ppo_18874368 (fresh optimizer): s0 `R-13d5385de950`/`RCPT-3b0d1b195c5e1daf`, s1 `R-87266f76b889`/`RCPT-d8914f4966b3094e`, s2 `R-d6d816a9f6a9`/`RCPT-dbe8f8e6eea9e8e7` — 38/38 outputs each. Inspector open throughout ("episode start" verified); closed after this report.

## 2. Gate results (16 evals s0 + 8×2 evals s1/s2, every 524,288)

| metric | s0 (16 ckpts) | s1 (8 ckpts) | s2 (8 ckpts) |
|---|---|---|---|
| start ≤1.5 s | 0.92–1.00 | 0.92–1.00 | 1.00 |
| **stop→stable DS** | **0.00 flat** | **0.00 flat** | **0.00 flat** |
| perch fraction | 0.96–0.99 | 0.96–0.98 | 0.97–0.98 |
| hold lift | 40–42 mm | 40–43 mm | 39–42 mm |
| hold spd10 | 19→49 mm/s (noisier) | 26→44 | 29–43 |
| stride | 1.36→1.44 | 1.34–1.40 | 1.32–1.44 |
| stand-falls | 0 | 0 | 0 |
| τ P99 | ≤7.9 | ≤8.2 | ≤8.1 |

Training telemetry (s0): `p_cap` batch-mean −0.205/step at the ceiling (tax live and binding), `mean_ep_ret` 2.55 → 0.64 while `mean_ep_len` 45 → 253, training falls 53 → 781/rollout. Deterministic behavior static.

## 3. Why the tax was not enough (measured mechanism)

1. **Uniform plateau, no gradient:** the −12-floored tax lowers the VALUE of the whole perch basin roughly uniformly. PPO follows local gradients, not rankings — a cheaper plateau with no downhill direction toward DS teaches nothing.
2. **No partial credit:** relief requires 15 *consecutive* both-loaded frames. A half-lowered foot pays the full tax plus the fence for the motion — locally WORSE than perching still. Every incomplete attempt is punished; only a complete, sustained transition pays off, and
3. **the parent cannot sample it:** perch-converged weights at log_std −2 (σ ≈ 0.135) never complete a coordinated foot-down + weight-shift by chance. The 15× rise in training falls is the taxed exploration wobbling at the basin edge and falling instead of landing DS.
4. **C3's boundary (new law-grade lesson):** C3 orders *declared* subjects; it cannot certify *reachability*. A C3-correct book still fails acquisition when the intended behavior is exploration-unreachable from the init. Income + deterrence audits need a third leg: a dense-gradient (or in-distribution-parent) reachability argument.

## 4. Repair options (Joe-gated; nothing changed since the receipts)

- **(a) RECOMMENDED — graded exemption (dense partial credit):** stand-scoped `p_cap × (1 − min_foot_streak/15)` — every frame closer to sustained double support pays measurably less; relief is a *reduction of a negative* so toggling still earns nothing (predicate test extends directly). One-line change to the C11S14C predicate; C12-declared.
- **(b) RECOMMENDED (pair with a) — stand-scoped σ_foot 35 mm:** the imitation feet-group gradient then pulls the raised foot toward the pad reference *continuously* (the 44 mm hover currently keeps 76% of that income; at σ35 it keeps 45% with a live slope). Stand-scoped, so the C11S13B swing-profile constraint is untouched.
- **(c) Re-acquisition under the repaired book** from the mechanics-baseline widened init at the 24M ceiling (grant D-9e55f019 remains ACTIVE): stops then *form* under the tax instead of escaping a converged basin. Wall-cost ~15 min; outcome uncertain (the perch predates convergence in the original acquisition, but that acquisition never priced it).
- **(d) Exploration surgery** (stand-scoped log_std bump): C7 says formulation before architecture — last resort.

Each lands as a book change → per-mechanism probes (graded-relief gradient table; σ35 stand-scoped income slope; full deterrence battery + C3) → fingerprint re-pin → fresh sentences → bounded retry.

## 5. Artifacts

Runs: `rl/runs/walking_pilot/p12uc11s14c_startstop_20260804T214500Z/s{0,1,2}` (16 ckpts each, receipted) + probes `p12uc11s14c_probe_20260804T214500Z/`. Sweeps + receipts: `rgl/artifacts/c11s14b_startstop_20260804T150500Z/c11s14c/` (`c14c_s0_sweep.json`, `c14c_s12_sweep.json`, continuation receipt logs). The C11S14C book (`dc90bdd9…`) remains registered and correct — the deterrence and all its probe properties hold; what is missing is a path, not a ranking. Review head remains **C11S14 s1/ppo_18874368**. Viewer closed.
