---
id: BRITTLE-20260808T201753Z-986edaf0
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15CY
created_at: "2026-08-08T20:17:53.639906Z"
source_commit: 385dfc2
local_source_path: /home/robojoe/code/brittle/rgl/reports/LOCOMOTION_CY_FINAL_EXECUTION_GO_NOGO_20260808T230000Z.md
local_source_sha256: b7285dc9d8436cab87387e81815f735e9d7c0b78d4c0244ed3ed78afa0b629d9
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: LOCOMOTION_CY_FINAL_EXECUTION_GO_NOGO_20260808T230000Z
mirror_bytes: 6205
---
# LOCOMOTION_CY_FINAL_EXECUTION_GO_NOGO_20260808T230000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/LOCOMOTION_CY_FINAL_EXECUTION_GO_NOGO_20260808T230000Z.md` |
| sha256 | `b7285dc9d8436cab87387e81815f735e9d7c0b78d4c0244ed3ed78afa0b629d9` |
| brittle commit | `385dfc2` |
| lane | locomotion |
| unit | 12U-C11S15CY |
| mirrored at | 2026-08-08T20:17:53.639906Z |

<!-- brittle:mirrored-content -->

# RED TEAM — 12U-C11S15CY Final Execution GO/NO-GO: **`CY FINAL EXECUTION GO`**

**date:** 2026-08-08 · **unit:** 12U-C11S15CY · **lane:** locomotion · **role:** Red (adversarial audit)
**constitution:** C1, C6 · **potential violations:** none · **amendment proposed:** no
**audited:** `UNIFIED_LOCOMOTION_PHASE12UC11S15CY_FINAL_EXECUTION_FREEZE_20260808T220000Z.md` (row 105)
**result:** **48 checks, 0 blockers**
**changelog:** row 106 · post-append csv `sha256_16` **`e9589fbce8270614`**

> **Method note.** The audit itself was executed **strictly read-only** — no writes to the
> repository, the artifact package or the Guard live store; the only writes were a throwaway
> state copy under the session scratchpad. Blue's own freeze script was deliberately **not**
> re-run, because it rewrites the payload it would be checking. **This report file was written
> afterwards at Joe's explicit request**, after every verdict below was already fixed.

---

## 1. Final hashes — recomputed independently

| artifact | sha256_16 | verdict |
|---|---|---|
| owner ruling | `9636f8b5bafb7a79` | MATCH |
| authority package | `cbd2f61f7e255a10` | MATCH |
| registration payload | `5bef963d011a9643` | MATCH |
| launcher | `4f354fc205ca93aa` | MATCH |
| trainer | `b3d1329765f15675` | MATCH |
| estimator | `4127331df42baa8e` | MATCH |

0 mismatches.

## 2. Exact final topology

**7** target records · **8** canonical-mutation subjects · **51** construction members ·
digest recomputes to **`da7b18b13af274a6`**.

Also checked, because a digest that only matches itself proves nothing: the value the
construction record **carries** equals the recomputation; no duplicate member ids; **zero
removals** against the live 46-member set.

## 3. STEP 1 is executable

* `R` is pinned as a **full 40-hex** id and resolves to a real git **commit**.
* The ruling blob **at `R`** hashes to `9636f8b5bafb7a79` — byte-identical to the working tree.
* `--citation` is present and byte-equal to ruling A.
* **The cited sentence occurs verbatim inside the blob at `R`** — which is what
  `ledger._live_source_document` actually reads (`git show {campaign_commit}:{path}`), not the
  working tree. A citation that matched only the worktree would let an uncommitted edit supply
  the owner's words.
* All five required flags present (`--state-dir --commit --source-path --citation --repo`).

## 4. STEP 4 — coverage and verbatim

Ruling C's subjects are an **exact cover** of the registration request
(`[unit] + payload target ids`, `targets.py:106`): **no gap and no over-declaration**. The
verdict text in the runbook is byte-identical to ruling C, all eight subjects are listed, and
"triple" is gone.

## 5. Exactly one authoritative runbook

Walked the whole package: **one** live file contains an operator sequence
(`proofs/item9_authority_package.txt`). The competitor is present in `OBSOLETE_STAGING/` and
absent from `proofs/`.

## 6. D-11 over 51 members

* Merged store **schema-valid** (57 entries).
* **ALLOW** over all 51 from the real tree; the launcher is **script-bound**.
* Corrupted **estimator copy** → **DENY**, naming `estimator-path-speed-v1-c11s15cy` and only
  that member. The corruption is proven to have taken (hash moved) and the **real file is
  verified untouched** — a read-only copy would have failed the write silently and passed the
  test for the wrong reason.

## 7. Guard execution

* The patched script/argv invariant **ACCEPTS** the exact production form.
* `D-ed52647cd278` **REFUSES** under `12U-C11S15CY` and **still grants** under its own unit —
  out-scoped, not narrowed.
* STEP 7 forbids reusing it; ruling E covers the `run guarded --subject` set.

## 8. The chain executes from the current live baseline

**The decisive check — the trust root.** `_live_source_document` re-reads every decision's
source at the **current** `campaign_commit`, so advancing it to `R` silently re-verifies all
**241** decisions. If any active decision cited a file that moved or changed between the old
root and `R` — and this package *did* move files into `OBSOLETE_STAGING` — that decision would
stop granting the moment the trust root moved.

```
R is a descendant of ed7ea744…                                  YES
ACTIVE decisions carrying a cited source                        119
… whose verification CHANGES or FAILS at R                        0
```

**No trust-root regression.**

The rest of the chain:

| risk | finding |
|---|---|
| dedup | the five decisions differ in (type, unit, subjects) — they cannot collide even at identical `ts` |
| authority | 5/5 sentences ADMISSIBLE against the shipped validator |
| lifecycle | `canonical_mutation` is legal in `POST_ACQUISITION_AUDIT` and **not** in `ACQUISITION` — the B/D bracket is load-bearing, not decorative; `ppo` is legal **only** in `ACQUISITION`, so D must precede E; both bracket transitions are lifecycle-legal |
| unit | effective unit is `12U-C11S15` — what ruling A transitions from — and A's decision unit is the unit being **left** (`transitions.py:294`) |
| schema | payload valid alone and merged |
| dirty tree | telemetry has **no dirty tracked files**; `run guarded` would not refuse |

**Two execution risks beyond the ticket's list, checked because they fire late and hurt:**

1. **`$OUT` must be new or empty** or the launcher exits 3. `rl/runs/walking/tc_acq_s0`
   **does not exist**. Clear.
2. **The run must not dirty the tracked tree**, or `runs` refuses to close the receipt COMPLETE
   even after a successful 442k-step run. `rl/runs/` is gitignored (`.gitignore:40`) with **0
   tracked files** beneath it, and the capture dir is covered by the same rule. Clear.

## 9. Nothing was touched

Both trees **0 tracked modifications**; Guard live unchanged at **241 decisions / 66
transitions / 52 targets**.

## Verdict

# `CY FINAL EXECUTION GO`

The authoritative eight-step sequence is `proofs/item9_authority_package.txt`
(`cbd2f61f7e255a10`) and is reproduced there verbatim. **Nothing has been bootstrapped,
resolved, registered, mutated or run.** Step 1 remains a Guard mutation that moves the trust
root, and step 8 is the only sanctioned execution command.
