---
id: BRITTLE-20260805T153912Z-128fb942
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15J
created_at: "2026-08-05T15:39:12.757631Z"
source_commit: 245db8c
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15J_TURNING_ORACLE_20260805T090000Z.md
local_source_sha256: 58d10730fdf5fac781a980b25836552d794d92694ab5ae1a666ebd827907ae1e
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15J_TURNING_ORACLE_20260805T090000Z
mirror_bytes: 6972
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15J_TURNING_ORACLE_20260805T090000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15J_TURNING_ORACLE_20260805T090000Z.md` |
| sha256 | `58d10730fdf5fac781a980b25836552d794d92694ab5ae1a666ebd827907ae1e` |
| brittle commit | `245db8c` |
| lane | locomotion |
| unit | 12U-C11S15J |
| mirrored at | 2026-08-05T15:39:12.757631Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15J — Turning Oracle, Path-Frame & Positive-Control Audit: ORACLE VALID, CURRENT POLICY STRAIGHT

**date:** 2026-08-05 · **unit:** 12U-C11S15J · **lane:** locomotion · instruments only; no PPO, no reward mutation, no reference edits, no promotion, no policy claims
**constitution:** C1, C2, C4, C6, C8, C12 · **potential violations:** none · **amendment proposed:** no
**status:** the turning oracle is built, validated against the certified turning kinematics (positive controls PASS with correct signs and radii), and rejects every current checkpoint as straight/standing (negative controls PASS, matching Joe's review). Reward-oracle parity is machine-epsilon. The path frame conforms to COORDINATES.md, verified numerically.
**decision (exactly one):** **ORACLE VALID, CURRENT POLICY STRAIGHT.**
**changelog:** row 49 `12U-C11S15J, 20260805T090000Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. Subjects (exact provenance)

| subject | identity | oracle verdict |
|---|---|---|
| G-final `ppo_4194304` | sha `78298a7d0034632e` (run RCPT-5cf8455f) | **STRAIGHT** (κ_med −0.06, R ≥ 16 m; 3.69 m translation) |
| H-final `ppo_4194304` | sha `4553633de9134bf0` (RCPT-f6cefe05) | **STRAIGHT** (κ_med +0.03, R ≥ 32 m; 3.87 m) |
| H-1M `ppo_1048576` | sha `383c074e6e9f4a1e` (banked diagnostic) | **STANDING/NO-TRANSLATION** (ET at 244; teleport boundary correctly gated) |
| certified left-turn artifact `member_kP.npz` | c10q package member `3fe2aa40cdc14243`, certs 10/10 v7f, `VISUAL_ACCEPTED_BANKED` (12U-C10R) | **TURNING(LEFT+)** sustained 1.00 m at κ_med **+2.02** (R ≈ 0.49 m) |
| certified right-turn artifact `member_kM.npz` | `3f1591f0b7e0e53f`, same certification | **TURNING(RIGHT−)** 1.12 m at κ_med **−1.78** (R ≈ 0.56 m) |
| banked half-meter refs kP200 / kM200 | `8806f7c85c672013` / `d5b6f5c9066fe686` | TURNING(LEFT+) κ +1.86 R 0.54 / TURNING(RIGHT−) κ −1.84 R 0.54 |
| figure-eight (assembled per the c10q package: kP→kM end-pose continuation; closes at net yaw 0.0°) | derived kinematic, archived | **BOTH turn runs detected**: LEFT+ (κ +2.02, R 0.49) and RIGHT− (κ −1.84, R 0.54) |

The historical certified turning artifacts in the record are these kinematic members (certified + Joe-visually-banked); the p12t-era turning POLICIES remain untouched per the C11S15B diagnostic-only ruling (cross-contract evaluation is forbidden), and no rollout traces from their era exist.

## 2. Positive-control gate — PASS

Commanded κ = 2.0 (R = 0.5 m): the oracle reads κ 1.78–2.02 (R 0.49–0.56), correct sign on every member, both signs inside the figure-eight, sustained over the full turn arc (1.0–1.18 m), cross-track ≡ 0 on members (self-referenced sanity). Visual agreement: the members are the motions Joe visually accepted at banking (C10R), and my own-eyes path plots show the curved trails. If any future oracle change fails these members, the oracle is invalid — this gate is now a standing regression.

## 3. Negative-control gate — PASS

The same oracle classifies both current finals STRAIGHT and the 1M STANDING — exactly Joe's review. **Classification never consumes accumulated body yaw**: it requires translation along a curved path (cycle-smoothed world-XY heading progression), and pivot/rotation/drift/standing cannot qualify by construction. Teleport and done boundaries segment the data before any statistic is computed.

## 4. Oracle construction honesty (three defects found and fixed while building it)

(1) Global-median curvature washed out the composite member's concentrated turn → classification is by **sustained same-sign sub-segment runs** (|κ| ≥ 0.8 over ≥ 0.4 m). (2) 1-second heading windows aliased gait sway (a non-integer number of cycles flipped the sign of the median!) → **cycle-synchronous path smoothing** (66-frame box = one gait cycle) before heading extraction. (3) Rollout body-yaw was a relative unwrapped series → absolute initial yaw restored for tangent alignment. All three are in the archived instrument; the positive/negative gates re-run green after each fix.

## 5. Path-frame audit (vs COORDINATES.md)

Forward = body −Y = world +X ✓ (straight walkers progress along +X at body yaw ≈ +90°). Robot heading = body yaw − π/2 ✓ **verified numerically**: the oracle's tangent alignment (body yaw − π/2 − path heading) reads −1.7° ≈ 0 on the straight walker. Position transforms use the commanded-path heading (ψ from the profile integral), never raw body yaw, in: drift cross-track, altwin tangent progress, critic dims 57/58/59, and the oracle — one convention, one π/2 offset (in yawhold's `yaw − 90°` and the critic/oracle `yaw − π/2`), no double-offsets found. Reset anchors, phase sync, and mirror behavior per the E/I audits (anchor convention unified; kM tables exact mirrors; `NEG_OBS` negates obs45).

## 6. Reward-oracle parity — PASS

The oracle consumes the ENV-SAVED arrays (`env.tpos`, `env._cmd_psi_tab`, `env._path_anchor`) — the identical objects the reward terms read; numeric check: |oracle cross-track − candidate drift-term cross-track| **max 4.4e-16** on the G-final trace. Repricing on the single consistent basis (teleport-ghost family, live book, oracle-classified): **historical true turn (+19.2..+23.0/step, oracle TURNING) > partial curved progress (11.4–15.8, PARTIAL) > straight under turn command (10.7, STRAIGHT) > wrong-sign (8.4–9.9) > pivot/stand (−33..−45)** — the required ordering holds. Cross-basis: the real G-final prices 15.95/step live — still below every true-turn row. Rewards never favor a behavior the oracle calls straight over the certified turn. No mismatch.

## 7. Viewer identity and visual truth

`c15j_viewer.py` (archived): keys 1–6 with **on-screen `set_texts` labels** naming subject + sha + frame counter, identity beacon color, commanded path (green), pelvis trail (white), heading tangent ray (orange), wall-clock speed, close cam; per-frame viewer-state == trace-row asserted (exact equality every 300 frames); exact file + sha printed at every load; traces captured with RSI and every teleport source disabled. The display was asleep at launch time (the known GLFW monitor assert), so a watchdog is armed — the labeled viewer appears when the screen wakes.

## 8. Standing consequence

Any future turning claim must pass THIS oracle (positive-control-validated, path-geometry-only) before it reaches Joe — no accumulated-yaw statistic, no injection-armed battery, no unlabeled viewer. Per the ticket: further turn PPO is unblocked only in the sense that both controls now pass; the standing proposal (paired ±κ construction) remains Joe-gated and unchanged.

## 9. Artifacts

`rgl/artifacts/c11s15j_oracle_20260805T090000Z/` — `turning_oracle_v1.py`, 7 oracle JSONs + f8 assembly, 3 clean traces (terms + feet included), labeled viewer + watchdog. Path plots in `c11s15i_audit_.../`.
