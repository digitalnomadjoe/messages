---
id: BRITTLE-20260805T051501Z-8774472f
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15G
created_at: "2026-08-05T05:15:01.951067Z"
source_commit: bdc4bb1
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15G_ACQ_CONSTRUCTION_20260805T051000Z.md
local_source_sha256: 3ea9c54e46a634c17ce331e649d14d928c249d9632afd7b6ee686fb87937c610
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15G_ACQ_CONSTRUCTION_20260805T051000Z
mirror_bytes: 8689
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15G_ACQ_CONSTRUCTION_20260805T051000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15G_ACQ_CONSTRUCTION_20260805T051000Z.md` |
| sha256 | `3ea9c54e46a634c17ce331e649d14d928c249d9632afd7b6ee686fb87937c610` |
| brittle commit | `bdc4bb1` |
| lane | locomotion |
| unit | 12U-C11S15G |
| mirrored at | 2026-08-05T05:15:01.951067Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15G — Guarded kP Manifold-Acquisition Construction: FROZEN, all prelaunch reproduction PASS — awaiting sentences

**date:** 2026-08-05 · **unit:** 12U-C11S15G · **lane:** locomotion · construction only; NO PPO run; canonical stacks untouched
**constitution:** C1, C2, C3, C6, C7, C12 · **potential violations:** none · **amendment proposed:** no
**status:** the bounded kP acquisition construction is frozen on the C11S15F candidate. One material deviation from the ticket's frozen inputs, found BY this unit's own dry-run gate and disclosed in §1. All ten mandated prelaunch reproductions pass from the resolved card. **STOPPED for the owner sentences (§6).**
**recommend:** issue the sentence set — the kP probe then runs under the ACTIVE PPO grant with the two-battery evaluation and the visual stop.
**decision:** Joe — §6
**next:** await sentences
**changelog:** row 42 `12U-C11S15G, 20260805T051000Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. Frozen-input deviation (disclosed prominently)

The ticket froze candidate sha `3e0a5773188e7e84`. The G dry-run exposed a latent integration gap in it: the P15b income wiring sat inside the **ref-family branch** — on a single-member card it never runs, `yaw_cmd_target` stays False, the income silently degrades to the **fixed-scalar target**, and the resolved fingerprint does not move (**a live weight with no `walking_yaw_compliance` block — an unfingerprinted reward channel**). The C11S15F rig had set the attributes directly and could not see this. Repair: one anchored builder edit (P15b-v2) — the wiring moved before the resolved-contract build (runs on ANY cmd_profile lane) plus a **fail-closed guard**: `yaw_dominant_w>0` on a profile lane without `--yaw-cmd-target` now refuses to run. Reward mathematics byte-identical; the reproduction battery (§4) is numerically identical to F on every row. **New candidate pin `6f110da702bca0f2`** (builder `sha 58c0181b`-lineage, regenerates deterministically from the pinned canonical trainer). A byte-exact copy sits at `rl/mjw/train_tip_reftrack_c15f_candidate.py` so every `__file__`-derived path (package bootstrap, reward-book resolver) resolves natively — content pin unchanged by placement.

## 2. Immutable card + pins

| pin | value |
|---|---|
| card `launch_c11s15g_kp.sh` | **`416cf28ba33a903b`** — C11S15C kP base + G-1 candidate invocation (in-card sha assertion) + G-2 income flags `--yaw-dominant-w 4.0 --yaw-peaked --yaw-cmd-target --yaw-avg --yaw-rate-sigma 0.65` + **G-3b structural TOTAL ceiling 4,194,304** (over-ceiling invocation refused, exit 91 — proven) |
| candidate trainer `rl/mjw/train_tip_reftrack_c15f_candidate.py` | **`6f110da702bca0f2`** (new member `trainer-candidate-c15f`) |
| canonical trainer `trainer-source-c11s4` | **`5c17a4338e21913f` UNCHANGED** (asserted at payload build; adoption into the canonical member = a future Joe-gated mutation, not this unit) |
| reference | `reference-halfmeter-kp-c11s15c` `8806f7c8` (registered, unchanged) |
| init | `init-widened-c11s14` = banked milestone `7d176de4` (registered, unchanged) |
| settings candidate | `canonical_v8_settings_CANDIDATE_g.json` `c01fefe6b0054b6f` |
| **resolved fingerprint** | **`54f21532ec4d8afb`** — invariant across 2 seeds × 2 out-dirs; **includes `walking_yaw_compliance` {w 4.0, σ 0.65, cycle-averaged, respawn re-init}** |
| construction | 28 members (+`trainer-candidate-c15f`, +`launcher-c11s15g-kp`) hash **`7238c7333109e6dc`** |
| dry-runs | 17/17 card assertions PASS ×4 (plant v2/v3, contact params, ref pin, milestone D1b, plant-boundary D2, dims 54/60, DFL preamble, G-1 candidate pin) |

## 3. Two independent evaluation batteries (frozen instruments)

**Steady-turn battery (THE acquisition question):** deterministic, 64 worlds, ref-state injections at frames {260, 300, 340, 380, 420} via the candidate's `_bent_state` (commanded yaw rate injected, one anchor convention, live command refresh), 200 steps each. Metrics: signed net-yaw response fraction vs the commanded integral; realized curvature EMA(rate)/EMA(speed) + radius; path-tangent progress and cross-track from the manifold's own frame; alternating grounded steps (contact rising edges); τ P99/P99.9/max; slip; falls. **Gate:** response ≥50% on ≥80% of worlds AND anti-fake clause — path-tangent progress ≥50% of the ref chord rate, |cross-track| P90 ≤ 0.30 m, alternation present (excludes torso spin, pivot, crab, drag — rotation without path progression scores zero). **Full-member battery (context, NOT the acquisition gate):** from-spawn 0→900: DS-entry hold, commanded start (reported SEPARATELY — a start failure is the known C10Q transition issue, never counted as turning failure), window response, exit/tail. Mid-arc turning alone does not qualify the complete member; both facts are reported side by side.

## 4. Mandated prelaunch reproduction — from the resolved card (fingerprint asserted `54f21532` in the rig)

| clause | result |
|---|---|
| obs45/52/53 read the live tables; pads expose 0/0 | ✓ pad_max 0.0 / 0.0, `table_absmax_err` 0.0 (both cols) |
| yaw target = vx×κ | ✓ median error 0.0 (κ-clip-at-peak metadata blemish documented since C15C) |
| fwd/cross-track/heading/yaw income share one manifold | ✓ single lineage + live `r_yaw` ≡ offline recompute (ledger identity) |
| exact > partial > straight > wrong > exploits/fall | ✓ α-family kP 19.17/15.75/12.60/11.44/10.74/9.28–9.68/8.43; kM strictly monotone 21.42→9.93; exploits −33..−45; falls dominated |
| mirrored yaw income symmetric | ✓ per-α income kP≡kM within 0.004/step |
| correct-sign action blends improve return | kM ✓ (593→598→600 at λ≤0.10); kP flat (collateral-dominated, as frozen in F — the α-gradient is the acquisition-relevant measurement, monotone both sides) |
| falling/respawning dominated | ✓ mid-arc survive +9.4..+14.3 vs die −50; episode-truncated accounting |
| torque/contact gates valid | ✓ τP99 ≤ 8 in dynamics rows (teleport 12.5 = instrument artifact, documented); DFL preamble PASS in dry-runs |
| no fixed-world reward / stale command source reachable | ✓ E-lineage + **the new fail-closed guard makes the fixed-scalar path unreachable on profile lanes** |
| resolved fingerprint includes the yaw-compliance block | ✓ `walking_yaw_compliance` in `resolved_config.json`, all 4 dry-runs |

Every number equals the C11S15F frozen evidence exactly (deterministic subjects; the candidate's math is byte-identical).

## 5. Bounded PPO probe plan (proposal — runs only after sentences)

kP only, seed 0, `bash launch_c11s15g_kp.sh 0 <out> 4194304` under `run guarded` (subject `bound-construction-c11s4-walking`, campaign-ref HEAD). Deterministic evals every 524,288 (both batteries per checkpoint). **Early stop** if no steady-turn trend by 2,097,152 — trend = response ≥15% at any eval OR strictly increasing across the last three evals. **Visual stop:** the FIRST checkpoint passing the steady-turn gate stops the lane for Joe's curved-path replay (no further training until reviewed). **Inspector pre-staged and verified attached BEFORE `run guarded` is issued** (spectator on the run dir + live-training manifest; the standing rule), closed after the final report. Probe pattern: ALLOW (262,144) → 1-byte candidate tamper DENY → restore → re-ALLOW, then the probe. PPO authority: the ACTIVE grant `D-ed52647cd278` (unit 12U-C11S15, subject-scoped, 8,388,608/seed) covers 4,194,304 — no new PPO sentence required.

## 6. Sentences (proposed verbatim; subjects repeat per token at request time)

1. `I authorize transition to 12U-C11S15 for the lifecycle advance from ACQUISITION to POST_ACQUISITION_AUDIT.`
2. `I authorize canonical mutation for 12U-C11S15 scoped to: trainer-candidate-c15f, launcher-c11s15g-kp, canonical-settings-c11s4, reward-fingerprint-c11s12, bound-construction-c11s4-walking.`
3. `I authorize transition to 12U-C11S15 for the lifecycle advance from POST_ACQUISITION_AUDIT to ACQUISITION.`

Payload: `targets_register_c11s15g.json` (5 records; membership 28 = +candidate trainer +G launcher; canonical trainer member untouched). Arc objective preserved: reward-book + trainer-candidate members only — no runtime reference injection, no specialist switching, no interface change.

## 7. Artifacts

`rgl/artifacts/c11s15g_acq_20260805T051000Z/` — card + derived dry script, payload, settings candidate, resolved config + dry log, reproduction battery JSONs (8 passes), rig copy. Candidate + builder in `c11s15e_repair_20260805T041500Z/` (builder regenerates `6f110da7` from `5c17a433` deterministically).
