---
id: BRITTLE-20260826T031116Z-49d84742
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: WALK-DR06G3
created_at: "2026-08-26T03:11:16.484915Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06G3_CADENCE_RUNTIME_PARITY_20260826T030459Z.md
local_source_sha256: 42b9c410e383287805751ec5a75422a6ee576485988d28f084826b8de77b5420
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: GREEN_OPUS5__WALK_DR06G3_CADENCE_RUNTIME_PARITY_20260826T030459Z
mirror_bytes: 10984
---
# GREEN_OPUS5__WALK_DR06G3_CADENCE_RUNTIME_PARITY_20260826T030459Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06G3_CADENCE_RUNTIME_PARITY_20260826T030459Z.md` |
| sha256 | `42b9c410e383287805751ec5a75422a6ee576485988d28f084826b8de77b5420` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | WALK-DR06G3 |
| mirrored at | 2026-08-26T03:11:16.484915Z |

<!-- brittle:mirrored-content -->

# TEAM: GREEN — Opus 5 · WALK-DR06G3 — Cadence-branch runtime parity: **REPAIR BUILT AND PROVEN; DR06G FOUNDATION ACQUISITION IS FALSIFIED.** Under exact train/eval parity the ratchet candidate realizes **0.2021 m/s at command 0.272219 — 74.2 % of command** — against the policy it continued from at **0.2008 (73.8 %)**. The whole 884,736-step turn bought **+0.0013 m/s, 1.9 % of the remaining gap**. That is the answer your decision rule asked for, and it is the falsification branch: DR06G did not acquire the banked gait at its correct label. The repaired runtime and the DR06G2 consistent harness agree to **0.0000 on both policies**, so there is no discrepancy to explain — two independently built measurements landed on the same numbers. The repair itself is complete and proven but **NOT installed**: `rl/isaac_parity/runtime_port.py` is byte-unchanged at `bbe122433e9a399d`, the candidate is staged at `fa16e62f47b59567`, and the re-grade ran by injecting the staged module, so no banked path was mutated and no Guard authorization has been consumed. Default non-perturbation is proven twice over — the resolver returns the legacy value bit-for-bit on **170,001 grid points**, and four full rollouts under the staged runtime are **bit-identical by trace hash** to the same rollouts under the banked one. Train == eval now holds at all **12 probe commands** on and off the branch, including the 0.2400/0.2401 boundary, and a manifest declaring any cadence key the runtime cannot represent is **refused** rather than silently evaluated — which is precisely the failure mode that produced this whole detour. ZERO training, ZERO optimizer steps, ZERO promotion. **Installing the repair needs your sentences.** STOP for Joe.

**unit:** WALK-DR06G3 · **ts:** 20260826T030459Z · **team:** GREEN (ticket header: Opus 5; executing session: Claude Fable 5)
**constitution:** C1, C2, C6, C7 · **potential violations:** none · **amendment proposed:** no
Ticket: bus `BRITTLE-20260826T030455Z-8071f290`. No PPO, no reward change, no reference/member change, no branch redesign, no stride reward, no command-manifold expansion, no promotion, no gait-bank mutation.

---

## 1 · Runtime path and hash, BEFORE → AFTER

| | path | sha256_16 | state |
|---|---|---|---|
| before | `rl/isaac_parity/runtime_port.py` | **`bbe122433e9a399d`** | **unchanged on disk right now** |
| candidate | `rgl/artifacts/walkdr06g3_parity_20260826T030459Z/staged/runtime_port_dr06g3.py` | **`fa16e62f47b59567`** | staged, proven, **not installed** |

73-line diff, three changes and nothing else: a manifest-read for the branch, one shared resolver used by both cadence call sites, and the fail-closed unknown-key guard. `load_law()` gained an optional `manifest_path` argument defaulting to `MANIFEST`, so every existing caller is unchanged.

**Guard note.** `runtime_port.py` is **not** a registered Guard target — I checked all 143 targets and none reference it; it is "banked" by project convention (the JOINT-REPORT01 contract), not by Guard registration. So Guard did not block this work, but your ticket asks for authorization and registration of the exact runtime mutation before execution, and I have honoured that by not installing it. The re-grade was executed with the staged module injected as `rl.isaac_parity.runtime_port` at import time.

## 2 · Exact cadence-branch semantics

The branch is read from the **same manifest key the trainer is configured by**, at the same level as the existing schedule — `command.cadence_schedule_branch` = `{v_break, m2, b2, floor}` — and is copied verbatim from the WALK-DR06G run's own `runtime_manifest.json`, the authoritative record of what that run trained under. There is no DR06G special case in the code: absent key → `sched_branch = None` → legacy behavior.

```
resolve_cadence(law, v):
    c = clip(m*v + b, lo, hi)                       # the canonical law, unchanged
    if branch and v > v_break:  c = max(m2*v + b2, floor)
```

Both runtime call sites (`__init__` and `obs54`) now go through this one function, so `obs[53] = c/2` and the cyclic clock `csphi += c·dt` can no longer diverge from each other or from the trainer. Scope is unchanged: experimental and local to this walking arc, not promoted as the global cadence architecture.

## 3 · Default-runtime non-perturbation (two independent proofs)

**Grid.** With the crown manifest (no branch declared — what every existing caller binds), `resolve_cadence` returns **exactly** `clip(m·v+b, lo, hi)` on a dense grid of **170,001 commands from −0.5 to 1.2: 0 mismatches**, compared with `!=` rather than a tolerance.

**Rollout.** Four full 700-step rollouts (both policies × 2 seeds) at command 0.272219, run under the banked runtime and under the staged runtime, compared by trace hash:

| rollout | banked | staged | |
|---|---|---|---|
| DR06G-884736 s0 | `45f5b94da8de3a44` | `45f5b94da8de3a44` | **bit-identical** |
| DR06G-884736 s1 | `5e8e022e10342c9c` | `5e8e022e10342c9c` | **bit-identical** |
| DR05D-737280 s0 | `9d3a3d3a515d9242` | `9d3a3d3a515d9242` | **bit-identical** |
| DR05D-737280 s1 | `e177059dd09725b3` | `e177059dd09725b3` | **bit-identical** |

## 4 · Train / eval parity probes and the fail-closed contract

Trainer expression vs runtime resolver, both with the branch absent and present:

| vx | train (no branch) | eval (no branch) | train (branch) | eval (branch) |
|---|---|---|---|---|
| 0.0000 | 0.700000 | 0.700000 | 0.700000 | 0.700000 |
| 0.1530 | 0.813168 | 0.813168 | 0.813168 | 0.813168 |
| 0.2000 | 0.914100 | 0.914100 | 0.914100 | 0.914100 |
| 0.2399 | 0.999785 | 0.999785 | 0.999785 | 0.999785 |
| 0.2400 | 1.000000 | 1.000000 | 1.000000 | 1.000000 |
| 0.2401 | 1.000000 | 1.000000 | 0.999124 | 0.999124 |
| **0.272219** | 1.000000 | 1.000000 | **0.717721** | **0.717721** |
| 0.3000 | 1.000000 | 1.000000 | 0.717720 | 0.717720 |
| 0.4000 | 1.000000 | 1.000000 | 0.717720 | 0.717720 |

All 12 probes agree exactly (0.05/0.10/0.2722-rounded rows omitted for width). At the ratchet command the runtime now produces **cadence 0.717721 and obs[53] 0.358861**, against the trainer's 0.717721 / 0.358860 — the target this unit was set. Commands at and below the 0.24 break are untouched in both modes.

**Fail-closed.** `load_law` carries a whitelist of the cadence keys the runtime can represent. A manifest declaring anything else — tested with a synthetic `cadence_warp_v2` — is **REFUSED**: *"runtime cannot represent declared cadence configuration ['cadence_warp_v2'] — refusing rather than silently evaluating under a different cadence law than training."* `cadence_law_fingerprint(law)` exposes schedule, branch, native rate, the obs[53] convention and the clock update as one object, so a caller can assert TRAIN == EVAL == DEPLOY rather than infer it. Nothing depends on `cp54_acc`, which remains dead state.

## 5 · Re-grade at command 0.272219 (world displacement, 2 seeds, zero optimizer steps)

| policy | TRUE speed | ratio | cadence | stride | inter-foot sep | falls | foot-foot |
|---|---|---|---|---|---|---|---|
| **DR06G-884736** | **0.2021** | **74.2 %** | 1.0622 | 0.1903 | 14.06 mm | 0 | **0** |
| DR05D-737280 | 0.2008 | 73.8 % | 1.0093 | 0.1990 | 14.98 mm | 0 | **0** |
| banked member | 0.2722 | 100 % | 0.7177 | 0.3793 | — | — | — |

τ P99 3.73 (DR06G) / 3.60 (DR05D), peaks well inside the 12.5 N·m limit. No falls, no physical foot-foot collision in any rollout.

**Agreement with DR06G2.** The consistent harness reported DR06G 0.2021 and DR05D 0.2008; the repaired runtime reports **0.2021 and 0.2008** — agreement 0.0000 on both. Two independently constructed measurements (an obs-channel override versus a manifest-driven runtime repair) converge exactly, so the DR06G2 finding stands and there is no discrepancy to explain.

## 6 · Verdict

**DR06G foundation acquisition is FALSIFIED.** Under exact train/eval parity the candidate sits at 74.2 % of its commanded speed, and its entire 884,736-step turn produced **+0.0013 m/s over its own initialisation — 1.9 % of the gap it was meant to close**. The gait it produces at that command is not the banked member's: cadence runs **48 % above** the commanded 0.7177 and stride is **50 % short** of 0.3793. Per your decision rule I stopped here: no PPO turn was spent, no member share was raised, DR06G2 was not launched.

Worth stating plainly, because it reframes the arc: the mechanism DR06G2 was written to correct — "cadence already too high, stride 12 % short" — was itself a mismatch artifact. The real behaviour at the ratchet command is far more extreme, and it is shared by DR05D, which never trained under the branch. **Both policies respond to the branch's low cadence observation by stepping faster and much shorter.** That is a property of the operating point, not of DR06G's training, and it means raising member authority was never going to be the lever.

## 7 · What I need from you

Installing the repair mutates a banked runtime path, so per your ticket it needs authorization and registration first. To install `fa16e62f47b59567` at `rl/isaac_parity/runtime_port.py` I need the usual chain: a transition to WALK-DR06G3, a certification window, and a canonical-mutation sentence naming the runtime BEFORE → AFTER hashes so the mutation is bound rather than asserted. No ppo sentence is required — this unit runs no training. Until then the file stays at `bbe122433e9a399d` and the repair lives only in the artifact directory.

Beyond that, the falsification puts a real question in front of the arc, which is yours and not mine: the branch's cadence at the ratchet point is one the policies actively fight. Whether the ratchet should be pursued at the legacy clipped cadence (where DR06G measures 94.4 %, and where train and deploy already agree), or whether the branch's operating point itself needs revisiting, is a design decision I have deliberately not taken.

## 8 · Scope and artifacts

ZERO training, ZERO optimizer steps, ZERO promotion, ZERO banked-file mutation, no Guard grant consumed. n = 2 seeds at a single command on the nominal plant — enough to falsify an acquisition claim against a 26 % shortfall and to confirm exact agreement between two harnesses, **not** a gate claim (reps-16 unmet). Artifacts under `rgl/artifacts/walkdr06g3_parity_20260826T030459Z/`: `staged/runtime_port_dr06g3.py` (the candidate), `staged/runtime_manifest_branch.json` (crown manifest + the DR06G run's own branch record), `dr06g3_parity_proof.py` + `parity_proof.json` (proofs A–D), `dr06g3_regrade.py` + `regrade/` (proof E and the re-grade, three runtime modes).

STOP for Joe — DR06G acquisition falsified at 74.2 %; the runtime repair is proven and waiting on your authorization to install.

**changelog row:** `WALK-DR06G3-PARITY, 20260826T030459Z` — post-append `rgl/LOCOMOTION_CHANGELOG.csv` sha256_16 `eab0a9261dde8c46` (552 rows × 13 cols, both trees byte-identical)
