---
id: BRITTLE-20260806T032402Z-519d5c2f
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15O
created_at: "2026-08-06T03:24:02.168569Z"
source_commit: b56284b
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15O_TEACHER_BRIDGE_20260806T030443Z.md
local_source_sha256: 4c19371203647ed548d5d45d06c5a0872a983ba3c6b718a76a79d61c8fc80f4c
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15O_TEACHER_BRIDGE_20260806T030443Z
mirror_bytes: 6651
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15O_TEACHER_BRIDGE_20260806T030443Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15O_TEACHER_BRIDGE_20260806T030443Z.md` |
| sha256 | `4c19371203647ed548d5d45d06c5a0872a983ba3c6b718a76a79d61c8fc80f4c` |
| brittle commit | `b56284b` |
| lane | locomotion |
| unit | 12U-C11S15O |
| mirrored at | 2026-08-06T03:24:02.168569Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15O — Historical Turner Distillation-Bridge Construction: DYNAMIC TURN TEACHER BRIDGE READY

**date:** 2026-08-06 · **unit:** 12U-C11S15O · **lane:** locomotion · construction + preflight; **no PPO launch, no promotion, no banking, no canonical mutation, no interface change, no runtime teacher/reference injection; the card refuses live launch (exit 93)**
**constitution:** C1, C2, C3, C5, C6, C7, C8, C9 · **potential violations:** none (cross-contract diagnostic disclosed §3) · **amendment proposed:** no
**final decision (exactly one):** **`DYNAMIC TURN TEACHER BRIDGE READY`**
**honest label:** training-time bridge from the K1-recovered July turner into a current-contract κ=+2 specialist. The deployed actor stays one reference-free 54-in current-contract policy; the ONLY bridge artifacts are a teacher-action table and a straight-retention replay set (both training losses, sha-bound into the reward contract).
**changelog:** row 64 `12U-C11S15O, 20260806T030443Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. Teacher dataset (native era stack, full provenance)

Era tree reconstructed by worktree at `44aa372`; run `p12t_pteach_s0_20260723T152708Z` intact on disk (ckpt `4c911c4c`, resolved fp `3e76d401`, era plant `2e38a5f4`, contract cmdphase54_v2_bodyangvel). 48 worlds round-robin, **8 on the positive member kP200-c1.00 (κ=+2.0, vx_design 0.153, cadence 1.0** — the current specialist speed class). Rollout 1400 steps, deterministic, injection-free, additive-DR OFF (nominal plant declared): **0/8 falls; all 8 traces TURNING_LEFT** (full-run worlds κ 1.75/1.95 over 4.16 m; tor-v1.6.0). **Dataset `882223acca9e7c50`: 11,184 samples** — state, phase, path-frame anchor, teacher action, next state, contacts, per-step world-max torque (P99 9.39), per-window realized κ (median 0.988), full provenance JSON embedded. Era-sim note: 44aa372's batch_sim predates the per-world tempo setter (reward-side only; dynamics unaffected; disclosed).

## 2. Physical mapping (only shared quantities)

qpos/qvel map 1:1 (same skeleton). Actions map by the **Δq_des-preserving rescale**: era hip-yaw action scale 0.28 vs current 0.20 → ratio 1.4 on dims [0,6], 1.0 elsewhere; **clip 2.3%** of mapped samples (disclosed). Law delta (era→current): ankle-roll kd 1.0→0.2 (walking-law v2 — the current stack is the transfer target, applied in every probe). No old-contract observation is ever mapped.

## 3. Transfer proofs (current plant v2 + walking-law v2; receipts `o_transfer_receipts.json`)

**A (decisive, disclosed diagnostic — era obs to the era actor only):** the teacher CLOSED-LOOP on plant v2, era obs semantics reconstructed from current-plant states, its own member solo, actions Δq_des-mapped: **0/24 falls over 1400 steps, TURNING_LEFT κ_run 1.51 over 4.42 m** (τP99 9.4, lift 27.6 mm; trace `9fd358cb`). The historical behavior survives the plant+law change. **B:** open-loop mapped teacher-ACTION replay from 24 mapped dataset states, 120-frame branches: **0/24 falls, κ_med 1.116, 24/24 valid** — the actions themselves are dynamically transferable (contrast N2: kinematic-label replay fell 8/8). **C:** mapped states hosted under the CURRENT contract: L2-final falls 11/24 from deep-arc states (the competence gap training must close — states are feasible, the current walker isn't arc-competent); distillation gap |a_L2F − a_teacher| mean 0.218 / p95 0.634 / hip-yaw 0.514. **D:** straight-retention replay set: L2-final on the current slow-straight member, **24/24 fall-free, 16,800 (obs, action) pairs** (`e51b20b930caaa81`).

## 4. Bridge artifacts + candidate mechanisms (in-tree, candidate `28ad39948a9b1836`)

**Teacher-action table** (`teacher_action_table_local.npz`, `910c60ed29e218a0`): per-frame MEAN mapped teacher action, member-local [0,648), NaN outside coverage — **zone coverage 1.00**, per-phase action std 0.091 (the teacher is near-phase-functional on its limit cycle — a table is a faithful target). **P24** (`--n-sup-table`): supervision targets load from the table instead of kinematic jq labels; NaN rows masked (fail-closed); zone-coverage ≥0.5 asserted; table sha recorded in the reward contract. Dims default ALL-12 — these are **dynamic teacher actions** (probe B), not the N2-closed kinematic class. **P25** (`--n-ret-set/--n-ret-w`): straight-retention replay head — w·MSE(actor_mean(obs), action) over random minibatches of the banked set; training-only, deployment-free; set sha in the contract. **Weights:** w_sup **0.25** (gradient receipt: g_ppo 2.06 vs g_sup@1 6.41 → nearest bounded; realized ratio 0.777, disclosed above the 0.5 target); w_ret **0.25 by rationale** — its init-gradient is 0 BY CONSTRUCTION (targets are the parent's own actions; the head is a drift-proportional trust-region; disclosed).

## 5. Member extraction + mirror

The card member is the **teacher's own** kP200-c1 slice from the era concat (the ccref9_sym rebuild's kP200 is a different generation — vx_design 0.255 vs the teacher's 0.153): base `cdca240c`, **kP200 `26c1e4f890684cc3`** (oracle `--member`: TURNING_LEFT κ 1.991, R 0.502 — the K1 numbers), mirror kM200 `4645c76e` via the mirror map **re-fitted and validated on the certified ccref9 pair** (all fields exact 0.0; 1 ULP at π on the yaw involution, disclosed). Injector suite **11/11** on this family; oracle regression 16/16 sha-bound.

## 6. Proposed acquisition card (prepared, NOT launched)

`launch_c11s15o_kp.sh` (sha **`fe37cf64971734bc`**): L2-final init + bookend-only anchor (P21, zone [862,1111)), solo κ=+2.0, current contract/obs/actions, P24 teacher table + P25 retention head, member-scoped arc-rsi 0.85, yaw-dominant 8, log_std −2.0 (P18), P21b global-step occupancy gate, ceiling 2,097,152, evals 262,144, `O_AUTHORIZED` gate, table+set sha pins, dry ×2 → **fingerprint `8689c69b4f6071da` STABLE** (carries table + retention shas). Qualification stays injection-free native-start tor-v1.6.0; no persistent runtime teacher anywhere.

## 7. Artifacts

`rgl/artifacts/c11s15o_bridge_20260806T030443Z/` (14 MB): teacher dataset + receipt, action tables (era-abs + local), transfer receipts + traces, family_era ×3 + mirror-fit receipt + member oracle, retention set, weight receipt, card + dry ×2 + resolved config, injector log, `o_teacher_dataset.py`/`o_transfer_probes.py`/`o_extract_member.py`/`o_weight_receipt.py`. Candidate P24/P25 in-tree. **Next action is Joe's: authorize the bridge acquisition (Guard sentences; re-pin candidate `28ad3994` + launcher `fe37cf64` + fp `8689c69b` + family/table/set records; one unused seed ≥3).**
