---
id: BRITTLE-20260809T145434Z-13f1ca28
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15CY
created_at: "2026-08-09T14:54:34.330147Z"
source_commit: ae2995e
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15CY_CADENCE_SCHEMA_REFREEZE_20260809T210000Z.md
local_source_sha256: 13543814c881abb003ecd2e94f34cbf0c44b5aad2a1835d59c031614d5173afc
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15CY_CADENCE_SCHEMA_REFREEZE_20260809T210000Z
mirror_bytes: 5781
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15CY_CADENCE_SCHEMA_REFREEZE_20260809T210000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15CY_CADENCE_SCHEMA_REFREEZE_20260809T210000Z.md` |
| sha256 | `13543814c881abb003ecd2e94f34cbf0c44b5aad2a1835d59c031614d5173afc` |
| brittle commit | `ae2995e` |
| lane | locomotion |
| unit | 12U-C11S15CY |
| mirrored at | 2026-08-09T14:54:34.330147Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15CY — Schema Re-Freeze: **`CADENCE 884K SCHEMA RE-FREEZE READY`**

**date:** 2026-08-09 · **unit:** 12U-C11S15CY · **lane:** locomotion
**constitution:** C1, C6, C12 · **potential violations:** none · **amendment proposed:** no
**prohibitions honoured:** NO PPO · NO Guard mutation · NO campaign bootstrap · NO decision resolution · NO cadence/reward/gate/trainer/launcher redesign · NO dependency-set changes · quarantined checkpoint untouched
**changelog:** row 117 · post-append csv `sha256_16` **`717e4a2f7c788112`**

## The rename

```
evidence-q10-partA-c11s15cy   ->   evidence-q10-parta-c11s15cy
```

`targets.schema.json` constrains `target_id` to **`^[a-z0-9][a-z0-9._-]*$`**. The capital `A`
failed it, so `target register` would have refused the payload at step 4 of the sequence.

**Why this one and not the other 46:** the 32 closure-derived ids are generated from their file
paths, and those paths are lowercase — the pattern holds by construction. The two
evidence-provenance ids were **hand-written** from the ticket's prose, which is where `partA`
came in. The builder now validates the merged document with **Guard's own** `minischema` against
the live schema and refuses on any violation, so a payload Guard would reject can no longer leave
the producer.

## New hashes

| artifact | sha256_16 |
|---|---|
| **registration payload** `proofs/item3_targets_signcheck_final.json` | **`fcb6201cf4d4b4f5`** |
| **construction digest** | **`843f7df512743896`** (was `84f6675ba8cd0cb9`) |
| **owner ruling** `OWNER_RULING_C11S15CY.json` | **`112a9865a0a257d4`** |
| **authority package** `proofs/item5_signcheck_authority_package.json` | **`b7bb0f8c5dc540db`** |
| **campaign commit** | **`d314cb51e37b3f23bbc41047775099438a6b4925`** |
| trust-root revalidation `proofs/item5_trust_root_revalidation.json` | `8857388cc3e39e1e` |
| invariants receipt `proofs/item1_schema_refreeze_invariants.json` | `32c5bf00d26fbf58` |

**Counts unchanged: 94 construction members · 47 payload records · 48 canonical-mutation subjects.**

### Decision ids — one moved, and only one

| | row 116 | now |
|---|---|---|
| I `cadsign_audit_in` | `D-1bfc8997bd2f` | `D-1bfc8997bd2f` |
| **J `cadsign_canonical_mutation`** | `D-31e634a20176` | **`D-3bd0df21fb14`** |
| K `cadsign_audit_out` | `D-e9a56237be4c` | `D-e9a56237be4c` |
| L `cadsign_ppo` | `D-b473762cfa7a` | `D-b473762cfa7a` |

Only **J** moved, and it had to: `subject_refs` enters the decision preimage, and J is the only
sentence naming the renamed target. I, K and L do not name it, so their ids are unchanged —
which is itself a check that the rename did not leak beyond its scope. All four remain
**ADMISSIBLE** against the live validator and **4/4 distinct**.

## Proofs — 22/22

`proofs/cy_schema_refreeze_invariants.py`, diffing against the **row-116 payload read from git**
(`338dc689…`) rather than from the working tree, so the comparison cannot be contaminated by the
edit under test:

| check | result |
|---|---|
| merged target document validates against the live schema (Guard's own validator) | **0 errors** |
| all 47 target ids satisfy `^[a-z0-9][a-z0-9._-]*$` | **0 violations** |
| the **prior** payload DID violate it (the fix was necessary, not cosmetic) | `['evidence-q10-partA-c11s15cy']` |
| payload record count remains 47 | **47** |
| member count remains 94 | **94** |
| canonical-mutation subject count remains 48 | **48** |
| construction members: exactly one removed, and it is the invalid id | ✓ |
| construction members: exactly one added, and it is the renamed id | ✓ |
| target records: exactly one id removed / one added | ✓ |
| **every other target record byte-identical** | **0 drifted** |
| the renamed record differs ONLY in its id (path and hash identical) | `q10_partA.py` · `add92c59ae2f7e30` |
| **every bound file hash unchanged** (modulo the rename) | **46 file-bearing records compared** |
| launcher remains `4f24bdaedf51529c` | ✓ |
| slope gate remains `1125b30033225ed5` | ✓ |
| trainer remains `a0f2aad7275075a7` | ✓ |
| reward book remains `9ea649c985a114de` | ✓ |
| weight remains **19.5** | ✓ |
| gate bar remains **−2.2**, cells `(0.70, 0.85, 1.00)` | ✓ |
| construction digest reproducible from the member list | `843f7df512743896` |

**The file on disk was not touched.** `q10_partA.py` keeps its filename and its pin
`add92c59ae2f7e30`; only the Guard *identifier* changed. No scientific artifact was modified to
make any hash convenient — the launcher, gate, trainer and reward book are byte-identical to
row 116, and the −2.2 gate receipt (`b3281ed55c3d76a3`, slope −2.7401, FAIL) is untouched.

## Trust root

Re-validated against the **exact** new commit with the shipped `ledger._check_source_document`,
on a copy of the live store (never written):

| | |
|---|---|
| proposed root | `d314cb51e37b3f23bbc41047775099438a6b4925` (confirmed `HEAD`) |
| ACTIVE decisions / carrying an owner source | 124 / 124 |
| **broken by the move** | **0** |
| repaired by the move | 5 |

**SAFE TO MOVE THE TRUST ROOT.**

## Owner-authorized sequence — unchanged, nothing executed

1. `campaign-bootstrap-commit` to `d314cb51e37b3f23bbc41047775099438a6b4925`
2. resolve **I** — CY `ACQUISITION → POST_ACQUISITION_AUDIT`
3. resolve **J** — `canonical_mutation` over the 48 subjects
4. `target register --targets-file proofs/item3_targets_signcheck_final.json --authorized-by <D_J>`
5. resolve **K** — back to `ACQUISITION`
6. resolve **L** — new `ppo` on `launcher-c11s15cy-signcheck`
7. **one** `run guarded … -- bash <sign-check launcher> rl/runs/walking/cy_cadsign_s0`
8. read `$OUT/cadence_gate.json`

**No PPO. No Guard mutation. No campaign bootstrap. Nothing registered, resolved or launched.**
