---
id: BRITTLE-20260805T155721Z-7cce8f51
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15K
created_at: "2026-08-05T15:57:21.014258Z"
source_commit: 75e41d6
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15K_ORACLE_RCA_INTEGRATION_20260805T100000Z.md
local_source_sha256: 7c7e6de362043862826b1bd7dcba15b77e0f05a4be9471e49ae9220904361890
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15K_ORACLE_RCA_INTEGRATION_20260805T100000Z
mirror_bytes: 6762
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15K_ORACLE_RCA_INTEGRATION_20260805T100000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15K_ORACLE_RCA_INTEGRATION_20260805T100000Z.md` |
| sha256 | `7c7e6de362043862826b1bd7dcba15b77e0f05a4be9471e49ae9220904361890` |
| brittle commit | `75e41d6` |
| lane | locomotion |
| unit | 12U-C11S15K |
| mirrored at | 2026-08-05T15:57:21.014258Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15K — Mandatory Turning Oracle + Failure-RCA Integration: ORACLE AND RCA LOOP INTEGRATED

**date:** 2026-08-05 · **unit:** 12U-C11S15K · **lane:** locomotion · infrastructure only; no PPO, no continuation, no reward/interface/reference change, no promotion, no canonical-policy mutation
**constitution:** C1, C2, C4, C6, C8, C12 · **potential violations:** none · **amendment proposed:** no (playbook enforcement only; law count unchanged)
**final decision (exactly one):** **`ORACLE AND RCA LOOP INTEGRATED`**
**changelog:** row 50 `12U-C11S15K, 20260805T100000Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. Oracle contract frozen — `tor-v1.0.0`

`rl/scripts/turning_oracle.py` (sha `a2b5f4da41f0eadb` bound into the regression record). Classification enum per spec: `STANDING / STRAIGHT / TURNING_LEFT / TURNING_RIGHT / FIGURE_EIGHT / SPIN_OR_PIVOT / FALL / INVALID_TRACE`. Outputs: commanded + realized signed curvature, realized radius, valid translation, tangent alignment, cross-track, turn runs, excluded segments with reasons, teleport/DONE/fall contamination counts, pelvis-rotation (never used for qualification), oracle version + trace sha in every result. Preserved validated requirements: cycle-synchronous smoothing (66-frame gait box), sustained same-sign runs (|κ|≥0.8 over ≥0.4 m), heading = body yaw − π/2, no statistics across DONE/reset/RSI/teleport boundaries, no turning without translation, body-yaw never sufficient, visual disagreement blocks claims (report rule + T12).

## 2. Regression protection — 12 frozen controls, all green

`rl/scripts/turning_oracle_regression.py` → `rl/runs/oracle_regression/latest.json` (PASS bound to the oracle's sha — any edit stales the record and fails the launch gate). Positive: `member_kP` (+2.02, R 0.49), `member_kM` (−1.78, R 0.56), kP200/kM200 (±1.84–1.86), figure-eight (BOTH signs). Negative: G-final STRAIGHT, H-final STRAIGHT, H-1M STANDING, synthetic standing/spin/straight, synthetic teleport contamination (detected AND never classified turning).

## 3. Launcher / evaluator / report behavior

**Launcher**: `rgl/scripts/locomotion_intent_gate.sh` — `declare_intent` (designed_behavior / primary_metric / target / minimum_acceptable; **accumulated-yaw primary metrics refused**, exit 92), `check_oracle_regression` (missing/failing/sha-stale → refuse), `require_rca_clearance <parent>` (sect-10 hard gate, exit 93; exact-run binding). Mandatory for every relevant run per the PRE_TRAINING_CONTRACT §K addition; historical pinned cards predate the block and every NEW construction (already Joe-gated) must carry it — no parallel launcher stack created. **Evaluator**: `rl/scripts/turning_eval_battery.py` — saves the authoritative trace (injection disabled), runs the oracle on THAT trace (same-trace requirement), writes the identity block (checkpoint/config/fingerprint/plant/manifest/reference/trace hashes + oracle version), compares to the declared target, marks `RCA_REQUIRED` on miss, and refuses `is_turning_checkpoint` unless the oracle says TURNING_*; ranking law = realized signed curvature vs target (CHECKPOINT_MINING §K). **Reports**: MOTION_CLAIM_REVIEW §K mandates the intended-outcome block, the full oracle block, exactly one visual-agreement line, and exactly one outcome line; misses require the RCA result before any training recommendation. **Viewer**: `rl/scripts/turning_review_viewer.py` (on-screen labels, hashes, frame counter, commanded path, trail, forward ray; state==trace asserted).

## 4. RCA state machine

`rl/scripts/locomotion_rca.py`: `require → complete(record with all 10 layers + closed-set RCA-9) → clear(8 mandatory clearance fields) → check` (launchers fail closed). Procedure: `playbooks/LOCOMOTION_RCA.md` (triggers, forbidden shortcuts, the RCA-1…10 layers, hard gate); `DEEP_RCA.md` cross-linked. Trigger list and "do not immediately train longer / reweight / add supervision" prohibitions embedded verbatim.

## 5. Regression tests — 16/16 PASS

`rl/scripts/test_c15k_oracle_rca.py`: intent-field refusal · straight-intent acceptance · accumulated-yaw refusal · positive controls · negative controls · teleport rejection · regression-freshness gate · miss→RCA_REQUIRED · launch blocked pre-clearance (both states) · clearance unblocks · clearance exact-run binding · trace-sha in results · oracle version in results · spin-never-turning · visual-disagreement blocks · command-insensitive naming rule. Two defects found by the tests during integration (dash-vs-bash, gate tail-status) — fixed and re-run green.

## 6. Existing-run reconciliation (`rl/runs/rca_state/ARC_STATE.json`)

G-final (`78298a7d`) and H-final (`4553633d`) = oracle-classified STRAIGHT; H-1M (`383c074e`) = STANDING with the accumulated-yaw turning claim retracted; C11S15J controls = the regression basis; next-training = **RCA-GATED**. The real RCA for the G/H turning miss is filed (`c11s15k_integration_.../rca_record_c11s15h_turnmiss.json`, all 10 layers built from the arc's audited evidence): **primary = OBSERVATION/COMMAND-CONDITIONING DEFECT** (single-member lanes cannot force command-reading; measured blindness), secondary = optimization/exploration + the historical metric defect (fixed). Both parent runs walked the full chain to **CLEARED**, carrying the RCA-10 experiment (paired ±κ members, frozen everything else, oracle thresholds ±1.4 on ≥50% per side, ceiling 4,194,304, stop rules). **Clearance ≠ authorization**: the paired-±κ launch still requires Joe's construction ticket + Guard sentences — not run in this unit. **Unresolved historical evidence**: the p12t learned-turn same-trace closure remains OPEN (no era rollout traces exist; the policies are cross-contract diagnostic-only; kinematic controls do not replace it).

## 7. Files changed / integration points

New: `rl/scripts/turning_oracle.py`, `turning_oracle_regression.py`, `turning_eval_battery.py`, `turning_trace_capture.py`, `turning_review_viewer.py`, `locomotion_rca.py`, `test_c15k_oracle_rca.py`, `rgl/scripts/locomotion_intent_gate.sh`, `playbooks/LOCOMOTION_RCA.md`, RCA/clearance/ARC_STATE records, regression record. Appended: `playbooks/PRE_TRAINING_CONTRACT.md`, `CHECKPOINT_MINING.md`, `MOTION_CLAIM_REVIEW.md`, `REWARD_INCOME_PROBE.md`, `DEEP_RCA.md` (§12U-C11S15K blocks; mirrored to both trees — the main tree lacked two playbooks entirely and now carries them). No parallel stacks; no Guard-pinned file touched (canonical trainer, candidate, cards all unchanged — verified by the pins).

## 8. Next owner decision

The paired ±κ acquisition construction (RCA-cleared, oracle-gated, intent-gated) awaits Joe's ticket; nothing else is pending.
