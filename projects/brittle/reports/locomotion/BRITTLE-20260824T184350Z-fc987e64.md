---
id: BRITTLE-20260824T184350Z-fc987e64
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: WALK-DR05
created_at: "2026-08-24T18:43:50.155230Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR05_SENSOR_DR_STAGED_20260824T180003Z.md
local_source_sha256: a68338ce8bee452ce47af30d4db9facf6c1cfad50b4993b9823bc9f73cd127a2
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: GREEN_OPUS5__WALK_DR05_SENSOR_DR_STAGED_20260824T180003Z
mirror_bytes: 10431
---
# GREEN_OPUS5__WALK_DR05_SENSOR_DR_STAGED_20260824T180003Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR05_SENSOR_DR_STAGED_20260824T180003Z.md` |
| sha256 | `a68338ce8bee452ce47af30d4db9facf6c1cfad50b4993b9823bc9f73cd127a2` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | WALK-DR05 |
| mirrored at | 2026-08-24T18:43:50.155230Z |

<!-- brittle:mirrored-content -->

# TEAM: GREEN — Opus 5 · WALK-DR05 — sensor-noise DR + estimator integration: FULLY STAGED and STOPPED at the Guard authorization boundary. The estimator is now IN the training loop for the first time (the Crown trained on TRUE base velocity and deploys on leg-odometry — a train/deploy gap this unit closes): a host-side α=0.4 leg-odometry port proven bit-identical-to-numerical-noise against the deployed math (8.7e-15 m/s), consuming the SAME per-step noisy encoder/gyro draws as the policy obs, nominal-anchored σ mixture per episode. Card DRY-validates end-to-end (pins · viewer preflight · argv assertions · parity receipt MEASURED-chain · dry-proof, zero optimizer steps). The F8 stand-in-distribution variant is staged but BLOCKED by its own parity gate (cyclic 0.746 — an f8-lane property; recalibration is Joe's). RECOMMENDED: launch the BASE card. Owner sentences below. ZERO training has occurred.

**unit:** WALK-DR05 · **ts:** 20260824T180003Z · **team:** GREEN (ticket header: Opus 5; executing session: Claude Fable 5)
**foundation:** frozen Phase-12 Crown `876025a9cb883f37` (α=0.4 EXPERIMENTAL — canonical runtime/manifest untouched, per ticket).
**constitution:** C1, C2, C3, C6, C9, C12 · **potential violations:** none · **amendment proposed:** no
Ticket: bus `BRITTLE-20260824T175958Z-57dae097` (owner-direct 2026-08-24).

---

## 0 · OWNER DECISION PACKAGE (the unit's STOP)

Guard live state: campaign bound to **WALK-DR03** (`campaign status --check ppo` → "authorized by D-6e9513762448 for unit WALK-DR03") — no WALK-DR05 launch is sanctioned and no agent may author the ruling. Joe's sentences (plain, no conditionals; the standard chain — bootstrap-commit → decision request/resolve → transition → target register → run guarded — is driven by me once the sentences exist):

1. `I authorize transition to WALK-DR05 in the acquisition lifecycle, from WALK-DR03.`
2. `I authorize ppo for WALK-DR05 on launcher-walkdr05-sensor, 1 seed of 884736 steps with a 884736 step ceiling.`
3. `I authorize transition to WALK-DR05 in the pretraining certification lifecycle, preparing the sensor noise launch.`
4. `I authorize canonical mutation for WALK-DR05 to register the sensor noise launcher and to re-pin the trainer, the batch sim and the gpu module.`

If Joe prefers standing IN the training distribution instead (§4 below), sentence 2 names `launcher-walkdr05-sensor-f8` and a fifth ruling is required to recalibrate the card's cyclic-fraction gate for the f8 lane (0.746 measured vs the walking-lane 1.0 invariant) — I will not touch that gate without it.

## 1 · What is new (and proven) — the estimator in the loop

**Discovery that shaped the unit:** the GPU training substrate's obs[6:9] is the TRUE root velocity (`tip_observation_kernel`, kernels_tip.py:1439-1444); the leg-odometry estimator exists only in deployment. "Estimator in the loop" therefore meant BUILDING it into training. Implementation is HOST-side (no Warp kernel or CUDA-graph surgery): `rl/mjw/walkdr05_sensor.py` applied by a single flag-gated hook (`_dr05_apply`) on the live 50-d obs block in the qualified trainer, before `_augment_obs`.

Proofs (all green, `walkdr05_sensor.py` selftests + smoke):
- **FK**: vectorized leg-chain FK vs the deployed `EstimatorFK` — scalar replica 2.9e-16 m; vectorized vs scalar 0.0 (bit-exact). Constants READ from the live model (never copied).
- **α-identity (§4)**: streaming a recorded WALK-DR05A trace through the trainer's estimator reproduces the recorded α=0.4 estimate to **8.7e-15 m/s** (float64); the production float32 obs pathway adds a bounded 1.1e-6 m/s quantization (5 orders below the estimator's 0.088 RMS).
- **Channels consumed / no hidden clean path (§4)**: with noise on, obs q-dims move AND the estimator diverges from a clean twin on identical true state (noise reached it); with the estimator on, obs[6:9]==estimate. The hook is the only write site (single call in step() + reset()); PD/physics/reward stay clean (declared, matching every qualified eval instrument).
- **Noise semantics = the eval instruments**: two independent draws per step (obs draw, estimator draw — the DR04/05A Perturber sequential contract that produced every acceptance boundary), per-episode per-world σ mixture (30% exact zero-noise; σ~U(0,hi), hi = 0.25° / 0.30 rad/s / 0.10 rad/s — the ticket §1 bands), white per policy step.
- **Flags-off neutrality**: the hook returns its input untouched (no allocation, no draw); trainer/batch-sim edits are additive (one accessor `get_knee_tau`, read-only, graph-neutral); full end-to-end DRY run under the card reproduces the lineage parity receipt (law, plant `81d4e6d8226ed12b`, kp/kd/τ/ACD, cyclic 0.99856 — the DR02-recalibrated band).
- **GPU smoke**: the real trainer, real MJWarp, sensor flags ON — receipt `DR05_SENSOR_RECEIPT {est_alpha 0.4, nom_frac 0.3, ...}` printed, one full 2304-world rollout batch collected, healthy return strata, dry-proof written, zero optimizer steps.

## 2 · The card (base, RECOMMENDED)

`rgl/artifacts/walkdr05_sensor_pilot_20260824T180003Z/launch_walkdr05_sensor.sh` — copy+marked-deltas from the DR03 card (`84409f219454fd68`): OUT `rl/runs/walking/walkdr05_sensor_s0`; trainer re-pin `ca910749a1b5889c`; dep pins now 13 modules (adds `batch_sim_tip 71490259fd522e08`, `walkdr05_sensor 0509718c6f93319b`, `walkdr05_estfk 8dcc39d720bdd101`, the viewer + CPU-eval chain); **--dr-shell REMOVED** (§3 isolation: no kp/mass/terrain/friction/latency DR; `--no-additive-dr` asserted — actuation exact-nominal per world); **+ `--est-in-loop-alpha 0.4` + `--sensor-dr 0.30,0.25,0.30,0.10` + `--sensor-dr-seed 20260824`** (the causal change set); anchor unchanged (frozen Crown, w0 0.15, decay 8388608); 1 seed × 884,736 steps, checkpoints every 147,456 (6 + final). Argv assertions extended fail-closed (flag presence + exact values + `--dr-shell` explicitly REFUSED). DRY mode: **PASS end-to-end.**

**Live inspector (§8):** `rl/isaac_parity/walkdr05_viewer.py` — follows the newest settled checkpoint, exports its bundle, and visibly cycles STAND → NOMINAL WALK → TURNING (κ+0.35) → NOISY WALK (encq 0.25°) under the α=0.4 CPU runtime; supervised by the card's §-9 RESTART-NOT-KILL loop (5 bounded restarts; a healthy trainer is never killed). Preflight through a crown-checkpoint fixture is part of the card (caught and fixed: the old bank-actor fixture has a different architecture — `CrownActor` refuses it, disclosed).

## 3 · Checkpoint qualification (§5/§6 — three batteries, never collapsed)

`rl/isaac_parity/walkdr05_monitor.py` per checkpoint (MuJoCo truth arm, α=0.4 runtime, per-ckpt bundle):
- **A STAND**: 0 falls · **0 physical foot-foot overlap frames (hard invariant)** · min-sep > 0 · DS within 1.0× materiality of the Crown α1.0 basis.
- **B NOMINAL WALK**: 12-cell battery — 0 falls · worst materiality ≤ 2× · τ99 ≤ 12 · zero overlap · κ-symmetry tracked.
- **C NOISY WALK**: encq {0.10, 0.25, 0.5}° + encqd {0.3} + gyro {0.15} — §6 targets: 0.10° clean, 0.25° clean, 0.5° < the α0.4 frozen baseline 10/12, encqd/gyro ≤ baseline.
Candidate = **two CONSECUTIVE checkpoints** with all three verdicts PASS (no transient rescue); then Isaac confirmation (visible viewer) on the candidate; cross-engine regression = FAIL (§7).

## 4 · The standing question (§2) — decision for Joe

The lineage training distribution contains **zero stand worlds** (22-member manifold, vx ≥ 0.10; vx=0 outside the f8 machinery is stall-terminated at 4.5 s and collects no stand income — measured in the audit). Two staged options:
- **BASE card (recommended)**: stand is NOT in the training distribution; the stand co-gate is measured per checkpoint (battery A). Plausible repair mechanism: noise-DR training deweights the policy's reliance on the poisoned obs[6:9] channel, which is exactly the channel whose α-filtering shifts the stand posture (DR05A: −1–1.7 mm sole brush). If battery A never passes, the pilot FAILS its co-gate honestly and the f8 route is next.
- **F8 variant** (`launch_walkdr05_sensor_f8.sh`): commanded stands legal in training via the qualified f8 start/stop machinery, moderated mode probabilities (`--f8-mode-probs 0.70,0.10,0.10,0.05,0.05:0.55,...`, a new flag, default = built-ins = byte-identical). DISCLOSED: activates the existing f8 stand-income terms (an income-book activation vs the lineage card) and its DRY run **fails the card's cyclic-fraction gate (0.746 vs 1.0)** — a true f8-lane property, not a defect; unblocking requires Joe's ruling to recalibrate that gate for the f8 lane. Staged, not runnable today.

## 5 · After the grant (pre-declared)

Guard chain on sentences 1–4 → `run guarded` on the base card → pilot (MJWarp, minutes-scale) with the §8 viewer live → `walkdr05_monitor.py --all` over the 7 checkpoints (three batteries each) → candidate by consecutive qualification → Isaac confirmation (visible viewer, receipt-gated sensor patches) → §9 verdict (SENSOR/ESTIMATOR HARDENING PASS iff stand collision-free + nominal preserved + noise tolerance materially expanded in BOTH engines) → final report → STOP for Joe. No latency/terrain training follows automatically (§9).

## 6 · Scope compliance + artifacts

ZERO training (dry-proof only — "NO OPTIMIZER STEP" asserted in-log). Canonical runtime, manifest, actor, obs dims/meanings, action interface, commands, cadence law, plant, reward formulation, torque limits, terrain: untouched. Trainer/batch-sim edits are flag-gated additive (defaults byte-identical); no other DR family present (asserted in argv). α=0.4 remains experimental.

- Staging: `rgl/artifacts/walkdr05_sensor_pilot_20260824T180003Z/` (both cards + this staging chain's DRY receipts in `rl/runs/walking/walkdr05_sensor_s0/` incl. `parity_receipt.json`, `tc_dryproof.json`).
- Code: `rl/mjw/walkdr05_sensor.py` (+ selftests), `rl/mjw/walkdr05_estfk.py`, trainer hook + flags in `rl/mjw/train_tip_reftrack.py` (`ca910749a1b5889c`), `get_knee_tau` in `rl/mjw/batch_sim_tip.py` (`71490259fd522e08`), `rl/isaac_parity/walkdr05_monitor.py`, `walkdr05_viewer.py`.

STOP for Joe — awaiting the §0 sentences.

**changelog row:** `WALK-DR05, 20260824T180003Z` — post-append `rgl/LOCOMOTION_CHANGELOG.csv` sha256_16 `8e884331f5d7b332` (484 rows × 13 cols, both trees byte-identical)
