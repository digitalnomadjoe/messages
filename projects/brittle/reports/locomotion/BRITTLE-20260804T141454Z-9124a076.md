---
id: BRITTLE-20260804T141454Z-9124a076
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S14A
created_at: "2026-08-04T14:14:54.891653Z"
source_commit: ca38ba2
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S14A_STARTSTOP_PREFLIGHT_20260804T141454Z.md
local_source_sha256: 4492b61b4ba20ff95e5b7c96a7d489522e4dcead092cc2339d53aaabc7e16ac9
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S14A_STARTSTOP_PREFLIGHT_20260804T141454Z
mirror_bytes: 6912
---
# UNIFIED_LOCOMOTION_PHASE12UC11S14A_STARTSTOP_PREFLIGHT_20260804T141454Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S14A_STARTSTOP_PREFLIGHT_20260804T141454Z.md` |
| sha256 | `4492b61b4ba20ff95e5b7c96a7d489522e4dcead092cc2339d53aaabc7e16ac9` |
| brittle commit | `ca38ba2` |
| lane | locomotion |
| unit | 12U-C11S14A |
| mirrored at | 2026-08-04T14:14:54.891653Z |

<!-- brittle:mirrored-content -->

# 12U-C11S14A — Baseline Correction and Start/Stop Preflight

**date:** 2026-08-04 · **unit:** 12U-C11S14A · **lane:** locomotion · no PPO
**constitution:** C1, C2, C3, C7, C9, C12 · **potential violations:** none · **amendment proposed:** no
**status:** baseline corrected + banked; machinery + behavioral audits complete; **one minimum start/stop PPO construction returned (§4)**. Rebind payload staged — **stopped for the three corrective sentences** (final section).
**recommend:** issue the sentences; then the C11S14B ticket can authorize the exit-arc member build + contract widening + PPO.
**decision:** Joe — sentences now; construction ruling next
**next:** await sentences

---

## 1. Baseline correction (landed)

- **Banked:** `rl/models/walking/mechanics_baseline_c11s12_18874368/` — C11S12 `s2/ppo_18874368` (sha1 in-dir), CARD.md marked **MECHANICS MILESTONE — NOT A CHAMPION** (deferred-polish gate cited), runtime manifest + plant id included. C11S13B σ35 checkpoints REJECTED for baseline use.
- **σ_foot restored 35 → 60 mm** (trainer `79a141934ab9ee63`): the resolved book is **bit-identical to the C11S12 baseline book — fingerprint `59d492060bdd3fe1` reproduced exactly**. Settings registry re-pinned (`37b8391bca820339`); 4-target rebind payload staged (`targets_payload_c11s14.json`). Roadmap/manifest naming: exact checkpoint + fingerprint recorded here and in the CARD.

## 2. Machinery audit (`_f8_*`, read-only)

- **Activation:** `--f8-sched` requires the family-manifold path (`--ref-family` + `--cadence-set` + `--cmd-phase`) whose members carry `cmd_vx`/`cmd_cadence` tables AND a standing pad (fail-closed checks at :4257).
- **Schedule:** per-world modes {walk, stop, hold, start, **cycle** (walk→stop→hold→restart…)} sampled per respawn (anchor worlds walk-heavy 0.55; curriculum worlds 0.10/0.25/0.15/0.20/0.30); stop_t∈[150,400], start_t∈[50,150], taper_grace 75, holds 60–120 steps; commands drive per-world `cadence_cmd`/`vx_cmd` + reward targets.
- **Command observation surface:** the **cmdphase54 contract** — `[obs50 | sinφ cosφ | vx_cmd | cadence_cmd/2]` (+κ̂ col if `obs_kappa_cmd`). Commands ride NEW columns 52–53; obs45 stays the yaw profile — **no dim-semantics inversion risk**.
- **Protections already structural (all previously measured):** commanded stands refresh the real-step ET clock (no ET murder of stands); `sp_w=10` stand scoping zeroes cadence income during stands; the C11S8B latch is born unarmed and re-arms only on an alternating pair (stand→restart safe); `f8_stand_vel_w=20` velocity fence on commanded stands; altwin_ref pays 0 without alternating touchdowns (stand r_fwd = 0 by construction).
- **Exit-arc family inventory:** `ccref4b_f8mirror3_20260721T162437Z/` (+siblings) — members `ccref_vx026–028_c1p000–1p100[_k±]`, 648 fr, binary `cmd_cadence`∈{0,1}, 60-frame standing pads. **Cadence mismatch: members are 1.00–1.04 s same-foot at vx 0.26–0.28 vs the baseline's 1.36 s @ 0.28** — using them as-is would drag the baseline's stride back; a new member at the c344 gait is required (§4).

## 3. Behavioral audit (banked checkpoint)

- **Command blindness (measured):** full 0→1 swing on obs45 across 400 live walking states moves actions by mean 0.016 (8% of typical |a| 0.194) — incidental coupling, no trained response. The policy cannot stand, stop, or start on command today.
- Commanded stand retention: N/A behaviorally (no command channel) — spawn-standing worlds begin walking unconditionally.
- DS stand → first alternating touchdown: med **64 steps (1.28 s)** from spawn (C11S8B trace).
- Steady walk → stop / last td → stable DS: no trained response (blindness above) — the PPO objective.
- Step-economy silence while standing: **measured 0 on all five channels** (C11S8B); stance-entry slip 2.4 mm; pelvis excursion (walk): z p2p 19.6 mm, roll p2p 10.6°, lat-vel RMS 96 mm/s; touchdown impulse: vz −0.455 m/s (deferred polish defect, on file); τ P99 7.48 / P99.9 < cap, occupancy 0; falls/stalls 0/0 deterministic full-horizon.

## 4. Minimum start/stop PPO construction (returned; nothing landed)

1. **Contract:** cmdphase54 — warm-widen the banked policy: preserve all weights, zero-init two new input columns (52–53) actor-side (critic 58→60 likewise); fresh optimizer; raw_v1. (Contract change = its own ruling, next unit.)
2. **Reference:** ONE new straight exit-arc member at the baseline gait — `ccref_vx031_c0p752` (vx_design 0.31, gait_cycle_hz 0.752 → 1.34 s stride, 60-frame standing pads, binary cmd tables) built by the existing ccref4b generator; candidate under `rgl/artifacts/<unit>/refs/` (canonical untouched); RGL dynamic preflight + FK parity + H-style exact-execution (track 16.000) before adoption; wired via `--ref-family` + `--cadence-set 0.752` + `--cmd-phase` + `--f8-sched`.
3. **Schedule:** the proven f8 mixture and ranges (§2), anchor-mask on ~50% of worlds for walk-quality retention (C9).
4. **Reward channels:** the registered book unchanged (fingerprint semantics preserved) + the f8-native stand fence (`f8_stand_vel_w 20`); no new channels.
5. **Spawn/reset:** 70% standing / 30% arc-RSI unchanged; f8 per-respawn schedule resampling; latch unarmed at birth (verified).
6. **DS entry/exit gates (per eval):** stop→stable DS ≤ 1.5 s after the last alternating td (pelvis vel < 35 mm/s, both feet loaded, zero step-economy events thereafter); start→first alternating td ≤ 1.5 s; walk-segment stride 1.34 ± 0.07 s.
7. **Static C3 adversaries (probe before launch):** stand-farmer under walk command (must stall via et_real and earn no step economy); walk-through-stand (stand-vel fence + zero income must dominate); creep-walker during stand; stop-skipper; restart-without-alternation (latch).
8. **Stopping rules:** fall-rate +15pp twice; commanded-stand fall-rate > 2%; walk-stride outside band 3 consecutive evals; ceiling 24,117,248/seed × 3 seeds (each its own grant sentence).
9. **Visual-review rule:** first checkpoint with start-gate ≥ 90% AND stop-gate ≥ 90% AND stride in band → STOP for Joe with the viewer + close replays of three random stop events and three start events.

## 5. Rebind (staged, awaiting sentences)

Payload `targets_payload_c11s14.json`: trainer `79a141934ab9ee63`, settings `37b8391bca820339`, fingerprint pin `59d492060bdd3fe1`, construction members unchanged. Then ALLOW / tamper-DENY / restore / re-ALLOW per the standing pattern. Required verbatim:
1. `I authorize transition to 12U-C11S12 for the lifecycle advance from ACQUISITION to POST_ACQUISITION_AUDIT.`
2. `I authorize canonical mutation for 12U-C11S12 scoped to: trainer-source-c11s4, canonical-settings-c11s4, reward-fingerprint-c11s12, bound-construction-c11s4-walking.`
3. `I authorize transition to 12U-C11S12 for the lifecycle advance from POST_ACQUISITION_AUDIT to ACQUISITION.`
