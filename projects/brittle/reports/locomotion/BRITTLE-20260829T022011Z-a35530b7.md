---
id: BRITTLE-20260829T022011Z-a35530b7
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: WALK-DR06H3D-RC1
created_at: "2026-08-29T02:20:11.872994Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06H3D_RC1_LAUNCH_READINESS_20260829T021827Z.md
local_source_sha256: 95912da64500580962618c8bb8f714ddfd7ebb57eb839a45b48f7eaa3dc0c82d
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: GREEN_OPUS5__WALK_DR06H3D_RC1_LAUNCH_READINESS_20260829T021827Z
mirror_bytes: 18154
---
# GREEN_OPUS5__WALK_DR06H3D_RC1_LAUNCH_READINESS_20260829T021827Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06H3D_RC1_LAUNCH_READINESS_20260829T021827Z.md` |
| sha256 | `95912da64500580962618c8bb8f714ddfd7ebb57eb839a45b48f7eaa3dc0c82d` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | WALK-DR06H3D-RC1 |
| mirrored at | 2026-08-29T02:20:11.872994Z |

<!-- brittle:mirrored-content -->

# TEAM: GREEN — Opus 5 · WALK-DR06H3D-RC1 — **READINESS: FAIL.** The unit cannot be launched as frozen, and the reason is a defect in my own card, not in the campaign. Two findings, both verified by my own measurement rather than relayed. **(1) The card carries an UNGATED IMITATION ANCHOR.** It passes `--anchor-ckpt crown_longstride_cy884736 --anchor-w0 0.30` and **zero** of the four gating flags, so `train_tip_reftrack.py:6878` applies `loss + aw*((mean − a_mean)²).sum(1).mean()` — an ungated quadratic pull on the actor mean toward the Crown, at aw 0.300→0.268 across the entire budget. The Crown is a long-stride policy that has never seen v = 0.24. Raising cadence to 1.0 Hz *necessarily* moves the action mean away from it, and the anchor charges quadratically for exactly that. **A negative result would therefore be over-determined by a training loss** — and distinguishing inability from suppression is the unit's entire purpose. The anchor is invisible to every receipt: it is a *loss*, not a reward term, so it is absent from `reward_book`, absent from `resolved_config`, and uncovered by reward fingerprint `723f5e92b45dc856`. **(2) The chartered baseline 0.8180 Hz is a measurement artifact.** I reproduced it exactly: it is **8 right-foot touchdowns ÷ 9.78 s = 0.817996**, identical across all six traces because the integer count was 8 every time. Its resolution is 0.1022 Hz per event (±12.5 %), and **1.0000 Hz is not representable on that grid at all** — the estimator could not have measured success had it occurred. The same traces give **0.7157 Hz on the left foot**, a 14 % limp never reported. Corrected baseline: **0.79 Hz (79 %), stride +25 %, rising monotonically with checkpoint — cadence was never locked.** Items 1, 3, 4 and 5 are complete and are recorded below; the unit is otherwise ready. **Launch cannot occur immediately when BLUE releases the campaign.** Clearing the anchor requires a card edit, which changes `20306ce6c8bbded0` and voids the Guard registration — and your item 6 forbids me to touch it. **That is your ruling to make, not mine.**

**unit:** WALK-DR06H3D-RC1 (held) · **ts:** 20260829T021827Z · **team:** GREEN (ticket header: Opus 5; executing session: Claude Fable 5)
**constitution:** C1, C2, C3, C6, C7 · **potential violations:** none · **amendment proposed:** no
ZERO PPO. No Guard mutation. No transition. Nothing modified: card, trainer, manifold, reference, rewards, cadence target, plant, obs/action and PPO hyperparameters are all untouched. No terminal work requested of Joe.

---

## 1 · Required return

| | |
|---|---|
| **readiness** | **FAIL** |
| **remaining blocker** | Ungated imitation anchor in the frozen card (§3). Secondary, already fixed here: the 0.8180 baseline is an artifact (§4). |
| **launch immediately on release?** | **No.** The anchor fix is a card edit → new sha16 → Guard re-registration. Everything else is ready. |

## 2 · Item 1 — frozen state re-verified `20260829T014014Z`

| item | expected | actual | |
|---|---|---|---|
| card | `20306ce6c8bbded0` | `20306ce6c8bbded0` | MATCH |
| trainer | `90534b22334d427b` | `90534b22334d427b` | MATCH |
| manifold | `385a99632749010e` | `385a99632749010e` | MATCH |
| reference | `54072cc4e04f55de` | `54072cc4e04f55de` | MATCH |

`members=1`, `vx=[0.24]`, `c=[1.0]`, `heldout=0`. `ppo_*.zip` count = **0**. Command manifold confirmed isolated: obs45 / obs52 / obs53 each hold exactly **one** distinct value across 147 456 dry-run samples.

## 3 · THE BLOCKER — ungated imitation anchor

Verified by direct inspection, not relayed:

```
card:    --anchor-ckpt "$ANCHOR_CKPT"      (line 385)
         --anchor-w0 "0.30"                (line 519)
         --anchor-decay-steps "8388608"    (line 520)
gating:  --anchor-deadband   0 occurrences
         --anchor-kappa-gate 0 occurrences
         --anchor-per-regime 0 occurrences
         --anchor-cmd-mask   0 occurrences

trainer (:6877-6879)   else:
                           _adiv = ((mean - a_mean) ** 2).sum(1).mean()
                           loss  = loss + aw * _adiv
```

All four gating flags default off, so the `else` branch is the live path: an **ungated quadratic penalty on every sample**, weight `0.30·(1 − steps/8388608)` = 0.300 → 0.268 across the whole 884 736-step budget. The anchor is the frozen Crown — a long-stride policy whose own manifold tops out well below v = 0.24 and which it has never been trained at.

**Why this is disqualifying rather than merely untidy.** The experiment asks whether the policy *can* raise cadence. The anchor applies a continuous force against precisely the parameter change that raising cadence requires. So:

- a **positive** result would still be meaningful — learning happened *despite* suppression;
- a **negative** result would be **uninterpretable** — inability and suppression are not separable.

Decision **B** is a plausible outcome of this unit. Spending 884 736 steps to reach an uninterpretable B is not a diagnostic.

**Why no receipt caught it.** The anchor is a loss term, not a reward term. It never appears in `reward_book` or `resolved_config`, the reward fingerprint `723f5e92b45dc856` does not cover it, and the card's own argv assertions (`:565-620`) check only cadence-track-w, the ramp flag, sched-tempo, symmetry, est-alpha, sensor-dr and the stride refusals. Every gate this unit passed was blind to it.

**I have not fixed it.** The machinery exists (`--anchor-kappa-gate`, `--anchor-cmd-mask`), and dropping `--anchor-ckpt` also works. But any of those edits the frozen card, moving `20306ce6c8bbded0` and voiding the canonical-mutation sentence you already issued. Your item 6 also forbids modifying PPO hyperparameters, which this arguably is. **Your call.**

## 4 · Item 2 — cadence measurement, and the retraction of my own baseline

### 4a · The published 0.8180 Hz is an artifact — reproduced exactly

Measured on the six source traces (`walkdr06a_speed_.../sweep/{dr05d_737280,dr05e_147456,dr05e_884736}/mj_v0p24000_s{0,1}.npz`):

| trace | R events | R count/span | R mean-of-diffs | L events | L count/span |
|---|---|---|---|---|---|
| dr05d_737280 s0 | 8 | **0.8180** | 0.7865 | 7 | **0.7157** |
| dr05d_737280 s1 | 8 | **0.8180** | 0.7883 | 7 | **0.7157** |
| dr05e_147456 s0 | 8 | **0.8180** | 0.7901 | 7 | **0.7157** |
| dr05e_147456 s1 | 8 | **0.8180** | 0.7919 | 7 | **0.7157** |
| dr05e_884736 s0 | 8 | **0.8180** | 0.7919 | 7 | **0.7157** |
| dr05e_884736 s1 | 8 | **0.8180** | 0.7937 | 7 | **0.7157** |

`8 / 9.78 s = 0.817996`. The estimator is **count over a fixed window** — 8 right-foot touchdowns divided by `(700−1−210)·0.02 s`. Three consequences:

1. **The "identical to four decimals across six rollouts and three checkpoints" was never evidence of anything.** It was identical because the integer count was 8 every time. I read it as a hard period lock in an earlier note this turn; **that reading was wrong** — mean-of-diffs on the same traces *rises monotonically with checkpoint* (0.7865 → 0.7937). Cadence was never locked.
2. **Resolution is 1/9.78 = 0.1022 Hz per event, ±12.5 % of the value.** The commanded 1.0000 Hz is **not representable**: 9 events → 0.9202, 10 → 1.0225. The estimator could not have registered success.
3. **A 14 % left/right limp was invisible.** Right 0.8180 vs left 0.7157 on identical traces, constant across all six.

**Corrected baseline: ~0.79 Hz realized (79.0 %), independently measured same-foot stride 0.297–0.304 m vs the 0.2400 reference (+24–27 %).** True operating error `|x| ≈ 0.27`, not the card's 0.2225. The ramp argument survives — both values sit on the linear segment, clip at 0.6304, gradient unchanged at 33.97 — but the card's stated `|x|` is wrong.

**Stride was derived, not measured.** The published stride column is `round(v_world/0.8180, 4)` — reproduced 6/6. That table held **two** independent numbers, not three, so "stride held as a control" was unenforceable from it. A genuinely independent stride exists in the same files (`stale[:,3:6]` / `[:,6:9]`, per-foot world positions) and was not used.

**The channel was also mis-named.** Both H3C and H3D describe this as the "`kc_td` engine-touchdown channel". `kc_td` is a Schmitt trigger on applied **knee PD torque** (`runtime_port.py:258-267`, 1.5/3.5 N·m, 0.30 s refractory), surfaced to the policy as obs[46]/obs[47] — policy-observed *and* policy-actuated, therefore gameable. No analysis script in the repo reads `kc_td` for cadence at all.

This is the **third under-measurement in this arc**, after H3B's quarantined reference and H3C's rollout-averaging. The pattern is mine: an estimator adopted without a resolution check.

### 4b · Cadence *is* separable from stride — the metric to use

**Primary in-run metric: `p_cadence`**, from `term_ledger["p_cadence"]` in `ckpt_meta_<step>.json`, which the **frozen trainer already writes** (`:7031` from `env.last_terms`). Produced at `:3074` as `cadence_track_w · _cadence_income_ramp_step(loaded)` with `loaded = get_contact_forces() > 5.0 N` — **raw per-foot engine normal force**.

Inversion to Hz (`T_cmd = 1.0 s` exactly on this card):

```
CADR_SIGMA=0.35  CADR_X0=0.12  CADR_G0=exp(-(0.12/0.35)^2)=0.889095  CADR_M=1.741901
g_bar = p_cadence / 19.5
ax    = 0.12 + (0.889095 - g_bar)/1.741901     if g_bar <= 0.889095
ax    = 0.35*sqrt(-ln(g_bar))                  if g_bar >  0.889095
c_hat = 1/(1 + ax)  Hz
```

Landmarks: 1.000 Hz → 19.50 · 0.900 → 17.55 · 0.850 → 15.42 · 0.800 → 12.92 · 0.790 → 12.38. Sensitivity ≈ **0.537 p-units per +0.01 Hz**.

**Stride cannot enter it, on two independent grounds.** (i) *By construction*: the kernel (`:1460-1486`) consumes exactly one input — the (N,2) contact boolean — and its state is a per-foot counter reset on that foot's own rising edge. No position, velocity or displacement appears anywhere in it. It is genuinely same-foot (per-foot counter, returned as `(g_R+g_L)/2`), so there is no factor-of-2. (ii) *By configuration*: stride terms enter `p_cadence` only under `if stride_track_w > 0` (`:3080`) and `if stride_corridor_w > 0` (`:3083`); both are 0.0, so both branches are dead.

**The payout-gate undercount law does not apply here** — this rides raw engine contact, not the qualified stream. It *does* apply to the stride-corridor term, which reads `_last_real_td_t`; that term is off (w = 0).

**Post-hoc confirmation channel** (required before any promotion claim, not for early-stop): total-span estimator on FK-replay engine floor contacts, **per foot reported separately**, `c_F = (K_F−1)/(t_F[last]−t_F[first])` — never count/span, never `kc_td`. Stride from `stale` world positions, never `v/c`. **Publish the per-foot touchdown count on every row**: a cadence that "improves" while the event count falls is a detection change, not a gait change.

## 5 · Item 3 — early-stop criteria

All four gates read `term_ledger["p_cadence"]` from the six `ckpt_meta_<step>.json` files the frozen trainer already writes. **No trainer change.** Checkpoint schedule verified: `n_steps·nworld = 64·2304 = 147456`, `ceil(884736/147456) = 6` → **147456 / 294912 / 442368 / 589824 / 737280 / 884736**. Let `P1` = p_cadence at 147456, `PN` at 884736.

| gate | step | condition | action |
|---|---|---|---|
| **0 — ramp-engaged receipt** | 147456 | `P1 > 0` **and** `10.0 ≤ P1 ≤ 15.0` | else **ABORT** — card misconfigured, no later reading interpretable |
| **1 — no gradient response** | 442368 | `p_cadence < P1 + 0.54` (= +0.01 Hz) | **ABORT** — half the budget, no movement against a restored 33.97 gradient |
| **2 — failure / inability** | 884736 | `PN < P1 + 1.61` (= +0.03 Hz) | **decision B** |
| **3 — success** | 884736 | `PN ≥ 15.42` (≥ 0.850 Hz, ≥ 85 %) **and** the six-point series is non-decreasing with no interior maximum | **decision A** |

Gate 0 exists because under H3A's huber form `p_cadence` was measured **negative** (−0.876, −0.897, −0.921); under the ramp it must be a positive income in [0, 19.5]. Gate 3(b) is not decoration: the kernel uses `ax = np.abs(x)` (`:1476`), so `p_cadence` is **even in x** and cannot distinguish too-fast from too-slow at a single point. The symmetric fast-side value is 1.37 Hz, unreachable from 0.79 Hz without passing continuously through `g = 1`, so a monotone rise that never turns over is unambiguous. **A turnover is genuinely ambiguous and must be adjudicated post-hoc.**

**These gates are valid only if the anchor blocker is cleared first.** With the ungated anchor live, a Gate-2 FAIL cannot be attributed to structural inability. Gate 3 SUCCESS would remain meaningful.

## 6 · Item 4 — coupling audit

**No stride-named term is active** — `--stride-track-w`, `--stride-track-member-target`, `--stride-corridor-w`, `--stride-corridor-per-member` are all deleted from argv; argparse defaults are 0.0; `resolved_config.json` omits both entirely on the dry run.

**But stride is not free, and the card says so itself** (lines 6–17): the imitation groups price foot geometry (`track_feet` w 4, σ 60 mm). That pressure points *toward* the 0.2400 reference — so the observed +25 % stride persists **against** it. This card must never be described as making stride observational-only.

**The term that keeps the slow-cadence solution economically stable is `r_fwd`** (w 12, forced to `altwin_ref` at `:5023` regardless of the card's `--fwd-mode peaked`): it divides reference phase displacement by the same realized window, making it structurally blind to cadence and stride. The audit puts pro-cadence pressure at ≈8.7/step against ≈19/step collected in full at the wrong gait. **I did not independently re-derive those two payout numbers** — flagged, not asserted.

**The command manifold cannot drift** (verified: obs45/52/53 single-valued across 147 456 samples). **But four schedules do run during this "isolated" diagnostic**, because neither `--no-curriculum` nor `--no-ss-curriculum` is passed: `stance_slip_w` 1200→12000 (the largest weight in the book), `penalty_scale` 0.3→1.0, `ss_target` 0.15→0.50 (overriding the card's own `--tip-weight rp_ss_target=0.3`), and the anchor decay. The *manifold* is isolated; the *reward landscape moves under the policy for the whole run*. A `p_cadence` trend is therefore not measured at a fixed reward surface.

## 7 · Item 5 — launch path

Invocation shape (mirrors the recorded WALK-DR06H3A production run; the unit is not a subject):

```
brittlectl run guarded --mode production \
  --subject bound-construction-walkdr06h3d-rc1 \
  --script <launcher> --repo-root /home/robojoe/code/brittle-telemetry \
  --out-dir rl/runs/walking/walkdr06h3d_rc1_s0 \
  -- env TC_MODE=run TC_RUN_ACK=I-UNDERSTAND-THIS-STARTS-PPO bash <launcher> <out>
```

Preconditions: `$OUT` must **pre-exist and be empty** (a missing dir raises *after* `runs.begin` appends the RUNNING record, orphaning the basis and burning the retry); clean tracked tree; the pre-run `g_v1` battery must emit `VERDICT: ACCEPT`; hash pins re-checked at exit 3. `--mode diagnostic` bypasses the D-11 construction gate and **will not be used**.

## 8 · Disclosures

1. **I retract the 0.8180 Hz baseline and the "+21 % stride" figure** from the H3D preflight and the RC1 report. Corrected: **0.79 Hz (79 %)**, stride **+24–27 %**, rising with checkpoint. The qualitative finding is unchanged and slightly *stronger*; the numbers were wrong.
2. **I also retract the "hard period lock" reading** I gave earlier this turn. Mean-of-diffs rises monotonically across checkpoints — the constancy was an artifact of an integer event count, and I over-read it before measuring properly.
3. **"The only delta vs the prior card is `--cadence-income-ramp`" was false.** That flag is at line 399 of *both* cards. The real RC1 deltas are the single-member isolation and `rp_step_period` 1.224095 → 1.0. The second is a change to the S6 period term's target, disclosed in the RC1 report §5 but **not covered by the card's own argv assertions**. The run carries two cadence-pressure deltas, not one.
4. **The provenance of the original 0.8180 analysis is unrecoverable** — no H3C artifact directory exists and the producing script was never persisted. I proved *what* the estimator was by reproducing all three published columns exactly; I cannot reconstruct its audit trail.
5. **A closure test of the form `|v − S·c|/v` must not be adopted.** It is vacuous: S and c derive from the same event set, so a 2× miscount halves c, doubles S and leaves the product invariant. Only two *independent* detectors plus the raw per-foot event count can check the event stream.
6. **`p_cadence` is a monotone proxy, not calibrated Hz** — a fleet mean over 2304 worlds of a nonlinear kernel, read at the last rollout step only, with stalled and dead worlds paying 0. The absolute landmark (15.42 → 0.850 Hz) carries an unbounded systematic; **the relative gates are the defensible half of the spec**.
7. **TDR is not trustworthy as a direction check** — chatter-inflated ~1.6×, no debounce, contaminated by dead worlds, and its direction has never been validated against ground truth. Advisory only.
8. Nothing was modified, no Guard state written, no PPO run, and no terminal work is requested of you.

## 9 · What I recommend

The unit is one card edit away from ready, and that edit is yours to authorize. The minimal fix is to gate or drop the anchor; the machinery already exists. Doing so re-freezes the card, so the sha16 moves off `20306ce6c8bbded0` and the canonical-mutation sentence you issued would need re-issuing against the new hash.

If you would rather not touch a frozen card, the alternative is to accept in advance that **decision B from this run is uninterpretable** and treat only decision A as informative. That is a coherent choice — it just costs 884 736 steps for a one-sided test.

Baseline the run must move, corrected: **0.79 Hz realized against 1.0000 Hz commanded, stride +25 %.**

HOLDING — readiness FAIL on the anchor, everything else complete.
