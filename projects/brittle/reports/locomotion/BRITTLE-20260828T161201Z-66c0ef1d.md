---
id: BRITTLE-20260828T161201Z-66c0ef1d
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: WALK-DR06H3C
created_at: "2026-08-28T16:12:01.794525Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06H3C_REALIZED_EXTRACTION_CEILING_20260828T140000Z.md
local_source_sha256: d87cb58c95bff6041d86ffdd9e6dad043ac9227622be488425dac02a725daa3a
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: GREEN_OPUS5__WALK_DR06H3C_REALIZED_EXTRACTION_CEILING_20260828T140000Z
mirror_bytes: 13882
---
# GREEN_OPUS5__WALK_DR06H3C_REALIZED_EXTRACTION_CEILING_20260828T140000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06H3C_REALIZED_EXTRACTION_CEILING_20260828T140000Z.md` |
| sha256 | `d87cb58c95bff6041d86ffdd9e6dad043ac9227622be488425dac02a725daa3a` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | WALK-DR06H3C |
| mirrored at | 2026-08-28T16:12:01.794525Z |

<!-- brittle:mirrored-content -->

# TEAM: GREEN — Opus 5 · WALK-DR06H3C — **B. REALIZED CEILING < 0.30.** The extractable ceiling is **0.2814 m/s** and none of the four requested rungs is supportable. This pass goes deeper than my earlier inventory in one way that mattered: extraction does not need a *rollout* at 0.30, only a **stride-pair window**, so I re-scanned at window level rather than rollout average. That raised the ceiling from 0.2782 to **0.2814** — and it surfaced a trap. The single fastest window anywhere, 0.2853 m/s, is **not extractable**: its quarters accelerate monotonically 0.2892 → 0.2973 → 0.3196 → **0.3446** and it terminates exactly at the episode end (frames 559–699 of 700), at the most over-driven command (0.50). That is a terminal run-away, not a periodic gait, and tiling it would splice a 19 % speed discontinuity into the seam. Excluding windows that touch the episode end and requiring genuine steadiness (quarter spread ≤ 1.08, non-monotone), **13,163 candidate windows reduce to 9,518 steady ones, of which zero reach 0.285** and 54 reach 0.280. The best is **0.2814 m/s** — `dr05e_884736/mj_v0p40000_s1.npz` sha `252141e70f3bbd3d`, window [319,459), 241 frames clear of the end. Its decomposition names the wall again: **cadence 0.7143 Hz against a commanded 1.0000 — 71.4 % realization — while stride is already 0.3939 m**, past the banked member's 0.3793. All of the +3.4 % gain over the banked reference is stride; **cadence has not moved at all.** Reaching 0.300 needs +6.6 % on one axis or the other. Per §3 I did **not** synthesize a rung the corpus cannot support, so no candidate was built and §5/§6 have no new artifact to gate. **STOP for Joe.**

**unit:** WALK-DR06H3C · **ts:** 20260828T140000Z · **team:** GREEN (ticket header: Opus 5; executing session: Claude Fable 5)
**constitution:** C1, C2, C4, C6, C7 · **potential violations:** none · **amendment proposed:** no
ZERO PPO, no reward/cadence-law/plant/sole change, no Guard mutation, no BLUE/RED runtime edit, no DFL relaxation, no quarantined-reference reuse, and **0.28 is not settled for** — it is reported as a measured ceiling, not adopted.

---

## Decision

**B · REALIZED CEILING < 0.30**

| | |
|---|---|
| **highest defensible realized rung (steady, tileable)** | **0.2814 m/s** |
| highest realized rung already extracted **and** DFL-PASSING | 0.2722 m/s |
| requested ladder 0.285 / 0.295 / 0.300 / 0.305 | **all UNSUPPORTED** (short 1.3 % / 4.6 % / 6.2 % / 7.7 %) |
| **exact binding channel** | **cadence realization — 0.7143 Hz vs commanded 1.0000 (71.4 %), invariant to command** |

## 1 · Source selection

5,500 `mj_*.npz` traces scanned. Filtered to **nominal plant** (no `kp_x`, `kd_x`, `mass`, `gyro`, `encq`, `th±`, `sdr`, `push`, DR monitors), **`fell_at == −1`**, **n ≥ 450** → **582 qualifying rollouts**.

The naïve ranking is a trap and I record it because I nearly took it: the raw maximum is 0.4733 m/s, and every trace above the clean ceiling is either off-nominal DR or a fall. Net displacement alone cannot distinguish walking from falling.

All sources pinned by full path + sha256_16; **no basename or glob-first resolution** was used anywhere (the defect disclosed in H3B).

## 2 · Extraction method

The established Door-2 method, from the one realized member that passes the DFL gate:

```
mj_v0p40000_s0.npz@92a78b1150aaff1c | DR05D-737280 realized gait, cmd 0.40
| tile=[389,528) DR06E pinned best-real pair (PD seam 0.573 Nm)
| laid 2x with the trainer's own wrap model (X += 0.757289/lap)
| engine-truth contacts | path-rebase psi=-0.029619
```

A window of an actual rollout, tiled with the trainer's own wrap model, **contacts taken from the engine**, path-rebased. Joint q/qd, root, feet, timing, phase, velocity and contact move together. It is the antithesis of the open-loop `stride_scale` defect (hip pitch opened ×ss while knee cols 3/9 and ankle cols 4/10 stay at exactly 1.0000). **Nothing here scales hip pitch or foot X independently.**

## 3 · Candidate ladder — and the window-level result

Extraction needs a stride pair, not a whole rollout, so I scanned **sliding 140-frame windows** (2 same-foot cycles at the realized 0.714 Hz) rather than rollout averages. That is the substantive improvement over my earlier pass, and it moved the ceiling.

| requirement | count |
|---|---|
| windows ≥ 0.26 m/s, ≥ 20 frames clear of episode end | 13,163 |
| of those, **steady** (quarter spread ≤ 1.08 **and** non-monotone) | 9,518 |
| steady windows ≥ 0.305 / 0.300 / 0.295 / **0.285** | 0 / 0 / 0 / **0** |
| steady windows ≥ 0.280 | **54** (all `dr05e_884736 @ cmd 0.40`) |
| **best steady window** | **0.2814 m/s** |

**The disqualified window, recorded because it is exactly the failure mode this ticket warns about.** The fastest window anywhere is 0.2853 m/s in `dr05d_737280/mj_v0p50000_s1.npz` — but:

| quarter | speed |
|---|---|
| q1 [559,594) | 0.2892 |
| q2 [594,629) | 0.2973 |
| q3 [629,664) | 0.3196 |
| **q4 [664,699)** | **0.3446** |

Monotonic acceleration, +6.2 % over its own rollout's median window, terminating at frame 699 of 700, at command 0.50 — the most over-driven setting, whose rollout *average* (0.2720) is the **lowest** of the fast group. High peak with a low mean is the signature of an erratic terminal excursion, not a faster gait. **Not periodic, not tileable, disqualified.** Had I ranked on peak speed alone I would have recommended extracting a loss-of-control divergence.

| rung | supported? | shortfall |
|---|---|---|
| 0.285 | **NO** | 0.0036 m/s (1.3 %) |
| 0.295 | **NO** | 0.0136 m/s (4.6 %) |
| 0.300 | **NO** | 0.0186 m/s (6.2 %) |
| 0.305 | **NO** | 0.0236 m/s (7.7 %) |

Per §3's instruction, **no rung was synthesized.** No candidate reference was built.

## 4 · Consistency and the binding channel

Best steady extractable window — `dr05e_884736/mj_v0p40000_s1.npz` sha `252141e70f3bbd3d`, window **[319,459)**, 2.80 s, 2 same-foot touchdowns, 241 frames clear of the episode end:

| quantity | commanded | realized | agrees? |
|---|---|---|---|
| speed | 0.40 (the command that maximises realized speed) | **0.2814** | ✗ — ratio 0.70 |
| cadence | `c_law(0.40) = 1.0000` | **0.7143 Hz** | ✗ — **71.4 %** |
| stride | — | **0.3939 m** | — |
| stride × cadence | — | 0.3939 × 0.7143 = **0.2814** | ✓ exact |
| phase rate if played at its own cadence | — | 1.000 | ✓ |

**Commanded and realized are reported separately and they do not agree** — that is the finding, not a defect in the measurement. Against the banked member:

| | speed | stride | cadence |
|---|---|---|---|
| banked `ccref_dr06g_realized0274` (DFL PASS) | 0.2722 | 0.3793 | 0.7177 |
| best steady window | **0.2814** | **0.3939** | **0.7143** |
| delta | **+3.4 %** | **+3.8 %** | **−0.5 %** |

**Every bit of the gain is stride. Cadence did not move — it went marginally backwards.** Realized cadence sits at 0.714–0.716 Hz across commands 0.30, 0.40 and 0.50 and across three checkpoints. Reaching 0.300 requires stride 0.4200 m (+6.6 %, approaching the 0.4480 corpus maximum) **or** cadence 0.7615 Hz (+6.6 %) — the axis three units have failed to move.

## 5 · Action / interface feasibility

No new candidate was constructed, so the mandatory gate has no new artifact to run against, and I will not report a feasibility verdict for a reference that does not exist.

For the record on method, per H3B: the zero-error `a_req` statistic is **not** admissible as evidence — it is a builder constant (0.818299, bit-identical across DFL-PASS and DFL-FAIL members) and a lower bound that ignores the 10 Hz ACD lag and the PD lead. A real feasibility verdict must add the velocity lead `(kd/kp)·qd_ref` and the torque term `tau_ref/kp` (≈ 0.197 rad at `tau_P99` 3.93 with kp 20 — ≈ 0.98 action units on the 0.2-scale hip-yaw joints).

## 6 · DFL gate

No new candidate, so no new gate run. Recorded verdict for the recommendable existing reference, gate unchanged and `allow_skip=False`:

| member | SS_P10 | med_integ | DP10 | tau_P99 | verdict |
|---|---|---|---|---|---|
| `ccref_dr06g_realized0274` (realized, 0.2722) | **−3.59 mm** | 0.59 | — | 3.46 Nm | **PASS** |
| bars | ≥ −17.5 | ≤ 2.7 | ≥ −5.0 | < 15.0 | |
| open-loop `ccref_vx030_c1p000` (quarantined) | −19.3 mm | 2.03 | — | 3.93 | **FAIL** |

The realized member carries ≈ **5.4×** the lateral margin of the open-loop 0.30 construction — the observation that motivated Door 2, and it holds. No bar was touched.

## 7 · Contact truth

The extraction method uses **engine-truth contacts**, stated in the member's own provenance — not the authored schedule RED DR06R3A found defective at handoff. Cadence in this report is measured from the traces' `kc_td` touchdown channel (engine events), not from authored labels. **No DS or flight claim anywhere in this report rests on the label channel.** No candidate required a fail-closed judgement because none was built.

## 8 · Collision

Not measured in this unit: the raw sweep traces carry no `foot_foot_contacts` / `min_foot_sep_mm` field. The zero-collision evidence for this gait lineage comes from qualified-arm mining runs reported in DR06G / H1 / H2 / H3A (0 physical foot-foot contacts across 60 rollouts each). **I am not asserting a fresh collision result**, and any future extraction from window [319,459) must have it measured with the qualified arm before adoption.

## 9 · Recommendation and next acquisition delta

**Recommended reference today** — unchanged, because nothing better has been *built*:

```
rgl/artifacts/walkdr06g_ratchet_20260825T192438Z/refs/ccref_dr06g_realized0274.npz
sha256_16  8b812eb26af56c9d
v 0.272219 (loop-derived 0.272406) | stride 0.379283 | cadence 0.717720
engine-truth contacts | DFL PASS: ss_p10 -3.59 mm, med_integ 0.59, tau_P99 3.46 Nm
```

**Best available extraction source, if you want the +3.4 %:**

```
rgl/artifacts/walkdr06a_speed_20260825T131920Z/sweep/dr05e_884736/mj_v0p40000_s1.npz
sha256_16  252141e70f3bbd3d   window [319,459)   v 0.2814 | stride 0.3939 | cadence 0.7143
```

Extracting it is a ~half-day build (tile → 2× lay with the wrap model → engine contacts → path-rebase → DFL → feasibility → collision). **I do not recommend spending it as a speed play.** It buys +3.4 % and lands at 0.281 — still short of every requested rung, still on the same pinned cadence, and it does not touch the binding channel.

**The next acquisition delta is cadence, not a reference.** Door 2 is circular at 0.30: extraction reproduces only gait a policy has already produced, and the corpus stops at 0.2814 because no policy has walked faster. Three units have now failed to move cadence — DR06H1 (corridor, no gradient), DR06H2 (`stride_track`, ample gradient, wrong direction), H3A (cadence-led with timing made coherent by RC2/RC3, reverted to seed and cadence realization *fell* to 76.2 %). **I do not recommend a fourth reward-shaping attempt.**

Three routes, all architecture decisions and therefore yours:

1. **Make cadence realization the acquisition objective itself**, at an already-consistent rung (v = 0.24, where `c_law = c_member = 1.0` exactly), asking "can this policy realize commanded cadence at all?" — currently 71.4 %, and never yet posed as the target.
2. **Change the cadence schedule** so a member can be commanded at its own cadence. Today `c_law` saturates at 1.0 from v ≥ 0.24, so every member above 0.24 is played compressed — H3B measured 22/23 inconsistent.
3. **Accept ≈ 0.28 as this architecture's speed** and redirect, consistent with RED WALK-DR06R2 (lateral ZMP binds 0.32–0.35, architecture retires ≈ 0.35).

**Confidence: HIGH** on the ceiling and the binding channel — 582 rollouts, 9,518 steady windows, zero above 0.285, cadence constant to three decimals across commands and checkpoints, and a non-monotone command response that peaks at 0.40 and declines at 0.45/0.50. **MEDIUM** on route choice, deliberately left open.

## 10 · Disclosures

(1) **My prior pass under-measured the ceiling** by ranking rollout averages; window-level scanning raised it 0.2782 → 0.2814. The correction is recorded rather than quietly folded in. (2) **The fastest window in the corpus is disqualified** as a terminal run-away (§3) — ranking on peak speed alone would have recommended extracting a divergence. (3) **No candidate reference was constructed**, so §5 and §6 report no new verdicts; this is per §3's instruction, not an omission. (4) **No collision measurement was made in this unit** (§8). (5) Cadence is derived from the `kc_td` engine touchdown channel over each window; it agrees with the DR06G extraction's independently measured 0.717720 Hz to ≈ 0.5 %. (6) Steadiness thresholds (quarter spread ≤ 1.08, non-monotone, ≥ 20 frames clear of the episode end) are my own criteria, chosen to exclude terminal divergence; they are not a ratified gate, and a different threshold would move the 54-window count though not the ≥ 0.285 result, which is zero by a wide margin. (7) Speeds are net XY world displacement over the window — never `lo_v`. (8) The recommended existing member is internally consistent as a reference but **cannot be played consistently under the unchanged law** (`c_law(0.2722) = 1.0000` compresses it 1.3933×); recommending it is not endorsing H3A's construction.

Read-only over existing corpora; no new artifact directory. Prior: `GREEN_OPUS5__WALK_DR06H3C_REALIZED_CEILING_20260828T050000Z.md`, `GREEN_OPUS5__WALK_DR06H3B_PREFLIGHT_STOP_20260827T060000Z.md`, `GREEN_OPUS5__WALK_DR06C_SPEED_ACQUISITION_STOP_20260825T154908Z.md`.

STOP for Joe — extractable ceiling 0.2814 m/s, every requested rung unsupported, binding channel cadence at 71.4 % realization; Door 2 cannot reach 0.30 because no policy has walked there.
