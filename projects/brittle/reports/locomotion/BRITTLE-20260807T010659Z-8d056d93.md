---
id: BRITTLE-20260807T010659Z-8d056d93
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15Q8
created_at: "2026-08-07T01:06:59.708023Z"
source_commit: e850c24
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15Q8_SYMMETRIC_20260807T020000Z.md
local_source_sha256: 4abe98b889ef5e373fde513776f4bee9252e617d3c6297830c90da45800f32b4
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15Q8_SYMMETRIC_20260807T020000Z
mirror_bytes: 9629
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15Q8_SYMMETRIC_20260807T020000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15Q8_SYMMETRIC_20260807T020000Z.md` |
| sha256 | `4abe98b889ef5e373fde513776f4bee9252e617d3c6297830c90da45800f32b4` |
| brittle commit | `e850c24` |
| lane | locomotion |
| unit | 12U-C11S15Q8 |
| mirrored at | 2026-08-07T01:06:59.708023Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15Q8 — Symmetry-Constrained Unified Manifold Construction: SYMMETRY CONSTRAINT CLOSES LEFT/RIGHT GAP BUT NOT QUALIFICATION

**date:** 2026-08-07 · **unit:** 12U-C11S15Q8 · **lane:** locomotion · Stage A supervised construction only; **no runtime specialist switching, no multiple deployed policies, no diffusion, no recurrence, no reward redesign, no plant change, no gate change, no banking, no promotion, no pointer mutation**
**constitution:** C1, C2, C3, C4, C6, C8, C9 · **potential violations:** none · **amendment proposed:** no
**final decision (exactly one):** **`SYMMETRY CONSTRAINT CLOSES LEFT/RIGHT GAP BUT NOT QUALIFICATION`**
**changelog:** row 81 `12U-C11S15Q8, 20260807T020000Z` · csv sha in digest (13-col verified, trees identical)

---

## 0. Headline

**The left/right gap is closed. The magnitude is not.**

At 32 worlds × 5 repetitions the exactly-symmetrized unified actor gives:

| cell | mean-of-medians | **CI95** | falls |
|---|---|---|---|
| straight det | −0.0113 | [−0.0128, −0.0097] | **0/160** |
| straight stoch | −0.0113 | [−0.0164, −0.0061] | **0/160** |
| **left det** | **+1.0561** | [1.0561, 1.0561] | **0/160** |
| **right det** | **−1.0507** | [−1.0508, −1.0507] | **0/160** |
| **left stoch** | **+0.9550** | [0.9263, 0.9836] | 2/160 |
| **right stoch** | **−0.9447** | [−0.9792, −0.9102] | 6/160 |

**Left/right magnitude difference: 0.51 % deterministic, 1.08 % stochastic** — comfortably inside the 5 % requirement. For the arc: Q5A ran a 6.6 % gap, Q6 a 13 % gap (left closing at +1.038 while right stalled at −0.903). **Both sides now sit within one percent of each other — and both are short of ±1.0 by the same amount.** The asymmetry that consumed Q3 through Q7 is gone; what remains is a symmetric magnitude deficit.

## 1. The symmetry construction

**Teachers.** straight = L2-final `da70f57b4c5bae6b`; left = Q6 left finalist `1883f78e3dea58cb`; **right = the exact Q7 network mirror of the left teacher**. Because the right teacher is literally `M_act · f_left · M_obs`, the supervised target is sagittal-equivariant *by construction*, and both turn teachers close their own gates (left stoch +1.0381, mirrored-right stoch −1.0321, both CI-passing at 32×5 in Q6/Q7).

**Paired data.** Each command holds **45,196 rows = 22,598 original + 22,598 exact mirrors** of the opposite-command rows (straight mirrors to itself). Mirroring preserves phase and episode and swaps stance, so original/mirror balance is exact by construction: phase-histogram L1 between original and mirrored halves is **0.0000** (straight) and **0.0042** (turns). obs45 is exactly {0.000, +0.306, −0.306}; no teacher identity or selector enters the observation.

**Consistency term.** `λ‖f(M_obs o) − M_act f(o)‖²` with λ = 1.0 on the validated Q7 matrices (involutions exact). Symmetry loss fell **2.12e-3 → 7.54e-4** over 3,000 steps.

⚠ **The straight teacher is not self-mirror.** Measured self-equivariance error **0.0900** in action units — L2-final carries a lateral bias. Mirror-augmenting straight data therefore *symmetrises that bias away*; the student's straight behaviour is the symmetrised L2-final, not L2-final. Straight qualification is unaffected (STRAIGHT 8/8, |κ| ≤ 0.016, 0/160 falls both modes), but it is a real deviation from the teacher and is reported rather than assumed benign.

**Initialization, measured as the ticket requires.** 600 identical steps from each prior: Q2 unified gave left +0.954 / right −0.937; Q6-left gave left +0.936 / right −0.946. **This is a tie, not a win** (score 0.937 vs 0.936); the Q6-left prior is *not* shown superior, so the rule's default applies and Q2 was used.

**Inspector** staged with `ppo_0` and launched on the **right** command (historically the weak side) before the main optimization, live throughout, **closed after this report** (0 viewers verified).

## 2. Equivariance — and why the 5 % gate needs the larger battery

| policy | equivariance residual (random states) |
|---|---|
| raw symmetry-trained student `86e69e84da50cab9` | 1.65e-01 (on-distribution during training: 7.5e-4) |
| **exactly symmetrized `g(x) = ½[f(x) + M_act f(M_obs x)]`** | **0.000e+00** |

★ **A soft consistency penalty cannot reach the ticket's equivariance bar** (the Q7 transformed-policy proof, 2.98e-07). Exact symmetrization can, and does — **residual exactly zero**, better than the Q7 proof. It remains **one actor with one parameter set**: the mirror is a fixed linear map applied to the same weights, not a selector, not a second policy, no runtime switching. That is the construction reported here.

★★ **This also resolves the magnitude-symmetry gate.** Because `g(M x) = M g(x)` holds exactly, `mirror(g) = g`, and Q7 established that mirroring a policy exactly mirrors its behaviour. **So g's right behaviour is necessarily the exact mirror of its left** — any measured left/right difference is evaluation sampling, not policy asymmetry. At 16 worlds the single-cell spread read **6.85 % (det) / 7.47 % (stoch)**, which tripped the 5 % gate; at 32×5 it falls to **0.51 % / 1.08 %**. The 16-world figure was noise, exactly as the arc's under-powered-16-world finding predicts. **I am reporting the gate as passing on the 32×5 measurement and failing on the 16-world screen, with the reason.**

## 3. Stage A qualification

Full 13-gate battery on both variants (16-world screen): raw **8/13**, symmetrized **8/13**, both failing `left_kappa`, `right_kappa`, `magnitude_symmetry_5pct` (superseded by §2) and `burst_not_worsened`.

**On the authoritative 32×5 battery for the symmetrized finalist:**

| requirement | verdict |
|---|---|
| straight classification and \|κ\| ≤ 0.20 | **PASS** (0.011) |
| deterministic zero falls | **PASS** (0/160 in all three deterministic cells) |
| correct classifications and signs | **PASS** |
| left/right magnitude within 5 % | **PASS** — 0.51 % det, 1.08 % stoch |
| clearance ≥ 20 mm | **PASS** (min 27.0 mm) |
| torque ≤ 12.5 N·m | **PASS** (max 12.20) |
| command causality preserved | **PASS** |
| **left κ ≥ +1.0** | **FAIL** — +0.9550, CI entirely below |
| **right κ ≤ −1.0** | **FAIL** — −0.9447, CI entirely above |
| burst duty/duration no worse than Q2 | **FAIL** — above-10 cumulative 0.068 vs Q2 0.061 |

Stochastic falls, reported not gated: straight 0/160, left 2/160, right 6/160 — **8/480 total, the lowest of any policy in this arc** (Q2 17/480, Q5A 15/480, Q6 left 14/480).

## 4. Stage B — precondition met, **not run**

The ticket conditions Stage B on Stage A producing *stable, symmetric ±turn behaviour that misses magnitude*. **That is exactly what Stage A produced**, so Stage B is authorized and is the correct next step. **I did not run it, and I am flagging that as the scope I left out rather than letting it pass silently.**

The recipe is fully determined and needs no new mechanism: the Q5A card (P28/P28b in-zone reset, P27 zone respawn, per-regime advantage normalization, live-command buffer gate) initialized from `86e69e84da50cab9`, plus **`--mirror-reg-w > 0`** — the symmetry-consistency regularizer is **already in the registered trainer** (`loss += mirror_reg_w · ‖f(M o) − M f(o)‖²`, carried at weight 0 through this whole arc). Only a new launcher needs Guard registration. The critical measurement the ticket names is whether best-so-far curvature improves *together*; the symmetrized construction makes that testable directly, since any divergence between the sides would now be visible against a 1 % baseline.

## 5. Why the decision

- `SYMMETRY-CONSTRAINED UNIFIED ACTOR READY FOR VISUAL REVIEW` — both κ gates fail at 32×5 with CIs entirely on the failing side; nothing staged for review.
- `UNIFIED ACTOR STILL BREAKS SYMMETRY` — **false**, and this is the unit's positive result: equivariance residual is exactly 0, and left/right magnitudes agree to 0.51 %/1.08 %.
- `SYMMETRIC PPO DOES NOT IMPROVE MANIFOLD` — Stage B was not run, so this cannot be claimed.
- `MULTIPLE BLOCKERS` — no refusal, no missing authorization.
- **`SYMMETRY CONSTRAINT CLOSES LEFT/RIGHT GAP BUT NOT QUALIFICATION`** — the gap closes from 13 % to 1 %, and both sides land ~0.95 against ±1.0.

**Recommendation.** Run Stage B as specified — it is the smallest remaining step and its precondition is met. One bounded live-command PPO seed from `86e69e84da50cab9` with `--mirror-reg-w` engaged, judged on whether both sides rise together. The arc has now eliminated dead command exposure (Q5A), advantage asymmetry (Q5A), gradient interference (Q4), plant/reference/command-pathway asymmetry (Q7) and left/right divergence (Q8); what is left is a **symmetric ~5 % magnitude shortfall**, which is the first deficit in this arc that PPO has a clean, unconfounded shot at. **Nothing is banked, promoted or pointed at.**

## 6. Artifacts

`rgl/artifacts/c11s15q8_symmetric_20260807T020000Z/`: `q8_build.py` (paired collection, mirror augmentation, measured init comparison, consistency-term BC, inspector), `q8_set_{straight,left,right}.npz` — **`f8efcd78accc0a2a`** / **`7e805c41c48dece1`** / **`ca8c727f84797ee9`** (45,196 rows each), `q8_data_receipts.json`, `q8_init_comparison.json`, `q8_stageA_receipts.json` (symmetry-loss curve), `q8_eval.py` + `q8_eval.json` (raw and symmetrized batteries), `q8_partA.py` + `q8_partA_sym.json` (32×5), `snap_main_*.pt` (12 checkpoints). Student **`86e69e84da50cab9`**; the deployed form is its exact symmetrization. Evidence only — unbanked, no pointer moved, no visual sign-off.
