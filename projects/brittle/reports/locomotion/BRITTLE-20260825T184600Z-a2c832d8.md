---
id: BRITTLE-20260825T184600Z-a2c832d8
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: WALK-DR06E
created_at: "2026-08-25T18:46:00.285808Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06E_CONTRACT_CLOSURE_20260825T183856Z.md
local_source_sha256: 8f4c1a45f5655b91f7c236806cf8ec01d41dec76cc0f5744e3afa69a0fcabf5a
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: GREEN_OPUS5__WALK_DR06E_CONTRACT_CLOSURE_20260825T183856Z
mirror_bytes: 7099
---
# GREEN_OPUS5__WALK_DR06E_CONTRACT_CLOSURE_20260825T183856Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06E_CONTRACT_CLOSURE_20260825T183856Z.md` |
| sha256 | `8f4c1a45f5655b91f7c236806cf8ec01d41dec76cc0f5744e3afa69a0fcabf5a` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | WALK-DR06E |
| mirrored at | 2026-08-25T18:46:00.285808Z |

<!-- brittle:mirrored-content -->

# TEAM: GREEN — Opus 5 · WALK-DR06E — Extraction contract closure RESULT: **C — OWNER CHOICE STILL REQUIRED, but narrowly: THREE of the four axes are now CLOSED BY MEASUREMENT** (adversarially verified, high; A and B both attacked and refuted). **SEAM (closed):** all 13 real cycle/tile candidates evaluated per seed; the best genuine pair gives a predicted PD seam transient of **0.573 N·m** (s0: 2-cycle tile, frames 389→528) / **0.485 N·m** (s1: single 69-frame cycle — short-loop caveat flagged) against the 12.5 N·m law limit and the 9.2 N·m measured gait peak (DR06D) — consumable with zero blending or fabrication; distributions ranged up to 4.94/3.65 N·m, so pair selection matters and the identities are pinned. **CONTACT TOPOLOGY (closed):** ENGINE-truth replay (per-frame mj_forward contacts on the arm's own plant) shows **ZERO flight frames and zero flight events** — the 2–5 mm kinematic "fly" (34.9%/22.2%/7.5% at 2/3/5 mm, 0% from 8 mm) is clearance-threshold artifact over soft-contact sole compliance, not physical flight; the claim is exactly "zero bilateral-airborne frames, engine truth" (per-foot alternation is inference, not separately verified); the gait is a true fast walk and the representation question dissolves — no gate-convenient choice was made or needed. **CADENCE BRANCH (closed as SPEC, nothing implemented):** `cad(vx) = clip(2.1475·vx + 0.4846, 0.70, 1.00)` for vx ≤ 0.24 EXACTLY as today (2.1475·0.24+0.4846 = 1.0000 — C0 at the break), and `cad(vx) = max(1.00 − 8.2815·(vx − 0.24), 0.7205)` for vx > 0.24, anchored solely by the measured (0.2737, 0.7205); slope break +2.1475 → −8.2815; floor at the ratchet point. **STOP COMPETENCE (the one OPEN axis):** the vx-command path measurably CANNOT stop — both seeds ramped 0.274 → 0 FALL-FREE (1000 steps, tilt ≤ 6.5°, nominal plant, no DR) but settle into a NON-DECAYING drift-walk at ~0.18 m/s under vx = 0 (steady in the last 2 s; DS 8.6–9.7%, walk-like), because the deployment cadence law floors at 0.70 Hz with no stop mode (source-verified) — "command to zero" is REFUTED as a stop mechanism. Safe-degraded, not hazardous — so not BLOCKED; but the three viable stop routes are all owner decisions: (i) ramp-to-low-speed + the existing DS-bookend residual-α state machine (its PASS 5/5 provenance is from-DS-at-low-speed; composition from THIS drift-walk entry is UNTESTED), (ii) a deployment stop mode added to the law (beyond the specified branch), (iii) an authored decel-bookend member (2b). ZERO training, ZERO banking, ZERO cadence-law mutation. STOP.

**unit:** WALK-DR06E · **ts:** 20260825T183856Z · **team:** GREEN (ticket header: Opus 5; executing session: Claude Fable 5)
**constitution:** C1, C2, C5, C6 · **potential violations:** none · **amendment proposed:** no
Ticket: bus `BRITTLE-20260825T183848Z-7bcf7d3f`. Measurements only; n = 2 seeds throughout (cross-seed agreement: hold-drift Δmean 0.0005 m/s; the reps-16 held-out rule applies to any downstream GATE claim built on these numbers); stop rollouts clone the qualified arm loop verbatim (same substep application, `d.qfrc_applied[jd] = tau` + `mj_step`), α0.4 runtime, nominal plant condition (no DR — declared per the additive-dr measurement split).

---

## 1 · Measurements

| axis | result | key numbers |
|---|---|---|
| 1 SEAM | **CLOSED** — best genuine pairs consumable | s0 best: span-2 tile fr 389→528, dq 12.3 mrad, dqd 0.24, **PD 0.573 N·m** (of 13 cands, range 0.57–4.94); s1 best: span-1 fr 527→596 (69-frame loop — short; the span-2 alternative is available), dq 18.9 mrad, **PD 0.485** (range 0.49–3.65). Limits for scale: τ_limit 12.5 (law), measured gait peak 9.2 (DR06D). Δrpy ≤ 16.5 mrad, Δz ≤ 1 mm. |
| 2 STOP | **OPEN (owner)** — command path cannot stop | both seeds fall-free through hold→ramp→hold-0; hold-0 drift 0.176→0.183/0.184 m/s (steady, NOT decaying), DS 8.6–9.7%, tilt ≤ 6.48°; DS returns to 8–10% in the vx<0.10 ramp segment (bookend latch point exists); mechanism: cadence floor 0.70 Hz, no stop mode in the law (source-verified). |
| 3 CONTACT | **CLOSED** — no physical flight | kinematic fly_frac 0.349/0.222/0.075/0.000/0.000/0.000 at 2/3/5/8/10/15 mm; **engine truth 0.0000, 0 events** — threshold-sensitive clearance only (soft-contact sole compliance); claim scope: zero bilateral-airborne frames. |
| 4 CADENCE | **CLOSED as SPEC** — fully measurement-determined | branch above: anchors (0.24, 1.00)–(0.2737, 0.7205), slope −8.2815 Hz/(m/s), C0 exact at 0.24, ≤0.24 law byte-preserved. SPEC ONLY — no behavioral claim, nothing implemented. |

## 2 · What the stop finding means (stated precisely)

Ramping down from 0.274 through the qualified range is SAFE (no falls, DS returns) — the 2a "loop-only member" premise survives for LOCOMOTION. What fails is the last meter: under the current command interface the robot cannot be brought to a stand by speed command alone (it drift-walks at ~0.18 forever at vx=0). Stopping today lives in the DS-bookend state machine, whose certified entry is DS at low speed — reachable from this ramp (DS present at 8–10%) but never composed/tested from a 0.274 start. The three routes above are genuinely different products (test-the-composition vs law-mode vs authored member) — an owner call, not an agent default.

## 3 · Verdict + the single question for Joe

**C — OWNER CHOICE STILL REQUIRED**, reduced to one axis: pick the stop route — (i) zero-training composition test of ramp + existing DS-bookend machine from 0.274 (cheapest; recommended first), (ii) deployment stop-mode law design (bigger scope), (iii) authored decel-bookend member (2b, authoring cost). Axes 1/3/4 need no further decisions: seam = best-real-pair (identities pinned), contact = engine-truth continuous support, cadence = the specified branch awaiting your implementation approval alongside the ratchet turn.

## 4 · Verification + scope + artifacts

Adversarial verification `wf_a622380a-86b` — NOT REFUTED, high: A killed (untested bookend composition + law/scope choices cannot be declared READY under C1), B killed (no-stop is safe-degraded by design, not infeasibility; 3/4 axes closed). All its honesty items are carried above (distributions + pair identities + short-loop flag; engine-claim scope; solref explanation; spec-only cadence; n=2 + reps-16 note; plant-condition declaration; 12.5/9.2 provenance named).

ZERO training/banking/law mutation; no member built; qualified components only (arm loop cloned verbatim for the stop schedule; `rt.vx` live-reread is the documented contract). Artifacts under `rgl/artifacts/walkdr06e_contract_20260825T183856Z/`: `contract_measure.py`, `contract_measurements.json`, `stop_s0.npz`, `stop_s1.npz`.

STOP for Joe — choose the stop route; everything else on the ratchet contract is measured and waiting.

**changelog row:** `WALK-DR06E-CLOSURE, 20260825T183856Z` — post-append `rgl/LOCOMOTION_CHANGELOG.csv` sha256_16 `0c156b321832f196` (533 rows × 13 cols, both trees byte-identical)
