---
id: BRITTLE-20260823T232356Z-e2515dc2
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: WALK-DR01
created_at: "2026-08-23T23:23:56.372394Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/WALK_DR01_ISAAC_CROWN_DR_BASIN_STAGE1_MAP_20260823T230443Z.md
local_source_sha256: 7c1424122edaf0e29955839efd8ec7d8d11f43257137881650d2f37dde4933e1
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: WALK_DR01_ISAAC_CROWN_DR_BASIN_STAGE1_MAP_20260823T230443Z
mirror_bytes: 18137
---
# WALK_DR01_ISAAC_CROWN_DR_BASIN_STAGE1_MAP_20260823T230443Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/WALK_DR01_ISAAC_CROWN_DR_BASIN_STAGE1_MAP_20260823T230443Z.md` |
| sha256 | `7c1424122edaf0e29955839efd8ec7d8d11f43257137881650d2f37dde4933e1` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | WALK-DR01 |
| mirrored at | 2026-08-23T23:23:56.372394Z |

<!-- brittle:mirrored-content -->

# WALK-DR01 — Isaac Crown DR Basin: Stage-1/2 sensitivity map, first failing shells, and the Stage-3 training-target choice

**status:** REVIEW · Stage 1–3 complete (zero training); Stage 4 (nominal-anchored PPO) fully staged, awaiting Joe's Guard transition + ppo grant (campaign still bound to 12U-C11S65A). STOP for Joe per ticket.
**recommend:** Train the ACTUATION family, kp-high shell (kp ×[1.00, 1.20], 15% exact-nominal fraction), nominal-anchored PPO from the frozen Crown, 3 seeds × 2,654,208 steps.
**decision:** Training target = PD-gain (kp) high-side shell — earliest failing shell in BOTH engines, physically relevant, no owner hardware assumption, trainable on the existing qualified stack.
**next:** Joe issues the Guard transition + ppo grant sentences (§8); then Stage 4 launches through `run guarded` with the pre-staged card, integrity receipt, and inspector.

**constitution:** C1, C2, C4, C6, C8, C10, C11, C12
**potential violations:** none
**amendment proposed:** no

Ticket: WALK-DR01 (bus `BRITTLE-20260823T225100Z-a6bee574`, TEAM GREEN, owner-direct 2026-08-23). Unit executed read/measure-only; no reward, obs, action, reference, plant, or interface change; no terrain; no TIP; no promotion; no canonical mutation.

---

## 0 · Headline

1. **The Crown's first failing DR shell is ACTUATION kp-high, and it is present in BOTH engines.** Isaac: sharp cliff at kp ×1.15 (κ±1.0 cells fall; ×1.125 still 0 falls). MuJoCo (canonical plant, same qualified port): first fall already at **×1.10** (κ−1.0 @ step 617), 3 cells at ×1.15, all four κ±1.0 cells at ×1.30. The cliff is a policy capability limit, not an engine artifact — and since it exists on the trainer-side plant family, **PPO will see the failures it must fix** (the gradient exists).
2. **The tighter engine governs: MuJoCo.** Certified kp region (both engines, all channels): **≤ ×1.075**. Transition ×1.10–×1.125 (MJ fall at 1.10; Isaac micro-graze at 1.10 and a 10.1° tilt excursion at 1.125 vs 4.3° nominal; κ±0.35 symmetry 1.33→2.10). First clearly failing: **×1.15**.
3. **Torque-scale is the same family, further out**: certified ≤×1.15 both engines; MJ falls from ×1.20 (one cell), Isaac from ×1.30. C14S01's "acts like a Kp error" reading stands.
4. **No other family produces an earlier shell.** Contact: NO survival failure down to μ=0.2 (quality-only drift; slip stays 0.5–1.0 mm/step p95 — μ-insensitive, consistent with Phase-13). Plant: mass ×0.90 fails F8 only and is a measured knife-edge (×0.875 is clean — non-monotone, disclosed); CoM/inertia/armature never fall. Observation family: **N/A — no sensor-noise/latency machinery exists in the qualified implementation** (ticket scopes family D to existing machinery; none was invented).
5. **Foot-collision invariant measured on real colliding geometry (sole spheres): ZERO overlap at nominal, first micro-graze exactly at the shell edge** (kp×1.10, −0.13 mm, 1 frame, both engines ~same level), deep overlap through the cliff (kp×1.15: −4.99 mm; kp×1.3: 13 frames). Foot-crossing interference is part of the kp-shell failure mechanism, so the invariant channel is load-bearing in Stage-5 eval.
6. **The Crown was TRAINED under additive_dr kp ×U(0.7,1.3)** (verified from `cy_stride_polish_r_s0/settings_summary.json`: `additive_dr: true`, nworld 2304) **and still owns no kp×1.15 basin.** DR-ensemble survival pressure alone never bought the corner; WALK-DR01's shell-focused sampling + basin-scored checkpoint selection is the missing ingredient. This also means the Stage-4 mechanism (per-world kp draw) already exists in the qualified trainer.

## 1 · Provenance and gates

- Crown FUNCTION (unchanged, frozen): `rl/models/walking/crown_longstride_cy884736/ppo_884736.zip` (`876025a9cb883f37`) + C11S51 mirror bank (`c9cf6319f673f7b3`/`23289b73d9b23faa`) + manifest `f7d950212cf3d68e`. All rollouts through the Phase-13-qualified ports (`actor_port.g_v1`, `runtime_port` law asserted from the bound manifest).
- Plant: canonical `mujoco/mini_top/scene.xml` fingerprint **81d4e6d8226ed12b** (read live via the harness's own `plant_spec` pin). Isaac construction: `isaac_arm.py --foot-material --pos-iters 100 --dyn-friction` (the C13S18 repaired construction; pos-iters passed explicitly — the flag defaults to 8).
- **C11 parent-reproduction + C12 negative control PASS:** fresh nominal level `nom_r9` (this unit, 13 rollouts incl. F8) is **bit-identical on every stored array to C14S01's `nom_r1`** (9 days apart, same machine). Process A/A remains exactly zero; the Isaac map is deterministic and directly comparable.
- **New MuJoCo arm negative control PASS:** `walkdr01_mj_arm.py` with no perturbation is **bit-identical (20/20 arrays) to the banked `mj_reference_arm.py`** on the control cell. Perturbation application is copied verbatim from `isaac_arm.py` (kp/kd pre-multiplied inside the law; torque scale applied downstream of the ±12.5 controller clamp, no second clamp).
- Instruments: `analyze.c62_metrics`/`q51_metrics` (immutable Phase-13 instruments) via `analyze_c14s01.cell_metrics` and its exact materiality rules (stride/cadence/speed 5% rel · curv20 10% · DS 2.5 pts; CLIFF = fall/numerics/τ99>10). Foot separation: analytic OBB-SAT + exact sphere math (`se3_footfix44o._sat/_pt_obb`) on FK replay — **mj_geomDistance is not used anywhere** (known-unreliable).
- Plant condition of every number in this report: **nominal one-factor perturbation, deterministic single-IC cells** (NOT the additive_dr training ensemble).

## 2 · Method

Adopted evidence: 14-C14S01 (63 levels · 819 rollouts, `rgl/artifacts/c14s01_sensitivity_20260814/SENSITIVITY.json`) is adopted verbatim as the base one-factor map — its artifact dir is untouched. This unit adds:

- **Isaac bracket sweep** — 13 new levels × 13 cells (169 rollouts, ~28 min · 4 CPU workers): kp ×{1.025, 1.05, 1.075, 1.10, 1.125}, torque ×{1.15, 1.20, 1.25}, mass ×{0.925, 0.875}, ground-μ {0.3, 0.2}, + nominal replicate `nom_r9`. Driver `rl/isaac_parity/walkdr01_sweep.py` (imports `sweep_c14s01.cells_for_level` — cell set byte-identical: stand/crown/κ±0.35/κ±1.0 ×2 seeds @700 + F8 @4700).
- **MuJoCo cross-engine arm** — `rl/isaac_parity/walkdr01_mj_arm.py`, control-side kinds only (engine-independent semantics), 12 levels × 12 cells (144 rollouts): nominal + kp ×{1.05, 1.075, 1.10, 1.125, 1.15, 1.30} + torque ×{1.10, 1.15, 1.20, 1.25, 1.30}.
- **New channels recomputed offline from traces** (no re-simulation): 3-way analytic foot separation (plate-plate OBB / sphere-sphere real geometry / sphere-vs-plate-AABB bound), κ symmetry (|curv20+|/|curv20−|), tilt, loaded-foot slip, action-saturation duty.
- Analyzer: `rl/isaac_parity/walkdr01_analyze.py` → `rgl/artifacts/walkdr01_dr_map_20260823T230443Z/WALKDR01_MAP.json` (+ `foot_sep_split.json`, `tilt_slip_supplement.txt`). It refuses to classify if the nom_r9 bit-gate fails.

## 3 · Family maps — certified / transition / first-failing

| family | axis | CERTIFIED (all channels, both engines where measured) | TRANSITION | FIRST CLEARLY FAILING |
|---|---|---|---|---|
| B actuation | **kp scale** | ≤ **×1.075** | ×1.10–×1.125 — MJ fall (km10@617); Isaac micro-graze −0.13 mm; tilt 10.1° excursion; κ±0.35 sym 1.33→2.10; stride drift 2–5× materiality | **×1.15** — Isaac κ±1.0 falls; MJ 3 cells; MJ ×1.30 all four κ±1.0 |
| B actuation | torque scale | ≤ ×1.15 | ×1.20–×1.25 — MJ kp10_s1 falls @386/@403; Isaac clean but 6–7× materiality (κ±0.35 curv20) | ×1.30 (Isaac cliff; MJ 1 cell from ×1.20) |
| B actuation | kd scale | ≤ ×1.30 measured, no falls (C14S01) | — | none found |
| C plant | link mass | ±5% | ×0.925–×0.875 — 3–5.6× materiality, no falls | ×0.90 — **F8-only, knife-edge** (×0.875 clean; non-monotone, disclosed) |
| C plant | CoM / inertia / armature | CoM ±5 mm, inertia ±30%, armature full measured band (14 levels) | quality-HIGH only | none found |
| A contact | ground μ | μ ≥ 0.6 (hw band) | μ 0.4–0.2 — curv20 drift at κ+1.0 up to 7.8× materiality (μ0.2); slip UNMOVED 0.5–1.0 mm/step | **none found down to μ=0.2** |
| A contact | joint static/dyn friction | C14S01 bands | quality-HIGH | none found |
| D observation | noise/latency | — | — | **N/A: no existing machinery** (nothing invented, per ticket) |

Success semantics preserved: C14S01's exact classification rules, thresholds and cells; no gate was created, moved, or relaxed. The κ±0.35 symmetry ratio is judged against the Crown's own Isaac **nominal baseline of 1.333** (a known C13S18-era small-κ residual), not against 1.0.

## 4 · Cross-engine verdict

The kp cliff is **NOT engine-specific** — it reproduces on the canonical MuJoCo plant through the same port, and MuJoCo is the **tighter engine** (first fall ×1.10 vs Isaac ×1.15; torque ×1.20 vs ×1.30). Per the Phase-14 law ("a stability line drawn on one engine must be re-verified on the other; the tighter engine governs"), the shell boundary is drawn at the MuJoCo edge. Deterministic non-monotonicity is real and disclosed: MJ kp×1.10 falls / ×1.125 clean / ×1.15 falls; mass ×0.90 F8 falls / ×0.875 clean — knife-edges, not noise (all cells deterministic).

## 5 · Channel evidence (staples)

- **Survival:** Isaac bracket 169/169 rollouts no falls; C14S01 cliffs as adopted; MJ falls exactly as tabled above. Fresh deterministic single-IC cells; one boot per command cell.
- **Speed/stride/cadence/DS/curv20:** per-level worst-materiality ratios in `WALKDR01_MAP.json` (all bracket levels HIGH: 2.1–7.8×; no numerics trips anywhere).
- **Slip:** loaded-foot p95 0.54–1.01 mm/step across every level incl. μ=0.2 — μ-insensitive (Phase-13 solver-limited micro-glide unchanged).
- **Tilt:** nominal p95 ~4.3°; kp×1.125 kp10 tilt_max 10.06° (pre-cliff excursion signature).
- **Torque:** τ99 max 6.26 N·m (MJ, kp×1.3) / 8.97 (Isaac, C14S01 worst) vs cap 12.5 — the shell failures are balance failures, NOT torque saturation; nothing rides the clamp (ankle-roll clip duty 0 everywhere).
- **Action saturation (command authority):** |a|≥0.999 duty ≈ 0.0000 at every level (worst per-dim 0.4% at kp×1.3) — full corrective authority available inside the action box at the shell edge. **Command-authority preflight for the kp shell: PASS with margin.**
- **Foot-foot collision (ticket invariant, real colliding geometry = sole spheres):** nominal 0 overlap frames both engines (min +0.35 mm Isaac stand-adjacent, +3.76 mm MJ); first graze at kp×1.10 (−0.13 mm, 1 frame); kp×1.15 −4.99 mm/3 fr; kp×1.3 −4.66 mm/13 fr; mass×0.9 −5.82 mm/1 fr. The plate-AABB channel (conservative overbound) grazes −1.6 mm in nominal STAND cells — reported for context; the invariant is scored on real geometry. Full table: `foot_sep_split.json`.
- **Command symmetry/monotonicity:** κ±1.0 ratio 0.96–1.07 across all levels (healthy); κ±0.35 ratio degrades one-sidedly along the kp shell (1.33→2.10) and flips side under torque scale (0.64–0.69) — tracked as a Stage-5 preservation channel against the nominal baseline.

## 6 · Stage-3 choice: ACTUATION kp-high shell

Per the ticket's criteria:
- **Earliest meaningful degradation:** kp×1.10 (MJ fall + Isaac graze) — earlier than torque (×1.20), mass (×0.90, F8-only knife-edge), contact (none), observation (N/A).
- **Physically relevant:** PD-gain/drive error is a real actuator uncertainty; C14S01 classes ±15% as hardware-plausible, and torque-gain accuracy is UNEVIDENCED in the repo (no Kt/gearbox η) — the same-family sibling backs the choice.
- **No owner hardware assumption:** we assert no hardware band as truth; we expand tolerance around nominal. (Armature — the only band-grounded parameter — has no failure to fix.)
- **Trainable without architecture change:** the qualified trainer already draws per-world kp scales (`batch_sim_tip.py` additive_dr); the shell needs only a flag-gated, default-off variant of that existing draw. No reward/obs/action/reference/teacher/specialist change.

No equally-plausible competing target exists — this is not an owner coin-flip. The STOP below is Guard-structural, not ambiguity.

## 7 · Stage-4 staged plan (pre-declared; not launched)

- **Card** = crown lineage card (`cy_stride_polish_r_s0` settings) + marked deltas ONLY: `--continue-ckpt` = the frozen Crown; `dr_shell` sampling; 3 seeds; true `--total-steps 2654208` per seed (3× the Crown's own budget; ckpt every 294,912 → 9 ckpts/seed; MJWarp ≈ minutes/seed).
- **Sampling (ticket §4):** 85% of worlds kp ×U(1.00, 1.20) (the transition+first-failing shell, capped below the ×1.25+ deep-cliff), **15% exact nominal** (kp=kd=nominal, delay 0). Implemented as a config-gated branch beside the existing additive_dr draw in the reset kernel — default off ⇒ byte-identical; neutrality proven by dump-identical smoke; PRE-PPO integrity receipt (`rl/integrity/`) binds card argv + import closure + term ledger before launch. Reward book byte-identical to the Crown card (resolved book diff must be EMPTY; receipt echoes law/plant/scales).
- **Anchor (nominal preservation):** `--anchor-ckpt <crown ppo_884736.zip> --anchor-w0 0.15 --anchor-decay-steps 8388608` (the lineage trust-region recipe; anchor gradient exactly 0 at the seed).
- **Capability buckets + retention window (C9/§9c, declared now):** N1 stand · N2 straight nominal · N3 κ±0.35 · N4 κ±1.0 · N5 F8 · N6 foot-clearance (real-geometry overlap = FAIL), each with the frozen nominal acceptance envelope (materiality thresholds above); D1 shell survival · D2 shell quality vs the frozen-Crown baseline table in this report. **NOMINAL SCORE** = worst N-bucket violation; **DR SCORE** = shell survival then quality. A checkpoint qualifies only if DR SCORE improves with NOMINAL SCORE inside the envelope; two-consecutive-checkpoint qualification; retention window ≥ 2× steps past first qualification within the ceiling; persistence claims quoted across the 3 seeds with direction frequencies (C10). **If PPO improves DR by degrading nominal → FAIL/STOP** (kill-gate families: survival/cadence/speed/curvature/touchdown-pelvis on nominal cells).
- **Evaluation:** per-checkpoint deterministic MJ-arm battery (nominal 12 cells + shell levels kp×{1.05,1.10,1.15,1.20,1.25,1.30}) — CPU truth path ranks; Isaac arm battery + F8 on finalists (the deliverable basin is Isaac's); GPU numbers never make claims. Checkpoint-mining table with best/balanced/final/recommended. Walking oracle + frame strips before any report; motion claims wait for Joe's eyes (C8).
- **Inspector:** spectator pre-staged BEFORE `run guarded` (atomic checklist item): `spectator_watchdog.sh <run_dir> <trainer_pid>` → `isaac_spectator.py --watch --mujoco` with the run's live-training manifest sidecar; "episode start" verified; one viewer; closed by exact PID after the final report.
- **Turning-intent gate:** `locomotion_intent_gate.sh declare_intent` + `check_oracle_regression` run at card registration (κ cells are in scope).
- **CACHE_WORKER_ENV disclosure:** the flag exists only in `rl/se3/se3_recover39a.py` (CPU worker-pool collector). The walking-lane qualified PPO stack is MJWarp `train_tip_reftrack.py`, which has no worker pool. WALK-DR01 sets `CACHE_WORKER_ENV=True` in any executed path that consumes it; none of Stage 1's harnesses do. Disclosed, not silently dropped.

## 8 · OWNER DECISION PACKAGE (the unit's STOP)

Guard live state: `evaluate_authority(live,'ppo')` → **allowed=False**, campaign bound to `12U-C11S65A`. No PPO can launch, and no agent may author the ruling artifact. Joe's sentences (plain, no conditionals — NEGATION words void grants):

1. `I authorize transition to WALK-DR01 in the acquisition lifecycle, from 12U-C11S65A.`
2. `I authorize ppo for WALK-DR01 on <launcher-target-id>, 3 seeds of 2654208 steps each with a 2654208 step ceiling.`

Then the standard chain: ruling JSON in a tracked artifact path → commit → `campaign-bootstrap-commit` → `decision request` (subjects: WALK-DR01 + every named target) → `decision resolve` → `transition --authorized-by D-…` → `target register` for the new launcher → the ppo decision → `run guarded`. I drive the chain; Joe supplies only the sentences. If Joe prefers a different shell (e.g. include torque-scale co-draw, or a wider/narrower kp band, or different seed/step counts), the card takes it without redesign.

Also for Joe's awareness (no action needed now): the nominal Crown's STAND cells carry a −1.6 mm foot **envelope** (plate-AABB overbound) proximity — real colliding geometry is clear by +4.5 mm there; flagged for whenever the walking-plant foot-geometry question (cf. recovery-lane 44O) is next opened.

## 9 · Scope compliance

No promotion, no canonical/latest mutation, no obs/action/interface change, no terrain, no TIP, no reward change, no gate relaxation. New files are unit-named additions (`walkdr01_sweep.py`, `walkdr01_mj_arm.py`, `walkdr01_analyze.py`); banked C13/C14 instruments and artifact dirs untouched. Family D honestly reported N/A rather than invented. This unit ran zero PPO.

## 10 · Artifacts & replay

- Map + channels: `rgl/artifacts/walkdr01_dr_map_20260823T230443Z/` (`WALKDR01_MAP.json`, `foot_sep_split.json`, `tilt_slip_supplement.txt`, `levels_walkdr01.json`, `cells_level.json`, `traces/` 169 npz, `mj/` 146 npz, `logs/`). Traces carry per-rollout PERTURB_RECEIPTs.
- Adopted base: `rgl/artifacts/c14s01_sensitivity_20260814/` (immutable).
- Replay any Isaac level: `isaaclab_env/bin/python rl/isaac_parity/isaac_arm.py --foot-material --pos-iters 100 --dyn-friction --rollout <adir>/cells_level.json --out-dir <dir> [--perturb '<json>']`. MuJoCo: `.venv/bin/python rl/isaac_parity/walkdr01_mj_arm.py --cells <adir>/cells_level.json --out-dir <dir> [--perturb '<json>']`.

**changelog row:** `WALK-DR01, 20260823T230443Z` — post-append `rgl/LOCOMOTION_CHANGELOG.csv` sha256_16 `467360159cf1db51` (461 rows × 13 cols, both trees byte-identical)
