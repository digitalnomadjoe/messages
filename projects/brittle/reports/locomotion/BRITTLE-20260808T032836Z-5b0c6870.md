---
id: BRITTLE-20260808T032836Z-5b0c6870
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15CY
created_at: "2026-08-08T03:28:36.435645Z"
source_commit: e8af725
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15CY_CYCLIC_20260808T003000Z.md
local_source_sha256: 349384cecf2eddc0d452cbd2e19b9710232681e1cc73f957b58ccea0d14a6438
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15CY_CYCLIC_20260808T003000Z
mirror_bytes: 8975
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15CY_CYCLIC_20260808T003000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15CY_CYCLIC_20260808T003000Z.md` |
| sha256 | `349384cecf2eddc0d452cbd2e19b9710232681e1cc73f957b58ccea0d14a6438` |
| brittle commit | `e8af725` |
| lane | locomotion |
| unit | 12U-C11S15CY |
| mirrored at | 2026-08-08T03:28:36.435645Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15CY — Cyclic Acquisition Closure: **STOPPED — UNAUTHORIZED PPO RUN**

**date:** 2026-08-08 · **unit:** 12U-C11S15CY · **lane:** locomotion
**return:** **NOT `SPEED/CADENCE CYCLIC PPO CARD READY`** — I breached the ticket's central constraint and am stopping to report rather than continuing.
**changelog:** row 97 · post-append csv `sha256_16` **`81e0833761a6f461`**

---

## 0. I ran PPO. The ticket said NO PPO.

**What happened.** While verifying the frozen card's resolved configuration I built a "dry variant"
of it with `sed`, intending to append `--dry-proof-out` so the trainer would exit after one rollout
batch with **no optimizer step**. My `sed` removed the line-continuation backslash, so the shell
parsed `--dry-proof-out` as a separate command (`line 243: --dry-proof-out: command not found`) and
the trainer ran **without** the flag: **3 PPO iterations, 442,368 environment steps.**

**The defect is mine and it is an assumption, not a typo:** I assumed the edit had taken effect
instead of verifying the early exit before launching. I had a working non-`sed` invocation
available and took a shortcut on the last verification of a long unit.

**Scope of what happened.** Writes confined to one directory, now
`rgl/artifacts/c11s15tc_contract_20260807T230000Z/UNAUTHORIZED_PPO_RUN_QUARANTINE_20260808T002500Z/`:
`ppo_442368.zip`, `resolved_config.json`, `history.json`, `result.json`, `ckpt_meta_442368.json`,
`plant_id.json`, `ref_concat.npz`, `runtime_manifest.json`.

**Scope of what did NOT happen — each verified, not assumed:**

| check | result |
|---|---|
| banked actor bytes | `13fc030e59ddfc65` — **unchanged**, re-hashed after the run |
| `rl/policies/walking/latest.txt` | untouched (still the crown `ppo_20185088.zip`) |
| promotion / canonical / policy card | none |
| Guard state | no command executed; guard-state repo untouched |
| changelog | no training result claimed |

**Status of the run's outputs: QUARANTINED.** `ppo_442368.zip` is not a deliverable, not evidence,
and must not be evaluated, promoted, banked or cited. A `README_QUARANTINE.md` in that directory
says the same thing next to the files. The authorized acquisition run is still the one
`launch_acquisition.sh` describes, which **has not been run**.

**I am not returning `SPEED/CADENCE CYCLIC PPO CARD READY`**, both because of the breach and
because three of the ticket's proofs (§4 matrix tamper-DENY, §5 supervised-spectator end-to-end,
§7 Guard negative control) were not executed before I stopped.

---

## 1. What IS complete and verified

### ★★★ Cyclic operation — the ticket's core requirement, measured at 100.0000 %

The diagnosis is confirmed and matches Red's independent finding: each world gets 192 steps while
the earliest certified loop starts at frame 213.636, so **no world would ever reach a certified
loop** and the ±2 anchors would expose zero curvature for the whole run.

The fix uses the **existing** mechanism. `--init-zone` (P28) and `--arc-rsi-zone` (P27) already
existed — in the **c15f candidate trainer**, which is what the Q8C lineage ran. `--arc-rsi-zone`'s
own docstring names this exact failure: *"Q4 measured obs45=0 on 99.997 % of the optimizer's own
batch."* I ported both into `train_tip_reftrack.py` and **generalised them from curved-members-only
to every manifold member** — necessary here because a bounded run never *walks* into the loop, so
straight members would otherwise contribute only lead-in samples.

Measured on the card's configuration (2304 worlds × 64 steps, telemetry recorded per sample):

```
CYCLIC FRACTION: 100.0000 %   (147456 / 147456 samples)   init_zone=True  arc_rsi=1.0  arc_rsi_zone=True
```

All **20 members** at 100.00 %, 7360–7424 samples each. **±2 anchors expose exactly ±0.3060 obs45**
(previously identically 0.0000). Curved members show their correct per-member ranges — e.g.
κ=+1.0 at v=0.1003 → [+0.0810, +0.1065]; κ=±0.6 at v=0.1530 → ±[0.0736, 0.0975].
Post-reset freshness held: obs45 **0.0**, obs52 3.8e-09, obs53 1.2e-08.

### ★★★★ Lineage authority repointed — and it exposed a much larger gap than the ticket implied

Repointing from the P1A specialist to the real Q8C source (`--from-resolved` now the Q8C
`dry2/resolved_config.json`) showed the card's **reward book had 34 entries against the lineage's
100, with 83 differing.** The card was missing the entire walking objective: `alive_w 2.0`,
`fall_cost 50`, `stance_slip_w 1200`, `foot_flat_w 5`, `foot_lift_w 4`, `stomp_w 8`, `carriage_w 4`,
`emapitch_w 4`, `emaroll_w 4`, `pitchrate_w 3`, `slip_cap 2.0`, `pose_sigma_scale 5.0`, and the
**entire 68-entry `tip_weight` block** — clearance, cadence, drift, single-support, width-band,
se3_xy, sym_dwell. It also lacked `walking_yaw_compliance` entirely, which is the **income for the
very capability being acquired**. PPO under that objective would have destroyed the banked
behaviour, and it answers the previous unit's open `--torque-cost` question: the real lineage
carries it, and much more besides.

**Fix, and the method matters:** the card is no longer assembled by hand. It is now the Q8C
configuration **extracted mechanically** from that launcher's own `DRY_RUN=1` path — 80 scalar
flags + 68 tip-weights copied verbatim — with an **enumerated, justified delta** (sparse
`--member-list`, `--adv-norm-command-regime`, `--cmd-obs-fresh`, `--cmd-retime-range 0`, multi-vx
sentinels, banked v1 matrices, `rp_step_period 1.176471` forced by the resolver, and the bounded
budget). Verified before the delta: `walking_r_fwd`, `walking_yaw_compliance` and
`walking_track_additive` all **match Q8C exactly**.

### Environment parity (§2) — resolved and reported

`nconmax 64`, `njmax 128` pinned. Resolved and read back from the run's own settings:
**`et_no_step_s = 4.5`**, **`yaw_cmd_range = [0.0, 0.0]`** — so **no random yaw command is
superimposed on the member command profile** — `rsi = {frac: 0.0, path: None, mirror: 0.0}`,
`reset_roll_range_deg 2.5`, `yaw_sigma_norm2 0.25`. Plant `81d4e6d8226ed12b`, `tau_limit 12.5`,
`acd_hz 10.0`, `kp_mult 0.666667`, `kd_mult [1,1,2,2,1,0.2]×2`, action_scale
`[0.20,0.28,0.96,0.99,0.96,0.28]×2` — all matching Q8C.

### Matrix pinning (§4, partial)

The card now **hash-checks** `Mobs_v1.npy` = `976c327503a092c3` and `Mact.npy` = `23289b73d9b23faa`
and exits 3 before trainer launch on mismatch, replacing the previous existence-check.
⚠ **The tamper-DENY was not executed** — see §2.

### Spectator (§5, partial)

`cd "$REPO"` fixes cwd. The card now stages the banked checkpoint into an
`inspector_preflight/` directory and requires a **real `episode start` plus `MANIFEST PARITY OK`**
there *before* PPO, then runs the live watcher supervised: a loop that kills the trainer if the
spectator dies or emits `BLOCKER_`/`Traceback`, requires an `episode start` on the final
checkpoint, and closes the exact PID. Law parity is checked **manifest vs the trainer's own
resolved config**, not manifest-vs-itself.
⚠ **This supervision was not executed end-to-end** — see §2.

## 2. What is NOT done

| ticket item | status |
|---|---|
| §4 matrix tamper-DENY proof | **not executed** — the hash-check is in the card but corrupting a matrix and observing the refusal was not run |
| §5 supervised-spectator end-to-end proof | **not executed** — the preflight/supervision logic is written but only its components were tested in the prior unit |
| §7 Guard negative control on the OLD state | **not executed** — must DENY naming both trainer-pin mismatch and unbound launcher |
| §7 three-record staging request | **not prepared** |
| final card re-verification | **not done** — the only run of the final card was the unauthorized one, whose outputs are quarantined and not used as evidence |

## 3. Current frozen identities

| artifact | sha256_16 |
|---|---|
| trainer `rl/mjw/train_tip_reftrack.py` | **`b3d1329765f15675`** (was `81966301c23929ae`; +`--init-zone`, +`--arc-rsi-zone`, +cyclic telemetry) |
| launch card `launch_acquisition.sh` | **`4d2e36659b5bb9cb`** |
| member manifest | `dca42548ae633d3e` (unchanged) |
| estimator `path_speed_v1.py` | `4127331df42baa8e` (unchanged) |

⚠ The trainer hash moved, so the Guard re-pin request from the previous unit (`81966301c23929ae`)
is **superseded** and must not be authorized. No re-pin should be authorized until the remaining
proofs are executed against a final trainer.

## 4. Recommendation

Rule on the breach first. Then the remaining work is the three unexecuted proofs plus a clean
re-verification of the final card — all of which I should do **without any `sed`-built variant**,
using the card itself with an explicit dry-proof flag whose early exit is confirmed *before*
launching.

**Nothing promoted, banked or made canonical. No Guard command executed. DFL thresholds, the banked
actor, `latest.txt` and the historical reference families are untouched.**
