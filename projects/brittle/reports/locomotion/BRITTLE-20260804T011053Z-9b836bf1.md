---
id: BRITTLE-20260804T011053Z-9b836bf1
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S6A
created_at: "2026-08-04T01:10:53.659749Z"
source_commit: ca38ba2
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S6A_CONSTRUCTION_AUDIT_20260804T010855Z.md
local_source_sha256: 5621f85c8c1c1ea5df1c79f7e079a2fd2c016e9516d1bcb0fcaa159454ea62e8
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S6A_CONSTRUCTION_AUDIT_20260804T010855Z
mirror_bytes: 13871
---
# UNIFIED_LOCOMOTION_PHASE12UC11S6A_CONSTRUCTION_AUDIT_20260804T010855Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S6A_CONSTRUCTION_AUDIT_20260804T010855Z.md` |
| sha256 | `5621f85c8c1c1ea5df1c79f7e079a2fd2c016e9516d1bcb0fcaa159454ea62e8` |
| brittle commit | `ca38ba2` |
| lane | locomotion |
| unit | 12U-C11S6A |
| mirrored at | 2026-08-04T01:10:53.659749Z |

<!-- brittle:mirrored-content -->

# 12U-C11S6A — End-to-End Walking Construction Audit

**date:** 2026-08-04 · **unit:** 12U-C11S6A · **lane:** locomotion (brittle-telemetry) · read-only, no edits, no PPO
**constitution:** C1, C2, C3, C6, C7, C12 · **potential violations:** none · **amendment proposed:** no
**status:** AUDIT COMPLETE — **VERDICT: NO-GO.** 5 of 11 primary objectives have NO live discriminating signal; the kernel's entire step-shaping quintet is dir-factor-gated to exactly 0 at straight walking; 22 of 37 kernel channels measured dead; the imitation channel currently pays standing 7.5× more than walking.
**recommend:** ONE next action — owner adjudication ticket **12U-C11S7 "straight-walking economy repair"** over the minimum ordered repair set R1–R7 (§8; design pass, still no PPO).
**decision:** Joe — R1–R7 adjudication
**next:** await ruling; nothing runs

---

## 0. Exact construction audited (pinned)

| element | pin |
|---|---|
| launcher | `rl/scripts/launch_c11s4_walking.sh` sha256_16 `c4986396051cb271` (frozen); crown book via 69 `--tip-weight` flags from `walking_sil027v2_rung0c…/rollpolish10/settings_summary.json`, appended LAST (last-wins) |
| trainer | `rl/mjw/train_tip_reftrack.py` `4fe4a5b5dc8ef163` (Phase-A corrected; supersedes bound pin `dd73337a…`, re-registration pending) |
| branch | `--behavior walking --obs-phase --cmd-profile --ref-truncate --entrain-arc --entrained-phase --arc-rsi 0.3 --rsi-frac 0` → ReftrackAdapter plain obs_phase path (walk52_v58); Stage-B1 manifold branch NOT taken (no `--cadence-set`) ⇒ `sp_w = 0` |
| reference | `carriage_sweep_20260716T211854Z/curved_walk_c344.npz` sha1 `c602133c…` (660 fr, 1.340 s same-foot stride, duty 0.64, pelvis lat-vel RMS 152.8 mm/s); phase φ = 2π·frame/659, arc-entrained (known 11.4% Δx/arc mismatch kept AS-IS by construction) |
| law / plant | walking-law v2 effective `7c05558aacc0f0c8` (kp 20/20, kd [1,1,2,2,1,0.2], τ 12.5, ACD 10 Hz, 50/500/500); plant v2 `81d4e6d8…` / complete v3 `c44ad73f…` |
| resolved book | dry-run resolved_config fingerprint `5ce8f588a2fd2a38`, reward_hash `279225caaa94ab65` (c8j.1), `rp_stance_min_t 0.30` |
| eval/oracle/inspector | `c11s5_deep_eval.py` (GPU, force-channel), `walking_oracle.py` (CPU, z-channel), `isaac_spectator`+`viewer_equiv_gate` (CPU, manifest-constructed; manifests now truthfully c344) |

## 1. Requirements-to-signal traceability (the master table)

| objective | obs/input | active reward/constraint | measured (per-step, deterministic 64-world) | discriminates? | **verdict** |
|---|---|---|---|---|---|
| sustained alternating walking | t-since-td 46–47, phase 50–51 | `r_alt_step` w=1.5 **× dir_factor = 0**; `r_airtime`, `r_single_support`, `r_ss_hold` same gate; constraint-side: real-step ET (honest post-0.30) | r_alt_step **+0.0000** all subjects | NO reward channel distinguishes alternation from any other locomotion; ET only enforces *some* qualified step | **FAIL** (constraint-only; alternation unpriced) |
| commanded forward speed | **no vx obs column** (52-d has none); speed only implicit in profile | adapter `r_fwd` w=12 peaked on arc rate | +10.98 walking vs 0.00 standing | yes at the FIXED 0.31 speed; command dead (single value, unobservable) | **PARTIAL** — pass at fixed speed, commandability dead |
| reference cadence / step freq | inferable (phase clock vs own td timing) | `r_cadence`: sp_w=0 → legacy branch **× dir_factor = 0** | **+0.000000** every subject | no | **FAIL** (dead channel, 4th dir-gate instance) |
| step length | inferable (wshift 48–49 spread ±0.108 on ref vs ±0.057 realized) | none (no term prices stride geometry; C11S4F blindness re-confirmed) | — | no | **FAIL** |
| stance/swing timing | t-since-td obs live | thresholds only (0.30/0.15/21 mm gates); `p_cadence` −0.0004 negligible | no gradient toward 1.34 s timing | no | **PARTIAL** (gates, not shaping) |
| foot clearance | priv foot-z (critic) | kernel `r_clearance` w=2.5 + adapter `r_lift` w=4 | +0.031 / +4.93 walking vs 0.00 / +1.98 standing | yes (duty-saturation caveat, C11S4F) | **PASS** |
| low stance slip | — | adapter `p_slip` (stance_slip_w 1200→6816 ramping) | −0.157 walking, 0 standing; slide exploit still nets +18/step overall | yes as tax; magnitude ~1.4% of book | **PASS** (weak) |
| pelvis smoothness | angvel/linvel obs live | `p_lvl_roll`, `p_lat_excursion`, `p_pelvis_linvel`, `p_pelvis_zvel`, `p_bob` **all measured 0**; live: `p_angvel_xy` −0.003, `p_lvl_fa` −0.0003; emaroll/emapitch = DC-lean fences (4° allow, satisfied at 2.76° bias), fold into unledgered `r_style` | Σ ≈ −0.006/step (0.03% of book) | no | **FAIL** (oscillation unpriced; only DC fences live) |
| heading retention | grav/angvel obs; priv yawrate−cmd | `r_yaw` +0.003, `p_drift` −0.165 (lat drift, largest live kernel penalty) | outcome good (yaw drift −1.3°, yd% ~1) | partially | **PARTIAL** |
| standing retention | — | in-lane: standing is PUNISHED (−50.5/step: p_cap −59, no r_fwd; ET kills at 225 by design) | — | n/a | **ABSENT in-lane** (C9 concern for a later unit) |
| speed+turn commandability | obs45 **frozen 0.000 in all states**; no vx col | yaw channels dir-gated; cmd_lo=cmd_hi=0; single cadence/ref | — | no | **FAIL** |

## 2. Observation audit (probe: standing / injected-c344-states / s2-final walking, 64 worlds)

Layout (kernel 50 + adapter 2; critic 58 = 52 + kernel priv6): 0–2 projected gravity (body), 3–5 ang-vel (body, rad/s), 6–8 lin-vel (body, m/s), 9–20 q (rad), 21–32 qd (rad/s), 33–44 prev_action, 45 yaw-cmd (profile), 46–47 per-foot time-since-ANY-touchdown (s, clamp 2.0), 48–49 body-X foot-minus-pelvis (m, wshift), 50–51 sin/cos φ (arc-entrained ref clock). Priv: rf_z, lf_z, com−midfoot x/y, yawrate−cmd, upright r22.

Findings (full per-dim table in `c11s6a_obs_stats.npz`):
- **obs45 = +0.000 constant across ALL states** (turn input dead); **no speed-command dimension exists** in the contract.
- All other dims live and frame-consistent; 46–47 clamp at 2.0 only when standing (correct); phase advances even at stand (kickstart clock — phase is a ref clock, not a gait state); prev_action live in walking.
- wshift 48–49 range on reference states (±0.108 m) vs realized walking (±0.057 m) — the short-step gait is visible to the actor; step length IS inferable.
- No contact-force obs (timing proxies only) — consistent with the onboard contract; critic priv has foot heights, not loads.
- **Train/eval/viewer parity: PASS** post-C11S6 (viewer constructs from the truthful manifest; obs_dim 52 verified; MANIFEST PARITY OK; law effective id equal).

## 3. Reward audit (37 kernel comps + 15 adapter terms, measured on every subject)

**Live economy (s2-final walking, per-step):** r_fwd +10.98, r_lift +4.93, r_zt +1.83, track +0.85, r_clearance +0.031, r_survival +0.020, r_upright +0.019, r_width +0.005, r_yaw +0.003 | p_drift −0.165, p_slip −0.157, base −0.094, p_flat −0.004, p_angvel_xy −0.003, p_action_mag −0.003, d_act_* +0.068 (bonus-shaped).

**Dead kernel channels (22/37 measured +0.0000 on every subject):** the dir-gated step-shaping quintet (`r_airtime, r_cadence, r_ss_hold, r_alt_step, r_single_support` — weights 1.5/0.8/1.5/1.5/… configured, **× dir_factor = 0 at zero yaw command**), plus r_wrongway, r_yaw_integ, r_yaw_rate, r_ds, r_under_com, p_bob, p_pitch_ath, r_com_stance, r_com_offset, p_lvl_roll, r_shift_step, r_shift_acq, r_step_yaw, p_wrongdir, p_pelvis_linvel, p_pelvis_zvel, p_lat_excursion, p_hip_yaw_neutral.

**Inversions / hazards:**
- **track pays standing +6.42 vs walking +0.85** — at current tracking quality the imitation gradient at spawn prefers stillness (mid-anneal σ 2.92 widens this; see §4).
- `r_yaw` and `r_pel_xy` also pay standing more than walking (+0.030/+0.032 vs +0.003/+0.0004).
- Same-foot hopping (analytic): cadence (if wired) pays per-foot periods without alternation; with `r_alt_step` dead, **no channel distinguishes hopping from walking** — alternation currently survives on dynamics alone.
- Ledger completeness (C12): emaroll/emapitch/swing_clr/stomp/carriage fold into `r_style` with **no ledger key** — unmeasurable channels in ckpt_meta.
- Known exploit set unchanged from H (slide 18.27/step; S1 21.94; margins +3.574) — orderings hold, but ALL of walking's margin lives in r_fwd+track, none in gait shape.

## 4. Spawn, curriculum, termination

- Spawns: 70% standing (phase clamped to lead-in) / 30% arc-RSI **on respawns only** (never at reset t=0 — C11S4I); reference-frame coverage uniform on respawn; injection state validity proven (C11S4I run-2); law v2 removed the 83% injection-fall hazard.
- Per-spawn behavior (C11S5 battery): stand vs arc-RSI parity (falls 0–4/64 both; ep-len equal).
- **Curriculum: AnnealGovernor ACTIVE and only 52% complete at the 48M ceiling** (pose σ 5.0→2.92 of target 1.0; slip-w 1200→6816 of 12000; err-ET 0.60→0.366 m): **the run never reached its final-book condition** — every C11S5 conclusion is a mid-anneal measurement.
- Termination: fall z<0.28 / tilt>50° (correct, genuine-fall only); real-step ET 4.5 s on the FORCE channel — post-0.30 it no longer kills valid walking (0/64 stalls) and still terminates standing at 225 (0 resets) and slide/chatter (0 qualified); clock-farming niche removed (walking episodes reach truncation legitimately).

## 5. Action & control

12/12 joints commanded; scales [0.20,0.28,0.96,0.99,0.96,0.28]×2 around the ACTIVE reference (q_des = q_ref + scale·a) — c344 representable at a=0 (H: foot_err 4e-9), clip measured 0.0000; ACD 10 Hz / PD 500 / policy 50; law v2 both substrates, CPU/GPU parity max|Δτ| = 0.0 (C11S4K); τ cap 12.5, occupancy ≤6e-4; init contract 52/58 enforced by launcher D3; reference injection only via declared arc-RSI; **no train/eval control-law divergence** (manifest-constructed viewer, effective-value readback).

## 6. Evaluator / inspector semantic-disagreement ledger

| # | disagreement | status |
|---|---|---|
| 1 | kernel measures stance/swing on FORCE-contact; oracle/viewer on z-height (20–40 ms gap) | DECLARED in-kernel (C11S6); thresholds force-calibrated; instruments must not reuse them on z |
| 2 | old `rp_stance_min_t` 0.35 sat inside that gap → training/GPU-eval killed walking the CPU oracle accepted | REPAIRED (0.30, regression-proven) |
| 3 | manifest stamped k0 profile for a c344 lane (viewer ran the wrong-but-timing-identical profile) | REPAIRED (D-31; manifests now c344) |
| 4 | trainer prints `walking_control_law_id` before kp/τ setters → printed id ≠ effective id | OPEN, display-only (**D-35**) |
| 5 | training ledger (ckpt_meta) lacks keys for r_style-folded taxes and all kernel comps (only 15 adapter terms) | OPEN (C12 gap) — kernel comps are measurable only via the comps buffer |
| 6 | my C11S5 deep-eval qualifier reimplemented kernel thresholds on the force channel with its own chatter logic → same misgrade as the kernel | superseded by the shared-predicate grader (C11S5A) |

## 7. Verdict

**NO-GO.** Alternating walking, cadence, step length, pelvis smoothness, and commandability have **no live discriminating signal** in the exact launch path; forward speed passes only at its single hard-wired value; the imitation channel's gradient currently favors standing over the acquired walking. The C3 exploit orderings hold (nothing here contradicts H), but C3 PASS does not substitute for five dead primary objectives — the acquired s2-final gait is what the LIVE economy (r_fwd+r_lift+r_zt+clearance+slip, all gait-shape-blind) actually purchases.

## 8. Minimum ordered repair set (design adjudication, NOT implemented here)

1. **R1 — un-gate the step-shaping quintet at straight:** dir_factor must not zero `r_alt_step`/`r_airtime`/`r_single_support`/`r_ss_hold`/cadence at |cmd|=0 (scope the gate to turn-following channels only). Prerequisite for objectives 1, 3, 5.
2. **R2 — wire `sp_w` in the plain path** (Stage-B1 mirror, `check_per_world_tempo`-guarded): activates the normalized cadence branch and its validity/tempo machinery.
3. **R3 — cadence weight ruling:** at w=1.5 the channel is ≤0.2% of book; pick a binding weight (or restructure) so period preference has economic force (C11S6 sweep tables give the σ landscape; σ alone proven insufficient).
4. **R4 — step-length economy:** either accept cadence×fixed-speed as the stride price (R2+R3 imply 205 mm at 0.31 m/s) and VERIFY via C3, or add an explicit stride term (owner design).
5. **R5 — pelvis-oscillation pricing:** replace the dead p_lvl_*/p_pelvis_* set with a reference-phase-locked lateral/roll deviation term (magnitude/EMA families proven inverted in C11S6 §4).
6. **R6 — track-vs-standing inversion:** complete/retune the anneal (governor ended at 52%) or re-weight so the imitation gradient prefers walking at achievable tracking quality; consider extending the ceiling so the final-book condition is actually trained.
7. **R7 — commandability (later unit):** vx-cmd obs column + cmd ranges + multi-cadence reference family (Stage-B machinery exists and is proven; requires fresh contract decision — obs width change ⇒ fresh policy).

**Exactly one next action:** issue **12U-C11S7 — straight-walking economy repair adjudication** (owner ruling on R1–R6 scope + weights; C3 re-probe after landing; only then a PPO ticket with construction re-registration over trainer `4fe4a5b5…`).

## 9. Artifacts

`rgl/artifacts/c11s6a_construction_audit_20260804T010855Z/` — obs probe + per-dim stats npz. Evidence reused (already committed): `c11s6_quality_formulation_20260804T001601Z/` (37-comp recordings, sweep), `c11s5a_reconciliation_20260803T234301Z/` (parity, touchdown audits), C11S4G/H probes (exploit books). No source edits were made in this unit.
