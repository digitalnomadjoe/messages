---
id: BRITTLE-20260729T051155Z-44221a2f
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: CERT-BUS-LIVE
created_at: "2026-07-29T05:11:55.609140Z"
source_commit: df7c7b5
local_source_path: /home/robojoe/code/brittle/rgl/reports/CERT_SYNTHETIC_MESSAGE_BUS_LIVE_LOOP_20260729T051500Z.md
local_source_sha256: c69c3e75c7ffb06beb9817d031f448ca91c84aefaf1aaffdaacdd4277a1a81a9
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: SYNTHETIC certification probe — message bus live loop
mirror_bytes: 2858
---
# SYNTHETIC certification probe — message bus live loop

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/CERT_SYNTHETIC_MESSAGE_BUS_LIVE_LOOP_20260729T051500Z.md` |
| sha256 | `c69c3e75c7ffb06beb9817d031f448ca91c84aefaf1aaffdaacdd4277a1a81a9` |
| brittle commit | `df7c7b5` |
| lane | locomotion |
| unit | CERT-BUS-LIVE |
| mirrored at | 2026-07-29T05:11:55.609140Z |

<!-- brittle:mirrored-content -->

# SYNTHETIC CERTIFICATION ARTIFACT — message bus live-loop probe

> **This is not a BRITTLE experimental result.** It is a deliberately synthetic
> report published to certify the autonomous message loop end to end against
> the real OpenAI API. It describes no training run, no policy, no plant and no
> robot measurement. Any number below is infrastructure telemetry.

**status:** message bus installed and serving; live loop under certification
**decision:** none
**key result:** 164/164 bus tests green; 4 user services active; CI green on `main`
**next action:** see *Open verification item* below
**report path:** `rgl/reports/CERT_SYNTHETIC_MESSAGE_BUS_LIVE_LOOP_20260729T051500Z.md`

## What was installed

The GitHub-backed message bus (`digitalnomadjoe/messages`) replaced manual
report/ticket shuttling. Components: an append-only immutable protocol, a
single-source validator library, `messagesctl`, an OpenAI reviewer daemon, two
read-only lane watchers, and a 5-minute sync timer.

## Infrastructure telemetry

| metric | value |
| --- | --- |
| bus unit tests | 164 passed, 0 failed |
| synthetic end-to-end steps | 12 of 12 passed |
| user services active | 4 of 4 |
| GitHub Actions `validate-messages` | green |
| median publish→push latency | ~1.2 s |
| deferred pushes outstanding | 0 |
| spooled outbox depth | 0 |

## Verified behaviours

- Local reports remain byte-identical across mirroring (SHA-256 checked before
  and after).
- Published messages are immutable; status is derived by folding receipts.
- Cross-lane claims are rejected; leases expire and are reclaimable.
- Confidence never overrides a hard gate — enforced in daemon code, independent
  of the model.
- Secrets and forbidden file types are rejected before a commit exists.
- Three daemon restarts produced zero duplicate messages.

## Open verification item

The reviewer's own operating documentation has not been checked for internal
consistency since the daemon gained its startup credential-fingerprint log
line. Specifically: `skills/brittle-reviewer/SKILL.md` describes the daemon's
pass sequence step by step, and that description should be confirmed to still
match what `scripts/reviewer_daemon.py` actually does, with any mismatch
recorded.

This is a **documentation-consistency check only**. It requires reading two
tracked text files in the messages repository and writing down whether they
agree. It involves no BRITTLE code, no training, no simulation, no policy, no
canonical artefact, no Guard interaction, no plant, no interface change, no
promotion and no `latest` movement.

## Prohibitions carried by this report

Any successor work must not touch BRITTLE training, simulation, policies,
canonical artefacts, Guard, the plant, observation/action interfaces, promotion
or `latest`. This artifact exists solely to exercise the message loop.
