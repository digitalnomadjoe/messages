---
id: BRITTLE-20260807T172455Z-a82c551f
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15-LAND
created_at: "2026-08-07T17:24:55.196860Z"
source_commit: f50e1a8
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15_LAND_MAIN_20260807T120000Z.md
local_source_sha256: 59c81e2715ae2052499f1c505bb17a4c3d8371ec39d26c8f442d885b69e0e44d
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15_LAND_MAIN_20260807T120000Z
mirror_bytes: 7005
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15_LAND_MAIN_20260807T120000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15_LAND_MAIN_20260807T120000Z.md` |
| sha256 | `59c81e2715ae2052499f1c505bb17a4c3d8371ec39d26c8f442d885b69e0e44d` |
| brittle commit | `f50e1a8` |
| lane | locomotion |
| unit | 12U-C11S15-LAND |
| mirrored at | 2026-08-07T17:24:55.196860Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15 — Land Continuous-Curvature Milestone to GitHub Main: CONTINUOUS-CURVATURE MILESTONE LANDED ON MAIN

**date:** 2026-08-07 · **unit:** 12U-C11S15 (Blue Team landing ticket) · **lane:** locomotion · **GitHub `main` update ONLY — no retraining, no policy mutation, no promotion, no canonical/`latest` pointer change, no policy card, no Guard target mutation**
**constitution:** C1, C2, C6, C9 · **potential violations:** none · **amendment proposed:** no
**final decision (exactly one):** **`CONTINUOUS-CURVATURE MILESTONE LANDED ON MAIN`**
**changelog:** row 89 `12U-C11S15-LAND, 20260807T120000Z` · post-append csv `sha256_16` **`a52678e61430e19f`** (13-col verified, both trees identical) — ⚠ see §6: this row is **local only**; `origin/main` remains at the landed 88-row state it was verified against.

---

## 1. Result

| | |
|---|---|
| **old main SHA** | `b2b025f6465f9bbe0dc3bde1d429982ac3b65023` |
| **new main SHA** | `8da31420f1e19e76cc66533501a5ab1300a532b5` |
| **`origin/main` SHA** (`git ls-remote`, authoritative) | `8da31420f1e19e76cc66533501a5ab1300a532b5` |
| **fast-forward** | **confirmed** |
| repository | `https://github.com/digitalnomadjoe/brittle.git` |
| source branch | `records/locomotion-reconciliation-20260807` |

**Fast-forward evidence:** git reported the ref update as `b2b025f..8da3142` — a **two-dot** range, which git prints only for a fast-forward; a forced or non-FF update prints `+ b2b025f...8da3142 (forced update)`. The branch was **349 ahead, 0 behind** `origin/main`. No merge was created, no `--force` was used, and none was needed.

## 2. Preconditions — all three verified *before* anything moved

| # | check | result |
|---|---|---|
| 1 | source branch remote tip == `8da31420f1e19e76cc66533501a5ab1300a532b5` | **PASS** — `ls-remote` returned exactly that |
| 2 | source contains the bank commit `08597c9` | **PASS** — `git merge-base --is-ancestor 08597c9 8da3142` |
| 3 | source is a descendant of the then-current `origin/main` (`b2b025f`) | **PASS** — 349 ahead, **0 behind** ⇒ fast-forward possible |

Had #3 failed the ticket required a stop; it did not fail, and no merge resolution was invented.

## 3. How the "clean checkout, nothing unrelated" constraint was met

The push was made as **`git push origin 8da31420…:refs/heads/main`** — the exact commit SHA to the target ref.

★ **No working tree participated at all.** Nothing was checked out, nothing was staged, no commit was created during the landing. Local modified and untracked files therefore could not be included even in principle — which is a stronger guarantee than cleaning a worktree and hoping. This matters here because these checkouts carry large untracked evidence directories by design.

Git rejects a non-fast-forward push by default, so **the push command was itself the fast-forward guard** — a second, independent enforcement on top of the ancestry check in §2.

Local `main` was then moved to match (`b2b025f` → `8da3142`). It is not checked out in any worktree (`brittle` is on `l24-march-demo`, `brittle-telemetry` on `dfl-actuator-telemetry`, `brittle-controlplane` on `control-plane-design`), so this touched no working tree either.

## 4. Post-push verification — read directly from `origin/main`

| item | measured on `origin/main` | expected | verdict |
|---|---|---|---|
| main SHA | `8da31420f1e19e76cc66533501a5ab1300a532b5` | `8da31420…` | **PASS** |
| banked checkpoint | `13fc030e59ddfc65` | `13fc030e59ddfc65` | **PASS** |
| `Mobs_v1.npy` | `976c327503a092c3` | `976c327503a092c3` | **PASS** |
| `Mact.npy` | `23289b73d9b23faa` | `23289b73d9b23faa` | **PASS** |
| changelog rows | **88** | 88 | **PASS** |
| changelog `sha256_16` | `65c7e385f1b1974d` | `65c7e385f1b1974d` | **PASS** |
| golden playbook | `playbooks/LOCOMOTION_ACQUISITION.md` = `900a7c119c3c2188` | `900a7c119c3c2188` | **PASS** |
| owner receipt on main | `owner_visual_approval.quote_verbatim` reads **"yea looks good!"** | — | present |

The SHA was taken from `git ls-remote origin refs/heads/main` (the remote's own answer, not a cached local ref), and every file digest was computed from blobs read out of the fetched `origin/main` tree.

⚠ **The banked weights zip is not tracked on `main` (0 files), by the existing bank convention** — prior banked entries track only the receipt. Its identity travels as the digest recorded in `SHA256SUMS` and `BANK_RECEIPT.json`, both of which *are* on main and both of which verify to `13fc030e59ddfc65`. The bytes themselves remain immutable on disk at `rl/models/walking/banked/continuous_curvature_manifold_20260807T110000Z/` (`chmod 444`, digests re-verified after the mode change). **Anyone cloning `main` gets the identity and the proof, not the weights.**

## 5. Non-mutation — verified, not assumed

Diffing the landed range `b2b025f..8da3142` against the protected paths:

| protected surface | changed files |
|---|---|
| `rl/policies/walking/latest.txt` and any `latest.txt` | **0** |
| canonical policy pointers (`paths.py`) | **0** |
| policy cards (`*policy_card*`) | **0** |
| `rl/policies/walking/` | **0** |

⚠ **13 files named `targets.json` do appear in the diff, and they are not Guard state.** Every one is a per-unit Guard *registration payload* under `rgl/artifacts/…` (`q3_targets.json`, `q8c_targets.json`, and so on) — the inputs I authored for `brittlectl target register` in each unit. The **live Guard state** is `/home/robojoe/code/brittle-guard-state/live/targets.json`, which sits **outside the repository entirely** and cannot be reached by any push. Guard targets were not mutated by this landing; the live state remains at 52 entries / 46 construction members, lifecycle `ACQUISITION`.

Nothing was promoted. Nothing was retrained. No optimizer ran in this unit.

## 6. One honest note on the changelog

This landing ticket, unlike the preceding ones, did **not** ask for a changelog row — it asked for the SHAs and the verification. I have appended row 89 anyway, because the standing project rule is that a unit is not complete without one.

★ **That means the changelog on disk is now 89 rows (`a52678e61430e19f`) while `origin/main` carries 88 rows (`65c7e385f1b1974d`).** This is not a discrepancy to be reconciled away: `65c7e385f1b1974d` / 88 rows is precisely the state that was landed and verified in §4, and the landing record itself necessarily comes *after* the landing. The row and this report sit one commit ahead of `main` locally. **Landing them would need a further push, which this ticket does not authorize and I have not done.**

## 7. Artifacts

Nothing new was created under `rgl/artifacts/` for this unit — the landing consumed existing commits. Evidence is the git history itself: `origin/main` at `8da31420f1e19e76cc66533501a5ab1300a532b5`, containing bank commit `08597c902888d18975edd4244ab92cd19d0d1849` and the bank directory `rl/models/walking/banked/continuous_curvature_manifold_20260807T110000Z/`.
