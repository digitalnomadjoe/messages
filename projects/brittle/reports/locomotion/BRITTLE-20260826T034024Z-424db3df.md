---
id: BRITTLE-20260826T034024Z-424db3df
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: WALK-DR06G4
created_at: "2026-08-26T03:40:24.691344Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06G4_RUNTIME_PARITY_INSTALLED_20260826T032638Z.md
local_source_sha256: 0bad7f1a96536d3d21541daa3fe50445f64226da897d2b17eeb8e64fae8b361f
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: GREEN_OPUS5__WALK_DR06G4_RUNTIME_PARITY_INSTALLED_20260826T032638Z
mirror_bytes: 9049
---
# GREEN_OPUS5__WALK_DR06G4_RUNTIME_PARITY_INSTALLED_20260826T032638Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06G4_RUNTIME_PARITY_INSTALLED_20260826T032638Z.md` |
| sha256 | `0bad7f1a96536d3d21541daa3fe50445f64226da897d2b17eeb8e64fae8b361f` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | WALK-DR06G4 |
| mirrored at | 2026-08-26T03:40:24.691344Z |

<!-- brittle:mirrored-content -->

# TEAM: GREEN — Opus 5 · WALK-DR06G4 — **INSTALLED AND VERIFIED. The train/eval/deploy cadence parity defect is closed.** `rl/isaac_parity/runtime_port.py` moved **`bbe122433e9a399d` → `fa16e62f47b59567`** under your three grants, byte-for-byte from the WALK-DR06G3-proven candidate with no edits at install, and the full proof suite was re-run **against the installed path** — not against a staged copy, and with no module rebinding, so the installed file resolving its own `REPO`/`MANIFEST` is itself part of what passed. Every requirement in your §2 holds on the installed runtime: dense-grid legacy parity **0 mismatches over 170,001 commands** (compared with `!=`, not a tolerance); **all four default-manifest rollout trace hashes bit-identical to the pre-install banked traces** recorded before the file was touched; train == eval at **all 12 probe commands** with and without the branch; the **0.2400 / 0.2401 boundary** exact on both sides (1.000000 → 0.999124); **vx 0.272219 → cadence 0.717721, obs[53] 0.358861**, matching the trainer; obs[53] and the cyclic clock now come from one resolver so they cannot drift apart; and an unrepresentable cadence key **fails closed** rather than being silently evaluated. The mutation is now **bound, not asserted** — registered as Guard target `runtime-port-isaac-parity` at CANONICAL `fa16e62f47b59567`, so a future trainer-side cadence law can never again outrun the runtime unnoticed. **ZERO optimizer steps ran**: no PPO, no run directory, no checkpoint written anywhere by this unit. DR06G2 was not launched and no cadence-strategy decision was taken here. STOP for Joe.

**unit:** WALK-DR06G4 · **ts:** 20260826T032638Z · **team:** GREEN (ticket header: Opus 5; executing session: Claude Fable 5)
**constitution:** C1, C2, C6 · **potential violations:** none · **amendment proposed:** no
Ticket: bus `BRITTLE-20260826T032638Z-40c0a2c4`. Scope honoured: no PPO, no reward change, no reference/member change, no branch redesign, no stride reward, no command-manifold expansion, no promotion.

---

## 1 · Runtime BEFORE → AFTER

| | sha256_16 | note |
|---|---|---|
| before | **`bbe122433e9a399d`** | asserted equal to the proven BEFORE *before* the backup was taken |
| candidate | **`fa16e62f47b59567`** | asserted equal to the WALK-DR06G3-proven staged file *before* install |
| after (installed) | **`fa16e62f47b59567`** | re-hashed from the banked path after copy — exact match, no edits at install |

Backup of the original retained at `rgl/artifacts/walkdr06g4_install_20260826T032638Z/runtime_port.py.before_bbe122433e9a399d`. The installer is fail-closed in both directions: it refuses to start unless both hashes are exactly as proven, and any post-install check failure restores the backup and exits non-zero. No rollback was triggered.

## 2 · Authorization receipts

Your three sentences, transcribed verbatim to `OWNER_RULING_WALKDR06G4.json`, committed `77f1e58c`, campaign trust root advanced to that commit. All three validated against `permits` **before** anything was recorded (3/3 accepted).

| grant | decision | filed under |
|---|---|---|
| transition to WALK-DR06G4, acquisition lifecycle | **`D-e0de832e8073`** | WALK-DR06G (Guard CP-07: a transition decision is filed under the SOURCE unit) |
| transition to WALK-DR06G4, pretraining certification lifecycle | **`D-d59fb715c460`** | WALK-DR06G4 |
| canonical mutation, `runtime-port-isaac-parity`, `bbe122433e9a399d` → `fa16e62f47b59567` | **`D-492a714af3ba`** | WALK-DR06G4, subjects `[WALK-DR06G4, runtime-port-isaac-parity]` |

Transitions: `TR-63d30f385803` (DR06G → DR06G4, acquisition) · `TR-18477a105d4e` (certification window open) · `TR-8ea5cfc1b3bb` (window closed, back to acquisition). Campaign now reads **ACQUISITION · active unit WALK-DR06G4**. No ppo grant was requested or consumed — this unit runs no training.

**Disclosure on the closing leg.** Returning from the certification window to acquisition is a transition for unit WALK-DR06G4, and the only DR06G4-filed transition grant is `D-d59fb715c460` (your certification-window sentence); `D-e0de832e8073` is filed under WALK-DR06G and Guard correctly refused it for that leg. I used `D-d59fb715c460` to close the window it opened — a window cannot be opened without being closed — and I am flagging the pairing rather than leaving it implicit. This is the same pattern used in WALK-DR06G and disclosed there.

## 3 · Installed-path verification

**Registration.** `runtime-port-isaac-parity` → CANONICAL, path `rl/isaac_parity/runtime_port.py`, hash `fa16e62f47b59567`, registered under `D-492a714af3ba` inside the certification window. The evaluation/deployment cadence law is now a bound Guard target rather than a convention.

**Proof suite, re-run against the installed file** (`proof_installed.json`, log alongside):

| requirement (your §2) | result |
|---|---|
| legacy / no-branch dense-grid parity | **PASS** — 170,001 commands from −0.5 to 1.2, **0 mismatches**, exact `!=` comparison |
| default full-rollout trace hashes bit-identical | **PASS** — 4/4 (below) |
| branch train == eval probes | **PASS** — 12/12 |
| 0.2400 / 0.2401 boundary | **PASS** — branch: 1.000000 at 0.2400, 0.999124 at 0.2401, train and eval identical |
| vx 0.272219 → cadence 0.717721 | **PASS** — cadence **0.717721**, obs[53] **0.358861** (trainer: 0.717721 / 0.358860) |
| obs[53] / cyclic-clock semantics match trainer | **PASS** — both derived from one `resolve_cadence()`; fingerprint `{obs53: "cadence/2", clock: "csphi += cadence*dt"}` |
| unknown cadence configuration fails closed | **PASS** — synthetic `cadence_warp_v2` **REFUSED** |

**Default-runtime non-perturbation.** Four 700-step rollouts under the **installed** runtime with the crown manifest (no branch declared), compared against the trace hashes recorded **before** installation in WALK-DR06G3:

| rollout | pre-install (original banked) | installed | |
|---|---|---|---|
| DR06G-884736 s0 | `45f5b94da8de3a44` | `45f5b94da8de3a44` | **bit-identical** |
| DR06G-884736 s1 | `5e8e022e10342c9c` | `5e8e022e10342c9c` | **bit-identical** |
| DR05D-737280 s0 | `9d3a3d3a515d9242` | `9d3a3d3a515d9242` | **bit-identical** |
| DR05D-737280 s1 | `e177059dd09725b3` | `e177059dd09725b3` | **bit-identical** |

The comparison is against DR06G3's committed receipt rather than a fresh baseline, because once the candidate is installed the original runtime no longer exists on disk to re-measure — the recorded hashes are the only honest reference.

**Cadence law fingerprint, branch declared:** `sched [2.1475, 0.4846, 0.7, 1.0]`, `branch [0.24, −8.761247733, 3.102699456, 0.71771978]`, `native_hz 1.0`, `obs53 "cadence/2"`, `clock "csphi += cadence*dt"` — one object a caller can assert TRAIN == EVAL == DEPLOY against, instead of inferring it.

## 4 · Zero optimizer steps

Confirmed by construction and by inspection: no PPO was invoked, **no `rl/runs/walking/` directory exists for this unit**, and **zero `ppo_*.zip` files** were written anywhere by it. The only rollouts executed were the four read-only non-perturbation cells, which run the arm's evaluation path with a fixed actor bundle. No reward, reference, member, branch parameter, stride reward, or command dimension was touched. DR06G2 remains unlaunched, and no decision about future cadence strategy was taken in this unit — per your §3 that is explicitly out of scope here.

## 5 · What this does and does not settle

**Closed:** the infrastructure defect. A policy trained under the >0.24 branch can now be evaluated and deployed under the same cadence law it trained on, the runtime refuses configurations it cannot represent, and the binding is registered in Guard.

**Still open, and yours:** the DR06G3 falsification stands unchanged — under parity the ratchet candidate sits at 74.2 % of command and its whole turn bought +0.0013 m/s, with both it and DR05D fighting the branch's cadence observation at that operating point. Whether the arc proceeds at the legacy clipped cadence (where train and deploy already agreed, and DR06G measured 94.4 %) or the branch's operating point is revisited is the design decision I have deliberately left untouched.

## 6 · Artifacts

`rgl/artifacts/walkdr06g4_install_20260826T032638Z/`: `OWNER_RULING_WALKDR06G4.json`, `dr06g4_install_verify.py` (installer with rollback), `dr06g4_proof_installed.py` + `proof_installed.json` + `proof_installed.log`, `dr06g4_rollout_check.py` + `rollout_check.json` + `rollout_check.log`, `install_receipt.json`, `runtime_port.py.before_bbe122433e9a399d` (backup), `rollouts/` (the four verification traces). Prior proof: `rgl/artifacts/walkdr06g3_parity_20260826T030459Z/`.

STOP for Joe — parity defect closed and bound; the cadence-strategy decision is untouched and waiting.

**changelog row:** `WALK-DR06G4-PARITY-INSTALLED, 20260826T032638Z` — post-append `rgl/LOCOMOTION_CHANGELOG.csv` sha256_16 `15a64078ef331978` (556 rows × 13 cols, both trees byte-identical)
