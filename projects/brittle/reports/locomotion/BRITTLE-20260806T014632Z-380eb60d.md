---
id: BRITTLE-20260806T014632Z-380eb60d
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15N
created_at: "2026-08-06T01:46:32.964317Z"
source_commit: 9283bd7
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15N_SPECIALIST_CONSTRUCTION_20260806T012634Z.md
local_source_sha256: d68107758165f00770e154fb514eca253074433ef3605712369dcf8eb64e66ee
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15N_SPECIALIST_CONSTRUCTION_20260806T012634Z
mirror_bytes: 7323
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15N_SPECIALIST_CONSTRUCTION_20260806T012634Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15N_SPECIALIST_CONSTRUCTION_20260806T012634Z.md` |
| sha256 | `d68107758165f00770e154fb514eca253074433ef3605712369dcf8eb64e66ee` |
| brittle commit | `9283bd7` |
| lane | locomotion |
| unit | 12U-C11S15N |
| mirrored at | 2026-08-06T01:46:32.964317Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15N — Positive-Turn Specialist Construction: READY FOR AUTHORIZATION

**date:** 2026-08-06 · **unit:** 12U-C11S15N · **lane:** locomotion · construction + preflight; **no PPO launch, no promotion, no policy card, no latest-pointer, no canonical mutation, no plant/control-law change, no observation/action-interface change; the card refuses live launch (exit 93)**
**constitution:** C1, C2, C3, C4, C6, C8, C9, C12 · **potential violations:** none (rig corrections disclosed §5) · **amendment proposed:** no
**final decision (exactly one):** **`POSITIVE-TURN SPECIALIST READY FOR AUTHORIZATION`**
**honest label:** training-only positive-turn specialist (one active member κ=+1.0); success would mean ONLY `CURRENT-CONTRACT POSITIVE-TURN SPECIALIST ACQUIRED` — no command-manifold, negative-turn, or figure-eight claim. The deployed contract is unchanged: one reference-free 54-in actor, 12 joint setpoints.
**changelog:** row 61 `12U-C11S15N, 20260806T012634Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. Construction (candidate `c1af9283fca3f2f2`; three new mechanisms, all fail-closed)

**P20 solo-member assignment** (`--manifold-solo-kappa 1.0`): ALL 4096 worlds on the certified kP100; the resolver's structural members (straight base, kM mirror) stay present-but-INACTIVE (the base-file and P/M-pairing laws are load-bearing; the specialist restricts WORLD ASSIGNMENT, not the enumeration). Fail-closed unless exactly one member matches; mutually exclusive with anchor-frac. **P21 anchor zone mask** (`--anchor-zone-mask 1`): parent-anchor L2 ZEROED on samples whose pre-step frame lies in the member-local curved zone — bounds from member metadata (concat-absolute `loop_lo` — a double-offset defect was caught and fixed during construction), zone **[862, 1111)** inside the member window [648, 1296), 399 bookend frames (DS lead-in, native start, standing pad, exit) where the decayed anchor stays ACTIVE; it=0 occupancy gate [0.05, 0.995]. **P22 curved supervision** (`--n-sup-w`): training-only, full-12-dim `L2(actor_mean, a*[frame])`, `a* = clip((q_ref − Q_DEFAULT)/action_scale, ±1)` (the H0-corrected deployed residual-on-default coordinates), applied ONLY in-zone AND inside the explicit reference-neighborhood gate (pos < 0.10 m, |roll/pitch − ref| < 15°, captured pre-step), CONSTANT weight, labels precomputed — **no runtime reference actuation, no reference observation, no hidden trajectory base, no deployed switching**.

## 2. Family, labels, oracle

Family = the certified files byte-identical: straight base `7f45464d` (inactive), **kP100 `5a53bdf2` (sole active)**, kM100 mirror `c69cae60` (pairing file, inactive). Labels over the member: **zero clipping** (per-joint pre-clip max 0.82 worst, zone max 0.71); deployed-coordinate round-trip `QD + scale·a* = q_ref` exact to **2.8e-17 rad**. Oracle (tor-v1.6.0, regression 16/16 sha `b8cc40ff`): kP100 → **TURNING_LEFT, κ_run 0.997, R 1.003 m**, 1.93 m valid translation.

## 3. Reward audit — yaw-dominant **8.0 SELECTED**

Live income matrix under the P1 command, subjects {true-turn ghost, straight-under-turn ghost, live parent, stand, pivot, fall}, run in BOTH books. **w=8: true turn 18.25 top; straight-under-turn 12.20 (−6.05/step); stand 8.39 / pivot 8.37 / fall −7.94 all dominated; pivot 8.37 < 0.6×18.25 (not paid); yaw income 7.69 ≤ tangent+cross-track 7.98.** w=4 also passes all five (true 14.4 / straight 8.53 — the M-matrix numbers reproduce); per the ticket rule, 8 is selected only because the probe proves it — receipt `n_preflight_receipts.json`. Disclosure: `r_fwd` is event-gated (alternating-touchdown windows) and pays 0 on teleported ghosts, so the yaw-vs-tangent condition is effectively yaw vs cross-track/imitation income; both books pass with margin. No other weight changed.

## 4. Supervision weight — **0.25 SELECTED** (gradient receipt, no sweep)

On a real solo rollout at the card horizon (max_steps 660, arc-rsi 0.85, parent policy at σ 0.135): first-epoch PG gradient norm **2.563** vs supervision gradient at w=1 **5.336** → w_raw 0.24 → **w = 0.25** (bounded set {0.25, 0.5, 1.0, 2.0}), realized ratio **0.52** vs target 0.5. Batch composition: in-zone 59%, neighborhood-valid 61%, supervised (zone∧valid) **24.4%**; zone-valid income 16.4/step; supervision distance 1.32. Constant (persistent), never decayed.

## 5. Preflight proofs (ticket list — all green; rig corrections disclosed)

Only κ=+1 active: solo print (4096/4096) + census **0 out-of-member events** in 1315 injections (rate 0.845 ≈ 0.85). Anchor mask: zone/bookend split + boundary test [F,T,T,F] + fail-closed-on-missing-metadata proven; trainer it=0 occupancy gate armed. Labels representable: zero-clip, disclosed above. Deployed coordinates: round-trip exact. Injector: **11/11** on this family (T4 mirror auto-skip). log_std −2.0 survives loading: P18 re-assert + readback **−2.0000** live in both dry runs + it=0 gate. Walking viable: parent on the solo rung **0/32 falls det AND stoch** (τP99 6.46/7.6). kP100 classifies TURNING_LEFT κ≈+1.0 R≈1.0 ✓. Oracle regression green ✓. Dry ×2 → fingerprint **`34216b03fd16a69e` STABLE** (config carries solo/zone-mask/supervision blocks). *Rig corrections:* the supervision-receipt rollout initially (a) collected before the walkers reached the zone, then (b) ran a horizon-free env where no P17 respawns occur — fixed to the card-exact 660 horizon with a 700-step burn; both defects were rig-side only (receipts show the correction chain).

## 6. Proposed run (prepared, NOT launched) + proposed Guard targets

Card `launch_c11s15n_kp.sh` (sha **`00488ed8c069e1f7`**): **Stage A** fixed-κ acquisition, arc-rsi 0.85, init = L2-final `da70f57b`; **Stage B** arc-rsi 0.40, init = Stage-A final, REFUSED unless `stageb_gate.json` proves an injection-free oracle-visible positive-curvature trend (written by the evaluator, never the card). Per-stage ceiling **2,097,152**, absolute **4,194,304**, evals/ckpts 262,144; anchor L2-final w0 0.15 decay 8,388,608 (bookend-only via P21; Stage-B decay-clock restart disclosed in-card); inspector staged pre-PPO with the M1 manifest patch law (vx 0.153 + cadence keys), closed after reporting; RCA-on-miss mandatory. Future qualification (injection-free, native starts): TURNING_LEFT, κ ≥ +0.8 over ≥ 0.6 m, zero falls, no pivot/spin, valid contacts/torque/gait continuity, DS entry + stable exit. Proposed targets (`n_targets_proposed.json`, NOT registered): candidate re-pin `c1af9283`, launcher `00488ed8`, settings fp → `34216b03`, bound-construction **38 members hash `d63bc22bb29857cb`** (+launcher only; member references REUSE the existing records — the unselected mirror stays OUT of membership per the M1 D-11 law).

## 7. Artifacts

`rgl/artifacts/c11s15n_specialist_20260806T012634Z/`: family ×3 (sha-pinned), `n_preflight.py` + `n_supsel_fix.py` + `n_preflight_receipts.json`, `oracle_member_kP100.json`, injector log, card + dry ×2 (`n_dry_resolved_config.json`), `n_targets_proposed.json`. Candidate P20/P21/P22 edits in-tree, commented. **Next action is Joe's: authorize the specialist (Guard sentences; candidate re-pin `c1af9283` + launcher `00488ed8` + settings/fingerprint/membership per proposal; ONE seed ≥1).**
