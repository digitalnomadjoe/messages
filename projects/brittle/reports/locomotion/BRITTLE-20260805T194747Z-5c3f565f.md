---
id: BRITTLE-20260805T194747Z-5c3f565f
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15K2C
created_at: "2026-08-05T19:47:47.881764Z"
source_commit: f053ad9
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15K2C_BC_PPO_DESTRUCTION_AUDIT_20260805T193900Z.md
local_source_sha256: 0bebcc6ce73a85d150b137adf002b278a0cded190687e5881b9c195833d386b6
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15K2C_BC_PPO_DESTRUCTION_AUDIT_20260805T193900Z
mirror_bytes: 6776
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15K2C_BC_PPO_DESTRUCTION_AUDIT_20260805T193900Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15K2C_BC_PPO_DESTRUCTION_AUDIT_20260805T193900Z.md` |
| sha256 | `0bebcc6ce73a85d150b137adf002b278a0cded190687e5881b9c195833d386b6` |
| brittle commit | `f053ad9` |
| lane | locomotion |
| unit | 12U-C11S15K2C |
| mirrored at | 2026-08-05T19:47:47.881764Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15K2C — BC-to-PPO Locomotion Destruction Audit: BC PRIOR NOT STOCHASTICALLY ROBUST

**date:** 2026-08-05 · **unit:** 12U-C11S15K2C · **lane:** locomotion · bounded diagnostics only; **no continuation, no nursery/injector construction, no full PPO, no reward change, no promotion, no policy card, no canonical mutation; zero optimizer steps applied anywhere in this unit**
**constitution:** C1, C2, C4, C6, C8, C12 · **potential violations:** none new (this unit CORRECTS two earlier instrument overclaims — §1) · **amendment proposed:** no
**final decision (exactly one):** **`BC PRIOR NOT STOCHASTICALLY ROBUST`** — in the stronger form: the seed is **not even deterministically robust**; the "walking BC initialization" that PPO allegedly destroyed never existed. PPO destroyed nothing.
**recommended next path (exactly one):** **member-scoped injector plus current-contract arc nursery.**
**changelog:** row 55 `12U-C11S15K2C, 20260805T193900Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. The premise was false — two instrument overclaims corrected of record

The K2A receipt "BC seed walks 8/8 × 260 steps under every command" came from a **closed-loop check run on the injection-armed data-generation env** (`arc_rsi=1.0`: fallen worlds were silently re-injected mid-member and kept moving) **with 'walking' counted by a displacement threshold (≥0.2 m), not by falls+duration**. Clean protocol (§2) shows the truth. This false receipt then seeded K2B's design ("the seed carries the arc-prior role") and its RCA layer rca3 ("PPO DEGRADED locomotion relative to its own init") — both now corrected append-only in the K2B RCA record and state. **Instrument laws banked:** closed-loop competence = falls + duration, never displacement thresholds; never measure closed-loop on an injection-armed env; verify live `log_std` against card intent from the entropy telemetry at it=1.

## 2. §1 BC robustness (frozen seed, all four members, exact training protocol, injection-free)

8 worlds/member, native reset, card env, H=660:

| mode | σ | first-fall med (steps) | walk dist med | τ P99 | falls |
|---|---|---|---|---|---|
| deterministic | 0 | **64–69** | **0.17–0.19 m** | 4.0 | 32/32 |
| as-run (log_std −1.0) | 0.368 | **28–44** | 0.17–0.23 m | 9.0 | 32/32 |
| card intent (−2.0) | 0.135 | 77–108 | 0.27–0.37 m | 5.3 | 32/32 |
| reduced (−3.0) | 0.05 | 65–73 | 0.18–0.21 m | 4.3 | 32/32 |

**The seed falls 8/8 in EVERY mode — including σ=0.** Oracle on the deterministic curved traces: INVALID_TRACE (nothing sustained). Small dither (σ 0.135) mildly *helps* (limit-cycle breakup); the as-run σ 0.368 halves survival and triples torque noise. **Co-finding (card wiring defect):** the strict `continue-ckpt` state-dict load OVERWROTE the card's `--log-std-init −2.0` with the seed's stored default (−1.0) — K2B trained at σ 0.368, 2.7× intent (entropy 4.98 at it=1 proves it live). The rig-attrs≠card-args law strikes again; future cards must re-assert `log_std` after load.

## 3. §2 update evidence (no training steps run in this unit)

K2B's own per-update telemetry is the first-update record: kl 0.013–0.014, clip 0.19–0.21, EV −0.06→0.85, entropy 4.98→3.90 (log_std −1.0→−1.09), one late kl spike (0.085 @3.4M) where ep_len jumped 79→130. Drift from the seed on a fixed det-BC state bank: first ckpt action-L2 **0.078**, KL **0.027** (early updates barely moved the actor); final ckpt 0.484 / KL 1.44 (it moved far — *as it improved*). **Gradient decomposition on one as-run batch (offline, no step applied):** PPO actor-grad norm 2.158; **anchor grad ≡ 0 at t0 by construction** (policy==anchor: the anchor is a restoring spring with zero force until departure — it cannot ADD competence, only resist leaving a FALLING seed; ratio/cosine undefined-at-zero, reported as 0). No simulator training steps ran, so no inspector was required; all rollouts here are measurement-only.

## 4. §3 actual-state reward audit (genuinely visited states, live book, 240×32)

| state class | reward/step | falls |
|---|---|---|
| deterministic BC walking(-then-falling) | 5.40 | 96 |
| stochastic BC (as-run σ) | 3.41 | 186 |
| first PPO ckpt (524288) | 6.58 | 113 |
| final PPO ckpt (4194304) | **5.85** | **32** |

Per-term: det-BC pays for its crumbling gait (p_slip −0.62, p_fall −0.63/step); the final policy earns via yaw-compliance + low slip with far fewer falls. **The live economy correctly prices the actual options: PPO's outcome out-earns the falling seed it started from — no actual-state reward-economics defect against good walking exists (the K2A matrix stands: true walking prices highest; no policy reached it).** The return favored exactly what PPO did: reduce falls from a non-walking start.

## 5. §4 one-variable discriminators (each single-variable)

(i) noise-vs-deterministic: **deterministic already fails** → noise is not the root (it's an aggravator at σ 0.368); (ii) update-vs-no-update: no-update seed at as-run σ survives 28–44 steps; with updates K2B reached ep_len 130 → **PPO improved, did not overwrite**; (iii) reduced exploration: no σ in {0.05…0.368} rescues walking; (iv) anchor-strength: at t0 the anchor gradient is exactly zero and its target is a faller — no anchor weight can fix a prior that isn't competent (matching magnitudes would only pin the policy to falling harder).

## 6. Decision + the one recommended path

**`BC PRIOR NOT STOCHASTICALLY ROBUST`** (stronger form: not deterministically robust; premise of "destruction" dissolved; co-findings: the log_std wiring defect and the two corrected instrument overclaims). **Recommended path: member-scoped injector plus current-contract arc nursery** — reference-state BC cannot produce a robust walker (twice-proven: K2B outcome + this unit's clean batteries); July's positive built arc competence exactly this way (nursery PPO with mid-member injections); the injector (currently manifold-unaware, the P17 gap) is the missing tool. Nursery init choice stays open for the construction ticket; per the owner guardrail, the straight start/stop milestone is NOT recommended as an anchor here — if the nursery ticket wants it as init/anchor, its ±κ action-authority counterfactual must be a preflight proof. Card-law fix to fold into P17: re-assert `log_std` after `continue-ckpt` load.

## 7. Artifacts

`rgl/artifacts/c11s15k2_construction_20260805T164857Z/`: `k2c_bc_robustness.py` + `k2c_bc_robustness.json`, `k2c_grad_reward.py` + `k2c_grad_reward.json`, det curved traces + oracle receipts, K2B RCA record + state with the `k2c_correction` blocks (append-only; original text preserved). No checkpoints produced; no training runs.
