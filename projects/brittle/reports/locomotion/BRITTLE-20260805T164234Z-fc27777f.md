---
id: BRITTLE-20260805T164234Z-fc27777f
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15K1
created_at: "2026-08-05T16:42:34.027769Z"
source_commit: b252c18
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15K1_NATIVE_LEARNED_TURN_CLOSURE_20260805T163308Z.md
local_source_sha256: e12e0ca7c915d9c65478d957aae3035fa1222c3d3a0fd38fdffe234a129e0441
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15K1_NATIVE_LEARNED_TURN_CLOSURE_20260805T163308Z
mirror_bytes: 11819
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15K1_NATIVE_LEARNED_TURN_CLOSURE_20260805T163308Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15K1_NATIVE_LEARNED_TURN_CLOSURE_20260805T163308Z.md` |
| sha256 | `e12e0ca7c915d9c65478d957aae3035fa1222c3d3a0fd38fdffe234a129e0441` |
| brittle commit | `b252c18` |
| lane | locomotion |
| unit | 12U-C11S15K1 |
| mirrored at | 2026-08-05T16:42:34.027769Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15K1 — Historical Learned-Turn Native Replay Closure: ORACLE FAILS LEARNED POSITIVE

**date:** 2026-08-05 · **unit:** 12U-C11S15K1 · **lane:** locomotion · read-only recovery + instrument repair; no PPO, no continuation, no reward change, no new construction, no contract adaptation, no promotion, no canonical-policy mutation
**constitution:** C1, C2, C4, C6, C8, C12 · **potential violations:** disclosed in §7 (capture-time `err_et` off; `cmd_retime` pinned 1.0; trace reward channel recorded under the c14d1d6 reward kernels — diagnostic-only) · **amendment proposed:** no
**final decision (exactly one):** **`ORACLE FAILS LEARNED POSITIVE`** — the frozen `tor-v1.0.0` misclassified the recovered, genuinely-turning native policy as SPIN_OR_PIVOT. The sanctioned repair (`tor-v1.1.0`) is landed, revalidated 13/13, and the learned positive now PASSES under the rebound record; unblocking the paired-±κ gate is Joe's acceptance of the version bump.
**changelog:** row 51 `12U-C11S15K1, 20260805T163308Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. The historical learned turner — RECOVERED (native provenance)

| field | value |
|---|---|
| checkpoint | `rl/runs/walking_pilot/p12t_pteach_s0_20260723T152708Z/ppo_6029312.zip` · sha256_16 `4c911c4c90e61524` |
| certification | 12T report: P-TEACHER CERTIFIED, all three cadence cells (yaw 105.1/105.2/105.7, speed 97.2/98.9/99.9, R 0.46/0.47/0.47, 0 falls) |
| native commit | cert tree `44aa372` (trainer + members + report). Run-time sim/kernels had floated ahead uncommitted; first committed at `c14d1d6` (§3) |
| trainer | era `rl/mjw/train_tip_reftrack.py`; behavior=walking, cmd-phase clock, w_pose 16, yaw_dominant_w 8.0, BC-init + imitation anchor (w0 0.15, decay 10M) from the arc-competent 12S nursery ckpt `p12s_ppo_s0_*/ppo_2097152.zip` |
| launcher | inline session command (never committed). The `.launch.log` preserves every resolved config print; the era cell-cert battery script was likewise never committed — its numbers exist only in the 12T report (provenance gap; the C11S15K oracle+regression stack is the permanent fix) |
| plant / law | **pre-v2 plant `2e38a5f4cf93f323`** (reconstructed from the era tree and fingerprint-verified) · kp20 all / kd 1,1,2,2,1,1 / τ12.5 / acd 10 Hz / ASO ×1 · **action law residual-on-default** (`q_des = Q_DEFAULT + scale·a`) — same law family as today |
| obs/action contract | `cmdphase54_v2_bodyangvel` actor 54 = [obs50 (dim45=live yaw-cmd, dims3:6=body angvel) \| sin/cos member-local phase \| vx_cmd, cadence/2]; action 12 @ scales [0.28,0.28,0.96,0.99,0.96,0.28]×2 |
| command/reference member | 6-member manifold (`ref_concat.npz`, ntot 3724; 3 straight vx≈0.26 + 3 κ2.0-P vx≈0.15, cadences 1.0/1.05/1.1, round-robin world binding). Certified cell = `ccref_vx026_c1p000_kP200` (κ +2.0, vx 0.153) |
| seed | 0 · resolved-config fingerprint `3e76d40151f4eb5a` — **recomputed bit-exact from the saved resolved_config** |
| original viewer | standard spectator per the run dir's `runtime_manifest.json` |
| original visual-review record | **NONE** — 12T discloses "(f) no visual review; none claimed". The inspector was attached live during all three teacher runs; that live view is the probable source of the remembered observation. No formal visual record of a learned policy turning exists anywhere in the record (12U-C10R visual banking covered the *kinematic* members only). |

## 2. Native replay — the policy TURNS (evidence)

Native rig (`k1_capture.py`, archived): era modules only, era plant (fingerprint assert PASS), era law, era adapter + verbatim manifold wiring, run-dir `ref_concat.npz`, deterministic actor-mean, seed 0, **reset-free and injection-free** (walk/arc-RSI 0, no respawns occurred: 0 falls, 0 teleports, 0 dones in all 24 worlds × 1400 steps). 4 worlds per member:

- **κ2.0-P worlds (all 12): sustained LEFT turning.** Battery horizon (one member play-through, 648 ticks): path heading +139.7…+157.2°, speeds 97.3–103.1% of commanded vx.
- **Straight worlds (all 12): straight** (|Δhead| ≤ 8.7°, R ≥ 20 m) — in-battery negative control, matching today's negative-control classes.
- **Era-cert reproduction:** speed% 97.4/99.3/103.1 vs cert 97.2/98.9/99.9 (c1.00 near-exact); realized geometry (oracle): **κ +1.988 vs commanded +2.0 (99.4%), radius 0.503 m vs commanded 0.5** — reconciling the era cert's R 0.46–0.47 as real path geometry. The era yaw≈105% is loop-aware overshoot (the member's cyclic zone wraps and the command keeps turning), not a metric artifact.

## 3. One authoritative trace + same-trace validation

`rgl/artifacts/c11s15k1_native_20260805T163308Z/traceK1_pteach6029312_native.npz` · **sha256_16 `82edcccdb8ac9e60`** · world 3 (kP200 c1p000), 1400 frames, reset-free. Contains: qpos/qvel, actor obs54 (command observations included), actions, phase, done/fell markers, foot xpos + contact forces, per-step path anchor, member ref tables (tpos/trpy/ψ/yaw_cmd_profile/cmd_vx/cmd_cadence), reward channel, full identity block. The oracle, the review viewer (subject 7, on-screen label + this sha + frame counter, commanded path, trail, forward ray; per-frame state==trace assert), and this report consume THIS file. Viewer is OPEN for Joe's same-frames verdict (keys 1–7 put the straight negatives and certified kinematic turns one key away). **Joe visual verdict: PENDING — no visual claim is made here.**

Working-tree drift disclosure: the run-time stack was trainer@`44aa372` + sim/kernels first committed at `c14d1d6` (additive-only diffs verified: per-world tempo buffers + injection helpers; PD/observation kernels bit-identical across the span). Reconstruction validated behaviorally by the cert reproduction above.

## 4. The oracle verdict — and the instrument failure

**Frozen `tor-v1.0.0`: `SPIN_OR_PIVOT` ("454° with 0.00 m translation") — WRONG.** The trace genuinely translates 4.156 m. Mechanism (quantified): the κ2.0 members are stride-scaled to vx 0.153; cycle-smoothed 25-sample heading-span chords measure 0.067–0.072 m — ALL below the `MIN_TRANS = 0.08` span floor (tuned on ≥0.21 m/s walkers and 0.26 m/s kinematic members). Every heading window was excluded → valid translation counted 0 → rotation-without-translation classification. **The K regression suite contained no slow learned-rollout positive — a missing control class, exactly the gap this unit's ticket existed to close.**

**Sanctioned repair — `tor-v1.1.0`:** `MIN_TRANS` 0.08 → 0.03 (noise-referenced: ≥5× a stander's smoothed span residual ~0.006 m; standers/spins still excluded by the same gate). Per the C11S15K freeze discipline: version bumped in-file with rationale, **regression suite rerun 13/13 GREEN** — all 12 previous controls unchanged (standers still STANDING, spins still SPIN_OR_PIVOT, teleports still detected-and-never-turning) **plus new control #13 `native_pteach_slow_turner`** (this trace → TURNING_LEFT, κ band [1.4, 3.2]). Record rebound: `rl/runs/oracle_regression/latest.json`, oracle sha `5c95b520582ce2ad` (snapshot archived). Under v1.1.0 the learned positive reads: **TURNING_LEFT, realized κ +1.99 (99.4% of commanded), R 0.503 m, 4.156 m valid translation, zero contamination.**

Two adjacent instrument defects fixed in passing: the promoted review viewer's trace paths pointed at `rl/scripts/` (would not load; corrected to the artifact dirs) and the regression runner hardcoded its oracle-version string (now read from the oracle file).

## 5. Why the previous arc visibly turned while current policies are straight

Controlled-for (identical): action law (residual-on-default), gain/torque/acd law, architecture (256×256, 54/60), robot. Differences, ranked by evidence:

1. **★ In-batch command contrast (PRIMARY; confirms and sharpens RCA-9).** p12t bound worlds round-robin across 6 members — straight (κ=0, vx 0.26) AND curved (κ=2, vx 0.15) commands present in every optimization batch, so obs45/52/53 carried live, varying information the optimizer had to use. Every C15-era lane was single-member: command channels constant across the whole batch — nothing forces command-reading. Notably p12t was sign-restricted (P-only) and still acquired turning: the load-bearing contrast was straight-vs-curved (the full command tuple), not L-vs-R.
2. **★ Arc-competent initialization + anchor (co-primary).** pteach BC-initialized from AND anchored to the 12S nursery checkpoint — a parent already turning (12S: kP 100.6% @ R 0.49, trained with quota'd mid-arc injections). C15 lanes initialized from straight-walking/standing milestones and anchored to them (G) or to reference labels (H). p12t consolidated an existing skill; C15 asked PPO to invent one under a straight-walking anchor.
3. Secondary/supporting: yaw_dominant_w 8.0 live income (the F-repair's yaw_avg W=4.0 is the corrected analog); nursery curriculum (12S → sign-restricted 8M).
4. Differences present but not evidenced causal: pre-v2 chiral plant vs v2 symmetric; `bodyangvel` obs variant (dims 3:6 semantics); episode/reset regime. Flagged, untested, cross-contract evaluation remains forbidden.

**Smallest causal difference supported by evidence: single-member versus multi-member command exposure, compounded by initialization/anchor provenance.** Consequence for the (Joe-gated) paired-±κ construction: the native positive argues for curved+straight member mixtures (not only ±κ) and an arc-competent init/anchor parent; the RCA-10 experiment design should absorb this before launch.

## 6. Unrecoverable-case inventory (partial gaps only)

Recovered: checkpoint ✓, manifold+members ✓, resolved config (fingerprint bit-exact) ✓, plant ✓, law ✓, contract ✓, runnable native stack ✓, native rollout ✓. Not recoverable: the exact launcher command and the era cell-cert battery script (session scratch, never committed — numbers survive only in the 12T report; behaviorally reproduced here), and any formal visual record (none was ever made). No archived era video/rollout NPZ exists; this unit's hashed native trace now IS the same-trace learned positive control, permanently in the regression suite.

## 7. Disclosed deviations from era training conditions (capture only)

`err_et` off (fall/no-step ET remained native; zero terminations occurred); `cmd_retime` pinned 1.0 (native commanded cell; era training drew ±10%); trace reward channel computed under the c14d1d6 reward kernels (includes the C6 net-yaw wrap fix, committed 2 days after the run) — dynamics, observations, and actions are native; the reward channel is diagnostic-only.

## 8. Consequences

1. **The historical question is CLOSED**: the July learned turner is real, recovered, and oracle-verified from one native hashed trace — the turning loss between eras is a training-recipe difference, not a plant/law/metric ghost.
2. **The oracle contract advances to `tor-v1.1.0`** (13 controls, sha-bound). Joe's acceptance of the bump converts this unit's gate to PASS for the paired-±κ program; the intent-gate machinery enforces the rebound record automatically.
3. Joe's same-trace visual verdict is pending in the open viewer (subject 7); no motion claim beyond the oracle's is made.

## 9. Artifacts

`rgl/artifacts/c11s15k1_native_20260805T163308Z/` — hashed native trace, both oracle verdicts (v1.0.0 FAIL, v1.1.0 PASS), 24-world battery JSON, capture rig, oracle v1.1.0 + 13-case regression snapshots. Canonical-path edits: `rl/scripts/turning_oracle.py` (v1.1.0), `turning_oracle_regression.py` (control #13, dynamic version), `turning_review_viewer.py` (path fix, subject 7). Native era stack (worktree extraction) in session scratch only; every durable input is a run-dir or git object referenced above.
