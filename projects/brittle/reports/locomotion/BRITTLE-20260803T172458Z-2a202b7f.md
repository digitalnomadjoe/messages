---
id: BRITTLE-20260803T172458Z-2a202b7f
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S4
created_at: "2026-08-03T17:24:58.158768Z"
source_commit: ca38ba2
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC10T_C11S4G_HANDOFF_CONTINUITY_AUDIT_20260803T172105Z.md
local_source_sha256: f2798357fea3f5c75d20ebfb0959ae19bda2403443c5742f41f7265dca8722db
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC10T_C11S4G_HANDOFF_CONTINUITY_AUDIT_20260803T172105Z
mirror_bytes: 25874
---
# UNIFIED_LOCOMOTION_PHASE12UC10T_C11S4G_HANDOFF_CONTINUITY_AUDIT_20260803T172105Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC10T_C11S4G_HANDOFF_CONTINUITY_AUDIT_20260803T172105Z.md` |
| sha256 | `f2798357fea3f5c75d20ebfb0959ae19bda2403443c5742f41f7265dca8722db` |
| brittle commit | `ca38ba2` |
| lane | locomotion |
| unit | 12U-C11S4 |
| mirrored at | 2026-08-03T17:24:58.158768Z |

<!-- brittle:mirrored-content -->

# 12U-C10T → C11S4G — Model-Handoff Continuity and Error Audit

**unit:** 12U-C10T→C11S4G audit · **timestamp:** 20260803T172105Z
**constitution:** C1, C2, C4, C6, C8, C10, C12 · **potential violations:** none (this unit is read-only) · **amendment proposed:** no
**scope:** read-only. No PPO, no source/reward edit, no D-27/D-28 implementation, no Guard mutation, no archive deletion, no promotion/policy-card/latest.txt/canonical change, no motion claim, no visual acceptance. No file outside `rgl/reports/` (+ bus mirror) written. Audit executed as a 16-agent workflow (11 chronological readers, code archaeology, Guard deep-read, hygiene sweep, cross-unit contradiction hunter, C11S4H readiness adversary; run `wf_f137ace1-b35`, 1.51M agent tokens) plus direct primary-source verification (commit chain, resolved configs, kernel constants, Guard live state).

**status:** Arc reconstructed C10T→C11S4G with commit-level provenance. Self-correction discipline is genuinely strong (26 formal retraction chains); measurement lineage is the weak axis (D-27/D-28 contaminate every track-income number back to the Crown, and both PRE-DATE C10T by 2–3 weeks). The recorded model handoff is **mid-C11E, not C10T** (this brief's premise is corrected by the repo). Guard authority is coherent — the "OPEN blocker vs ppo PERMITTED" disagreement is a **display-layer defect**, not an authority conflict.
**recommend:** **C11S4H: GO-WITH-CONDITIONS** (§7 — four conditions, all packagable into the single owner ruling C11S4G already requested).
**decision:** three corrections to this session's own prior filings recorded (§4.E); two new defect numbers proposed (D-29 display-layer, D-30 lane-law hazard); ledgers §2–§5 are the current error state of record.
**next:** Joe issues the C11S4H ruling package (§8, one action).

---

## 1. Chronological change table

All commits on `dfl-actuator-telemetry` (dual-committed to `l24-march-demo` for C11P-closeout→C11S with different hashes, same content). Dates local (UTC−7/8). "Auth" = the authorization evidence the unit itself records.

| unit | when | what changed | auth | outcome |
|---|---|---|---|---|
| C10T | 07-27 21:46 | Continuous lemniscate fig8 authored+certified (`ca60091d`); +0.0993° de-drift rotation of banked episode | C10S owner verdict JSONs; visual gate honored | READY→later **BANKED** (`VISUAL_ACCEPTED_BANKED.json`) |
| C10U | 07-27 22:33 | 6-gate command-manifold preflight; 48-D scout contract frozen (`c1e2448d`-class); envelope/authority/income gates | owner bank verdict; launch = 3 withheld owner decisions | PASSED, NOT LAUNCHED |
| C10V | 07-28 06:43 | First PPO of arc (262,144 steps = exactly 1 iteration) | `SCOUT_AUTHORIZATION.json` verbatim | BLOCKED-TORQUE (torch init rails 15 N·m from step 0) |
| C10W | 07-28 07:27 | Torque-safe near-zero init; quiet-hold spawn law | `C10W_AUTHORIZATION.json` | BLOCKED-TORQUE (any command roughness rails ankles) |
| C10X | 07-28 12:49 | ACD one-pole setpoint filter fc=6 qualified; 2.1M green steps | `C10X_AUTHORIZATION.json` | INCONCLUSIVE — **standing local optimum measured** (standing pays ~0.53, walking must cross fall-risk valley — the arc's first income-ordering warning) |
| C10Y | 07-28 15:14 | **ACD provenance restored**: canonical 10 Hz v1 law was real and C10V/W/X had bypassed it; exact law restored + lane-requalified | ticket + disclosure | BLOCKED-RSI (absolute transform yanks gait spawns) |
| C10Z | 07-28 15:41 | State-anchored (residual-on-prev-setpoint) transform qualified; 3-wall RSI ledger | ticket; C10U qd_des=0 retro-identified as wall-3 co-author (disclosed) | BLOCKED-RSI at arithmetic level |
| C11A–C11D | 07-28 (am, TS anomalies §5) | Rate-matched qd_des; BC clone (first closed-loop-stable learned policy); 2 DAgger rounds; shell curriculum; 8-ckpt PPO recovery arc | per-unit `*_AUTHORIZATION.json`, all recorded | all BLOCKED (BC init / coverage / shell / standing collapse) |
| **C11E** | 07-28 13:39 | **Root cause: ankle-roll armature 6.6e-5 × kd=1 @500 Hz discrete-unstable** (dose-response, both substrates); blocker MOVING_SPAWN_CONSTRUCTION opened; LTA1 addendum (armature = Steadywin hardware truth) | `C11E_AUTHORIZATION.json`; repair owner-gated | BLOCKED; **mid-unit model handoff recorded** (§3) |
| C11F | 07-28 14:28 | Guard campaign genesis + post-handoff revalidation: 9/10 findings reproduced; **LTA1 universal claim REFUTED** (2445/3178 legacy ckpts postdate armature commit; plant fp v1 blind to `dof_armature`) | `c11f_genesis` owner record | BLOCKED — finding did not reproduce |
| C11G–C11I | 07-28 16:03–16:29 | Ankle-roll Kd sweep (optimum 0.2); replay harness restored fail-closed (contract registry); obs-builder provenance (cmdphase54/55 UNDERIVABLE) | tracked `*_AUTHORIZATION.json` @ commits | crown-confound REFUTED (identical death at Kd 1.0/0.2) |
| **C11J** | 07-28 16:40 | **Canonical mutation #1: control-law v2 `fe5a6fed`** — ankle-roll Kd 0.2 (both sides) + solref scope all 12 soles; CPU/GPU parity 0/12 | `C11J_AUTHORIZATION.json` — owner approval **asserted, never quoted verbatim** (weakest canonical-mutation link, V4-C2) | QUALIFIED, landed |
| C11K–C11M | 07-28 16:46–17:12 | cmdphase54 provenance recovered (e87ac89:946-949); bit-exact CPU builders; **F8/k55 = trained by uncommitted code → permanently `runtime_semantics_ambiguous`** | tracked auth artifacts | NO LEGACY TEACHER |
| C11N (×4) | 07-28 17:44–18:18 | Guard fail-closed on acquisition (all 4 derivations false); seed quiet-drift 0.99 rad diagnosed (net.4.bias crouch); DS-only sampler proven; action-formulation audit | tracked auth + interleaved owner rulings (3 files share one TS) | BLOCKED — drift divergent |
| **C11O** (×3) | 07-28 18:44–19:19 | **Canonical mutation #2: control-law v3 `f2973b2c`** — leaky entry-anchored accumulator (λ=0.86); seed `a9a7e4f0` qualified after Joe's σ=0.015 and drift-gate 0.055 rulings | tracked `C11O_AUTHORIZATION.json` (strongest chain); 2 rulings referenced-not-quoted | LANDED; seed qualified; contract re-mint ×3 (§4.C) |
| C11P (~20 sub-units) | 07-29 | Vacuous-preflight caught; optimizer repaired (Variant B); 4.72M acquisition → gate-calibration stop; **QUALITY_CARD: policy NEVER STEPS, standing reward-optimal**; reward_v2 repairs (cadence NaN-credit, yaw stride-term, action-rate allowance) — ordering "correct for good walking" but preflight still fails; DAgger oracle inverts superseded law (76% of supervision); **support-instrument defect: 3 reports' numbers retracted** (spawn 2.6 mm lift; contact flag watched plate 2.5 mm above feet, missed 51% of contact) | Guard `evaluate_authority` allowed=True + owner grant `D-02102a54743b`; per-ticket rulings (KL 0.125, gait gate) | acquisition NOT achieved; instruments repaired; **first Opus-trailer commits** |
| C11Q | 07-29 21:51 | Crown↔C11P parity: 167 differences; "audited the wrong crown last ticket" (576f4dff SUPERSEDED per CROWN.md → zdecup20M); crown's own teacher fell 29/660 and launched anyway | owner ticket quoted | AUDIT COMPLETE — Joe's 2 PPO conditions now met |
| C11R | 07-29 22:54 | Reconstruction preflight: STOP at contract (48-D is inside Joe's signed binding `d65edefc`; 4 options tabled); ref c344 re-verified GO; C11Q's ledger join found defective (5/31 verified) | ticket stop-rule quoted verbatim | STOP FOR JOE |
| C11S | 07-30 06:34 | **Pivot to walk52_v58 (owner-authorized 52/58)**: reconstruction launcher built; preflight 16 PASS/1 BLOCKED — zero plant-legal standing-certified checkpoints exist | "the contract you authorized" | NOT LAUNCHED — remedy = standing foundation |
| C11S1 | 07-30 07:24 | plant-fingerprint **v3** (`c44ad73f`) + shared construction path landed; SFC-1 datum gate — **its −8.78 mm injection finding was WRONG** (measured a path the trainer doesn't execute) | ticket | STOP — datum repair required (later retracted) |
| C11S2 | 07-30 08:16 | **RETRACTED C11S1 by execution**: spawn = constant `CANON_ROOT_Z` 0.3373 (`batch_sim_tip.py:575`); conditional 4M standing run — **trained on a plant with ZERO visual geoms** (self-declared C8 tension) | ticket | datum resolved; 702/1500 survival — not certified |
| C11S3 | 07-30 09:11 | Minimal-delta standing foundation: 3 seeds, `s0_fine/ppo_6291456` (sha1₁₆ `d9268c4e`) survival 1486/1500; kernels_tip contact **floor filter** (the arc's only reward-kernel change) | ticket | **CERTIFIED** (but see §2 R22) |
| C11S4 pre | 07-30 12:33 | STOPPED at iteration zero: exact-ckpt cert fails 8.5 N arm (92.5% < 95%); walking launcher frozen (`c4986396`), NOT executed; Guard refuses ppo | fail-closed honored | STOP → owner rulings |
| GUARD-D236 | 07-30 14:11–18:22 | Owner rulings 1–5; mutations.jsonl ledger; C11S4 bound live | tracked ruling artifacts + receipts | ACQUISITION blocked on one owner sentence |
| C11S4A | 07-30 19:13 | **Owner ruling 6 VERBATIM** (lifecycle→ACQUISITION); crown_walk 48M launch; first genuine alternating stepping; oracle "DOES NOT WALK" (later demolished) | ruling 6 @ e77b30a; **no executor-gate receipt (D-10, disclosed)** | run complete |
| C11S4B | 07-30 19:21–19:33 | **Owner ruling 7 = 60M absolute ceiling** (@ 439c92f, +2 lines to `C11S4_OWNER_RULING.json` — recorded, though the S4B report never cites it); D-10 closed with real receipt; continuation to 59,768,832; **D-11 opened** | ruling 7 artifact; grant `D-258ae133a7be` | 3 of 4 walking bars pass |
| C11S4C | 07-30 20:16 | Step-225 stall artifact root-caused (real-step gate + `et_no_step` 4.5 s); oracle survival channel repaired; D-12 closed; **`ppo_13107200` banked as candidate PENDING Joe visual** | ticket | 2 prior conclusions retracted |
| C11S4D/E/F | 07-31 | Cadence audit → income verification (S6 paid exactly 0) → adapter decomposition (sway +3.406, Rank-1 tie, DR/survivor confounds) | tickets; D-11 blocking honored | per this session's takeover audit |
| takeover, C11S4G | 08-03 (Fable 5) | Takeover audit; C3 income-ordering probe: **FAIL** robust; **D-27/D-28 discovered**; repair options priced | Joe direct session instruction; G ticket | C3 FAIL blocks PPO on this book |

## 2. Retraction / correction ledger

26 formal chains (V4 R1–R26; full text in the workflow record). The load-bearing ones:

| # | retracted claim | retracted by | class |
|---|---|---|---|
| R5 | entire scout lane C10V→C11D measured on non-canonical solref + unstable ankle-roll DOF | C11E blanket caveat | wrong plant/law |
| R7 | LTA1: "every legacy ckpt predates the armature commit" (**the one corruption that crossed the model handoff** — token exhaustion killed 7/12 LTA1 agents) | C11F census (733/2445) | invalid evidence |
| R8 | crown-death-as-ankle-chatter confound | C11H (byte-identical collapse at Kd 1.0 vs 0.2) | falsified |
| R10 | comparability resets ×2 without re-measurement (pre-C11J numbers; pre-C11O checkpoints) | C11J / C11O declarations | eval basis |
| R17 | support deficit (3 reports' numbers; 0.921×BW headline) | SETPOINT_TORQUE_AUDIT (native solver: no deficit) + SUPPORT_REPAIR (94%→3%) | instrument defect |
| R18 | C11P Crown-semantics compared the WRONG (superseded) crown | C11Q | wrong asset |
| R21 | C11S1 datum injection −8.78 mm | C11S2 by execution (`CANON_ROOT_Z` constant) | instrument measured unexecuted path |
| R23 | S4A oracle "DOES NOT WALK" | S4B (metric pinned at horizon/2) + S4C (stall-timer artifact) | invalid metric |
| R24 | S4D ranked cause (S6 tempo mismatch) + retarget rec | S4E (paid exactly 0.000000) | dir_factor zero gate |
| R25 | S4F's own 0.6525 s stride + H3 demotion | S4F R1–R3 (DR ensemble, survivor corr −0.899) | survivor bias |

**Never formally closed (V4):** (a) R14 — C11N/C11O "seed qualified" vs QUALITY_CARD's proof the seed never steps (qualification was for standing admissibility; the tension is framing, but no banner links them); (b) R22 — C11S3 "CERTIFIED" vs C11S4's exact-checkpoint 8.5 N failure (92.5% < 95%) — **adjudicated by owner ruling 6** (recorded verbatim) but the C11S3 report carries no forward notice; (c) R26 — **D-27/D-28 upstream contamination: every `track`-income number back through the Crown was produced by the defective `_pose_err`; no unit has re-read them** (includes the S4A/B TRACK 0.74→0.33 trajectory and yd%≈70 narrative — U8).

## 3. Model-handoff continuity

- **The brief's premise ("transfer at C10T") is corrected by the repo.** The only in-file handoff record is C11E §"CONTINUITY + MODEL HANDOFF (recorded per owner instruction)": *"this unit began on Claude Fable 5 and completed on Claude Opus 5; the switch was forced mid-unit when the 20× Max token allocation was exhausted"* (commit `2f3c05a`, 07-28 14:08). C10T→C11E-start was Fable 5 work.
- **Commit-trailer evidence:** C10S→C11A commits carry no model trailer; C11E→C11N commits carry `Co-Authored-By: Claude Fable 5`; from C11P (07-29) every commit carries `Claude Opus 5`; 08-03 units are Fable 5 (this session). The C11F–C11O window (07-28 evening) therefore has **conflicting attribution** (in-file record says post-switch; trailers say Fable) — most plausibly stale session boilerplate; recorded as ambiguous, not resolved.
- **What crossed the boundary badly:** exactly one confirmed corruption — the LTA1 universal claim (R7), produced by the token-exhaustion-truncated agent fleet, entered C11E's filed addendum, and was refuted by the very next unit's Guard-backed revalidation (C11F). No other loss or silent alteration is detectable (V4 H3): C11E/C11F re-derived every load-bearing pin from repository artifacts, and the return handoff (Opus→Fable, 08-03) re-verified state before acting (V4 H4).
- **No unstated architecture/product decision, no scope expansion, no dropped owner constraint attributable to the handoff itself.** The 60M ceiling, visual gates, PPO grants, and canonical mutations all have recorded owner chains of varying strength (§4.C).

## 4. Invariant / contract continuity table

| invariant | continuity verdict |
|---|---|
| **A. Plant** | v2 `81d4e6d8226ed12b` UNCHANGED in every unit C10T→C11S4G. v3 fingerprint `c44ad73f` ADDED (C11S1, recorded) because v1/v2 are blind to `dof_armature` (C11F) and solver settings. No plant mutation anywhere in the arc. |
| **B. Reference** | c344 `c602133c…` pinned and re-verified GO (C11R, 4-way adversarial). Fig8 family authored/banked under recorded owner visual verdicts (C10T input episode never hashed — output-only provenance, minor). |
| **C. Control law** | THREE laws live in the arc: legacy walking law (KP 30×0.6667=20; **KD 1.0×[1,1,2,2,1,1] → ankle-roll kd = 1.0**; τ 12.5 via `set_tau_limit_vec`; ACD 10) — used by C11S3 standing AND C11S4 walking (resolved configs verified identical this audit); scout-lane v2 `fe5a6fed` (C11J: ankle-roll 0.2 — authorization asserted-not-quoted) and v3 `f2973b2c` (C11O, strongest chain). **The C11S pivot returned to the legacy law; the ankle-roll 0.2 repair never reached the active walking lane** → §5 D-30. Torque limits differ BY LANE (scout kernel default 15.0 vs walking card 12.5) — declare-the-lane rule, not a defect (U4 resolved). |
| **D. Obs/action contract** | Canonical walk52_v58 restored at C11S (owner-authorized) and stable through C11S4G. The scout lane churned NINE contract hashes (`c1e2448d`→…→`d65edefc`), three sharing one TS (C11O), and **Guard target `contract-frozen-c11e` `959b49ed` was never rebound during the churn** — an unrecorded divergence at the Guard boundary until the C11S4 binding (V4 C3). Historical cmdphase54/55: permanently `runtime_semantics_ambiguous` (U9). |
| **E. Reward book** | Scout-lane reward_v2 changes (C11P) recorded per-ticket but **no reward_hash minted C10U→C11P** (V4 C5). Walking book c8j.1 `6b48d6e3` hashed from C11S4 on; changed only by the C11S3 contact floor filter (kernel-side, recorded). C11S4G proved the walking book C3-FAILS. |
| **F. Spawn / oracle / launcher** | All spawn changes recorded+ticket-bound (V4 C7). Oracle redefined at S4C (D-12 closure) — recorded. Launcher frozen and Guard-hash-bound (matches disk, verified twice this session). |
| **G. Owner constraints** | 60M ceiling: honored exactly (59,768,832) and recorded in the ruling artifact via commit `439c92f` — but never cited inside the S4B report (citation hygiene, not a violation). Visual gate: strict at C10T/C10U; **eroded mid-arc** — C11S2 trained 4M with zero visual geoms (self-declared), C11S3/S4A/S4B trained with Joe's visual review still pending; `ppo_13107200` remains visually unreviewed today (V4 C11). S4A ran without an executor-gate receipt (D-10, disclosed, closed at S4B). |

## 5. Unresolved-error ledger (current, beyond the debt register)

| id | item |
|---|---|
| U1 | **D-27/D-28 live in the trainer; both PRE-DATE C10T** — D-27 born with the file (`28b38a6d`, 2026-07-05); D-28 divergence at `--entrain-arc` (`448d5556`, 2026-07-13); per-window rebuild consciously kept world-X (`16b91f98`, 07-24, "left as-was"). Nothing in the audited arc introduced them. Bonus: C10T-family refs carry `rf_rel = zeros` → D-27 degenerates further on fig8 refs. |
| U2 | **Guard display-layer defect (proposed D-29):** `brittlectl.py:90-92` and `handoff.py:44` raw-scan the append-only ledger, printing blocker `B-a603393eeb21` as OPEN and 12 decisions as PENDING. Ground truth: the blocker was **RESOLVED 2026-07-29T03:46Z** (record `B-531ca9749d06`, resolution_ref = Joe's `D-23c6a79c4aec`); the permit path (`permits.py:464-479`) resolves threads correctly → **ppo PERMITTED is the correct authority answer**; only 2 decisions are truly pending. This display defect misled the 08-03 takeover audit and this audit's own brief. The 2 "contradictions" are unclosed pre-launch **test probes** from the C11S4B session (`/bin/true` probe + deny-path negative control; the real run `R-cf68618ac375` closed COMPLETE) — they need close records (control-lane write), nothing more. |
| U3 | **D-14 (trainer 69.4% young-death vs zero-fall eval) — new candidate mechanism (proposed D-30):** the active walking lane runs ankle-roll kd = 1.0 (verified: `mini_biped_yaw_cmd_env.py:31` KD=1.0 × kd_mult[5]=1.0), the exact pair C11E proved discrete-unstable and C11J repaired to 0.2 — and C11S4 training spawns 30% of worlds on reference frames (`arc_rsi 0.3`), the RSI-insertion class the instability is fatal to; the clean eval used `arc_rsi 0.0`. Falsifier: per-spawn-type death rates in one training-config rollout. Guard blocker B-a603393eeb21 is formally resolved, but its **subject hazard is live again on the pivoted lane** — closure of D-30 should be adjudicated by Joe, not assumed. |
| U5 | Crown identity triple ambiguity: baseline pin 576f4dff (sil027v2) vs CROWN.md canonicalizing zdecup20M `ppo_20185088` (sha1 only ever truncated `b1c08d58…`) — never reconciled to a full hash. |
| U6 | S4A's 92 and S4B's 25 produced checkpoints are unhashed at production (subject ckpt `20ab67fd` hashed only from S4D on); `warmed_critic.pt` unhashed. |
| U8 | Whether the S4A/S4B quality trajectory (TRACK 0.74→0.33, yd%≈70) is D-27/D-28-contaminated has not been assessed — those TRACK figures come from the defective `_pose_err`. |
| U9–U11 | F8/k55 permanent provenance dead-ends (by design, registry-flagged); C10T rotation-sign inconsistency (+/−0.0993°); C11A/C11B future-dated TS + shared-TS batches break name-TS ordering in two windows. |
| **E. This session's own corrections** | (1) Takeover audit F-A3 said ankle-roll kd "resolves to 2.0" — correct value **1.0** (direction of the claim — replay probes invalid at ≥ the unstable kd — survives). (2) Takeover E-1 framed the blocker/permit disagreement as an authority conflict with fail-closed PPO-blocked reading — corrected by U2: display defect; authority-layer ppo is PERMITTED; the operative PPO blocks are the C3 FAIL + the D-11 lane rule. (3) C11S4G's "static ordering flips (≈21.9 > 17.5)" compares repaired-S1 against the **defective-book** S2; the D-27 repair also uplifts the exploit's feet factor (bound ≤ e^0.9877 ≈ 2.69× on a 0.202 term → ≤ ~+0.34/step, far short of the ~4.4/step needed to un-flip) — the flip survives the bound, but the re-probe must re-price all scenarios (V5 condition 3). |

## 6. Guard / repository reconciliation

- **Guard live state** (`~/code/brittle-guard-state/live`, all ledgers read): campaign `brittle-unified-locomotion`, ACQUISITION, unit 12U-C11S4, campaign_commit `439c92f…`; 11 mutation receipts (seq 1–11, all production-mode, each with before/after digests + authorization). All four disk-resolvable canonical targets MATCH (launcher `c4986396`; parent `d9268c4e` = on-disk sha1 of `ppo_6291456.zip`, re-verified independently this audit; plant v2/v3); the four computed construction fingerprints are corroborated by receipted run outputs. Truly pending decisions: 2 (not 12 — §5 U2).
- **Repository:** all 72 common arc reports byte-identical across the two trees; **git protection is the headline gap** — MAIN has 36/73 arc reports untracked (LANE 4/72); **S4D/S4E/S4F/S4G are untracked in BOTH trees and the 08-03 takeover audit exists as a single untracked copy in MAIN only** — the arc's five newest documents have zero git-protected copies. `walk_changelog.md` exists (tracked in MAIN, newest entry 2026-07-11 — covers none of this arc; absent in LANE). Banner gaps: C11S1 has **no** retraction notice (worst); S4D lacks a post-hoc notice for its retracted S6 recommendation; `ranking.md` banner present and correct.

## 7. C11S4H readiness — verdict

**GO-WITH-CONDITIONS.** The core proposition survives adversarial attack (V5, source-verified):

- **D-27 and D-28 are the correct minimum-delta next repairs.** Both verified live under this card's exact flags (feet term at `:848` reached — `anchor_frame`/`profile_mix` off; entrained arc phase `:1415-1445`); both **pre-date C10T** (2026-07-05 / 2026-07-13 — they are inherited walking-stack defects, not arc regressions); R-A is single-source coherent (all 14 `ref_xnet` consumers read `self.ref_xnet` live).
- **R-C stays excluded from the implementation** — statically coherent (repaired S1 ≈ 21.9/step, disc ≈ 2,185 beats S2 on all four bases within the repricing bound) — but is **pre-decided in the same ruling as the contingency** if the post-H re-probe fails.
- **Conditions (all fit inside the one owner ruling):**
 1. The ruling must **explicitly supersede the frozen-launcher/construction binding** — the launcher card documents the D-28 mismatch as *"DELIBERATELY UNCHANGED … a BEHAVIOURAL CHANGE, not a bug fix"* (`launch_c11s4_walking.sh:41-46`) and `C11S4_OWNER_RULING.json` freezes it; approving the code change alone leaves H in violation of a standing owner binding.
 2. **Declare the obs-semantics shift:** R-A changes the phase clock the actor observes (obs cols 50–51; advance ~1.076→1.0 fr/step). Contract width is unchanged, but any later warm start from `ppo_13107200` is a declared distribution shift; and since Guard binds only the launcher hash, **extend fail-closed pins to the trainer file before any PPO** (D-11 closure alone cannot see a `train_tip_reftrack.py` edit).
 3. The post-H C3 re-probe **re-prices ALL scenarios under the repaired book** (S2's own track uplifts under D-27 repair; bound computed, flip survives, verification still owed).
 4. R-B **pins the frame convention** (rotate world foot offsets by the **robot's** pelvis yaw; G's executor pricing conflated ref-yaw with robot-yaw — equal for the executor, not in general) and **quarantine-marks the three dormant sibling feet-term sites** (`:821`, `:844-846`, `:1485-1487`) plus the critic `se` world-X note, so the repair doesn't leave four divergent feet semantics.
- **Confirmed non-prerequisites for the repair+re-probe** (they gate the subsequent PPO only): D-11, Guard blocker record, owner visual gate on `ppo_13107200`, D-24, D-25, C11S4C oracle coupling.
- **Flag carried into H's context, not its scope:** D-30 (§5 U3) — the lane-law ankle-roll hazard interacts with any future `arc_rsi>0` training decision and should be on the table at the same ruling.

## 8. The one recommended next action

**Joe issues the C11S4H ruling package in one sitting:** authorize R-A + R-B **with V5's four conditions embedded** (explicit supersession of the frozen-launcher binding; obs-semantics declaration + trainer-file hash-pinning requirement before PPO; full-scenario C3 re-probe; R-B frame-convention pin + sibling quarantine), **R-C pre-ruled as the contingency** if the re-probe fails, and — same sitting, same page — an explicit disposition of **D-30** (ankle-roll kd 1.0 + `arc_rsi` on the active lane). Everything else (D-11 closure, probe-record cleanup, git protection of the five unprotected reports, banners) queues behind it.

## 9. Caveats

- Reader fan-out initially omitted C10X/C10Y/C10Z (grep pattern defect, this unit's own); recovered from full commit messages + report header/status/decision blocks — table rows for those three carry header-level, not full-read, provenance. LTA1's standalone report was read only through C11E/C11F context.
- Model attribution for C11F–C11O rests on conflicting evidence streams (§3) and is recorded as ambiguous.
- V4's raw ledger contains two claims this audit corrected before adoption: the "60M ceiling untraceable" claim (the ruling artifact commit exists) and the U2 authority-conflict framing (superseded by the V2 state-file deep-read). The workflow's full per-agent outputs are preserved in the session transcript dir (`wf_f137ace1-b35`).
- Nothing in this audit re-measures physics; it audits records, source, and live state. No files edited (the prohibitions were honored; hygiene items are recommendations only).
