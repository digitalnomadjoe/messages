---
id: BRITTLE-20260824T011416Z-5d439d30
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: WALK-DR02
created_at: "2026-08-24T01:14:16.526139Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/WALK_DR02_KP_SHELL_PILOT_RESULT_20260823T234323Z.md
local_source_sha256: 76062a7ac0baa5bf89d3bc91884cf30ba6ad4949a24bee04e6588c73508c0a4f
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: WALK_DR02_KP_SHELL_PILOT_RESULT_20260823T234323Z
mirror_bytes: 13352
---
# WALK_DR02_KP_SHELL_PILOT_RESULT_20260823T234323Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/WALK_DR02_KP_SHELL_PILOT_RESULT_20260823T234323Z.md` |
| sha256 | `76062a7ac0baa5bf89d3bc91884cf30ba6ad4949a24bee04e6588c73508c0a4f` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | WALK-DR02 |
| mirrored at | 2026-08-24T01:14:16.526139Z |

<!-- brittle:mirrored-content -->

TEAM: GREEN — Opus 5

# WALK-DR02 — Kp High-Shell DR Pilot: RESULT — FAIL per §9, with the failure localized to two mechanisms

**status:** FAIL · The pilot ran to completion under full Guard authority (run `R-d0471748444f` COMPLETE); no checkpoint qualifies under §8 — nominal leaves the frozen envelope at every checkpoint and the MuJoCo-truth shell boundary regresses; the one genuine gain (Isaac ×1.15 survival repaired at the final checkpoint) is paid for outside the envelope. STOP for Joe.
**recommend:** No continuation as-is. The two localized mechanisms (§6) point at concrete redesign doors (§7) — all Joe-gated; none was taken.
**decision:** FAIL branch of §9/§10: DR improved only by degrading nominal; MuJoCo shell falls at ×1.05–×1.10 that the Crown never has; the ×1.15 gains are engine-split; single seed.
**next:** Joe reads §6–§7 and picks a door (or closes the lane); the frozen Crown remains untouched and remains the deployed function.

**constitution:** C1, C2, C4, C6, C8, C9, C10, C12
**potential violations:** none
**amendment proposed:** no

Ticket: WALK-DR02 (owner-direct; bus `BRITTLE-20260823T234323Z-ec6d7c8c`); staging report `WALK_DR02_KP_SHELL_PILOT_STAGED_20260823T234323Z.md`. Executing session: Claude Fable 5 (ticket header names Opus 5 — recorded). Single training seed, per the owner grant — every trajectory claim in this report is a single-realization observation (C10).

---

## 0 · Verdict

**The one question — "does focused exposure to the first failing Kp shell expand the basin?" — is answered NO at this budget and recipe, with the failure mechanism measured, not guessed.** What focused exposure actually did:

1. **Shifted the operating point instead of expanding competence.** The actor cannot perceive the drawn kp (onboard obs = encoders + IMU only), so one compromise gait must serve kp∈[1.00, 1.20]. PPO moved that compromise toward a slower, longer-stride, stiffness-tolerant gait — eroding nominal quality (both engines) and low-shell competence (MuJoCo).
2. **Broke CPU-truth transfer by removing the low-side draw.** The additive-DR mechanism this pilot replaced draws kp ×U(0.7, 1.3) — and its own in-kernel documentation records WHY: kp ±30% was a measured-delta widening to cover the float32-GPU vs float64-CPU equilibrium gap, after policies fine-tuned inside a narrower envelope "still broke on the CPU truth path". The [1.00, 1.20]-only shell deleted that coverage; the result is exactly the documented failure shape: the trained checkpoints FALL on deterministic CPU-MuJoCo cells at every shell level (×1.05: 4 falls at the final checkpoint, where the Crown has zero) while the SAME checkpoints run CLEAN on Isaac.

The single real gain: **in Isaac (qualified C13S18 construction), both the transient-best (442k) and the FINAL checkpoint survive all kp×1.15 cells where the frozen Crown falls twice** — genuine, persistent-in-Isaac ×1.15 survival repair — bought at crown-cell stride −9% / speed −8.8% (final ckpt, Isaac), which is outside the frozen ±5% envelope, so §8 disqualifies it. τ p99 never exceeded 8.62 (cap 12.5); action saturation ~0; **zero foot-foot real-geometry collisions in any nominal cell at any checkpoint** (collisions occur only inside falling shell cells, as the Crown's own do).

## 1 · Authorization & execution chain (all receipts)

Joe's four verbatim sentences (chat, 2026-08-24; ruling artifacts `OWNER_RULING_WALKDR02.json` @ `3ea7f0e3`, `OWNER_RULING_WALKDR02_B.json` @ `d20ca0b3` — transcribed, never authored): acquisition transition → `D-9dae28b5c8d4` / `TR-0c1b7598b25b`; ppo → `D-46c6db53868a` + `D-456f3ff7bf14` (launcher + bound-construction scope); certification window → `D-8634866acac4` / `TR-93440e7e5078` + return `TR-ba284d9025cc` (and re-open `TR-d2b194648370` / close `TR-986a18b40f13` for the delta-9 re-pin); canonical mutation → `D-742c95316c1b` (registered `launcher-walkdr02-kpshell` + re-pinned `trainer-source-c11s4` → `5e7ea6d5589f2d38`, `batchsim-source-c11s12` → `ea871961954d17c2`, `runtime-train-tip-gpu-c11s15cy` → `2bcc02bdc0f18621`, construction members + hash `258f482f49865d63`). Authority verified `allowed=True, reasons=0` before launch.

Three guarded attempts, all fail-closed honestly: `R-ad4b2babf474` FAILED — my stale `DISPLAY=:1` (only `:0` exists) killed the viewer and the card's supervision correctly killed the trainer; `R-4930eab357a1` FAILED at the very last card step — the bound slope-gate producer (pre-declared INVALID for phase-state runs in the inherited card header) now REFUSES under the v2 phase-bound manifest instead of mismeasuring; training and every receipt before it had passed. Card delta 9 replaced that step with a disclosed skip (card re-pinned `3d442c9b44561c59` under the mutation grant). **`R-d0471748444f` COMPLETE, exit 0, receipt `RCPT-568a1c78aafd93e2`, 28 outputs** — the pilot of record. Failed-attempt artifacts preserved beside the run dir.

## 2 · The run of record

`rl/runs/walking/walkdr02_kpshell_s0`: 884,736 steps (6 it × 147,456; 0.4 min GPU), 6 checkpoints, `--dr-shell 1.0,1.2,0.15` + `--no-additive-dr`, anchor = frozen Crown (w0 0.15, decay 8,388,608), nworld 2304. PARITY RECEIPT OK (27 measured fields — plant `81d4e6d8226ed12b`/`c44ad73fbd8b6f45`, crown law exact, ASO v3, τ12.5, ACD 10, 50/500/500). Final-iteration telemetry: ep_len 110.8, **yd% 0.0** (fresh-spawn cohort), EV 0.966, slip 0.41 mm, τ duty>10 1e-4, NETYAW 1.8°. Inspector: preflighted on a real checkpoint load, live on `:0` through the whole run (hot-reloaded every checkpoint), closed by exact PID by the card's EXIT trap. Bundle-export path re-proven on fresh weights (selftest vs the spectator-installed g_v1: worst |Δa| 3.58e-7, canonical C11S51 bank).

## 3 · Deterministic checkpoint monitor (MuJoCo fast truth path; vs the banked frozen-Crown baseline)

Trajectory table (crown straight cell + κ±0.35 symmetry + shell falls of 7 cells/level; baseline row = frozen Crown through the identical battery, reproduced bit-identical to its WALK-DR01 traces):

| ckpt | stride | cad | speed | sym κ±.35 | nominal viol. | ×1.05 | ×1.10 | ×1.15 | ×1.20 | ×1.25 | ×1.30 | τ99max |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CROWN | 0.2400 | 0.962 | 0.2260 | 1.12 | — | 0 | 1 | 3 | 4 | 4 | 4 | 6.26 |
| 147456 | 0.2440 | 0.923 | 0.2220 | 1.25 | 5 | **2** | **3** | 3 | 4 | 4 | **5** | 8.62 |
| 294912 | 0.2501 | 0.919 | 0.2261 | 1.06 | 14 | 0 | 1 | 3 | 4 | 4 | 4 | 6.16 |
| 442368 | 0.2622 | 0.847 | 0.2189 | **0.66** | 22 | 0 | **3** | **1** | **3** | **3** | 4 | 5.24 |
| 589824 | 0.2342 | 0.887 | 0.2081 | 0.81 | 19 | **2** | 2 | 2 | 4 | 4 | 4 | 6.11 |
| 737280 | 0.2370 | 0.864 | 0.2086 | 0.83 | 24 | **2** | 2 | **4** | 4 | 4 | 4 | 5.55 |
| 884736 | 0.2208 | 0.903 | **0.1985** | 1.02 | 17 | **4** | **4** | **4** | 4 | 4 | 4 | 5.59 |

Reading (channels per trend class): nominal speed **degrading** (−12% by final); stride **oscillatory** (+9% → −8%); cadence **degrading** (−6…−12%); κ±0.35 symmetry **oscillatory** and transiently severely one-sided (0.66 at 442k — a §9 "new one-sided behavior"); κ±0.35 curvature realization **degrading** (−15…−22% at several checkpoints — loss of curvature gain); MuJoCo shell survival **transiently improving then collapsing** (442k's ×1.15 1v3 / ×1.20 3v4 / ×1.25 3v4 → final 4-falls-everywhere including ×1.05 4v0). Violation lists per checkpoint in `monitor/QUALIFICATION.json`; the worst single fact: the final checkpoint falls at kp×1.05, a level at which the frozen Crown is spotless.

**§8 qualification: NO checkpoint qualifies** (DR-improves ∧ nominal-inside-envelope is never true, let alone at two consecutive checkpoints). Checkpoint-mining fields: `best_checkpoint` (DR, Isaac frame) = ppo_884736 (kp×1.15 survival repaired, symmetry preserved, but stride/speed outside envelope); `best_balanced_checkpoint` = none (ppo_294912 is closest: shell = Crown-equal, 14 nominal violations); `final_checkpoint` = ppo_884736; `recommended_continuation_checkpoint` = **NONE** (§10 FAIL stop; no early-checkpoint rescue).

## 4 · Isaac confirmation (qualified C13S18 construction, candidate + final)

| policy | nominal κ-cells falls | crown-cell stride / cad / speed | κ±.35 curv (+/−) | kp×1.15 falls (7 cells) |
|---|---|---|---|---|
| frozen Crown | 0 | 0.2289 / 0.904 / 0.2065 | +0.427 / −0.320 (sym 1.33) | **2** (km10@406, kp10_s1@597) |
| ckpt 442368 | 0 | 0.2461 / 0.856 / 0.2145 | **+0.299 / −0.426 (sym 0.70)** | **0** |
| ckpt 884736 | 0 | 0.2083 / 0.893 / **0.1884** | +0.409 / −0.316 (sym 1.30) | **0** |

Isaac agrees with MuJoCo on the nominal-quality erosion (442k: κ±0.35 asymmetry flip; final: stride −9% / speed −8.8%, symmetry preserved) and DISAGREES on shell survival: **both candidates are clean at kp×1.15 in Isaac where the Crown falls twice**. The cross-engine split on the same checkpoints (Isaac 0 falls vs MuJoCo 4 at ×1.15) is the documented low-side-coverage mechanism of §0.2 expressing itself: the shell-trained policy drifted off the CPU equilibrium that the deleted kp-low draws used to pin, while Isaac's TGS contact stack tolerates the same gait.

## 5 · Invariants & staples

Foot-foot (real colliding geometry, analytic sphere math; proxy-envelope never scored): **0 overlap frames in every nominal cell of every checkpoint and both engines' confirmations**; overlaps occur only inside falling shell cells (the Crown's own deep-shell falls do the same). Torque: τ p99 max 8.62 (147k) then ≤6.2 — under the sub-10 preference and far under 12.5; no clip-riding (ankle-roll duty 0). Action saturation ≈ 0 throughout. Slip 0.41 mm (training) / 0.5–1.0 mm p95 (batteries). Survival: nominal batteries 16/16 at every checkpoint, both engines. No motion-quality claim is made anywhere in this report (C8); the live viewer ran on `:0` throughout training for Joe's eyes; the verdict rests on instruments.

## 6 · Failure localization (§10 — measured, no redesign attempted)

1. **Unconditioned compromise (both engines).** kp is unobservable to the actor by contract; 85% shell exposure moves the single gait PPO can express toward the shell's demands. The 15% exact-nominal fraction plus the Crown anchor (~90% of w0 retained across this short run) did not pin the nominal operating point — the anchor acts on visited states, and 85% of visited states are shell-plant states. Consistent with the lane's prior laws (DR-ensemble vs nominal gait split; seesaw/limit-cycle capacity findings).
2. **Low-side deletion broke CPU-truth transfer (engine-split).** The replaced additive-DR draw's kp ±30% band exists — per its own kernel comment — to cover the GPU↔CPU equilibrium gap ("policies fine-tuned inside the DR envelope still broke on the CPU truth path"). The high-side-only shell removed it; the MuJoCo-side falls at ×1.05–×1.15 with Isaac-side cleanliness reproduce that failure shape on the first try.

## 7 · Doors (Joe's; none taken)

(a) Shell band including the low side (e.g. ×[0.85, 1.20], nominal fraction raised) — restores the documented CPU-coverage role while keeping high-side focus. (b) kp-conditioning via the PRIVILEGED CRITIC only (the critic is already asymmetric/privileged; actor untouched) — an interface change, therefore owner-gated. (c) Accept the measured tolerance-vs-quality frontier and keep the frozen Crown as-is (the Isaac ×1.15 hole is real but small: 2 deterministic cells). (d) Multi-seed, longer-budget replication of the 442k transient (C10: this pilot is one realization). Ticket §10 prohibitions honored: no auto-launch of any of these.

## 8 · Process notes

The lane's autonomous executor auto-blocked my WALK-DR02-EXEC bus ticket on prohibition-section keywords ("promotion/canonical-mutation/crown") — fail-closed keyword scan, not fought; the authoritative execution record is Guard + this report + the changelog. Both bus escalations resolved with the owner decision (`BRITTLE-20260824T010115Z-bd564500`, `BRITTLE-20260824T010124Z-b53b71a0`). A pre-existing week-old set of 7 tracked-dirty files (other lane's report appendices) was committed verbatim as housekeeping to satisfy the production-run reproducibility gate — content untouched, labeled. The stale `parent-c11s3`/`c344` "pin drift" from my earlier audit was my own algo error (sha1 vs sha256 fields) — both pins are in fact intact.

## 9 · Artifacts & replay

`rgl/artifacts/walkdr02_kp_shell_pilot_20260823T234323Z/`: cards (pilot `3d442c9b44561c59`, P2 parent, control), owner rulings, guard captures (3 runs), `monitor/` (crown baseline + 6 checkpoints × 58 cells + `QUALIFICATION.json`), `isaac_confirm/` (4 batteries × 7 cells). Run dirs: `rl/runs/walking/walkdr02_kpshell_s0` (of record) + `_failed_attempt3/4` (evidence) + `walkdr02_dry1`/`dryctl`. Replay any battery: `walkdr01_mj_arm.py --bundle <monitor/ckpt_*/actor_bundle.npz> [--perturb '{"kind":"kp_scale","value":X}']`; Isaac: `isaac_arm.py --foot-material --pos-iters 100 --dyn-friction --actor-bundle <bundle> --rollout <cells>`.

**changelog row:** `WALK-DR02, 20260824T010440Z` (result row) — post-append `rgl/LOCOMOTION_CHANGELOG.csv` sha256_16 `61fc20ac788079b3` (466 rows × 13 cols, both trees byte-identical)
