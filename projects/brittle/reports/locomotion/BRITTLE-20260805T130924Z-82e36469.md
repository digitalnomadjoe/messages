---
id: BRITTLE-20260805T130924Z-82e36469
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15H
created_at: "2026-08-05T13:09:24.086333Z"
source_commit: 33f14ad
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15H_ANCHOR_CONSTRUCTION_20260805T062000Z.md
local_source_sha256: bc1e4cd1e4887c7c3134d55abe1b0de40b24723d319d5eb8a69aec501a107ad8
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15H_ANCHOR_CONSTRUCTION_20260805T062000Z
mirror_bytes: 5785
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15H_ANCHOR_CONSTRUCTION_20260805T062000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15H_ANCHOR_CONSTRUCTION_20260805T062000Z.md` |
| sha256 | `bc1e4cd1e4887c7c3134d55abe1b0de40b24723d319d5eb8a69aec501a107ad8` |
| brittle commit | `33f14ad` |
| lane | locomotion |
| unit | 12U-C11S15H |
| mirrored at | 2026-08-05T13:09:24.086333Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15H — Anchored kP Acquisition: construction FROZEN, all pre-PPO gates PASS — awaiting sentences

**date:** 2026-08-05 · **unit:** 12U-C11S15H · **lane:** locomotion · construction only; NO PPO yet; canonical stacks untouched
**constitution:** C1, C2, C3, C6, C7, C12 · **potential violations:** none · **amendment proposed:** no
**status:** the reference-action anchor is implemented spec-exact on the frozen G construction (ONE mechanism added, everything else byte-inherited); all pre-PPO gates pass. **STOPPED for the owner sentences (§5).**
**decision:** Joe — §5
**next:** await sentences → probes → anchored kP probe (4,194,304, both batteries per 524,288, four-way verdict TRANSFERRED / ANCHOR-DEPENDENT / NO ACQUISITION / INVALID LABEL PATH)
**changelog:** row 44 `12U-C11S15H, 20260805T062000Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. Anchor construction (spec-exact, one mechanism)

Under today's full-setpoint transform (`ref_prior`: `q_des = jq[phase] + scale·a`) **the member's reference action is `a* = 0` for all 12 actor-owned setpoints** — the trainer's own v7 comment records the same identity ("the reference-anchored q_des IS the curved expert"). P16 adds: `loss += w(t)·mean(‖actor_mean‖²)` over ALL minibatch samples, `w(t) = 0.15·max(0, 1 − steps/4,194,304)` — **linear decay to exactly zero**, reached inside the bounded probe (the spec's 10M proportionally shortened, as the ticket allows, so zero-anchor evidence exists in-probe); training LOSS only — no reward change, no runtime reference observation, no injection, no switching. Precedent disclosure: the v6 full-action arc-prior at w=0.5 FIXED "marched in place"; this differs in weight (0.15), scope (all samples, not curved-only), and schedule (decays to zero) — and the stop rules ("follows the reference only while strongly supervised", "turning disappears as the anchor decays") exist precisely to catch the failure modes.

## 2. Label validity (mandated proofs)

Generated under today's transform + law ✓ by construction (the identity above; the card's live law pins). All 12 setpoints ✓. No historical action semantics ✓ (no datasets; labels are transform-native constants — the p12t-era buffers stay untouched). Torque/contact feasibility ✓ — executing the labels IS executing the reference: DFL PASS on today's stack (SS P10 9.0 mm, τP99 3.74) + the F teleport evidence; open-loop `a≡0` is not claimed to be dynamically stable (no supervised label set is) — the anchor is soft supervision PPO keeps balance feedback around, the C11S14 stand-prior pattern that worked. Consistent with speed/κ/cadence/phase ✓ (phase-indexed setpoints ARE the commanded schedule). Mirrored kM labels ✓ trivially (`a*=0` on the kM member identically). Provenance pinned ✓ (ref `8806f7c8` + candidate + the contract block carry the whole label definition).

## 3. Pre-PPO gates

| gate | result |
|---|---|
| curved > straight pricing unchanged | ✓ the anchor is loss-side; reward terms byte-identical to G — the F/G α-battery ordering carries verbatim |
| anchor does not override reward ordering | ✓ no reward term added; loss magnitude ≈ 0.15·‖a‖² ~ 0.005 vs pg_loss ~0.01–0.05 (same order, mild) — measured live per-iteration via the new `ref_anchor_w` telemetry |
| mirrored label generation for kM | ✓ |
| **zero anchor reproduces C11S15G exactly** | ✓ **zero-anchor dry-run fingerprint `54f21532ec4d8afb` — bit-equal to the registered G book** |
| anchor in the resolved fingerprint | ✓ `walking_ref_anchor` block in-contract; **H fingerprint `720de82a83f7d9ae`, invariant 2 seeds × 2 out-dirs** |
| label + reference provenance pinned | ✓ §2 |
| Guard ALLOW/tamper/restore/re-ALLOW | runs after the sentences (same triad as G, on the H card) |
| action interface unchanged | ✓ no INVALID LABEL PATH condition — no interface change was needed |

## 4. Frozen pins

card `launch_c11s15h_kp.sh` **`bd8369d991a50a9e`** (= registered G card + the two anchor flags; structural 4,194,304 ceiling inherited) · candidate trainer re-pin **`7fb93a87ca4e6d98`** (23 builder edits; regenerates deterministically from `5c17a433`) · settings candidate `ac60edaf477cf256` · fingerprint **`720de82a83f7d9ae`** · construction **29 members `803a1bab43997e40`** (+`launcher-c11s15h-kp`) · canonical trainer **UNCHANGED `5c17a4338e21913f`** (asserted). Probe plan unchanged from G (seed 0, evals per 524,288 with both frozen batteries + per-checkpoint anchor weight reported, early-stop no-trend-by-2,097,152, anchor-decay stop rules, first steady-turn-gate checkpoint stops for Joe's replay, inspector pre-staged + verified before `run guarded`, closed after reporting). Qualification requires turning to persist at zero anchor weight (in-probe: the final checkpoints train at w≈0) or a separate zero-anchor continuation. PPO rides the ACTIVE grant `D-ed52647cd278`.

## 5. Sentences (proposed verbatim; subjects repeat per token at request time)

1. `I authorize transition to 12U-C11S15 for the lifecycle advance from ACQUISITION to POST_ACQUISITION_AUDIT.`
2. `I authorize canonical mutation for 12U-C11S15 scoped to: trainer-candidate-c15f, launcher-c11s15h-kp, canonical-settings-c11s4, reward-fingerprint-c11s12, bound-construction-c11s4-walking.`
3. `I authorize transition to 12U-C11S15 for the lifecycle advance from POST_ACQUISITION_AUDIT to ACQUISITION.`

## 6. Artifacts

`rgl/artifacts/c11s15h_anchor_20260805T062000Z/` — card, dry + zero-anchor scripts, payload `targets_register_c11s15h.json`, settings candidate, resolved configs (H + zero-anchor), dry log. Builder + candidate in `c11s15e_repair_20260805T041500Z/`. No kM, no distillation, no figure-eight, no promotion.
