---
id: BRITTLE-20260803T153626Z-fc4b50dc
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S4
created_at: "2026-08-03T15:36:26.100566Z"
source_commit: ca38ba2
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S4_TAKEOVER_AUDIT_20260803T153351Z.md
local_source_sha256: 27e452a9151447a2e6fb35aa58a024f695ffc718596170009be967929e25f87b
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S4_TAKEOVER_AUDIT_20260803T153351Z
mirror_bytes: 16844
---
# UNIFIED_LOCOMOTION_PHASE12UC11S4_TAKEOVER_AUDIT_20260803T153351Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S4_TAKEOVER_AUDIT_20260803T153351Z.md` |
| sha256 | `27e452a9151447a2e6fb35aa58a024f695ffc718596170009be967929e25f87b` |
| brittle commit | `ca38ba2` |
| lane | locomotion |
| unit | 12U-C11S4 |
| mirrored at | 2026-08-03T15:36:26.100566Z |

<!-- brittle:mirrored-content -->

# 12U-C11S4 — Takeover Audit: C11S4F Independent Review and the Cadence Lane State

**unit:** 12U-C11S4 (takeover audit) · **timestamp:** 20260803T153351Z
**constitution:** C1, C3, C4, C6, C10, C12 · **potential violations:** C3 (suspected at whole-book level — unproven, see §4) · **amendment proposed:** no
**scope:** read-only. No PPO, no policy/reward/launcher/plant/reference/obs/Guard change, no promotion, nothing written outside `rgl/reports/` and the message-bus mirror.

**status:** Lane state verified end-to-end. C11S4F's decompositions, retractions and confound handling SURVIVE independent review; its §6 `|dx|`-only PPO diagnostic does NOT survive as "highest-information next experiment" — it is double-blocked, its predicted direction rests on a non-sign-robust gradient by the report's own caveat, and the C3 income-ordering probe this card has never had delivers most of the same discrimination for zero GPU.
**recommend:** ONE ticket — 12U-C11S4G, a read-only whole-book C3 income-ordering probe (§5). No PPO of any kind.
**decision:** the C11S4F audit verdict is UPHELD WITH ONE SUBSTITUTION: Rank-1 tie stands; next experiment replaced. Two Guard findings and one artifact-hygiene defect escalated (§6).
**next:** Joe reviews this audit + the 12U-C11S4G ticket; control lane owns the Guard reconciliation items.

---

## 1. Mandatory state verification (all checks run 2026-08-03)

### 1.1 Repos and worktrees

| checkout | branch @ HEAD | tracked state |
|---|---|---|
| `~/code/brittle` (main) | `l24-march-demo` @ `ca38ba2` | dirty: pre-existing modified tracked files + untracked evidence dirs (long-standing; untouched) |
| `~/code/brittle-telemetry` (**the C11S4 lane tree**) | `dfl-actuator-telemetry` @ `c7e114b` "12U-C11S4C: oracle survival repaired" | **tracked files CLEAN**; untracked artifact dirs only |
| `~/code/brittle-controlplane` | `control-plane-design` @ `09dbd1d` | untracked report mirrors only |

Note: C11S4D/E/F were **never committed** — HEAD of the lane tree is C11S4C. The D/E/F reports and artifacts exist as files (mirrored to the main checkout per standing rule) but are not in git history. Flagged as hygiene, not a defect (matches lane practice under the never-`git add -A` rule).

### 1.2 Assets and hashes

| asset | verified value |
|---|---|
| candidate ckpt `rl/runs/walking_pilot/p12uc11s4b_cont_20260731T022335Z/s0/ppo_13107200.zip` (telemetry tree) | sha1 `20ab67fd3cd529b390bc921b3e947fe68ea30f4a` — matches D/E/F |
| launcher `rl/scripts/launch_c11s4_walking.sh` | sha256₁₆ `c4986396051cb271` — **matches the Guard-registered `launcher-c11s4-walking` target** (so despite D-11's non-enforcement, the launcher is currently untampered) |
| C11S4D report | sha256₁₆ `1159a82f5b959f62` |
| C11S4E report | sha256₁₆ `dc6de7ae76d14a23` |
| C11S4F report | sha256₁₆ `a36aa0695d4abfe7` — byte-identical in main checkout and telemetry mirror |
| C11S4F artifact dir | present in telemetry tree only (correct per reports-to-main/artifacts-in-worktree rule). ⚠ `ranking.md` and the F report were revised **2026-08-03 07:48/08:08** — three days after the run TS; content is the current claim set. |

The prompt's paths (`~/code/brittle/rl/runs/...`, `~/code/brittle/rgl/artifacts/c11s4f_...`) do not exist in the main checkout — the lane lives in the **telemetry worktree**. Repo state treated as authoritative.

### 1.3 Processes / GPU / bus

- `nvidia-smi` compute apps: **none**. No `ppo|train|spectator|mjw|render` python processes. No stale viewers.
- Message bus: **no open locomotion ticket** (`next-ticket → null`), no active claims, no open escalations, autonomy ACTIVE, spend guard OK. Daemons running: ticket_watcher ×2, lane_executor ×2, browser_bridge (bus infrastructure, left alone). This audit therefore runs under Joe's direct session instruction; nothing was claimed.

### 1.4 Guard live state (`brittlectl session start --state-dir ~/code/brittle-guard-state/live`, read-only)

- **Active unit `12U-C11S4`, lifecycle ACQUISITION**, branch dfl-actuator-telemetry. Canonical targets include plant v2 `81d4e6d8226ed12b`, v3 `c44ad73fbd8b6f45`, reference c344 `c602133c…`, contract walk52-v58 — all matching the reports.
- **Q4 BLOCKER: `B-a603393eeb21 MOVING_SPAWN_CONSTRUCTION` OPEN — prohibits `canonical_mutation, motion_claim, ppo, promotion`.** Yet **Q6 computed permits list `ppo` PERMITTED**, and `D-23c6a79c4aec` ("authorize resolution of blocker B-a603393eeb21", ACTIVE) exists while the blocker remains OPEN. **The blocker record and the permit computation disagree.** Fail-closed reading (C6): PPO is blocked. Reconciliation is a control-lane/Joe question — not adjudicated here.
- **CONTRADICTIONS (2): runs `R-bb0ea219a872` and `R-ec597b87c95f` still RUNNING** — orphan candidates, outputs quarantined until closed. No live process corresponds to either (GPU idle). Control-lane closure needed.
- **12 PENDING agent-proposal decisions** awaiting Joe (incl. "bind the exact C11S4 walking construction", "authorize PPO … | hold").
- **D-11** (`run guarded` records but does not enforce the launcher hash against its registered target) — **OPEN, BLOCKING for any PPO** per the lane debt register (C11S4B §1, reaffirmed E §7, F §7). Independent of, and additive to, the Guard blocker above.

## 2. Independent audit of C11S4F

Method: full read of C11S4D/E/F; spot-verification of every load-bearing code citation at source in the lane tree (`rl/mjw/train_tip_reftrack.py:1416-1422` `ds_prog = np.hypot(dx, dy_arc)`; `:1544/:1552` `fwdrate = ds_prog/0.02`; `:1546/:1557` the concave exp kernel; `:1567-1572` loaded/slip/foot-lift; `kernels_tip.py` `dir_factor = clamp(dir_align/ramp)` with `dir_align = 0` at `abs_c ≤ 1e-6` and `_cad_gate = dir_factor` in the legacy branch); launcher flags verified (`--cmd-lo 0.0 --cmd-hi 0.0`, `--entrain-arc --entrained-phase --cmd-profile`, TW inherited from rollpolish10, no `--cmd-manifold`/`--f8-sched`).

**Verdicts, by the ticket's five asks:**

1. **Retractions (R1–R5): SOUND and correctly propagated.** R1 reinstates 0.560 s as the nominal-plant stride and withdraws this unit's own 2.04× restatement; R2 un-demotes H3; R3 names the survivorship (corr(kp-proxy, survived) = −0.899 — the validity filter selects on the dependent variable); R4/R5 are honest small corrections. One in-band gap: **`ranking.md` (revised today) still contains the pre-R2 Rank-2 demotion with no retraction notice inside the file** — the warning lives only in F §9's artifact table. Stale-artifact hazard for any future reader of `ranking.md` alone. → hygiene item H-1.
2. **DR / survivor-selection confounds: correctly handled after R1/R3.** The per-world kp×U(0.7,1.3)/kd×U(0.8,1.2)/delay draws at reset are real (source-verified) and D-23/D-24 correctly quarantine every per-stride slope until re-derived selection-free or on the nominal plant. The income book's robustness to the filter (≤0.81% movement on all terms except `track`) is the right mitigation and is quoted with the selection-free values.
3. **Nominal vs DR-ensemble cadence: correctly stated.** 0.560 s nominal / 0.6435 s all-64 / 0.6525 s valid-39. The campaign ratio 2.574× stands. Consistent with C11S4D/E and with the standing additive_dr measurement-split lesson.
4. **The Rank-1 tie (stride-geometry blindness vs `r_fwd` smoothness): GENUINE, and correctly argued.** Both lack a counterfactual; the capture is observational variation around one converged optimum and cannot sign local pressure (total-reward dR/dT flips sign with the control set). **However, the tie is partially separable WITHOUT PPO:** the two hypotheses price different things, so scoring counterfactual gaits through the live book separates their *static* contributions — the smoothness toll a reference-profile gait pays (measured pieces already exist: 8.629/step `r_fwd` charge, +0.805/step low-DS premium, −0.575/step `p_cap` at reference stance) versus the exactly-zero geometry pricing. What genuinely requires PPO is optimizer causality only.
5. **Is the `|dx|`-only PPO run the highest-information next experiment? NO.** Grounds:
   - **Double-blocked:** D-11 OPEN (lane register) and Guard blocker `B-a603393eeb21` as-recorded prohibits `ppo`. Any PPO needs Joe + control-lane action first, regardless of merit.
   - **Its predicted direction is INFERRED from a non-sign-robust gradient** — F's own §8: the −23.585 counterfactual slope is the same observational regression R2 indicted; clustered SE and permutation null never computed.
   - **Economy shock with collapse risk:** it removes +3.406/step, 18.7% of the book, in one step. If walking collapses, the outcome discriminates nothing (all four hypotheses predict possible collapse under a −19% book shock).
   - **Branch logic weaker than claimed:** "step length stays pinned ⇒ constraint outside the reward" is confounded by the real-step/stall boundary (H5) and the DR ensemble (D-23) unless those are priced/controlled first.
   - **C3 precedence:** the Constitution requires a quantified income ordering before ANY PPO on this card, and that probe (never run at whole-book level for this card) delivers most of the same discrimination for zero GPU. It is on the critical path no matter which hypothesis wins.

**Additional audit findings (new, not in F):**

- **F-A1 — the stall economy favors the REFERENCE, weighing against H5-as-driver.** The reference executed as designed passes the kernel real-step gate on its own raster (stance 0.80 ≥ 0.35 s, swing 0.54 ≥ 0.15 s, clearance ~95 ≥ 21 mm), so it never stalls under `et_no_step 4.5`; the policy's gait fails `step_ok` on 98.25% of touchdowns and 25/64 DR worlds stall at the first possible moment. Termination pressure, as constructed, *punishes* the fast gait and *spares* the intended one — the opposite of a fast-cadence driver. (It remains a boundary condition and a survivorship shaper — D-14/D-23.)
- **F-A2 — C5 representability is trivially satisfied, so "mechanical inability" can only be dynamic.** Under entrained reftrack, `q_des = q_ref(phase) + action_scale·a`: the reference posture is the zero-action point at every phase. The policy *chooses* a 16.5° pooled deviation from it (C11S4D q-chain) while PD tracks its own command to 5.8°, with zero torque/action saturation. A static representability block is excluded.
- **F-A3 — LANDMINE: the direct H4 test (reference replay through the plant) is INVALID-BY-CONSTRUCTION under the live law.** The law's ankle-roll kd resolves to 2.0 (base kd 2 × mult 1), above the kd = 1.0 already established discrete-unstable at 500 Hz against the hardware-true armature 6.6e-5 (standing repo lesson: fatal to REFERENCE-REPLAY/RSI-insertion, NOT to trained policies). Any future replay-based feasibility probe must declare an eval-side ankle-roll kd ≤ 0.2 repair or it will manufacture a false "mechanically infeasible" verdict. Do not run it naively.

## 3. Four-hypothesis discrimination status

| hypothesis | current evidence | discriminated by |
|---|---|---|
| Reward-driven short stepping (H1/H3/sway/DS) | Strong static suggestion, no whole-book ordering, no causality | **C11S4G probe** (static ordering + per-term toll ledger); PPO later for causality |
| Mechanical/control inability (H4) | Weakened: no torque/action wall, deviation lives in the command, C5 trivially satisfied (F-A2) | Only if C11S4G returns "economy indifferent"; then a kd-repaired replay/bench probe (F-A3 caveat), Joe-gated |
| Real-step termination pressure (H5) | Direction is anti-fast (F-A1); remains a cliff/survivorship shaper | **C11S4G scenario 3/5** (termination-economics table) |
| Missing stride-geometry authority | `p_anchor_stride`/`p_tempo`/`ss_tempo_w`/kernel gait section all exactly 0; `T = path/0.31` tautology | **C11S4G scenario 6** (smooth long-stride counterfactual: if ≈ current income, blindness confirmed as indifference-not-opposition) |

## 4. Why C3 is flagged as a potential violation

Assembling C11S4E/F's own measured pieces: a gait that executed the reference as designed would be charged ~8.63/step by `r_fwd`'s concavity (its own velocity waveform), ~0.80/step by the low-DS premium, ~0.57/step by `p_cap` — order 10/step against a ~17.5/step book — i.e. **the live economy plausibly ranks the current fast gait (the "nearest exploit" in C3 terms) far above the intended behavior**. If C11S4G confirms the ordering violation, the card is in measured C3 violation and the repair is a Joe-gated reward-design decision (price the missing dimension), not an anneal and not a diagnostic PPO. This is exactly the toe-skim precedent (17,585 vs 3,673) at whole-gait scale.

## 5. THE ONE NEXT TICKET — proposed, not issued

**12U-C11S4G — Whole-book C3 income-ordering probe for the c11s4 walking card (read-only, CPU-only, no PPO, no new rollouts).**

**Objective.** Execute `playbooks/REWARD_INCOME_PROBE.md` for this card through the live reward path on existing recorded data, producing the full-episode AND window-local income orderings, plus a per-term toll ledger that statically separates the Rank-1 tie.

**Scenarios** (identical horizon/termination handling; every channel labeled measured / bounded / inferred):
1. *Intended* — c344 executed as designed: kinematic scoring from the npz (`r_fwd` from its own arc-rate series — F §2.4 method; foot-lift/DS from its contact raster; `p_cap` from its stance durations; `r_zt`/`track` at ceiling as declared upper bound; `p_slip` ≈ 0 by FK truth 0.31 mm/frame; real-step gate: PASSES, no stall).
2. *Nearest exploit* — the current gait: already measured (17.511 valid-39 / 17.542 all-64 / 16.742 training ledger; quote all three, D-23 condition declared).
3. *Safe non-compliance* — stand-still/stall-farm under `et_no_step 4.5` (income-to-stall, then termination).
4. *Aggressive failure* and 5. *Early death* — from recorded termination economics (`p_fall`, truncated horizons, γ from `resolved_config.json`).
6. *Tie-splitter* — smooth long-stride counterfactual: recorded per-step `ds_prog` series retained verbatim (holds `r_fwd` at its measured value by construction), contact raster re-partitioned to the reference's hold-free duty (stance 0.80 / stride 1.34) — prices duty + cap + geometry channels at fixed smoothness.

**Deliverables.** (a) C3 ordering verdict, both bases, violation named per-term if present; (b) toll ledger {profile/Jensen toll, duty/foot-lift toll, `p_cap` toll, `p_slip` DS toll, geometry toll = 0 check}; (c) termination-economics table (current vs reference vs stand-still); (d) reuse of the F-capture reconstruction validators with closure residuals reported.
**Prohibitions.** No PPO (D-11 OPEN + Guard blocker as-recorded), no new GPU rollouts (if one seems needed → STOP and escalate), no reward/launcher/plant/reference/obs/policy/Guard change, nothing under `rl/mjw/` touched, no motion claims (blocker prohibits), no promotion.
**Acceptance.** Orderings computed per playbook; any violation blocks all future PPO on this card until repaired and re-probed (C3); artifacts under `rgl/artifacts/c11s4g_income_ordering_<TS>/` in the lane tree; report mirrored.
**Cost.** CPU-only arithmetic on existing artifacts + one npz file read. No sim.

## 6. Escalations for Joe / control lane (report-only; nothing touched)

| # | item | owner |
|---|---|---|
| E-1 | Guard blocker `B-a603393eeb21` OPEN (prohibits ppo/motion_claim/…) vs Q6 permits `ppo PERMITTED` vs ACTIVE `D-23c6a79c4aec` authorizing its resolution — reconcile | control lane + Joe |
| E-2 | 2 Guard contradictions: orphan RUNNING runs `R-bb0ea219a872`, `R-ec597b87c95f` (outputs quarantined; no live process exists) — close | control lane |
| E-3 | D-11 closure (launcher-hash enforcement in `run guarded`) — precondition for any PPO | control lane |
| E-4 | 12 PENDING agent-proposal decisions in Guard awaiting ruling | Joe |
| E-5 | Joe visual review of walking candidate `ppo_13107200` — still not obtained (Owner Visual Gate) | Joe |
| H-1 | `ranking.md` lacks its own in-band R2 retraction notice (warning exists only in F §9) | locomotion (1-line edit, next ticket) |

## 7. Caveats

- This audit re-verified structure, source citations, hashes, and internal consistency; it did **not** re-derive every number in D/E/F from raw npz. Figures quoted are the reports' own, verified where stated.
- The §4 C3-violation assembly mixes eval-basis and idealized pieces from E/F; it is a *suspicion with arithmetic behind it*, not a probe result — that is exactly what C11S4G exists to settle.
- No walking quality claim, no visual claim, no promotion. Nothing in Guard, the lane tree, or the launcher was modified. The bus carries a mirror of this report; the local file is authoritative.
