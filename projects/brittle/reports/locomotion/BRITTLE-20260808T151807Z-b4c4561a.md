---
id: BRITTLE-20260808T151807Z-b4c4561a
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15CY
created_at: "2026-08-08T15:18:07.793473Z"
source_commit: f781704
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15CY_AUTHORITY_WIRING_20260808T060000Z.md
local_source_sha256: 0aa2d135b9917b59dc83d652179d362e199d3b48a5b0dd844c4739f79e33950d
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15CY_AUTHORITY_WIRING_20260808T060000Z
mirror_bytes: 9739
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15CY_AUTHORITY_WIRING_20260808T060000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15CY_AUTHORITY_WIRING_20260808T060000Z.md` |
| sha256 | `0aa2d135b9917b59dc83d652179d362e199d3b48a5b0dd844c4739f79e33950d` |
| brittle commit | `f781704` |
| lane | locomotion |
| unit | 12U-C11S15CY |
| mirrored at | 2026-08-08T15:18:07.793473Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15CY — Final CY Authority Wiring Closure: **CY AUTHORITY WIRING READY**

**date:** 2026-08-08 · **unit:** 12U-C11S15CY · **lane:** locomotion · **NO PPO · NO Guard mutation · quarantine not inspected beyond its existing hashes**
**constitution:** C1, C2, C3, C5, C6, C7, C9 · **potential violations:** none · **amendment proposed:** no
**return:** **`CY AUTHORITY WIRING READY`**
**changelog:** row 100 · post-append csv `sha256_16` **`fb23e0da5b4ed4dd`**

| artifact | sha256_16 | |
|---|---|---|
| **launcher `launch_acquisition.sh`** | **`2c4945b0d8c92047`** | **NEW** — hardened for items 5 and 7 |
| trainer `rl/mjw/train_tip_reftrack.py` | `b3d1329765f15675` | unchanged |
| estimator `rl/bridge/path_speed_v1.py` | `4127331df42baa8e` | unchanged |
| member manifest | `dca42548ae633d3e` | unchanged |
| `Mobs_v1.npy` / `Mact.npy` | `976c327503a092c3` / `23289b73d9b23faa` | unchanged |

---

## 0. Orphan inspector — closed

PID 3431027 verified **from `/proc/3431027/cmdline`** (no `pgrep`, no `pkill`, no name matching):
it was `isaac_spectator.py --watch .../out_dry --mujoco --obs-cmdphase54 --exact-symmetry-matrices
<bank> --seed 0`, started 2026-08-07 21:00:25 — the leak from the dry run that exited 8 *before*
the EXIT trap existed. Terminated by exact PID; `/proc/3431027` is gone.

**Also swept:** enumerating `/proc` for this session's own processes returns nothing — no trainer,
spectator or probe remains. ⚠ On the seven open shells: I have no tool to close Claude Code's
shell handles, but they hold no compute.

## 1. The real CY authority path

Read from the implementation, not invented. **Guard derives the active unit from the verified
transition chain, never from `campaign.json`** (`transitions.effective_unit`, CP-08). So CY becomes
real by *recording a transition into it*. Current derived state, read-only:
`effective_unit() = '12U-C11S15'`, reasons: none.

`proofs/item1_cy_authority_sequence.txt` holds the exact sequence. The three binding requirements
were read out of `transitions.py`, each with its line:

| requirement | source |
|---|---|
| the transition grant's own `unit` must be **`12U-C11S15`** — the unit being *left* | `transitions.py:296` `authorization_reasons(dec, "transition", prior_unit)` |
| its `subject_refs` must declare **both** `12U-C11S15` and `12U-C11S15CY` | `transitions.py:457` `subject_refs=[prior_unit, next_unit]` |
| the owner's `verdict_verbatim` must **name the destination** `12U-C11S15CY` | `transitions.py:303` |

Then one command makes CY real and active:

```
brittlectl transition --from-unit 12U-C11S15 --to-unit 12U-C11S15CY \
    --lifecycle-from ACQUISITION --lifecycle-to ACQUISITION --authorized-by <D_TRANSITION> \
    --reason "12U-C11S15CY becomes the active acquisition unit; historical 12U-C11S15 ppo grants
              remain untouched and out of scope" --source-commit <telemetry HEAD>
```

### ★ Out-scoping proved, read-only, before any mutation

`proofs/item1_outscope_proof.log` calls `permits.authorization_reasons` directly on the live grant:

```
grant D-ed52647cd278  type=authorization  status=ACTIVE  authorizes=['ppo']
                      unit='12U-C11S15'  subject_refs=['12U-C11S15','bound-construction-c11s4-walking']

active_unit '12U-C11S15'   -> GRANTS    (no reasons — the current state, and why CY is needed)
active_unit '12U-C11S15CY' -> REFUSED
    "D-ed52647cd278 authorizes 'ppo' for unit '12U-C11S15', but the active unit is
     '12U-C11S15CY'. Owner ruling (CP-07): authority does not carry across units —
     record a new exact decision for '12U-C11S15CY'"
```

The historical grant is **untouched and still valid for its own unit**; under CY it is refused by
**unit scope alone**, before any subject check. That is out-scoping, not supersession, exactly as
directed.

## 2. Canonical-mutation authority — four subjects, derived not guessed

`targets.py:106` requires authority for `[unit] + [t["target_id"] for t in targets]`. With
`--unit 12U-C11S15CY` and the three records that set is **exactly**:

```
12U-C11S15CY · trainer-source-c11s4 · launcher-c11s15tc-acquisition · bound-construction-c11s4-walking
```

— the four the ticket names. The request is in the sequence file, bracketed by the
ACQUISITION → POST_ACQUISITION_AUDIT transitions because ACQUISITION forbids `canonical_mutation`.

## 3. Valid Guard payload only

`proofs/item2_targets_three_records.json`, schema-valid, launcher pin refreshed to the final hash:

| # | target_id | role | value |
|---|---|---|---|
| 1 | `trainer-source-c11s4` | `instrument` | `b3d1329765f15675` |
| 2 | `launcher-c11s15tc-acquisition` | `instrument` | `2c4945b0d8c92047` |
| 3 | `bound-construction-c11s4-walking` | `construction` | complete, **exactly 47 members** |

Obsolete staging moved to `OBSOLETE_STAGING/` with a README marking every hash in it VOID:
the request that used invented `--target-id` / `--add-member` / `role=launcher`, the
`tc_guard_staging.json` naming `81966301c23929ae`, and the stale root dry proof.

## 4. Repository state

The pending rename is resolved and committed; staged entries are text/JSON only.
⚠ **I used `git add -A` on the artifact tree first — against my own recorded rule** ("NEVER
`git add -A`; stage explicit paths"). It staged 200 entries including multi-MB `.npz` and the
quarantined `.zip`. Caught immediately, `git reset`, restaged explicitly at 12 entries. The rule
exists because that mistake is one keystroke from committing the quarantined checkpoint.

Frozen artifacts re-verified **unchanged**: trainer `b3d1329765f15675`, estimator
`4127331df42baa8e`, manifest `dca42548ae633d3e`, `Mobs_v1` `976c327503a092c3`, `Mact`
`23289b73d9b23faa`. **The launcher hash changed to `2c4945b0d8c92047`** because items 5 and 7
required launcher edits — reported as the ticket asks, and the targets payload was refreshed to
match.

## 5. Parity evidence now discriminates

`out_dry/parity_receipt.json`, verdict **OK**, splits every field into **measured** (independently
read back from this run's own artifacts) and **launch-pinned** (asserted, not observable):

**27 MEASURED** — `plant_sha1 81d4e6d8226ed12b`, **`plant_complete c44ad73fbd8b6f45`** and
`plant_complete_as_built` (both from `plant_id.json`), `kp` 20.00001 ×12 and `kd`
[1,1,2,2,1,**0.2**,…] compared elementwise against the trainer's resolved multipliers with an
explicit ankle-roll assertion on **both** sides, `action_scale [0.20,0.28,0.96,0.99,0.96,0.28]×2`,
`tau_limit 12.5`, `acd_hz 10.0`, `policy 50 / pd 500 / physics 500 / control_dt 0.02 / 10
substeps`, **`nconmax 64` verified by read-back** (`naconmax 147456 / nworld 2304`),
`legacy_rsi_frac 0.0`, `et_no_step_s 4.5`, `yaw_cmd_range [0.0, 0.0]`, `yaw_cmd 0.0`,
`init_zone_active true` (from the trainer's own stdout), `arc_rsi 1.0`, `arc_rsi_zone true`,
`cyclic_fraction 1.0`.

**1 LAUNCH-PINNED** — `njmax 128`, labelled honestly: *"the trainer persists njmax in no receipt
(settings_summary, resolved_config and plant_id all lack it); asserted by the launcher's
`--njmax 128` only."*

## 6. Evidence index

`EVIDENCE_INDEX.md` rewritten; **every path in it was checked to exist**. It names the current
receipts and carries an explicit *NOT evidence* section. All renamed-banked fixtures are now
described as **renamed BANKED zero-training-step fixtures** — including
`out_dry/inspector_preflight/ppo_0.zip`, whose hash was verified identical to the banked actor
(`13fc030e59ddfc65`). **No receipt calls renamed banked bytes a trained checkpoint.** References
to non-existent scratch dirs are gone. The quarantine was not inspected beyond its existing hashes.

⚠ One honest correction inside the negative-test log: its summary line counted "ppo_*.zip
produced: 1". That file is the synthetic preflight fixture, not a trained checkpoint; the log now
carries a clarification block saying so with the hash comparison. **No negative test produced a
trained checkpoint.**

## 7. Final launcher semantics — six refusals, all executed

| test | exit | refusal |
|---|---|---|
| bare `bash launch_acquisition.sh` | **2** | `no mode. This launcher never runs by default.` |
| `TC_MODE=train` | **2** | `unknown TC_MODE 'train'` |
| `run`, no token | **2** | requires `TC_RUN_TOKEN` = the launcher's own sha256_16 |
| **`run` + token, non-empty `$OUT`** | **3** | `requires $OUT to be new or empty` (lists the offending entries) |
| `run` + token, empty `$OUT`, no grant | **2** | requires `TC_GRANT_ID` |
| `run` + token + `D-ed52647cd278` | **2** | `a grant scoped only to the construction does NOT authorize this launcher` |

Also enforced: `--exact-symmetry` **and** `--exact-symmetry-matrices` are asserted present in the
launcher's own text (it refuses if either is ever dropped — the deployed actor is `g_v1`, not bare
`f`); the **real-state g_v1 battery is graded before the run** and a non-ACCEPT verdict refuses;
matrix hashes stay pinned; cleanup remains a trap-based exact-PID close.

★ **The card-local grant check is only an anti-accident interlock. Guard remains the canonical
authority** — stated in `proofs/item8_cy_ppo_decision.txt` so the two are never confused.

## 8. New CY PPO decision — prepared only

`proofs/item8_cy_ppo_decision.txt`: unit `12U-C11S15CY`, subjects including
`launcher-c11s15tc-acquisition`, scope = the exact frozen launcher `2c4945b0d8c92047`, **one**
442,368-step seed (structurally ceilinged at 500,000), **one** checkpoint, then STOP for the
blocking Owner Visual Gate. **`D-ed52647cd278` is not reused.** Not requested, not resolved.

**No PPO. No Guard mutation. Nothing promoted, banked or made canonical. The quarantined
checkpoint was not inspected beyond its existing hashes.**
