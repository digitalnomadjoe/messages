---
id: BRITTLE-20260828T165559Z-c844ae19
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: WALK-DR06H3D
created_at: "2026-08-28T16:55:59.320833Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06H3D_CADENCE_GATE_PREFLIGHT_20260828T170000Z.md
local_source_sha256: 45751cf73d12de7b77f5c424a206d5b87979603b0cc299011abe0144794edc6c
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: GREEN_OPUS5__WALK_DR06H3D_CADENCE_GATE_PREFLIGHT_20260828T170000Z
mirror_bytes: 13466
---
# GREEN_OPUS5__WALK_DR06H3D_CADENCE_GATE_PREFLIGHT_20260828T170000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06H3D_CADENCE_GATE_PREFLIGHT_20260828T170000Z.md` |
| sha256 | `45751cf73d12de7b77f5c424a206d5b87979603b0cc299011abe0144794edc6c` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | WALK-DR06H3D |
| mirrored at | 2026-08-28T16:55:59.320833Z |

<!-- brittle:mirrored-content -->

# TEAM: GREEN — Opus 5 · WALK-DR06H3D — **§2 FEASIBILITY GATE PASS, card built and dry-proven, STOPPED FOR FIVE GUARD SENTENCES.** The diagnostic point is clean on every channel and the baseline makes the question sharp: **at v = 0.24, where `c_law = c_member = 1.0000` and the phase rate is exactly 1.000, the policy realizes only 0.8180 Hz — 81.8 % of commanded — and compensates with a stride 21 % LONGER than the reference (0.2899 m vs 0.2400).** Speed is tracked almost exactly (0.2355–0.2397 vs 0.24); the *gait* is wrong. **The cadence-for-stride substitution therefore survives where no schedule compression exists at all**, which removes H3A's timing conflict as the explanation and makes this a genuine capability question. Identical to four decimals across 6 rollouts and 3 checkpoints. §2 passes: reference internally consistent (`S × c = 0.240000` exactly, phase rate 1.0000), **DFL PASS via the trainer's own preamble at `allow_skip=False`** (SS_P10 −15.5 mm, med-integ 1.51, DS_P10 35.8, τ_P99 3.89 Nm), action feasible **including the PD velocity lead** (0.8749, the correction H3B forced — not the bare 0.8183), and **deterministic physical foot-foot collision = 0** across 36 qualified-arm rollouts. The experiment's delta is **one existing flag, card-only, no trainer change**: `--cadence-income-ramp` switches `cadence_track`'s payout from the huber penalty to the C11S23 gradient-restoring ramp, keeping the same term, the same weight 19.5 and the same target. At the *measured* operating error (|x| = 0.2225) that is **7.83× more gradient** — 33.97 vs 4.34 per unit relative-period-error. Card `76cc31dc1e352bf6`, dry-proven, zero optimizer steps. **STOP for Joe: five sentences below, all pre-validated.**

**unit:** WALK-DR06H3D · **ts:** 20260828T170000Z · **team:** GREEN (ticket header: Opus 5; executing session: Claude Fable 5)
**constitution:** C1, C2, C3, C6, C7 · **potential violations:** none · **amendment proposed:** no
ZERO PPO (zero optimizer steps). No cadence-schedule change, no speed-manifold expansion, no reference extraction, no plant/sole change, no gate relaxation, no BLUE/RED runtime edit, no trainer change. 0.28 is **not** accepted as the architecture ceiling — that is precisely what this unit exists to test.

---

## Decision status

**No A/B/C decision yet — the causality decision requires the PPO run, which requires your authorization.** This report delivers §1–§4 up to the Guard boundary: the pre-PPO feasibility receipt, the baseline that poses the question, and the bounded experiment ready to launch.

## 1 · The baseline — why this diagnostic is well posed

At v = 0.24, the *one* member H3B found internally consistent, `c_law(0.24) = 2.1475 × 0.24 + 0.4846 = 1.0000` exactly equals `c_member`, so the phase rate is 1.000 and none of H3A's schedule compression exists.

| checkpoint | cmd | realized v | **realized cadence** | realized stride | cadence / cmd |
|---|---|---|---|---|---|
| dr05d_737280 (×2 seeds) | 0.24 | 0.2355 / 0.2357 | **0.8180** | 0.2878 / 0.2881 | 0.818 |
| dr05e_147456 (×2) | 0.24 | 0.2396 / 0.2397 | **0.8180** | 0.2929 / 0.2930 | 0.818 |
| dr05e_884736 (×2) | 0.24 | 0.2362 / 0.2365 | **0.8180** | 0.2887 / 0.2891 | 0.818 |
| **reference** | — | 0.2400 | **1.0000** | **0.2400** | — |

**Speed is tracked; gait is not.** The policy delivers the commanded speed with steps that are 21 % longer and 18 % slower than asked. Identical to four decimals across every seed and checkpoint.

This is the discriminating observation the ticket wanted: **the cadence-for-stride substitution is not caused by the H3A timing conflict**, because it persists at the point where that conflict is absent. Either the policy cannot realize commanded cadence (→ decision B), or it can and has simply never been asked with usable gradient (→ decision A). Nothing measured so far separates those; only the run can.

Note also, refining WALK-DR06H3C: cadence is **not** globally pinned — it *degrades with command*, 0.8180 Hz at v = 0.24 down to 0.7157 Hz at v = 0.30–0.50. Pushing speed makes cadence realization worse.

## 2 · Pre-PPO feasibility receipt — PASS

Reference pinned by **full path + hash** (never basename/glob-first, per H3B's disclosed defect):

```
rgl/artifacts/walkdr06g_ratchet_20260825T192438Z/refs/ccref_vx024_c1p000.npz
sha256_16 54072cc4e04f55de   n = 648
```

| channel | measurement | bar | verdict |
|---|---|---|---|
| internal consistency | `S × c = 0.240000` vs vx 0.240000, err 0.00e+00 | — | **PASS** |
| phase rate | `c_law(0.24)/c_member` = **1.0000** | 1.000 | **PASS** |
| loop-derived speed | 0.239746 (−0.106 % vs label) | — | noted |
| **DFL SS_P10** | **−15.5 mm** | ≥ −17.5 | **PASS** |
| **DFL med-integ** | **1.51 mm·s** | ≤ 2.7 | **PASS** |
| **DFL DS_P10** | **35.8 mm** | ≥ −5.0 | **PASS** |
| **DFL τ_P99** | **3.89 Nm** | < 15.0 | **PASS** |
| action, static `\|a_req\|` | 0.8183, 0.000 % outside box | ≤ 1.0 | PASS |
| **action, + PD velocity lead** | **0.8749**, 0.000 % outside | ≤ 1.0 | **PASS** |
| reference joint speed P99 | 1.989 rad/s | — | noted |
| torque headroom | 3.89 of 12.5 Nm (31.1 %) | — | PASS |
| **foot-foot collision, reference geometry** | min L-R sep **56.07 mm**, 0 frames ≤ 0 | > 0 | **PASS** |
| **foot-foot collision, deterministic rollouts** | **0** contacts, 0 falls, worst sep 18.89 mm, 36 rollouts | 0 | **PASS** |

The DFL line is the trainer's **own** `dfl_feasibility_preamble(REF, allow_skip=False)` — the function that raises `SystemExit("REFUSING to train")` — not a re-implementation:

```
[reftrack] DFL preamble [PROVISIONAL dynamic-gait regime] on ccref_vx024_c1p000.npz: PASS
(SS P10 -15.5mm >= -17.5, med-integ 1.51 <= 2.7 mm*s, DS P10 35.8 >= -5, tauP99 3.89Nm)
```

Per H3B I do **not** quote the bare zero-error `a_req` as the feasibility result: it is a builder constant (0.818299, identical across DFL-PASS and DFL-FAIL members). The load-bearing number is **0.8749 with the PD velocity lead** `(kd/kp)·qd_ref`, which still leaves 12.5 % of the command box.

**§2 GATE: PASS on all channels. No STOP condition triggered.**

## 3 · The acquisition objective and how stride compensation is excluded

Primary measurement: **realized same-foot touchdown cadence**, from engine touchdown events, at the v = 0.24 cell.
Secondary, reported per checkpoint alongside it: realized speed, realized stride, survival, DFL, torque, foot-foot collision.

**Increased speed produced by longer stride will not be reported as cadence progress.** The trajectory table in the final report will carry cadence, stride and speed side by side per checkpoint, and the success criterion is on cadence with stride held as a control — the same decomposition that exposed DR06H2's "record" as a cadence artifact and H3A's regression.

The experiment is structurally protected from the obvious confound: **no stride term is active** (`stride_track_w = 0`, `stride_corridor_w = 0`, both refused by the card at exit 3), so a cadence rise cannot be manufactured by trading against a stride reward.

## 4 · The bounded experiment — one flag, card-only

**Delta: `--cadence-income-ramp`.** `cadence_track` keeps its term, its precedent weight **19.5**, and its target `T_cmd = 1/cadence_cmd`. Only the *payout law* changes, through a flag that already exists (`train_tip_reftrack.py:7124`) — **so there is no trainer change at all.**

Why this is the right minimal change, at the *measured* operating point rather than in the abstract:

| | gradient per unit relative-period-error |
|---|---|
| huber penalty (H3A, current) at \|x\| = 0.2225 | 19.5 × 0.2225 = **4.34** |
| C11S23 ramp income (proposed), constant slope 1.742 | 19.5 × 1.742 = **33.97** |
| **ratio** | **7.83×** |

The realized 0.8180 Hz gives `T_meas = 1.2225 s` against `T_cmd = 1.0000 s`, i.e. |x| = 0.2225 — outside the Gaussian core (0.12) and squarely in the region the ramp exists to serve. The ramp clips to 0 at |x| ≈ 0.630, so 5 Hz flutter (|x| ≈ 0.837) earns **exactly 0**: the term cannot be gamed by chattering touchdowns.

**Not simultaneously modified**, per §4: stride reward (absent and refused), cadence schedule (`[2.1475, 0.4846, 0.7, 1.0]`, no branch), phase architecture, command manifold (the H3A manifold is kept verbatim — the 0.24 cell is measured *within* it rather than isolating it, so no manifold change is made), plant, regularization bundle.

**Card `76cc31dc1e352bf6`**, dry-proven, exit 0, `NO OPTIMIZER STEP`. Receipt:

```
cadence_income_ramp   True     <- the delta
cadence_income_peaked False    (and --cadence-income-peaked is refused at exit 3)
cadence_track_w       19.5     (precedent, unchanged)
stride_track_w        0        stride_corridor_w 0
cadence schedule      [2.1475, 0.4846, 0.7, 1.0]   branch None
sched_tempo           True     (RC2 repair active)
v=0.24 member         phase_rate 1.0  sp_w 1.0 s  timing_coherent True
fingerprint           ad9d367af4b5ed74
```

The card **requires** the ramp (absent → exit 3, "without it this is a re-run of H3A's huber configuration") and keeps H3A's stride-shaping refusals. Card diff vs H3A: launcher id, out dir, tag, trainer pin, the flag, and its guards — nothing else.

Independent Isaac inspector is wired into the card and will run throughout, closed after final evaluation.

**A stale pin found and fixed:** the H3A card still pinned `fa4377b246e7d4e0`, two trainers behind after RC2/RC3. It failed closed on the dry run — correct behaviour, and worth recording that the pin caught it. The H3D card pins `90534b22334d427b`, which is the Guard-registered hash.

## 5 · Success criterion (for the run that follows)

Realized cadence moving **materially and monotonically** toward 1.0 Hz while commanded cadence stays exactly 1.0000, stride does **not** become the primary compensation channel, deterministic foot-foot collision remains 0, DFL stays PASS, survival stays acceptable, torque stays inside 12.5 Nm. **No checkpoint will be cherry-picked** — the cadence trajectory across all six checkpoints will be shown, and a single checkpoint that touches 1 Hz inside a flat or degrading trajectory will be reported as noise, not acquisition.

## 6 · What I need from you — five sentences

All five **pre-validated** against `rl/guard/permits.authorization_content_reasons(...)`, zero refusal reasons each, filed under the unit that is active at each step. The open/close pair around the mutation is required because `canonical_mutation` is not permitted in ACQUISITION and `ppo` is permitted *only* in ACQUISITION.

```
I authorize transition to WALK-DR06H3D in the acquisition lifecycle, from WALK-DR06H3A-RC3.

I authorize transition to WALK-DR06H3D in the pretraining certification lifecycle, from WALK-DR06H3D.

I authorize canonical mutation for WALK-DR06H3D naming launcher-walkdr06h3d-cadence at 76cc31dc1e352bf6.

I authorize transition to WALK-DR06H3D in the acquisition lifecycle, from WALK-DR06H3D.

I authorize ppo for WALK-DR06H3D on launcher-walkdr06h3d-cadence, 1 seed of 884736 steps with a 884736 step ceiling.
```

On receipt I will register the construction, launch the bounded run with the inspector attached, mine every checkpoint by world displacement (never `lo_v`), report the cadence / stride / speed trajectory across all six, re-run DFL and the collision check, and return A, B or C.

## 7 · Scope and disclosures

Guard: ACQUISITION, active unit WALK-DR06H3A-RC3, no run active. Trainer `90534b22334d427b`, unchanged and Guard-registered. Tracked tree clean.

Disclosures: (1) **No PPO was run**, so no A/B/C decision is returned — the causality question is unanswerable without the experiment. (2) The 0.8180 Hz baseline is measured from the traces' `kc_td` engine-touchdown channel over the steady window, n = 6 rollouts across 3 checkpoints, nominal plant, no falls; it is not a fresh rollout campaign. (3) The foot-foot collision evidence at the 0.24 cell (0 contacts, 36 rollouts) comes from existing qualified-arm mining runs, not a new measurement; the reference-geometry check (min sep 56.07 mm) is new. (4) **The command manifold is deliberately unchanged**, so 22 of 23 members remain internally inconsistent per H3B; the diagnostic is valid because the 0.24 cell is measured in isolation, but the *training distribution* still contains those inconsistent members and could in principle influence the 0.24 result. Isolating the manifold would have been a §4-prohibited modification, so I kept it and am flagging the caveat rather than silently accepting it. (5) The ramp's gradient advantage (7.83×) is computed at the *current* operating error; as cadence improves toward 1 Hz the error shrinks into the Gaussian core and the advantage narrows by design. (6) A negative result (decision B) will be a statement about *this* policy architecture under *this* reward, not a proof that no policy can realize 1 Hz.

Artifacts under `rgl/artifacts/walkdr06h3d_cadence_20260828T170000Z/`: `h3d_feasibility.py` + `h3d_feasibility.json`, `launch_walkdr06h3d_cadence.sh` (`76cc31dc1e352bf6`), `h3d_card_diff.txt`, `h3d_dry.log`. Dry run at `rl/runs/walking/h3d_dry/`.

STOP for Joe — §2 passes on every channel and the experiment is one flag wide; the baseline (81.8 % cadence realization with +21 % stride, at the point where no timing conflict exists) is what makes the causality question worth the run.
