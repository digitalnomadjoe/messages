---
id: BRITTLE-20260805T133207Z-9003ff8a
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15H0
created_at: "2026-08-05T13:32:07.689810Z"
source_commit: f9b5b15
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15H0_DEPLOYMENT_BOUNDARY_AUDIT_20260805T064500Z.md
local_source_sha256: 262b27d149a3a823e068ac72a83427d4090ee33c98b7af0627ff106877f865d4
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15H0_DEPLOYMENT_BOUNDARY_AUDIT_20260805T064500Z
mirror_bytes: 6988
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15H0_DEPLOYMENT_BOUNDARY_AUDIT_20260805T064500Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15H0_DEPLOYMENT_BOUNDARY_AUDIT_20260805T064500Z.md` |
| sha256 | `262b27d149a3a823e068ac72a83427d4090ee33c98b7af0627ff106877f865d4` |
| brittle commit | `f9b5b15` |
| lane | locomotion |
| unit | 12U-C11S15H0 |
| mirrored at | 2026-08-05T13:32:07.689810Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15H0 — Deployment-Boundary Action-Label Audit: REFERENCE-FREE LABEL PATH VALID (after correcting H's labels — the as-frozen H construction is REJECTED)

**date:** 2026-08-05 · **unit:** 12U-C11S15H0 · **lane:** locomotion · audit + corrected construction; NO PPO, NO mutation
**constitution:** C1, C2, C5, C6, C7, C12 · **potential violations:** none · **amendment proposed:** no
**status:** the audit traced the exact `q_des` computation through all four regimes and settled the boundary question — **and caught a fatal label error in the frozen H construction before any PPO**. Verdict with the corrected labels: **REFERENCE-FREE LABEL PATH VALID.** Corrected construction frozen; sentences in §5.
**decision:** Joe — §5 (same sentence text as H; the payload now carries the corrected pins)
**next:** await sentences → probes → anchored kP probe (H ticket unchanged)
**changelog:** row 45 `12U-C11S15H0, 20260805T064500Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. The q_des trace (all four regimes)

`tip_pd_kernel` (`batch_sim_tip.py:1226`) receives **`_q_default_gpu`** — the Q_DEFAULT constant array — as the setpoint base: **`q_des = Q_DEFAULT + scale·ACD(a)`** ("residual_on_default … THIS env's frozen contract", `:127`, recorded in every settings_summary as `action_law`). This is the law in **H training, zero-anchor evaluation, the steady-turn battery, and deployment alike**. `ref_prior` is a reward/obs-kernel flag (`kernels_tip:663/:721`), not actuation. `jq[phase]` appears ONLY in: reward terms, RSI **state** injection (training-only), and the command tables — never in the runtime control path. This matches the standing `bdx_setpoint` doctrine ("q_ref is reward/diagnostic only") and the trainer's own v7 comment ("the reference never enters actuation").

**Hard gate: PASSES STRUCTURALLY.** The actor already owns all 12 setpoints from policy outputs alone; there is no injected reference base to remove; every G/C15C battery measured genuinely reference-free actuation. Hard-disabling every runtime reference path changes nothing in actuation (there is none to disable); the resolved records distinguish the constructions (`action_law` in settings_summary + the anchor-form contract string + three distinct fingerprints, §4).

## 2. The fatal finding in the frozen H construction

Because `a = 0` maps to `q_des = Q_DEFAULT`, **H's labels (`a* = 0` on all samples) supervised the policy toward STANDING, not toward the reference** — my C11S15H §1 "identity" claim was wrong (it described a `ref_prior`-based actuation law this stack does not use; the C11S14D stand-prior record even states "zero action = Q_DEFAULT standing"). Left uncaught, H's PPO would have anchored the actor toward standing on every sample at w0=0.15. **The as-frozen H construction (candidate `7fb93a87`, card `bd8369d9`, fingerprint `720de82a`, row 44) is REJECTED — superseded before any PPO.** Related scoped corrections: the C11S15E/F "blend toward the reference intent (a→0)" probe framing actually blended toward standing (the blend rows were action-shrinkage probes; the E FAIL-analysis and every pricing table stand — those subjects were state/behavior-defined and the α-family/teleport instruments involve no action semantics).

## 3. Corrected labels + mandated proofs

**`a*[phase] = clip((jq[phase] − Q_DEFAULT)/scale, ±1)`** — the actor output that produces the desired full joint setpoints under the deployed transform (the v7 formula, extended to all 12 dims).

| proof | result |
|---|---|
| covers all 12 joints | ✓ per-frame 12-vector table (877×12, archived `astar_kP_labels.npy`) |
| within valid action range | 10/12 dims fully in-range (p99 ≤ 0.78); **hip-yaw clips on 4.2/4.9% of frames (peak demand 1.44 vs ±1), max setpoint shortfall 5.1° during the deep-turn frames** — measured and disclosed |
| labels execute the reference under today's law | ✓ windowed open-loop a\*-replay (60 dense injections, frames 200–495, reference-free actuation end-to-end): **0% falls in 0.3 s windows, realized yaw rate +0.40 vs commanded +0.57 rad/s (70%, correct sign), τP99 5.2**; longer horizons diverge as any open-loop label replay must (feedback is the policy's job — the anchor is soft supervision, the stand-prior pattern) |
| no reference consumed after label generation | ✓ labels are a precomputed table consumed only by the training loss; runtime actuation is Q_DEFAULT-based |
| reference paths hard-disabled ⇒ behavior executable | ✓ vacuous in actuation (none exists) + the a\*-replay runs with reward/RSI machinery irrelevant |
| fingerprint distinguishes constructions | ✓ wrong-label `720de82a` / **corrected `8bd67c4031ab9294`** / anchor-free `54f21532` (zero-anchor dry reproduces G bit-exactly, re-proven on the corrected candidate) |

**Action-interface note (not a blocker):** the hip-yaw authority (scale 0.20 rad) cannot represent ~5% of the κ=2.0 turn's peak setpoints (5.1° shortfall; open-loop cost ≈ 30% of commanded rate together with ACD/PD lag). The label path is valid — the policy adds feedback and may exceed open-loop — but if H acquisition tops out near ~70% response, the hip-yaw action range becomes an explicit interface decision for Joe (raise scale vs accept the ceiling). Flagged now so it is never a silent limiter.

## 4. Corrected frozen pins

candidate re-pin **`99fbeca713204689`** (P16-v2: loss targets `a*[phase]` via the minibatch phase; contract form updated) · H card v2 **`8ee99d424e0031e2`** (H0 correction header; flags unchanged; ceiling inherited) · settings candidate `c357e635d8f1446f` · fingerprint **`8bd67c4031ab9294`** (invariant 2 seeds × 2 outs; `walking_yaw_compliance` + corrected `walking_ref_anchor`) · construction 29 `803a1bab` (names unchanged; member content re-pinned) · canonical trainer **UNCHANGED `5c17a4338e21913f`**. Everything else (manifold, reward book, plant, law, contract, batteries, probe plan, stops, qualification-at-zero-anchor) exactly per the H ticket.

## 5. Sentences (proposed verbatim — same scope as H)

1. `I authorize transition to 12U-C11S15 for the lifecycle advance from ACQUISITION to POST_ACQUISITION_AUDIT.`
2. `I authorize canonical mutation for 12U-C11S15 scoped to: trainer-candidate-c15f, launcher-c11s15h-kp, canonical-settings-c11s4, reward-fingerprint-c11s12, bound-construction-c11s4-walking.`
3. `I authorize transition to 12U-C11S15 for the lifecycle advance from POST_ACQUISITION_AUDIT to ACQUISITION.`

Payload: `targets_register_c11s15h_v2.json`. PPO rides the ACTIVE grant `D-ed52647cd278`.

## 6. Artifacts

`rgl/artifacts/c11s15h_anchor_20260805T062000Z/` — card v2 + dry/zero-anchor scripts, payload v2, settings candidate v2, resolved configs (corrected + zero-anchor), label table, dry logs. Label-representability + a\*-replay probes in this report's tables. Command manifold and repaired reward infrastructure untouched, per the ticket.
