---
id: BRITTLE-20260825T175740Z-8777b123
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: JOINT-REPORT02
created_at: "2026-08-25T17:57:40.200996Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__JOINT_REPORT02_UX_CLEAN_REBUILD_20260825T175003Z.md
local_source_sha256: 2c9acc022d34166d2a2fbc2aa9cd942b5ce78ca4998ce58bdab98d33d03056fe
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: GREEN_OPUS5__JOINT_REPORT02_UX_CLEAN_REBUILD_20260825T175003Z
mirror_bytes: 5468
---
# GREEN_OPUS5__JOINT_REPORT02_UX_CLEAN_REBUILD_20260825T175003Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__JOINT_REPORT02_UX_CLEAN_REBUILD_20260825T175003Z.md` |
| sha256 | `2c9acc022d34166d2a2fbc2aa9cd942b5ce78ca4998ce58bdab98d33d03056fe` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | JOINT-REPORT02 |
| mirrored at | 2026-08-25T17:57:40.200996Z |

<!-- brittle:mirrored-content -->

# TEAM: GREEN — Opus 5 · JOINT-REPORT02 — Report UX + clean rebuild: **ALL ACCEPTANCE CRITERIA MET; the refined report is OPEN IN OPERA on the desktop.** Exactly ONE canonical directory exists (`rgl/joint_characteristics/DR05D_737280__figure_eight__20260825_175242/`, containing only report.html + behavior.mp4 + telemetry.npz + manifest.json + data.js — the work dir now lives in scratch, never inside the canonical root). The video is **Isaac-NATIVE 1920×1080** (no post-processing; probe-measured render speed made 1080p comfortably reliable for the full F8; native width/height recorded in manifest and read back via ffprobe), 3490 frames = 3490 rows, 7/7 sync PASS on a FRESH record whose tripwire again measured **0.000e+00 N·m deviation over 34,899 pairs** — and the fresh rollout's telemetry hash is BIT-IDENTICAL to JOINT-REPORT01's (`0cf8987e79bd4017`): the Isaac record is deterministic across runs, a free reproducibility receipt. Torque graph now DEFAULTS to auto-scale around the selected traces (with a one-click "full ±12.5 N·m" toggle; limit lines and clamp shading are drawn in both modes — spikes and the knee clamp event are never hidden). A live-values table at the synchronized cursor shows, for every selected joint, the **nearest REAL 500 Hz ACDStepper torque sample** (global substep index s = round(t·500), row s÷10, substep s mod 10 — never interpolated) and the recorded qd, updating continuously on play/scrub/frame-step/plot-click. The corrupted negative test now runs in scratch OUTSIDE the canonical root (passed: check 1 fails, refused; scratch removed after). Opera is the default launcher — detected executable, hard assert if absent, NO Firefox fallback (the JR01 Firefox window was closed). ZERO training/policy/behavior changes. STOP for Joe.

**unit:** JOINT-REPORT02 · **ts:** 20260825T175003Z · **team:** GREEN (ticket header: Opus 5; executing session: Claude Fable 5)
**constitution:** C1, C2 · **potential violations:** none · **amendment proposed:** no
Ticket: bus `BRITTLE-20260825T174956Z-104156fe`.

---

## 1 · Changes (one file: `rl/scripts/generate_joint_report.py`; recorder/renderer untouched except CLI passthrough)

| ticket § | change |
|---|---|
| 1 Browser | `open_in_opera()`: `shutil.which("opera")` → `/snap/bin/opera` fallback path check → hard assert if absent ("refusing to fall back to another browser"); `--open` flag runs it after REPORT_OK. Verified: `OPENED_IN_OPERA`, opera process running. |
| 2 Video | orchestrator `--video-width/--video-height` (defaults **1920×1080**) → renderer's existing native Camera args; manifest `video_native_width/height` from ffprobe readback (1920/1080 confirmed). Audit: JR01 rendered at the renderer defaults 640×480. Probe: 150 frames at 1080p rendered in seconds on warm caches — full F8 reliable. No upscaling/post-processing anywhere. |
| 3 Torque graph | default y-range = min/max of the SELECTED envelopes +12% pad (floor 0.25); `y: auto-scale ⇄ full ±12.5 N·m` button; ±limit dashed lines drawn whenever in view (always in full mode); clamp-span shading in both modes; envelope fill alpha raised for visibility. |
| 4 Live values | `tau_full` (N,10,12 @ 3 dp) added to data.js; cursor readout table (joint | τ N·m | qd rad/s, color-swatched, selected joints only) driven from `s = round(t·fps·S)` → row `s÷S`, substep `s mod S` — the nearest real 500 Hz sample, no interpolation; qd from the same row; updates in the rAF loop (play/scrub/frame-step/plot-drag). |
| 5 Clean folder | canonical root inspected (3 dirs: the JR01 acceptance, a crashed remnant, the old in-root negtest) then deleted; work dir default moved to `$JOINT_REPORT_WORK_ROOT` (default `/tmp/joint_report_work/<ts>`); `--negative-test` writes its corrupted copy there too; scratch negtest removed after passing. |

## 2 · Acceptance verification

- Exactly ONE directory under `rgl/joint_characteristics/`: `DR05D_737280__figure_eight__20260825_175242/` with exactly the 5 files (report.html 8.7 KB · behavior.mp4 **114.2 MB @ 1920×1080** · telemetry.npz 2.4 MB · manifest.json · data.js 10.8 MB).
- Fresh record (not reused): tripwire PASS 0.000e+00 / 34,899 pairs, max|applied| 12.500 exactly; estimator receipt; joint-order receipt; 7/7 sync (REPORT_OK emits only on all-pass). Telemetry sha `0cf8987e79bd4017` — bit-identical to the JR01 rollout (deterministic record); video sha `9b14e3c6969e09f2`; plant fingerprint `81d4e6d8226ed12b` (law plant_fp, from the JR01 correction now built-in).
- 1080p frame visually inspected (crisp mid-lobe frame, soft shadows, clean geometry).
- Negative test: PASS in scratch (`1_frames_eq_rows` 3489≠3490 → refused, sync_failure.json, no report.html), scratch removed, canonical root untouched.
- Opera: opened via the tool's own launcher, running, left open for Joe. No Firefox fallback exists in the code path; the JR01 Firefox window was closed first (one report viewer at a time).

## 3 · One recommended next action

Same as JR01's: register `straight_walk` / `speed_ladder` / `stand` behaviors (registry entries only) — the report surface is now UX-complete for Joe's review loop.

STOP for Joe — scrub the 1080p F8 in Opera; the live table shows per-joint τ/qd at the exact cursor sample.

**changelog row:** `JOINT-REPORT02, 20260825T175003Z` — post-append `rgl/LOCOMOTION_CHANGELOG.csv` sha256_16 `2e66c89a3147a387` (527 rows × 13 cols, both trees byte-identical)
