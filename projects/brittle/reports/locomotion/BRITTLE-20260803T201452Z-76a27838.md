---
id: BRITTLE-20260803T201452Z-76a27838
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S4K
created_at: "2026-08-03T20:14:52.476573Z"
source_commit: ca38ba2
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S4K_LAW_PARITY_GUARD_BINDING_20260803T195454Z.md
local_source_sha256: 5c37ae8c954642add28047251fb4d9fe1ca45e409f3152bbf4b9639a34e0175e
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S4K_LAW_PARITY_GUARD_BINDING_20260803T195454Z
mirror_bytes: 10673
---
# UNIFIED_LOCOMOTION_PHASE12UC11S4K_LAW_PARITY_GUARD_BINDING_20260803T195454Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S4K_LAW_PARITY_GUARD_BINDING_20260803T195454Z.md` |
| sha256 | `5c37ae8c954642add28047251fb4d9fe1ca45e409f3152bbf4b9639a34e0175e` |
| brittle commit | `ca38ba2` |
| lane | locomotion |
| unit | 12U-C11S4K |
| mirrored at | 2026-08-03T20:14:52.476573Z |

<!-- brittle:mirrored-content -->

# 12U-C11S4K — Walking-Law v2 Parity and Guard Binding Closure

**unit:** 12U-C11S4K · **timestamp:** 20260803T195454Z
**constitution:** C1, C2, C6, C12 · **potential violations:** none · **amendment proposed:** no
**scope:** the C11S4K ticket. No PPO. Lane repo untouched except artifacts/changelog/report (trainer NOT touched — its bound hash is the point). Control-plane code changed exactly for the D-11 closure + one shipped-entry-point repair, suite-green.

**status:** §1 provenance exact ✓ · §2 CPU/GPU effective-law parity **PASS (all-zero)** with **no controller change needed** · §3 D-11 gate **implemented, wired pre-record, deny-by-default** on construction-adopted stores; suite **494 passed / 0 failed** · §4 tamper matrix **fully fail-closed** (20 tests: ALLOW exact construction + DENY for every enumerated isolated change) · live contradictions **2 → 0** (both C11S4B probe orphans dispositioned with receipts; the `run recover` CLI had never wired its liveness clock — found, fixed, regression-tested) · **live binding: one owner ruling short** — the registration attempt was made and REFUSED by the store's own fail-closed authority (verbatim below), which is itself the D-11-era hole proven shut at the state layer.
**recommend:** Joe issues the two-sentence binding ruling (§6); the prepared payload then lands in one command and the live tamper matrix (ALLOW arm) completes.
**decision:** D-11 **CLOSED in code** (enforcement exists, validated, wired before any record; live arming occurs at first construction registration — the D236D adoption pattern). Two collateral defects fixed with tests: the never-invokable `run recover` wiring; the targets schema gaining the `construction` role + `members`.
**next:** owner ruling → `target register` (payload ready) → live ALLOW probe → fresh-PPO formulation discussion may open.

---

## 1. Exact provenance (no changelog-only references)

| item | value |
|---|---|
| C11S4J commit | `f400168afe04e34360bccd01bd031e54d2eee98b` (lane worktree **clean** — 0 modified tracked files at unit start) |
| committed `rl/mjw/train_tip_reftrack.py` sha256 | `dd73337a63aae6faf830db7b0c543864a5f005d280d0a232f7f115a4bcbeed71` |
| `rl/configs/canonical_v8_settings.json` sha256 | `615745296e053021e8d8e3a413ca8b03c3f435bcaddd66736bb7a748565668f3` |
| launcher `launch_c11s4_walking.sh` sha256 | `c4986396051cb271c0f700c0180cc7eb583d5cbfdffb68150b26420df6d706be` (= Guard-registered target) |
| reward-book hash | `6b48d6e32e006e44` (contract c8j.1; source `straight_profile.py` sha256₁₆ `b307c801f921a413`) |
| walking-law v2 fingerprint | `7c05558aacc0f0c8` (v1 `8d515655b0af4178`) |
| contract registry `rl/runtime/contract_registry.json` sha256₁₆ | `c03c457b9c0e15b3` |
| checkpoint / reference / plant | `20ab67fd…` / `c602133c…` / v2 `81d4e6d8226ed12b` + v3 `c44ad73fbd8b6f45` |

## 2. CPU/GPU control-law parity — PASS

GPU path: the MJWarp walking build through the REAL lane resolution (`TR.apply_walking_law_v2`; law id asserted `7c05558aacc0f0c8` from the live build). CPU path: `MiniBipedYawCmdEnv` — **no controller code change was needed**: the env's torque line broadcasts per-joint arrays, and per-instance `_kp/_kd` variation is the JointCommand contract's sanctioned mechanism; law v2 is reached by configuration (`ctor(action_scale_overrides, torque_limit=12.5, acd_hz=10)` + the effective vectors).

| element | max \|GPU − CPU\| |
|---|---|
| Kp all 12 (20.0) · Kd all 12 (ankle-roll **0.2 bilateral**) · τ limits (12.5) · action scales | **0.000e+00** each |
| ACD alpha (identical formula `1−exp(−0.002·2π·10)`) | **0** at float32 precision (GPU stores float32; raw f64 diff 3.7e-09) |
| ACD rate / policy 50 / PD 500 / substeps 10 | identical |
| action-transform semantics | identical: `q_des = q_ref + scale·a`, ACD v1 first-order-hold + one-pole, `qd_des = 0` pure damping both sides |
| **command-level torque parity** (4096×12 random `(q, qd, q_cmd)`, both live laws, clip exercised on 8.3%) | **max \|Δτ\| = 0.000e+00** |

Artifacts: `c11s4k_parity.py`, `parity.json` (VERDICT PASS).

## 3. D-11 closure — the bound-construction gate

`rl/guard/runs.py::validate_bound_construction`, called by `wrapper.guarded_run` **after the authority verdict and before any record is opened** (a DENY leaves nothing observable). Contract:

1. production execution must name exactly ONE CANONICAL `construction` target;
2. the launched script must **be** one of its file-bearing members with bytes matching the pin — an altered or unbound script cannot execute (the exact D-11 hole);
3. **every** file-bearing member (launcher, trainer source, canonical settings, reward source, contract registry, reference) is re-hashed from `repo_root` **at launch** — the gate validates the files actually executed;
4. the walking-law fingerprint member is validated by its `verifier` (`json:walking_control_law_version.walking_control_law_id` against the settings file) — defense-in-depth: even a re-pinned settings file with a changed law id is DENIED;
5. computed plant/contract fingerprints: registry presence + CANONICAL status here; values by the bound launcher's own fail-closed runtime asserts (declared division).

**Adoption semantics** (the D236D `record_if_adopted` precedent): a store with no construction targets predates the gate and behaves as before; the moment one is registered, production execution is deny-by-default forever. Schema extended (`construction` role + `members`); pre-existing suite unaffected: **494 passed, 33 subtests, 0 failed**.

## 4. Tamper matrix — fully fail-closed (`tests/test_c11s4k_bound_construction.py`, 20 tests)

| arm | verdict |
|---|---|
| exact bound construction (script = bound launcher, all pins match) | **ALLOW** |
| either ankle-roll Kd changed (trainer constant flip; settings registry law-id flip) | **DENY** |
| trainer source / launcher / canonical settings / reward configuration / contract registry / reference (sha1 path) — isolated byte tampers | **DENY** each |
| seed or exploration edit (bound via the launcher bytes that carry them) | **DENY** |
| law fingerprint mismatch **even with the settings member re-pinned to the tampered bytes** | **DENY** (verifier) |
| unbound script; missing member registration; non-CANONICAL member; bound file missing on disk; zero or two construction subjects | **DENY** each |
| non-production mode; pre-adoption store | passthrough (documented) |
| wrapper wiring: production `guarded_run` DENY leaves **no run record** | proven |

**No test debris:** DENY paths open no records; synthetic stores live in pytest tmp dirs. **The two pre-existing live orphans** (`R-ec597b87c95f`, `R-bb0ea219a872` — C11S4B's unclosed `/bin/true` probes) were dispositioned INTERRUPTED with reasons via `run recover` (receipts `RREC-dd6a1bebaa25`, `RREC-8443fd4e9fd8`) — **live contradictions now 0; `session start` exits 0 for the first time in the campaign.** Doing so required fixing a latent CLI defect: `run recover` never wired `now_ts`, so the shipped verb refused every invocation (one-line fix + regression test).

## 5. Live binding — attempted, refused fail-closed, payload prepared

The registration payload (`targets_payload.json`: `trainer-source-c11s4`, `canonical-settings-c11s4`, `reward-book-source-c11s4`, `contract-registry-c11s4`, `walking-law-v2`, and `bound-construction-c11s4-walking` with its 11 members) was submitted via `brittlectl target register --authorized-by D-82931b9a637b`. **REFUSED**, verbatim reasons:

1. `lifecycle ACQUISITION does not allow 'canonical_mutation' (requires REFERENCE_AUTHORING/PRETRAINING_CERTIFICATION/POST_ACQUISITION_AUDIT/PROMOTION_REVIEW)`
2. `D-82931b9a637b is scoped to subjects ['12U-C11S4', 'contract-walk52-v58', …]; the request names ['bound-construction-c11s4-walking', …], which it does not cover`

Both refusals are **correct fail-closed behavior** — no owner sentence was fabricated to bypass them (the K ticket text contains no decision-form sentence; the verbatim transcription + this refusal are recorded in `C11S4K_TICKET_TRANSCRIPTION.json`). Note the deliberate consequence of the lawful path: POST_ACQUISITION_AUDIT makes `ppo` lifecycle-prohibited — which is honest (no PPO is authorized now; the future fresh PPO takes its own owner-ruled transition back to ACQUISITION).

## 6. The one remaining step — owner ruling (two sentences)

> **S1:** "I authorize transition to 12U-C11S4 for the lifecycle advance from ACQUISITION to POST_ACQUISITION_AUDIT."
> **S2:** "I authorize canonical mutation for 12U-C11S4K scoped to: trainer-source-c11s4, canonical-settings-c11s4, reward-book-source-c11s4, contract-registry-c11s4, walking-law-v2, bound-construction-c11s4-walking."

With those recorded, the binding is one `target register` command (payload ready), the gate arms on the live store, and the live ALLOW probe completes the matrix end-to-end.

## 7. Acceptance ledger

| criterion | state |
|---|---|
| CPU/GPU effective-law parity passes | ✅ all-zero |
| exact committed hashes reported | ✅ §1 |
| D-11 closed | ✅ in code — enforcement implemented, wired pre-record, proven fail-closed; live arming = registration |
| tamper matrix fully fail-closed | ✅ 20/20 on the adopted store; live DENY class additionally proven by the registration refusal |
| Guard binds walking-law v2 + repaired trainer | ⏸ payload prepared + attempted; **awaits the §6 ruling** (fail-closed refusal recorded) |
| no PPO launched | ✅ |

## 8. Caveats

- The live store is currently pre-adoption for the new gate (unchanged protection vs yesterday) until the §6 ruling lands the construction — the interim equals the status quo ante, and the arming is one command away.
- Computed plant/contract fingerprints are launcher-asserted at runtime, registry-checked at the gate (declared division of labor).
- The `authorization_reasons` sentence-parser requirements mean the §6 sentences should be transcribed verbatim by the control-side flow as with rulings 1–7.
- Guard-side changes are committed in the control-plane worktree (schema, runs, wrapper, brittlectl, tests); lane-side artifacts/changelog/report in the telemetry worktree. Trainer and launcher bytes untouched, re-verified post-work.

## 9. Artifacts

`rgl/artifacts/c11s4k_parity_binding_20260803T195454Z/` (lane tree): `c11s4k_parity.py`, `parity.json`, `targets_payload.json`, `C11S4K_TICKET_TRANSCRIPTION.json`. Control-plane: `rl/guard/{schemas/targets.schema.json, runs.py, wrapper.py, brittlectl.py, tests/test_c11s4k_bound_construction.py}`. Changelog row appended (both trees).
