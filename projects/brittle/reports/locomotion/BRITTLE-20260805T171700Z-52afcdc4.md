---
id: BRITTLE-20260805T171700Z-52afcdc4
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15K2
created_at: "2026-08-05T17:17:00.719082Z"
source_commit: 2dd85d2
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15K2_MIXED_MANIFOLD_CONSTRUCTION_20260805T164857Z.md
local_source_sha256: 9ff3363f1fc23310857f86dfeae451389907b214980aaf8d3b8cbae7c707ba25
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15K2_MIXED_MANIFOLD_CONSTRUCTION_20260805T164857Z
mirror_bytes: 10385
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15K2_MIXED_MANIFOLD_CONSTRUCTION_20260805T164857Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15K2_MIXED_MANIFOLD_CONSTRUCTION_20260805T164857Z.md` |
| sha256 | `9ff3363f1fc23310857f86dfeae451389907b214980aaf8d3b8cbae7c707ba25` |
| brittle commit | `2dd85d2` |
| lane | locomotion |
| unit | 12U-C11S15K2 |
| mirrored at | 2026-08-05T17:17:00.719082Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15K2 — K1 Closure + Mixed-Manifold Acquisition Construction: MIXED-MANIFOLD CONSTRUCTION READY FOR AUTHORIZATION

**date:** 2026-08-05 · **unit:** 12U-C11S15K2 · **lane:** locomotion · closure records + instrument repairs + construction only; **no PPO, no promotion, no policy card, no latest-pointer, no plant/control-law change, no cross-contract checkpoint reuse; the card REFUSES live launch (exit 93)**
**constitution:** C1, C2, C3, C4, C6, C8, C9, C12 · **potential violations:** disclosed — (a) oracle advanced v1.1.0→v1.6.0 in-unit under the K rebind procedure (Joe's §1 acceptance named v1.1.0; authorization of this card ratifies v1.6.0); (b) BC-stage command salience is median-dim (§5 disclosure); (c) fresh manifold-basis reward repricing deferred to the card's launch-time C3 income gate (E/F receipts stand); (d) BC nursery data was generated with the injection machinery (training-data build, not a measurement battery) · **amendment proposed:** no
**final decision (exactly one):** **`MIXED-MANIFOLD CONSTRUCTION READY FOR AUTHORIZATION`**
**changelog:** row 52 `12U-C11S15K2, 20260805T164857Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. K1 closed (owner records bound)

Joe's visual verdict recorded verbatim: **`VISUAL AGREES — the authoritative learned-policy trace visibly follows a sustained left-curving path.`** K1 decision of record updated to **`LEARNED POSITIVE CONTROL PASSES under tor-v1.1.0.`** Bound: oracle `tor-v1.1.0`, sha `5c95b520582ce2ad`, 13/13 regression record, trace `82edcccdb8ac9e60`. The tor-v1.0.0 failure is preserved in the K1 report §4 as the RCA evidence for the bump (addendum §9a appended, both trees; ARC_STATE.json recreated + snapshot archived). Review viewer closed after Joe's review.

## 2. Causal RCA updated (all three RCA records, append-only `k2_update`)

**Primary:** `OBSERVATION/COMMAND-CONDITIONING DEFECT — command channels were constant within single-member optimization batches.` Historical positive evidence recorded: every batch contained straight AND curved members; the command tuple varied across worlds; the actor had to distinguish command values; the historical policy responds with real path curvature (native κ +1.99 of 2.0, R 0.503); current single-member policies remain command-insensitive. **Secondary:** `ACQUISITION-PRIOR DEFECT — historical training consolidated an arc-competent parent, while C15 initialized from straight competence.` Non-causes affirmed: not primarily the plant, control law, action range, or oracle. RCA-10 next-experiment updated to this unit's mixed straight/±κ construction.

## 3. Oracle instrument campaign (v1.1.0 → v1.6.0, each regression-locked)

Building the member controls exposed four real defects the K1-era suite could not see; each fix was version-bumped, documented in-file, and revalidated:

| ver | defect found | repair |
|---|---|---|
| v1.2.0 | CYC=66 hardcoded (0.752 Hz) — a 66-frame box over the cadence-1.0 family's 50-frame sway leaks residual → a PERFECT κ2.0 arc (segment mean 2.004, R 0.499) classified STRAIGHT | cycle length from the trace's `cmd_cadence`/`gait_cycle_hz` (fallback 66) |
| v1.3.0 | window-chain run detector is smoothing-marginal on slow whole-segment turns (K1 trace flipped STRAIGHT/TURNING with the CYC choice; segment stats robust at every base) | segment-median criterion: \|κ_med\| ≥ 0.8, sign consistency ≥ 0.75, real translation — composites still require window runs |
| v1.4.0 | flat 0.03 m window floor admits accel/decel transients (straight member faked a κ5.2 "run") | self-scaling floor: max(0.03, 0.6 × segment-median chord) |
| v1.5.0 + v1.6.0 | loop-seam kink (sub-50 mm, invisible to the teleport gate) smears into fake headings; the decel PAD's settle-curl (R 0.19, 0.401 m) fired the segment-median basis by 1 mm | member mode segments at DECLARED boundaries (loop_lo/span); declared standing-pad segments (cmd_cadence 0) never qualify; segment-median basis floor 0.60 m |

**Final state: `tor-v1.6.0` sha `b8cc40ff49866a27`, regression 16/16** — all 13 prior controls (incl. every Joe-accepted v1.1.0 control) + 3 new locks: straight composite → STRAIGHT, kP/kM whole-member composites → TURNING ±1.99. Arc-zone tiled controls (end-pose SE2 continuation): κ +1.99 / −1.99, R 0.502 both signs — §6 "path-frame signs and radii pass" receipt.

## 4. The construction (CARD, frozen; refuses live launch)

`rgl/artifacts/c11s15k2_construction_20260805T164857Z/launch_c11s15k2_mixed.sh` — base = the G card byte-inherited, deltas [K2-1..7]:

- **[K2-1] family:** straight `ccref_vx026_c1p000` (vx 0.255, `cd7a0d18…`) + `…_kP200` (κ+2.0, `1551b4ac…`) + `…_kM200` (κ−2.0, `d8d243b1…`), single cadence 1.0, resolver-native (kinematic tables are contract-free; the C15C 877-frame certified pair lacks loop metadata and stays as oracle controls). **DFL PASS on plant v2 for all three** (SS P10 −15.5/−11.5/−11.5 ≥ −17.5, τP99 ≤ 3.96). Round-robin world binding: 2730/4096 worlds curved.
- **[K2-2] `--balance-kappa-minibatch`:** every minibatch = equal thirds by obs45 sign with wraparound — **command contrast per optimization batch BY CONSTRUCTION** (candidate's own stratifier). Measured live at steady state (the 12S first-batch blindness gotcha respected): strata straight/kP/kM = 5740/1769/683 in a 16×512 batch, obs45 std 0.162 over {−0.306, 0, +0.306}, obs52 varies per world and phase (profile channel), per-world path tables distinct (3 member windows; vx {0.153, 0.255}; κ {−2, 0, +2}).
- **[K2-3] current-contract arc prior:** BC seed `rl/models/walking/k2_bc_arcprior_20260805T164857Z/bc_seed.zip` (sha `d4d207133b499793`, 54/60, plant-v2 stamped) — fit on 4,883 same-member reference-state pairs (1905/1938/1040 per member) against the **zero-clipping deployed-coordinate labels** (`a* = clip((jq − Q_DEFAULT)/scale, ±1)`, max pre-clip 0.818, all 12 joints, labels sha `23df6e4c…`); va MSE 0.0113. Card: `--continue-ckpt` + policy-anchor `--anchor-ckpt` w0 0.15, decay 2,097,152 → **stage sequence encoded: (1) BC init, (2) mixed-manifold PPO under decaying anchor, (3) zero-supervision continuation 2.1M→4.19M, (4) oracle qualification** per the declared intent. No runtime reference actuation/observation/switching/hidden base (residual-on-default law; H0-certified).
- **[K2-4] `--arc-rsi 0` — construction constraint discovered:** the candidate's injector draws frames over the WHOLE concat (manifold-unaware; the era trainer's member-scoped injector was lost in the single-member lineage) — cross-member injection corrupts the command tuple. Never arm arc-RSI on a manifold card until a member-scoped injector lands (optional P17, Joe-gated).
- **[K2-5]** `rp_step_period=1.0` (resolver law) · **[K2-6]** structural ceiling 4,194,304, evals 524,288 (the RCA clearance envelope) · **[K2-7]** `--mirror-reg-w 0` kept (July-positive fidelity; open knob disclosed).
- **Dry-run receipt (the G lesson honored):** full construction executed — assertions PASS (3 member pins, BC-seed sha + plant + 54-in contract, plant v2 + COMPLETE v3, candidate pin `99fbeca713204689`), walking-law v2 active (ankle-roll kd 0.2), income + book blocks wired (reward contract `84136d96` c8j.1), **resolved fingerprint `c8927b74004936e6`**, anchor + BC-init loaded, exits before any optimizer step. Receipts archived.
- **Inspector** (mandatory, launch-blocked without): pre-stage `write_runtime_manifest --from-resolved` + `isaac_spectator --mujoco --manifest`, verify "episode start" BEFORE PPO, close after the final report.

## 5. Declared intent + counterfactual disclosure

Intent declared through the canonical gate (`intent.json` in the unit dir): targets κ +2.0/−2.0, minimum |κ| ≥ 1.4 correct-signed on ≥ 0.4 m per side, straight |κ| ≤ 0.25, no spin/pivot qualification, `tor-v1.6.0` mandatory, same-trace evaluator, RCA on miss; **identical-state counterfactual sign-response (+→+, 0→straight, −→−) required for qualification.** Gates run: `declare_intent` PASS · `check_oracle_regression` PASS (16 controls, fresh sha) · `require_rca_clearance` PASS for BOTH parents.

**Stage-1 counterfactual finding (disclosed, load-bearing):** after supervised initialization, actor outputs change under command counterfactuals (Δ L2: flip 0.029, zero 0.015 — nonzero per §6's letter) **but obs45 saliency is the median input dim (rank 25/54; obs52/53 read 20× stronger)** — at reference states, proprioception identifies the member, so BC alone does not force curvature-command reading (the Stage-B mechanism, reproduced at stage 1 as expected). The burden sits where the design puts it: stratified minibatches (⅓ of every gradient step turn-commanded), manifold-native yaw income, and the declared post-PPO counterfactual oracle gate that FAILS the run if command-following does not emerge. The flawed cos-alignment probe was discarded (the kM label difference is dominated by the L/R mirror gait swap — wrong comparison axis).

## 6. Remaining preflight items (scheduled, not skipped)

Reward ordering under a turn command: E/F receipts stand (exact turn top-priced on this book; frames member-keyed by the E repairs); the fresh manifold-basis probe is the card's own launch-time C3 income gate (fail-closed, C9). Start/stop: members carry DS lead-ins and declared standing pads (proven by the v1.6.0 segmentation itself); post-acquisition retention = the durable holds battery (card note); f8-sched not armed (arc-lane precedent, disclosed).

## 7. What Joe authorizes (one decision)

Authorizing this card = accepting `tor-v1.6.0` (§3 ledger; every previously-accepted control still green) + the paired construction launch under the existing PPO grant/clearance envelope with Guard sentences. Until then the card refuses live launch (exit 93).

## 8. Artifacts

`rgl/artifacts/c11s15k2_construction_20260805T164857Z/` — family + shas, labels (zero-clip receipt), member/arc-zone oracle receipts, probe scripts + `k2C_receipts.json` (variance/BC/counterfactual), BC seed CARD, card launcher, dry-run + resolved-config receipts, intent.json, 16-case regression + ARC_STATE snapshots. BC seed: `rl/models/walking/k2_bc_arcprior_20260805T164857Z/` (NOT a policy claim). Oracle: `rl/scripts/turning_oracle.py` v1.6.0 + runner controls #14–16.
