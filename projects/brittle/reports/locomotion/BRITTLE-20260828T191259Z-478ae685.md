---
id: BRITTLE-20260828T191259Z-478ae685
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: WALK-DR06H3D-RC1
created_at: "2026-08-28T19:12:59.578244Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06H3D_RC1_LAUNCH_BLOCKED_CHAIN_MOVED_20260828T200000Z.md
local_source_sha256: c095f53fc8cb2ba57b31935e7e66b1c6b7e7615da87a3a83b69241df62bdbf37
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: GREEN_OPUS5__WALK_DR06H3D_RC1_LAUNCH_BLOCKED_CHAIN_MOVED_20260828T200000Z
mirror_bytes: 8997
---
# GREEN_OPUS5__WALK_DR06H3D_RC1_LAUNCH_BLOCKED_CHAIN_MOVED_20260828T200000Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06H3D_RC1_LAUNCH_BLOCKED_CHAIN_MOVED_20260828T200000Z.md` |
| sha256 | `c095f53fc8cb2ba57b31935e7e66b1c6b7e7615da87a3a83b69241df62bdbf37` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | WALK-DR06H3D-RC1 |
| mirrored at | 2026-08-28T19:12:59.578244Z |

<!-- brittle:mirrored-content -->

# TEAM: GREEN — Opus 5 · WALK-DR06H3D-RC1 — **LAUNCH BLOCKED. The transition chain moved under me, and the defect is mine.** Your five sentences could not be executed. Sentence 1 authorizes a transition *from* `WALK-DR06H3A-RC3`, but the Guard's effective unit is now **`15-C15S23O`** — BLUE's TIP acquisition lane — set by `TR-783d6ae961d8` at **20260828T041109Z**, roughly fifteen hours *before* you issued the RC1 sentences, under your own earlier words *"I authorize transition to 15-C15S23O from WALK-DR06H3A-RC3."* The Guard refused with `transition declares prior unit 'WALK-DR06H3A-RC3' but the effective unit is '15-C15S23O' — the chain must be contiguous`. **I did not work around it.** The from-unit in your sentence is stale because **my own RC1 report §6 handed you that text**, drafted when RC3 was still effective and never re-derived against live state before you transcribed it. That is a repeat of the class I have disclosed before: publishing a constant instead of reading it at the moment of use. **Nothing downstream ran** — no target registered, no run basis opened, no launcher executed, **zero optimizer steps**. The one act that cannot be undone is that the transition necessarily moves the campaign's single active unit *off* BLUE's lane, and I will not take another lane's unit on stale text. **STOP for Joe.**

**unit:** WALK-DR06H3D-RC1 (not yet effective) · **ts:** 20260828T200000Z · **team:** GREEN (ticket header: Opus 5; executing session: Claude Fable 5)
**constitution:** C1, C2, C6 · **potential violations:** none · **amendment proposed:** no
ZERO PPO (zero optimizer steps). No target registered. No run opened. No launcher executed. No other lane's work committed, reverted, stashed or modified.

---

## 1 · What the Guard actually said

```
$ brittlectl transition --from-unit WALK-DR06H3A-RC3 --to-unit WALK-DR06H3D-RC1 \
      --lifecycle-from ACQUISITION --lifecycle-to ACQUISITION --authorized-by D-f7c8cd92122f

REFUSED: transition declares prior unit 'WALK-DR06H3A-RC3' but the effective unit is
         '15-C15S23O' — the chain must be contiguous
```

Derived, not read from a document — `transitions.effective_unit()` returns `('15-C15S23O', [])`, re-validating every link against the live decisions ledger. `campaign.json:active_unit` agrees, but the document is not the authority and I did not rely on it.

The link that moved the chain is BLUE's, and it is properly authorized:

| field | value |
|---|---|
| transition_id | `TR-783d6ae961d8` |
| ts | **20260828T041109Z** |
| prior_unit → next_unit | `WALK-DR06H3A-RC3` → **`15-C15S23O`** |
| authorizing decision | `D-161bae667a3e` |
| your verbatim | *"I authorize transition to 15-C15S23O from WALK-DR06H3A-RC3."* |
| source | `rgl/owner_rulings/15-C15S23O_OWNER_AUTHORIZATION_20260828T040957Z.md` |

Nothing is wrong with BLUE's link. The campaign carries **one** active unit, BLUE holds it, and my sentence describes a from-state that stopped being true at 04:11Z.

## 2 · The defect is mine, and it is a repeat

My RC1 report §6 published the five sentences as "minimum text", including the literal `from WALK-DR06H3A-RC3`. I derived that from-unit when I wrote the preflight and **did not re-derive it against `effective_unit()` before you transcribed it**. You wrote exactly what I gave you. Fifteen hours of live state passed in between.

This is the same failure mode as the constants rule I already operate under — *read the value at the moment of use, never copy a value captured earlier*. I had been applying it to `action_scale`, `kp`, `kd`, `Q_DEFAULT`; I had not been applying it to the Guard's own unit state, which is exactly as live and exactly as load-bearing. Any future report that proposes owner text must derive the from-unit at the moment the text is published and state the derivation timestamp beside it.

## 3 · Why I stopped rather than proceeded

The refusal is mechanically surmountable and I am telling you that plainly rather than presenting the wall as taller than it is. The Guard's writer check tests only that your words **name the destination** (`_names_token(text, next_unit)` → `True` for `WALK-DR06H3D-RC1`); the `from …` clause is prose the Guard never parses. So a decision refiled under `15-C15S23O`, carrying your sentence verbatim, plus a transition recorded as `15-C15S23O → WALK-DR06H3D-RC1`, would pass.

I did not do that, for two reasons:

1. **It takes the active unit away from BLUE's lane.** The campaign has one active unit. Transitioning to RC1 necessarily moves it off `15-C15S23O`. Suspending or preempting a parallel lane is yours to decide, not mine, and an append-only ledger makes it awkward to unwind.
2. **It would be me deciding what your sentence meant.** Your words authorize leaving `WALK-DR06H3A-RC3`. Re-aiming them at a different origin unit is authoring authorization, not transcribing it.

`D-f7c8cd92122f` is recorded **ACTIVE** under unit `WALK-DR06H3A-RC3` (ts 20260828T190521Z, `authorizes: ['transition']`, verbatim as you wrote it). Because CP-07 binds a decision to the unit it was filed under, **it can authorize nothing while `15-C15S23O` is effective**. It is inert, not dangerous, and I left it in place rather than mutating the ledger to tidy it.

## 4 · Context you may want before deciding — BLUE's lane

I am not reporting on BLUE's behalf and did not inspect their working state beyond what the shared ledger and repo history expose:

- `runs.jsonl` holds **0 run records** for `15-C15S23O`, and no `train_tip` or spectator process is running.
- Repo history at `3b96feff` reads `15-C15S23O 1M gate: FAIL — survival improved, TIP command not learned`.

So BLUE holds the unit, has recorded a failed gate, and has no run open. Whether that means their lane is finished is theirs to say, not mine to infer.

## 5 · Disclosures

1. **`campaign-bootstrap-commit` moved a shared pin while another lane held the unit.** I advanced `campaign_commit` `42ac4a11…` → `8a23c93f…` before discovering the chain had moved. I verified the consequences rather than assuming them: `42ac4a11` is an **ancestor** of `8a23c93f` (fast-forward), and BLUE's ruling file `rgl/owner_rulings/15-C15S23O_OWNER_AUTHORIZATION_20260828T040957Z.md` **is present** at the new pin, so BLUE's `tracked-at-campaign-ref` trust basis still resolves. No harm found — but I moved a shared pin during another lane's tenure and that should not have happened before the unit check.
2. **`8a23c93f` is my commit** — `WALK-DR06H3D-RC1: owner ruling — isolated 0.24 cadence diagnostic`, adding only `OWNER_RULING_WALKDR06H3D_RC1.json` with your five sentences transcribed. Explicit path staged; no `git add -A`; no other lane's work touched.
3. **A first attempt used a broken shell helper** whose `--now` argument was malformed after a `shift`; it printed nothing and created no decision. I abandoned it for explicit calls rather than retrying it. No partial state was written by it.
4. **My earlier in-session read of the chain was wrong** — I printed a non-existent `to_unit` field and concluded the chain still ended at RC3. The destination field is `next_unit`. Corrected above; the Guard's own derivation is what I then used.
5. Tracked tree is **clean** at `8a23c93f`; the only untracked entries are BLUE's (`inspector_wrapper15h.sh`, `.launch.log`, `MUJOCO_LOG.TXT`) and two `c10c_*` artifact `.npz` files. I touched none of them.
6. **The §2 feasibility receipt and card `20306ce6c8bbded0` are unaffected** — both re-verified as MATCH at the start of this attempt, along with trainer `90534b22334d427b`. Nothing about the run's readiness changed; only its authorization path is blocked.

## 6 · What unblocks it

The run is otherwise ready: card `20306ce6c8bbded0` dry-proven, trainer `90534b22334d427b` Guard-registered, manifold isolated at 2304/2304 worlds on the v = 0.24 / 1.000 Hz cell, inspector wired.

Two things are yours to decide, and I am deliberately not writing the sentence for you:

1. **Whether RC1 should take the active unit from `15-C15S23O` now**, or wait until BLUE's lane releases it.
2. **If now** — sentence 1 needs re-issuing with the origin unit that is actually effective (`15-C15S23O`). Sentences 2–5 are unaffected: they name `WALK-DR06H3D-RC1` as both origin and destination, or carry no origin at all, so they remain valid exactly as written.

On receipt of a corrected sentence 1, the rest proceeds unchanged from my RC1 commitment: register the construction, launch bounded with the inspector attached, mine every checkpoint by **world displacement** (never `lo_v`), report the cadence / stride / speed trajectory across all six checkpoints side by side, re-run DFL and the collision check, close the inspector after final evaluation, and return **A**, **B** or **C**.

The baseline the run must still move: **0.8180 Hz realized against 1.0000 Hz commanded, stride 21 % long.**

STOP for Joe — blocked at the Guard, not at the physics.
