---
id: BRITTLE-20260825T172453Z-684f977a
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: JOINT-REPORT01
created_at: "2026-08-25T17:24:53.938953Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__JOINT_REPORT01_ISAAC_PIPELINE_20260825T161258Z.md
local_source_sha256: aacdb4e0c4a6cc83117120fafb79867e464b9162cb713f874e00b6b94830c0be
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: GREEN_OPUS5__JOINT_REPORT01_ISAAC_PIPELINE_20260825T161258Z
mirror_bytes: 7862
---
# GREEN_OPUS5__JOINT_REPORT01_ISAAC_PIPELINE_20260825T161258Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__JOINT_REPORT01_ISAAC_PIPELINE_20260825T161258Z.md` |
| sha256 | `aacdb4e0c4a6cc83117120fafb79867e464b9162cb713f874e00b6b94830c0be` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | JOINT-REPORT01 |
| mirrored at | 2026-08-25T17:24:53.938953Z |

<!-- brittle:mirrored-content -->

# TEAM: GREEN — Opus 5 · JOINT-REPORT01 — Isaac joint-characteristics report pipeline: **IMPLEMENTED + FIRST ACCEPTANCE REPORT PASSED and OPEN ON THE DESKTOP.** The reusable pipeline (`generate_joint_report.py --policy <ckpt> --behavior <b> --simulator isaac`) produced `rgl/joint_characteristics/DR05D_737280__figure_eight__20260825_172124/` (report.html + behavior.mp4 + telemetry.npz + manifest.json): the exact DR05D-737280 policy on the canonical banked F8, ONE Isaac rollout supplying both the video states and the telemetry, torque = `ACDStepper.substep_torque` at full 500 Hz substep rate, qd = `robot.data.joint_vel[0, jmap]` rad/s in the fail-closed canonical 12-joint order, **7/7 sync checks PASS** (3490 rows = 3490 Isaac-rendered frames, 69.80 s @ 50 fps, frame k ⟺ row k structurally — zero drift by construction), and the **PhysX effort tripwire PASS with max deviation 0.000e+00 N·m over 34,899 aligned pairs** — PhysX echoes the issued effort bit-exactly, and max|applied| = 12.500 exactly (the F8 touches the rail once; no second actuator limit exists). The corrupted negative test FAILS CLOSED (one row removed → check 1 fails, `sync_failure.json` emitted, no report.html for the corrupted copy, valid report untouched). Implements the RED REPORT-VIS01 architecture and the REPORT-VIS02 torque contract verbatim. ZERO training/mutation/promotion/gate changes. STOP for Joe — the report is open in Firefox on the desktop.

**unit:** JOINT-REPORT01 · **ts:** 20260825T161258Z · **team:** GREEN (ticket header: Opus 5; executing session: Claude Fable 5)
**constitution:** C1, C2 · **potential violations:** none · **amendment proposed:** no
Ticket: bus `BRITTLE-20260825T161252Z-24d2a9cf`. Blueprints followed: RED REPORT-VIS01 (architecture: one recorder, two pure functions, index-locked timebase) + RED REPORT-VIS02 (Isaac torque semantics, the `applied_torque` trap, the §5 tripwire).

---

## 1 · Implementation (3 new files; banked isaac_arm/runtime_port untouched)

| path | role |
|---|---|
| `rl/scripts/generate_joint_report.py` | orchestrator + CLI + POLICIES/BEHAVIORS registries + telemetry assembly + 7 fail-closed sync checks + min/max-envelope decimation + full-rate stats + vanilla HTML/JS/Canvas emitter + `--negative-test` |
| `rl/scripts/joint_report_isaac_record.py` | instrumented Isaac recorder: the walkdr05a_isaac_confirm wrapper pattern (module unification + estimator patch) + the VIS02 tripwire installed on `Articulation.set_joint_effort_target` from a subclassed AppLauncher (post-boot, so carb's own signal handlers don't eat the SIGTERM flush; periodic dump every 2000 calls survives any termination) + fail-closed `plant_spec.JOINT_NAMES == runtime_port.JOINT_NAMES` order receipt |
| `rl/scripts/joint_report_isaac_render.py` | deterministic Isaac replay renderer: kinematic replay of the RECORDED states (`write_root_pose/joint_state` + `sim.render()`, physics never stepped) through the SAME converted USD/ground construction as the canonical arm, offscreen `Camera` sensor, follow camera, one frame per telemetry row via imageio/h264 |

Adding a behavior = one `BEHAVIORS` dict entry (cells + schedule); no frontend work. Default simulator: isaac.

## 2 · Acceptance report (all §20 criteria)

`/home/robojoe/code/brittle/rgl/joint_characteristics/DR05D_737280__figure_eight__20260825_172124/`

- **policy**: exactly DR05D-737280 — ckpt `35574a9855c188a2` (`rl/runs/walking/walkdr05d_polish_s0/ppo_737280.zip`), bundle `6c500d471935e820`, estimator α0.4/fdw1 — the latest QUALIFIED Green policy; DR06C artifacts are reference members, not policies, and were not substituted.
- **behavior**: the canonical banked F8 (`F8R1M_SCHEDULE.json` c11s61): straight → κ−1 lobe → straight → κ+1 lobe → straight, DS-gated; `f8_done` TRUE; both legs, both steering directions, changing torque/joint-speed demand — all in one 69.8 s report.
- **video rendered from Isaac**: 3490 frames from the recorded Isaac trajectory (frames inspected — Omniverse grid ground, follow camera, lobe yaw visible).
- **torque**: `ACDStepper.substep_torque` (N,10,12) at 500 Hz, post-clip — recorded values reused verbatim, never recomputed; NOT `applied_torque`, NOT `body_incoming_joint_wrench_b`.
- **tripwire**: PASS — 34,899 aligned (command, next-applied) pairs, max |Δ| **0.000e+00 N·m** (tolerance 1e-3); max|applied| 12.500 = the controller clip exactly, no second limit. Recorded in manifest as an integrity assertion, not a data source.
- **qd**: actual joint velocity, rad/s, canonical order (fail-closed receipt printed at record).
- **provenance** (`manifest.json`): control_law_id `kp20kd2ap1/ASO1.0/ry20kd1/tau12.5/acd10`, control_law_sha16 `bbe122433e9a399d` (runtime_port.py), plant fingerprint `81d4e6d8226ed12b` (law plant_fp; the scene-file byte hash is recorded separately as plant_file_sha16 — corrected in-place after first emission, disclosed), torque_limit 12.5, n_sub 10, 50/500 Hz, policy_dt 0.02 (read live, not hard-coded), telemetry `0cf8987e79bd4017`, video `296cad6fa23e4373`, sync_checks 7/7, tripwire block.
- **sync**: 7/7 — frames==rows (3490), fps==50==1/dt, uniform dt (0 error), t[0]=0, duration 69.80 vs 69.80, strictly increasing, dt==runtime contract.
- **HTML** (7.0 KB + 2.1 MB data.js; video 16.5 MB): opens from `file://` with no server (data embedded as data.js to dodge file:// fetch CORS); green SYNC VERIFIED banner; video + shared time cursor (rAF on video.currentTime) + frame-step buttons + click/drag-on-plot seeks the video; torque plot (per-step min/max envelope of the 500 Hz signal — spikes preserved, never naïvely subsampled; ±12.5 dashed limits; clamp span shaded — one span exists: l_knee_pitch brushes the rail, 0.003% of samples), qd plot, per-joint summary computed at FULL rate (max per-joint τ_p99 6.49 N·m; peak 12.5 at the single clamp event).
- **negative test**: `__CORRUPTED_NEGTEST` copy with one telemetry row removed → check `1_frames_eq_rows` FAILS (3489 vs 3490), sync_verified false, `sync_failure.json` emitted, NO report.html for the corrupted copy; the valid artifact untouched.

## 3 · Build findings (disclosed)

1. Isaac's `app.close()` hang (known) also bites the renderer — the orchestrator's render step is marker-based (`RENDER_DONE` then kill), like the record step. One earlier pipeline invocation stalled on this and was killed/resumed from the work cache (identical trace + frames).
2. carb overrides module-level Python signal handlers during app boot — the tripwire's SIGTERM flush must be registered post-boot (plus periodic dumps every 2000 calls as a belt).
3. Two aborted pipeline attempts (a pre-app `isaaclab.assets` import; a lost edit) and one carb mutex crash from overlapping Isaac app teardown are in the work logs; no artifact from any aborted attempt was used.

## 4 · One recommended next action

Register the remaining qualified behaviors (`straight_walk` at the canonical cell, `speed_ladder`, `stand`) as BEHAVIORS entries — pure registration, zero frontend — so "generate a joint report for X doing Y" covers the whole demo surface; optionally add the VIS01-A MuJoCo adapter later for truth-lane parity reports (the schema already carries `simulator`).

## 5 · Scope + status

ZERO training/mutation/promotion; DR05D-737280, the F8, rewards, gates, estimator semantics, torque limits all untouched; no dashboard creep (fixed 4-element layout). Desktop review is LIVE: `report.html` open in Firefox (left open per §21); Joe can play/scrub the Isaac F8, watch the synchronized cursor, toggle joints, and find the single knee clamp event. No phone delivery.

STOP for Joe.

**changelog row:** `JOINT-REPORT01, 20260825T161258Z` — post-append `rgl/LOCOMOTION_CHANGELOG.csv` sha256_16 `c31efac90a88bf5a` (525 rows × 13 cols, both trees byte-identical)
