---
id: BRITTLE-20260805T213504Z-d4fdc15a
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15L
created_at: "2026-08-05T21:35:04.449650Z"
source_commit: ac0671c
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15L_MEMBER_INJECTOR_NURSERY_20260805T213136Z.md
local_source_sha256: 90083d18a7b5f153e6306a2760c4bcb385886b86ef7ca141cb948f01852642ab
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15L_MEMBER_INJECTOR_NURSERY_20260805T213136Z
mirror_bytes: 7544
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15L_MEMBER_INJECTOR_NURSERY_20260805T213136Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15L_MEMBER_INJECTOR_NURSERY_20260805T213136Z.md` |
| sha256 | `90083d18a7b5f153e6306a2760c4bcb385886b86ef7ca141cb948f01852642ab` |
| brittle commit | `ac0671c` |
| lane | locomotion |
| unit | 12U-C11S15L |
| mirrored at | 2026-08-05T21:35:04.449650Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15L — Member-Scoped Injector + Curvature-Acquisition Nursery Construction: MEMBER-SCOPED NURSERY READY FOR AUTHORIZATION

**date:** 2026-08-05 · **unit:** 12U-C11S15L · **lane:** locomotion · instrument repair + construction + preflight; **no PPO launch, no continuation, no reward change, no promotion, no policy card, no latest-pointer, no plant/control-law change, no canonical mutation; the card refuses live launch (exit 93)**
**constitution:** C1, C2, C3, C4, C6, C8, C9, C12 · **potential violations:** disclosed in §7 · **amendment proposed:** no
**final decision (exactly one):** **`MEMBER-SCOPED NURSERY READY FOR AUTHORIZATION`**
**changelog:** row 56 `12U-C11S15L, 20260805T213136Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. Competence record (append-only, preserved)

`COMPETENCE_RECORD_C11S15L.json`: the K2 BC seed was NOT a walking prior (falls 8/8 deterministically); injection-armed displacement checks were invalid competence evidence; PPO improved survival relative to the seed; future competence = **fall-free duration + oracle-qualified motion on injection-free authoritative traces**; `log_std` must be verified from live telemetry after checkpoint loading. **The BC seed is RETIRED as anchor/parent.** Artifact-hygiene disclosure: `*.zip` is repo-untracked by law, and a receipt rerun regenerated `bc_seed_v2.zip` (f44fb757 → 023d8f39, original unrecoverable; the §5 battery measured the regenerated fit) — new law: receipt reruns write regenerated artifacts to fresh paths; the stale Guard pin for `bc-seed-k2-v2` is flagged for the authorization-time target update (the L card does not consume the seed).

## 2. P17 — member-scoped injector (candidate `b3d1074344a388d0`)

The arc-RSI frame draw was manifold-unaware (`integers(0, n−2)` + global quota pools — a state from one member could bind to another member's commands; K2B had to pin arc-RSI 0). Repair: frames come ONLY from the world's own member window via the already-tested `_rsi_window` helper (the walk-RSI and ref-truncate paths already used it); the per-side quota is bypassed in manifold mode (each world's member has one sign — exposure balance is by world binding); **two hard fail-closed gates**: draw-vs-window, and an **independent member-bounds table keyed by member index** so a corrupted per-world window array cannot silently pass. Clock accumulators (`_ph_acc/_phi_acc/loop_count`) were already atomically resynced for all done rows (verified). No cross-member fallback exists by construction.

## 3. Injector invariants — 11/11 PASS (`rl/scripts/test_c15l_member_injector.py`)

Injections on every member (25–31 events each) · every injected phase inside its OWN member window · injected-frame commands obey the member sign law · path anchor continuous at injection (cmd path == robot XY, med 0.0000) · **BLANKET: phase in own member window at every step, injections and respawns included** · intra-episode continuity (quiet rows advance smoothly or member-wrap) · mirrored members mirror (table-y exact 0; bent-state y 0.0000) · member-local cyclic wrap stays in-window · qualification batteries pin injection OFF (static) + injection-off runs never jump mid-episode · **adversarial member-ID mixing REJECTED fail-closed (SystemExit)**. Two test-side lessons folded in: same-step done→respawn jumps are reset bookkeeping (mask includes the current step's done), and the blanket in-window check is the load-bearing isolation invariant.

## 4. Family + §8 preflight reruns (on the EDITED candidate)

Family = the exact K2A deconfounded slice (straight-fast `cd7a0d18` speed sample / straight-slow `7f45464d` / kP `1551b4ac` / kM `d8d243b1`), DFL ×4 PASS, honest label carried in the card: **curvature-acquisition nursery, not the final independent command manifold**. Reruns against candidate `b3d10743`: same-state reward-switching matrix **11/11 orderings** + same-table parity 0.0 · post-stratifier minibatches **24 × exactly 1365/1365/1365, dup 0.0, obs45 std 0.2498** · oracle regression **16/16** (tor-v1.6.0 `b8cc40ff`, fresh) · member-scoped injection receipts (§3) · action representability (zero-clip labels, unchanged) · torque (τP99 ≤ 8.1 in all batteries).

## 5. Initialization selected: the start/stop milestone walker (measured)

| candidate | det falls | det first-fall | stoch (σ.135) falls | ±κ authority | label dist | τP99 |
|---|---|---|---|---|---|---|
| **milestone `7d176de4`** | **0/32** | **660 (none)** | **0/32** | 0.055 | 0.207 | 8.1 |
| G-final `78298a7d` | 0/32 | 660 | 0/32 | 0.068 | 0.230 | 7.8 |
| scratch | 32/32 | 225 (no-step ET) | 32/32 | 0.002 | 0.193 | 4.3 |
| BC seed (regenerated) | 32/32 | 66 | 32/32 | 0.179 | 0.037 | 5.0 |

**Milestone selected**: perfect deterministic AND stochastic survival on this exact nursery env, ±κ authority present (not suppressed — the §5 guardrail's measurement), certified start/stop provenance. G-final ties on survival with slightly higher authority but is an unbanked kP-lane probe artifact carrying straight-under-turn entrenchment. The BC seed's high authority/low label-distance without locomotion is exactly the K2C pattern.

## 6. P18 — exploration-law repair + sweep

Candidate repair: after any `continue-ckpt` load, `log_std` is **re-asserted to the card's `--log-std-init`, read back live, and fail-closed** — plus a **pre-optimization gate at iteration 0** (any hidden writer between load and first update refuses). Live in the dry-run: "log_std re-asserted to −2.0 (readback −2.0000)". No-update sweep on the milestone: **−2.5 and −2.0 → 0/32 falls; −1.5 → 3/32 falls + τ 9.3** → card sets **−2.0** (least destructive with standard exploration). §7 supervision: policy-anchor to the milestone (a 0/32-falls WALKER — the K2C law satisfied), w0 0.15, decay 8,388,608 (never zero inside the 4,194,304 probe — the July pattern); no label anchor; no runtime reference trajectory (residual-on-default law unchanged).

## 7. The card + disclosures

`launch_c11s15l_nursery.sh` — deltas [L-1..6] over K2B (P17+P18 candidate pin, milestone init+anchor, log_std −2.0, `--walk-rsi 0.75 --arc-rsi 0.85` nursery curriculum, seed retired); all assertions PASS in dry-run; **resolved fingerprint `3ca9e0fe795eda80` stable ×2** (env/reward contract unchanged by design — the card sha `launch_c11s15l_nursery.sh` pins the arg deltas); inspector block with the ckpt-pin-"any" lesson; intent + regression + K2B-RCA-clearance gates documented; qualification = sustained injection-free walking, κ ±1.4/0.4 m correct-signed, straight ≤0.25, identical-state signed response, falls/torque/contact/start gates, RCA-on-miss. Disclosed: (a) the 12S `ARC_RSI=0.85/frac 0.75` → `--arc-rsi 0.85 --walk-rsi 0.75` mapping is an interpretation (both paths member-scoped; flag for Joe); (b) the bc-seed artifact regeneration (§1); (c) candidate sha changed (`99fbeca7` → `b3d10743`) — Guard re-pin belongs to the authorization ticket.

## 8. Artifacts

`rgl/artifacts/c11s15l_nursery_20260805T213136Z/`: card, competence record, init battery (script+receipts), dry receipts + resolved config, oracle-regression snapshot. Candidate edits in `rl/mjw/train_tip_reftrack_c15f_candidate.py` (P17+P18, commented in-line); invariants test `rl/scripts/test_c15l_member_injector.py`. K2A dir: matrix/minibatch rerun receipts. **Next action is Joe's: authorize the nursery card (Guard sentences; targets update incl. candidate re-pin + seed disposition).**
