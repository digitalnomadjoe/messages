---
id: BRITTLE-20260824T170238Z-dd6bc092
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: WALK-DR05A
created_at: "2026-08-24T17:02:38.073958Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR05A_ESTIMATOR_NOISE_HARDENING_20260824T165133Z.md
local_source_sha256: f43085cc98212dad8a7d6165fcf369cf58a0697ccb89ae0f2c2ec5efc7c65143
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: GREEN_OPUS5__WALK_DR05A_ESTIMATOR_NOISE_HARDENING_20260824T165133Z
mirror_bytes: 10396
---
# GREEN_OPUS5__WALK_DR05A_ESTIMATOR_NOISE_HARDENING_20260824T165133Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR05A_ESTIMATOR_NOISE_HARDENING_20260824T165133Z.md` |
| sha256 | `f43085cc98212dad8a7d6165fcf369cf58a0697ccb89ae0f2c2ec5efc7c65143` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | WALK-DR05A |
| mirrored at | 2026-08-24T17:02:38.073958Z |

<!-- brittle:mirrored-content -->

# TEAM: GREEN — Opus 5 · WALK-DR05A — leg-odometry estimator noise hardening: YES, the encoder-noise wall is removable without training — a ONE-CONSTANT change (leg-odometry blend α 1.0 → 0.4) moves the wall 0.1° → 0.5° σ (0.25° goes 12/12-falls → 0/12), beats the baseline at EVERY one of 15 noise levels, keeps nominal falls at zero with the best quality profile of all candidates (estimator RMS error better than baseline, 40 ms channel lag, acceleration gain 0.96) — with ONE disclosed preservation caveat: the razor-edge STAND posture shifts (DS +3.8 pts, sole spheres brush −1–1.7 mm; walking cells stay clear +8–12 mm). Recommended implementation: α = 0.4. GATE = CONDITIONAL PASS, Joe judges the stand caveat. ZERO training. STOP.

**unit:** WALK-DR05A · **ts:** 20260824T165133Z · **team:** GREEN (ticket header: Opus 5; executing session: Claude Fable 5)
**baseline:** frozen Phase-12 Crown FUNCTION (`876025a9cb883f37`, manifest `f7d950212cf3d68e`), plant `81d4e6d8226ed12b` — canonical runtime NOT mutated; every variant is an eval-time subclass, receipt-printed per rollout.
**interface:** preserved exactly (ticket §1) — obs dims/meanings (obs 6:9 = leg-odometry base velocity), actor, reward, action, control law, commands, plant all untouched; no future information; no privileged state.
**constitution:** C1, C2, C6, C8, C12 · **potential violations:** none · **amendment proposed:** no
Ticket: bus `BRITTLE-20260824T165126Z-e4b7384e` (owner-direct 2026-08-24). ZERO training.

---

## 0 · Verdict against the §5 hard gate

| gate clause | α = 0.4 (recommended) | evidence |
|---|---|---|
| materially pushes the 0.1° encoder-noise wall | **PASS — wall now at 0.5°**: 0.1° 4/12→0/12, 0.25° 12/12→**0/12**, 0.5° 12/12→10/12 | §3 table |
| preserves nominal Crown behavior | **PASS on survival/walking, CAVEAT on stand**: 0 nominal falls; worst walking materiality ≤1.5×; but stand DS +3.8 pts and stand sole-spheres brush −0.9/−1.7 mm (baseline stood +0.07 mm clear) | §4 |
| no meaningful latency/phase-lag regression | **PASS**: velocity-channel lag 20→40 ms, acceleration gain 0.96, and the full noise battery — the functional lag test — is never worse than baseline at any level | §3, §4 |
| improves or preserves BOTH engines | **PASS**: Isaac nominal 0/12; gyro 0.5 12/12→2/12; encq 0.5° 12/12→11/12 with 4–8× longer survival — never worse on either engine | §5 |

**Recommendation (exactly one, per ticket §5): `LO_ALPHA 1.0 → 0.4`** — a single constant in the leg-odometry blend (`lo_v = (1−α)·lo_v + α·mean(cands)`), nothing else. NOT applied to the canonical runtime in this unit; Joe's adoption call, including whether the stand-posture caveat needs his visual review first (C8).

## 1 · Why this unit exists (mechanism recap)

WALK-DR04 isolated the encoder-noise collapse to the deployed estimator: α=1.0 means obs 6:9 carries raw 50 Hz FK differencing (noise amplification ≈ 1/DT); policy-only noise survives while estimator-only noise falls in <1 s. So before asking PPO to compensate (WALK-DR05), this unit asks whether a minimal, causal, physically plausible filter removes the wall.

## 2 · Candidates and provenance

Eval-time subclass of the frozen runtime (`walkdr05a_est.py::make_runtime_class`) — a byte-copy of `post_step` with exactly two parametrized deltas: blend α, and finite-difference window w (rdot over w·20 ms, guarded so the foot was planted through the window). **Negative control: (α=1.0, w=1) is bit-identical to the frozen runtime** (asserted against the banked DR04/DR01 nominal trace before any sweep). Candidates: A baseline (1.0,1) · B low-pass α {0.5, 0.3, 0.2, 0.1} (+0.4 refinement) · C short-window {(1.0,2), (0.3,2)}. All causal; no future info; no privileged state. Baseline full-battery levels are REUSED from the bit-identical DR04 traces, not re-simulated.

## 3 · Zero-training sensor battery (MuJoCo truth arm, 12-cell canonical battery; falls/12)

| level | A base (α1.0) | **α0.4 (rec)** | α0.3 | α0.2 | fd2+α0.3 |
|---|---|---|---|---|---|
| nominal | 0 | **0** | 0 | 0 | **1 — disqualified** |
| encq 0.05° | 0 | 0¹ | 0 | 0 | 0 |
| encq 0.1° | **4** | 0¹ | 0 | 0 | 0 |
| encq 0.25° | **12** | **0** | 0 | 0 | 0 |
| encq 0.5° | **12** | 10 | 8 | 6 | 1 |
| encqd 0.3 | 1 | **0** | 0 | 2 | 4 |
| encqd 0.5 | 4 | **1** | 3 | 6 | 10 |
| encqd 1.5 | 11 | —² | 11 | 12 | 12 |
| gyro 0.1 | 1 | —² | 0 | 0 | 0 |
| gyro 0.15 | 3 | 1 | **0** | 0 | 0 |
| gyro 0.5 | 12 | 7 | 5 | 5 | 11 |
| gyro 1.5 | 12 | —² | 12 | 12 | 12 |

¹ α0.4 ran the decisive-level set {nom, encq .25/.5, encqd .3/.5, gyro .15/.5} after screening; its encq ≤0.1 clean status is implied by α0.3/α0.2 both clean there and α0.4 clean at the strictly harder 0.25 — labeled inferred, not measured. ² not run (bracketed by α0.3/α0.2).
Screening (7 candidates × encq {0.1, .25, .5} × 4 cells) additionally showed: α=0.1 is OVER-filtered (falls at 0.1° from sluggishness) and fd2-alone (α=1.0) is WORSE than baseline (a 40 ms-old rdot at full gain = destabilizing lag) — filtering strength has a two-sided optimum near α≈0.3–0.4.

## 4 · Estimator quality (ticket §4; crown cell, walking window, offline vs true body velocity)

| candidate / condition | RMS err (m/s) | HF noise | channel lag | accel gain |
|---|---|---|---|---|
| baseline, no noise | 0.091 | 0.078 | 20 ms | 1.03 |
| baseline @ 0.1° noise | 0.134 | 0.141 | 20 ms | 1.13 |
| **α0.4, no noise** | **0.088 (better than baseline)** | 0.032 | 40 ms | 0.96 |
| **α0.4 @ 0.25° noise** | **0.117** | 0.083 | 40 ms | 1.07 |
| α0.3, no noise | 0.097 | 0.026 | 60 ms | 0.92 |
| α0.2, no noise | 0.115 | 0.020 | 100 ms | 0.83 |

At 2.5× MORE noise, α0.4's estimate is more accurate than the baseline's (0.117 @0.25° vs 0.134 @0.1°). Gait staples on α0.4 nominal: 0 falls, worst materiality 1.51× (stand DS +3.8 pts — the caveat), walking-cell stride/cadence/speed/curv within ~1.5× floors, τ and tilt nominal-band, walking foot-separation +8.1–12.3 mm with ZERO overlap frames.

**The stand caveat (both α0.3 and α0.4 — a property of any α<1):** the standing posture equilibrium responds to the smoother velocity estimate; stand cells stay upright and never fall, but DS shifts +3.8 pts and the sole spheres — which at baseline stand only +0.07 mm apart — cross into −0.9…−1.7 mm brush for 30–40 frames. This is a static posture shift on razor-edge stand geometry, not a dynamic collision (all walking/κ cells clear by ≥8 mm). Honest §4 "physical foot-foot collision" flag; Joe judges acceptability (and the walking-plant foot-geometry question from DR01 §8 is adjacent).

## 5 · Isaac confirmation (α = 0.4)

| condition | MuJoCo | Isaac | verdict |
|---|---|---|---|
| nominal (no noise) | 0/12 falls | **0/12 falls** | **PRESERVED both engines** |
| encq 0.5° | 10/12 (baseline 12/12) | 11/12, falls @50–604 (baseline 12/12 @31–80, DR04) | AGREE: 0.5° stays beyond the corrected wall, but survival time stretches 4–8× and one cell survives — improvement in degree on both engines |
| gyro 0.5 | 7/12 (baseline 12/12) | **2/12** (baseline 12/12, DR04) | **IMPROVED both engines** |

Estimator + sensor patches receipt-verified per level (fail-closed; the DR04 dual-module trap is closed by construction in `walkdr05a_isaac_confirm.py`). One confirmation cell was additionally run in the VISIBLE Isaac viewer (ticket §8); the MuJoCo inspector ran live throughout the sweeps.

## 6 · Hardware-facing requirements (ticket §6 — two SEPARATE requirements)

1. **Estimator requirement:** the deployed leg-odometry must not pass raw 50 Hz FK differences to obs 6:9. Minimum viable correction = single-pole low-pass on the candidate blend, α ≈ 0.4 at 50 Hz (−3 dB ≈ 4 Hz), which keeps acceleration gain ≥0.96 and adds ≤40 ms channel lag. (α≈0.1, or lagged differencing at full gain, over-corrects and is itself destabilizing — the optimum is two-sided.)
2. **Effective encoder/transmission noise requirement** (resolution + quantization + backlash + compliance + mounting, combined AT THE JOINT, per 20 ms sample — deliberately not conflated): with the corrected estimator the zero-training system tolerates **≤0.25° σ fully clean, degraded-but-mostly-standing at 0.5°**; without it, ≤0.05° clean and 0.1° already falls. Encoder RESOLUTION alone (e.g. 14-bit ≈ 0.02°) is comfortably inside either budget — on real hardware the budget is consumed by BACKLASH and transmission compliance, which is exactly the band the estimator correction buys back. Confidence: HIGH in-sim; MEDIUM transfer (white-noise model; real error is colored/load-dependent).

## 7 · Next unit (ticket §7)

If Joe approves an estimator (α=0.4 recommended): **WALK-DR05 = encoder q/qd + gyro noise DR with the approved estimator IN THE LOOP**, nominal-anchored mixture (DR03 30/20/50 lesson), frozen Crown foundation — targeting the residual 0.5° encq / 0.5 gyro band the filter alone doesn't close, and re-securing the stand posture. If Joe rejects on the stand caveat: WALK-DR05 proceeds with the baseline estimator per DR04's plan (harder, since the plant then includes the amplifier). Either way the ~15-line obs-noise draw in `tip_observation_kernel` + CPU parity remains the required plumbing (not built here).

## 8 · Scope compliance + artifacts

ZERO training. Canonical runtime untouched (subclass eval only; adoption is Joe's). No gate/threshold/plant changes; banked instruments untouched; new files unit-named. Baseline traces reused, not re-run. Visible MuJoCo inspector throughout; visible Isaac viewer for the confirmation cell; all viewers closed at filing.

- Results: `rgl/artifacts/walkdr05a_estimator_20260824T165133Z/` — `DR05A_RESULTS.json`, `mj/` (~1,000 traces incl. screen+full+α0.4), `isaac/` + `logs_isaac/`, `viewer.log`.
- Instruments: `rl/isaac_parity/walkdr05a_est.py` (candidate runtime subclass + screen/full/analyze; bit-gated negative control), `walkdr05a_isaac_confirm.py`.
- Replay: `walkdr05a_est.py full <adir> B_a04` · Isaac: `<adir>/run_isaac_confirms.sh`.

STOP for Joe.

**changelog row:** `WALK-DR05A, 20260824T165133Z` — post-append `rgl/LOCOMOTION_CHANGELOG.csv` sha256_16 `0091edfe910fc229` (481 rows × 13 cols, both trees byte-identical)
