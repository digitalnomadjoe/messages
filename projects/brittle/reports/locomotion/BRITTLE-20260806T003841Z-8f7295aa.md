---
id: BRITTLE-20260806T003841Z-8f7295aa
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15M
created_at: "2026-08-06T00:38:41.090775Z"
source_commit: 82d7a44
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15M_KP1_RUNG_CONSTRUCTION_20260806T001500Z.md
local_source_sha256: 660cae8d8bbf88ed43bfcce77b8d44a104b6663372526e4d965a50916f072da2
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15M_KP1_RUNG_CONSTRUCTION_20260806T001500Z
mirror_bytes: 7398
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15M_KP1_RUNG_CONSTRUCTION_20260806T001500Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15M_KP1_RUNG_CONSTRUCTION_20260806T001500Z.md` |
| sha256 | `660cae8d8bbf88ed43bfcce77b8d44a104b6663372526e4d965a50916f072da2` |
| brittle commit | `82d7a44` |
| lane | locomotion |
| unit | 12U-C11S15M |
| mirrored at | 2026-08-06T00:38:41.090775Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15M — Sign-Restricted Intermediate-Curvature Nursery Construction: POSITIVE INTERMEDIATE RUNG READY FOR AUTHORIZATION

**date:** 2026-08-06 · **unit:** 12U-C11S15M · **lane:** locomotion · construction + preflight; **no PPO launch, no continuation, no reward/oracle mutation, no promotion, no policy card, no latest-pointer, no plant/control-law change, no canonical mutation; the card refuses live launch (exit 93)**
**constitution:** C1, C2, C3, C4, C6, C8, C9, C12 · **potential violations:** disclosed in §7 · **amendment proposed:** no
**final decision (exactly one):** **`POSITIVE INTERMEDIATE RUNG READY FOR AUTHORIZATION`**
**honest label:** `positive-sign intermediate-curvature acquisition rung` — success would mean only `POSITIVE INTERMEDIATE CURVATURE ACQUIRED`; not a mixed-sign manifold, final command policy, or figure-eight claim.
**changelog:** row 59 `12U-C11S15M, 20260806T001500Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. Parent (recorded, not banked)

`rl/models/walking/p12uc11s15l2_nursery_20260805T235500Z/s0/ppo_4194304.zip` · sha256_16 **`da70f57b4c5bae6b`** · plant v2 `81d4e6d8` + COMPLETE `c44ad73f` (run plant_id) · walking-law v2 · contract 54/60 · trainer lineage candidate `b3d10743` (its training) · resolved fp `3ca9e0fe` (its card). Roles here: initialization + proposed full-policy anchor + robust walking substrate.

## 2. Minimal member family (exactly two primary members; identical speed and cadence)

`ccref_vx015_c1p000.npz` (straight, κ=0, `7f45464d…`) + **`ccref_vx015_c1p000_kP100.npz` (κ=+1.0, `5a53bdf2…`)** — both vx 0.153, cadence 1.0, identical lead-in/pad semantics and loop metadata. The κ+1 member = the **committed c10b curved recipe** applied to the slow-straight base: boxcar κ profile on the era zone [214,464) (the certified kP200's profile shape at half magnitude), `bend_episode` path math verbatim, `stance_twist` hip-yaw absorption, `step_diff` step-length split. **Resolver pairing law required the mirror FILE to exist**: `kM100` (`c69cae60…`) built by the exact structural mirror map **derived empirically from the certified kP200/kM200 pair and validated BITWISE (round-trip reproduces kM200 with 0.0 error on every field)** — jq perm [6..11,0..5] × signs [−,−,+,+,+,−]; feet swap + y-negate; contacts swap; trpy col0 copy / col1 anti-map about 0 / yaw about π (the euler-axis-swap convention). `--kappa-signs P` selects ONLY kP100 as a member — no κ−, no κ+2, no extra cadence/speed cells in this rung.

## 3. Oracle compatibility (tor-v1.6.0 unchanged; no threshold change)

kP100 member: **TURNING_LEFT, κ_run 0.997, R 1.003 m, 1.93 m valid translation** — essentially exact against commanded +1.0/1.0 m, comfortably above the unchanged ≥0.8 sustained-run threshold. Straight member: **STRAIGHT, κ 0.0**. No spin/pivot.

## 4. Command contrast (measured after the actual sampler + stratifier)

**P19 candidate repair** (the three-class guard silently DISARMED the balancer on sign-restricted cards): two present classes → equal halves per minibatch, same reshuffle+wraparound law; per-class share = minibatch//len(strata) (the //3 constant would have shrunk two-class minibatches). Measured on a real L2-final rollout batch (collected past the lead-in — the 12S first-batch blindness respected): **every minibatch exactly 256/256 straight/arc; obs45 std ≥ 0.076 (values 0 vs ≈0.14 zone command); obs52 and obs53 std EXACTLY 0.0 — curvature is the ONLY command difference; no unrelated privileged features vary by member** (speed/cadence identical by construction).

## 5. Reward switching (live book, matched states)

Matrix (ghosts + L2-final closed-loop + stand/spin/fall × {S, P1}): **GH_S|S 15.8 top of column S; GH_P1|P1 14.4 top of column P1; straight-under-turn 8.53 loses materially (−5.9/step); wrong-geometry 8.95 loses; stand/spin/fall dominated (≤7.1 / −10.6)** — all required orderings TRUE; every imitation/progress/heading/yaw/contact/phase target follows the active member (phase-in-window blanket + the K2A same-table parity carries). No repricing.

## 6. Member-scoped injector on the new family

The P17 suite was made **family-generic** (env-parameterized; sign law derived from member κ; mirror test auto-skips single-sign; the K2A-family regression rerun stays **11/11**). On the M family: **11/11 PASS** — injections on both members (59/60 events), own-window always, command sign law, anchor continuity 0.0000, blanket in-window, wrap, injection-off batteries clean, **adversarial member-ID mix REJECTED fail-closed**. Census: **85.3% member-scoped injection / 14.7% lead-in starts** (corrected, ≥5k events) — matches the declared `arc-rsi 0.85`, no dead flags.

## 7. Parent + anchor audit · exploration law

L2-final on the rung (native resets, injection-free): **0/32 falls deterministic AND stochastic (σ 0.135)**, τP99 6.4/7.6. Label distance: straight **0.279** vs kP1 **0.268** — near-equal (differential 0.011): **the full-policy anchor does NOT differentially suppress κ+1 acquisition** (the L1 logic with a 25× smaller differential than the milestone case); anchor retained: **init+anchor = L2-final, w0 0.15, decay 8,388,608** (never zero inside the 2.1M probe). Command sensitivity 0.014 and geometry branches unsigned (S −0.08 / P1 −0.33, 32/32 walking) — acquisition is PPO's job, as designed. BC seed not used (retired/DIAGNOSTIC). **Exploration/RSI preserved**: log_std −2.0 + P18 re-assert/readback/it-0 gate (live in both dry runs); `arc-rsi 0.85` member-scoped; no walk-rsi flag anywhere on the card. Disclosures: (a) P19 is a candidate edit → sha `6d9a62c94099ccb4` (Guard re-pin at authorization); (b) the three-class stratifier branch is code-reviewed unchanged (share divisor generalized; live three-class behavior unaffected); (c) matrix absolute totals differ from K2A's rig config — orderings are the requirement and basis-consistent.

## 8. Proposed bounded run (prepared, NOT launched)

`launch_c11s15m_kp1.sh` (sha **`cad4024efc496a0c`**): one seed (seed 0 is grant-exhausted — use seed ≥1), **structural ceiling 2,097,152, evals/ckpts every 262,144**, plant/law/contracts/book unchanged, inspector visible before optimization (ppo_0 staging + live-training manifest pin) and closed after reporting, injection-free authoritative traces at every checkpoint, RCA-on-miss. Dry-run ×2: all assertions PASS (member pins ×4 incl. the mirror file, parent init pin, plant v2+v3, candidate pin), 2-member manifold wired 4096 worlds, P18 live, **resolved fingerprint `18948a4ba193548c` STABLE ×2**. Qualification: κ ≥ +0.8 over ≥0.6 m on the positive command; straight |κ| ≤ 0.25; zero falls over the horizon; identical-state κ0/κ+1 geometric separation; no pivot/spin; walking/start/torque/contacts/continuity retained.

## 9. Artifacts

`rgl/artifacts/c11s15m_kp1_20260806T001500Z/`: member builder + receipts (`m_member_receipt.json`), family (straight + kP100 + mirror kM100), oracle receipts ×2, `m_preflight.py` + `m_preflight_receipts.json` (census/contrast/matrix/parent), card + dry receipts ×2. Candidate P19 edit in-tree; injector suite genericized (`test_c15l_member_injector.py`). **Next action is Joe's: authorize the rung (Guard sentences; candidate re-pin `6d9a62c9` + launcher `cad4024e` + family member records).**
