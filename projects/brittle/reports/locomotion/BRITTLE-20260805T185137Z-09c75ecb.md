---
id: BRITTLE-20260805T185137Z-09c75ecb
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15K2A
created_at: "2026-08-05T18:51:37.197904Z"
source_commit: "1409949"
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15K2A_REWARD_SWITCHING_DECONFOUND_20260805T164857Z.md
local_source_sha256: ad3a453198967c0253f52e9dfec30312a746dbf81d031d2a6ce0e7529471604d
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15K2A_REWARD_SWITCHING_DECONFOUND_20260805T164857Z
mirror_bytes: 7961
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15K2A_REWARD_SWITCHING_DECONFOUND_20260805T164857Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15K2A_REWARD_SWITCHING_DECONFOUND_20260805T164857Z.md` |
| sha256 | `ad3a453198967c0253f52e9dfec30312a746dbf81d031d2a6ce0e7529471604d` |
| brittle commit | `1409949` |
| lane | locomotion |
| unit | 12U-C11S15K2A |
| mirrored at | 2026-08-05T18:51:37.197904Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15K2A — Exact-Card Reward Switching + Command-Deconfounding Gate: REWARD SWITCHING AND COMMAND CONTRAST PROVEN

**date:** 2026-08-05 · **unit:** 12U-C11S15K2A · **lane:** locomotion · probes + one deconfounding member + card v2; **no PPO, no continuation, no promotion, no policy card, no latest-pointer, no plant/control-law change, no canonical mutation; the card still refuses live launch (exit 93)**
**constitution:** C1, C2, C3, C4, C6, C8, C9, C12 · **potential violations:** disclosed in §7 · **amendment proposed:** no
**final decision (exactly one):** **`REWARD SWITCHING AND COMMAND CONTRAST PROVEN`**
**changelog:** row 53 `12U-C11S15K2A, 20260805T164857Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. Exact-card identity (all asserted, fail-closed)

K2 base fingerprint `c8927b74004936e6` ✓ · oracle `tor-v1.6.0` sha `b8cc40ff49866a27` ✓ · reward contract `84136d9692b66e71` (c8j.1, from the dry receipt) · plant v2 `81d4e6d8226ed12b` + COMPLETE v3 gate ✓ · walking-law v2 (ankle-roll kd 0.2) ✓ · member shas: straight-fast `cd7a0d18…`, kP `1551b4ac…`, kM `d8d243b1…` (+ §5's straight-slow `7f45464d…`) ✓ · candidate pin `99fbeca713204689` ✓. Every probe ran through the candidate's LIVE env + the card book (`env.last_terms`), not an offline approximation.

## 2. Same-state command-switching matrix — DIAGONALLY CORRECT

8 subjects × 3 commands (slow-straight / κ+2 / κ−2), per-world command-member binding, t0 phase forced to the commanded member's cyclic zone, natural command-clock advance, teacher-forced state streams (ghosts = reference states + zero-clip a\* actions; K1 = the real recovered turner's rollout; falling = recorded; policy/stand/spin closed-loop). Mean total reward/step:

| behavior \ cmd | S | +κ | −κ |
|---|---|---|---|
| straight ghost | **23.07** | 14.00 | 12.27 |
| left-arc ghost | 13.08 | **22.28** | 8.92 |
| right-arc ghost | 12.09 | 7.74 | **20.31** |
| K1 real left-arc | 18.33 | 21.36 | 14.06 |
| current straight policy | 17.62 | 17.53 | 11.36 |
| standing | −55.0 | −57.6 | −57.9 |
| pivot/spin | −56.2 | −58.8 | −61.3 |
| falling | −121.9 | −121.8 | −123.7 |

**All eleven required orderings TRUE**: each column's matching behavior highest; straight-under-turn loses to the matching arc by ~8/step; wrong-sign loses to straight-under-turn; stand/spin/fall dominated everywhere; the real K1 turner prices near-diagonal (21.36) under its command. Term-level: under +κ, `track_lat` 1.91→0.003→0, `track_res` 1.27→0→0, `r_fwd` (altwin path-tangent) 5.94→3.17→0.24, `track` 9.15→5.87→5.11 across true/straight/wrong-sign — every command-dependent term follows the active member. **Instrument lesson (first pass failed honestly):** nearest-pose t0 sync parks mismatched subjects in the curved member's straight LEAD-IN — where the command legitimately IS straight — conflating the off-diagonal cells; zone-forced sync is the correct cell construction.

## 3. Per-term lineage + same-table proof

Classes (active terms, from the matrix + code): **command/member-dependent** — `track/track_jnt/track_lat/track_roll/track_res/track_feet` (imitation vs member tables at the commanded phase), `r_fwd` (altwin path-axis vs member window), `r_yaw` (yaw_avg vs commanded rate), `r_lift/p_slip` phase-gated; **phase/member-dependent** — cadence/step-economy (`rp_*`, per-world tempo targets); **command-invariant regularization** — `d_act_rate/d_act_acc/p_flat/torque`; **survival** — `base/alive/fall_cost`. **Hard-fail scan: NO stale straight target found** — phase stayed inside the commanded member's window for all 24 worlds × all steps (asserted), the critic's `se` priv dim is member-safe by cancellation (`tpos[0]` cancels), and the arc diagonal collects FULL imitation income (a stale straight target would zero it). **Same-table proof:** trace-side ψ (profile cumsum) vs the env's `_cmd_psi_tab` — max diff **0.0**; member `tpos` vs `env.tpos` — **0.0**; t0 anchor 2.6e-8. The reward evaluator and `tor-v1.6.0` consume identical anchor/tangent/heading/command tables.

## 4. ACTUAL optimizer-minibatch proof — PASS

The candidate's own `--balance-kappa-minibatch` stratifier (verbatim semantics), instrumented post-sampling and post-wraparound on a BC-v2 closed-loop rollout batch (steady-state window; the 12S first-batch blindness respected): **24 minibatches × exactly 1365/1365/1365 straight/kP/kM samples, duplicated-sample fraction 0.0000 in every one, obs45 std 0.2498 in every one** (values {−0.306, 0, +0.306}), obs52/53 live (std 0.046/0.070), member IDs mixed (both straights populate the straight stratum). Strata pool healthy at steady state; all three classes present in every minibatch. Raw buffer counts were NOT used as proof.

## 5. Curvature-speed confound REMOVED

New member `ccref_vx015_c1p000.npz` (sha `7f45464d4409b00c`) = **stride_scale(straight base, 0.60) — the arcs' UNGEBENT SIBLING** via the committed canonical transform (`build_crown_command_family.py::stride_scale`, "recovered from build_f8_curved_members.py, applied verbatim in mechanism"): realized 0.153 m/s at cadence 1.0, κ=0, DFL PASS on plant v2 (SS P10 −15.7), oracle STRAIGHT (declared-boundary segmentation). Straight/left/right commands now differ **only in curvature** at constant speed and cadence; the fast straight (0.255) is retained as the speed sample. Honest label carried in the card: **`current-contract curvature-acquisition slice with mixed command exposure`** (cadence fixed; curved × multi-speed not yet crossed — not an independent manifold).

## 6. Command-salience receipt (BC seed v2, evidence-only)

BC v2 refit on the 4-member family (2,026 same-member reference-state pairs, va MSE 0.0105, seed sha `f44fb757f14e4145`). On identical states, κ ∈ {−2, 0, +2}: actor Δ(kP vs kM) per-joint up to **0.050** (hip-yaw/knee lead), Δ vs zero 0.063/0.060 L2; first-layer obs45 column |W| **0.101 vs 0.080 median** (above-median — v1's was median-rank: **the deconfound measurably forces command-reading already at BC**); predicted q_des delta up to **2.83°** (mean 1.0°). Short-horizon closed-loop: walks 8/8 under every command, κ_med 0.35/0.22/0.19 (S/P/M) — no signed steering yet, reported as evidence per ticket; correct-signed geometric response remains the declared post-PPO oracle qualification.

## 7. Launch-gate checklist + disclosures

Matrix diagonal ✓ · straight-loses-under-turn ✓ · no stale target ✓ · actual-minibatch contrast ✓ · same-speed controls ✓ · oracle regression **16/16** (sha fresh) ✓ · card-v2 fingerprint **`3ca9e0fe795eda80` regenerated twice, STABLE** ✓ · inspector staging/shutdown encoded ✓ · card assertions all PASS (4 members + BC-seed v2 + plant + candidate pins). Disclosed: (a) P/M diagonal `r_lift` asymmetry (2.97 vs 0.97/step; kM diagonal total 20.3 vs kP 22.3) — the historical seesaw's residual echo, flagged for eval-side watch; (b) `r_yaw` discriminates weakly at this slice's 0.306 rad/s (STAND|P earns 3.43 vs true-turn 3.86; σ 0.65 was selected for faster commands) — ordering does not depend on it, no in-unit retune; (c) BC data via injection machinery (training-data build); (d) first-pass matrix conflation lesson (§2).

## 8. Artifacts

`rgl/artifacts/c11s15k2_construction_20260805T164857Z/`: `k2a_matrix_rig.py` + `k2a_matrix_receipts.json` (full totals + per-term matrix + orderings + parity), `k2a_minibatch_bc.py` + `k2a_minibatch_bc_receipts.json` (24 minibatch rows + salience + closed-loop κ), `k2a_build_slow_straight.py` + member + oracle receipt, `labels_astar_concat_v2.npz`, card v2 (`launch_c11s15k2_mixed.sh`), dry receipts ×2. BC seed v2 in `rl/models/walking/k2_bc_arcprior_20260805T164857Z/`. Authorization request is now permitted under the ticket's own gate; the card still requires Joe's ratification of tor-v1.6.0 + Guard sentences.
