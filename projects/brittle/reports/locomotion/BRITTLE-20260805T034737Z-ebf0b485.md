---
id: BRITTLE-20260805T034737Z-ebf0b485
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15D
created_at: "2026-08-05T03:47:37.096750Z"
source_commit: 38eedc8
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15D_MANIFOLD_REWARD_AUDIT_20260805T034500Z.md
local_source_sha256: 70b4b68fbf1c717c931045f7a292f6b77d51334ed781590c636b48491d1221f3
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15D_MANIFOLD_REWARD_AUDIT_20260805T034500Z
mirror_bytes: 13670
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15D_MANIFOLD_REWARD_AUDIT_20260805T034500Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15D_MANIFOLD_REWARD_AUDIT_20260805T034500Z.md` |
| sha256 | `70b4b68fbf1c717c931045f7a292f6b77d51334ed781590c636b48491d1221f3` |
| brittle commit | `38eedc8` |
| lane | locomotion |
| unit | 12U-C11S15D |
| mirrored at | 2026-08-05T03:47:37.096750Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15D — Command-Manifold + Reward Audit: BOTH GATES FAIL → REWARD OR MANIFOLD BLOCKER (no PPO run)

**date:** 2026-08-05 · **unit:** 12U-C11S15D · **lane:** locomotion · read-only + bounded instruments; NO PPO, NO mutation, NO repair implemented
**constitution:** C1, C3, C4, C6, C7, C8, C12 · **potential violations:** none · **amendment proposed:** no
**status:** Phase 0A **FAIL** (no single command lineage — 9 catalogued defects, two with measured book-dominating magnitude). Phase 0B **FAIL** (complete-book pricing on the exact C11S15C card: the required ordering is inverted end-to-end — **exact commanded-turn execution is the worst-priced walking behavior in the book at −36.4/step; a fall-respawn loop is the best at +7.6/step**). Phase 1 era-bisection **NOT RUN** per the gate — and its premise dissolves under provenance: **the "July successful single-member walk-loop specialist" does not exist** (§1).
**classification (exactly one):** **REWARD OR MANIFOLD BLOCKER**
**recommend:** R1 — repair the two fence frames from the path tables (the correct machinery already exists in the `profile_mix` branch), then RE-RUN this 0B audit before any turn PPO; adopt Lane-B-class constant-κ walk-loop members at the same time (R2). Ranked list in §5. No silent repair-then-PPO — every repair is Joe-gated.
**decision:** Joe — choose repair path (R1 / R1+R2 / R3 / hold)
**next:** await ruling
**changelog:** row 39 `12U-C11S15D, 20260805T034500Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. Provenance correction (changes what "the July success" means)

The sha-verified record splits July into two different lanes, and the doctrine merged them:

| | **Lane A** `launch_arc_lane1.sh` (Jul-13) | **Lane B** `p12t_pteach/mteach` (Jul-23/24) |
|---|---|---|
| mechanism | `--yawhold-w 6 --drift-w 12 --arc-rsi 0.4`, err-ET dropped, **single** curved member | 6-member manifold (3 straight + 3 **constant κ=2.0**), **`yaw_dominant_w 8.0`** income, **imitation anchor** w0=0.15/10M-decay, BC-init from p12s, err-ET *scoped off* for curved worlds |
| reference | `curved_walk_kplus.npz` (κ≤0.323, ramping profile, +40° net) | `ref_concat.npz` = sha-verified `ccref8_tightturn` members (walk-loops, first non-DS frame 3, **no stand pads**) |
| result | mean netyaw **3–4°**, realized κ 0.045 vs 0.323 (≈14%), 2/3 seeds GATE_FAIL | mean netyaw **19–32° sustained**, P-teacher **CERTIFIED** (`ppo_6029312`) |

**C11S15C reproduced Lane A's mechanism — a July failure — and correctly reproduced its failure.** The 19–30° curve used as budget justification belongs to Lane B, a different construction class on the pre-v2 plant/law/contract. Additionally: the July "manifold never turned" evidence (`f8t3s0/arc_eval_baseline.json`, 1.0–1.4%) comes from a run whose resolved config contains **no curved member at all** — it could not have turned; it says nothing about manifold PPO. The C11S15 forensic's "2-era manifold negative" therefore loses its July leg (the C11S15 3-seed negative itself stands, but for a construction unlike Lane B: no anchor, padded members, yaw-avg income). **The actual successful July mechanism — Lane B — has never been tested on today's stack.** Memory corrected (§7).

## 2. Phase 0A — command-manifold alignment audit: FAIL

**Contract facts (walk54_v60, this card):** actor sees obs45 = per-world yaw-rate cmd (rad/s, signed, +CCW; from `yaw_cmd_profile[phase]`, `:1387/:2932` → GPU obs `kernels_tip:1454`), obs50/51 = sin/cos entrained phase, obs52 = vx cmd (m/s), obs53 = cadence/2 (Hz/2). Critic +6: path-frame planar err, z err, joint err, **raw-world-x** schedule err, and dims 58/59 = literal copies of 50/51. Data identities verified: `yaw_cmd_profile` integrates to the ref heading within ≤2.8° over 877 frames, and `vx×κ` matches the profile (median 0) except where the wrap clipped κ at ±2.6 vs the profile-implied 2.94 peak (metadata blemish; κ table unread by the trainer). kM tables are exact mirrors (±0.1194); `NEG_OBS` negates obs45 under mirror — data-level mirror coherence holds.

**Defect catalog (trainer `5c17a4338e21913f`):**

| # | defect | site | measured consequence |
|---|---|---|---|
| **D1** | obs52/53 are **episode-constant flag scalars**; the per-frame `cmd_vx`/`cmd_cadence` tables are **dead** (only reader is f8-gated) | `:1357/:1366`, writers `:4196/:484`, reader `:1150-1153` | policy cannot distinguish commanded stand from walk — obs53 advertises walk cadence through the 170-frame DS pads; every `cadence≤1e-6` scope (ET refresh, p_cap DS exemption, altwin stand scope) is dead code on this card |
| **D2** | `yawhold` expected heading = `cmd×(tick+1)×0.02` — the true integral **only for constant cmd**; unbounded hinge, w=6 | `:1888-1891` | at frame 300 expects **+222.5°** vs true commanded +21.6°; post-turn expects ~0° vs +120°. Measured: **−42.5/step on EXACT execution**, −23/step on straight-from-spawn, −65/step on injected mid-arc states — 2–6× the entire rest of the book |
| **D3** | `drift` uses the same bogus ψ, falls back to **raw world-Y** when all cmds≈0, branch flips on a **global `np.any`** across worlds, integrator not reset on arc-RSI teleports | `:2358-2368` | −10.2/step on survivors at frame 420; penalizes the arc's own Y displacement |
| **D4** | `altwin_ref` forward income compares **raw world-x** rates (robot and ref); `--entrain-arc` arc-length never reaches it; `--target-fwdrate` dead in this mode | `:1723-1724` vs `:1544`, `:1738` | on the turn the ref's x-slope collapses (cos heading) — "forward progress" is not path-tangent progress |
| **D5** | arc-RSI injects ref frames with **zero angular velocity**, `tick=f0` (fence detonates instantly), `_path_anchor=0` vs spawn-anchor convention split, two lead-in moduli (`%66` vs `%round(50/cad)`) | `:838-856, :2851-2857, :2878/:1239` | the 40% RSI training distribution is born into −64.65/step; two path-frame conventions coexist in one batch |
| **D6** | CPU r_yaw targets a **fixed scalar** `--target-yawrate` unless manifold mode | `:1666` vs `:4273` | third yaw-command source (inactive weight here; lineage split) |
| **D7** | `p_cap` DS-scoped stand exemption is f8-gated → dead | `:2428` | commanded DS pads tax the **correct** behavior −13.1/step (teleport row) |
| **D8** | critic `se` raw world-x vs path-frame `pe`; dims 58/59 duplicate 50/51 | `:1348/:1351/:1352` | critic sees two frames for one concept; 2 dead dims |
| **D9** | no reference-index mirror operator exists; `MirrorReg` gets no phase rotation in this contract (dormant here — w=0) | `mirror_contract`, `:4718` | latent hazard for any mirrored-lane work |

**Coherence probes:** κ=0 reproduces the straight book **bit-identically** (registered evidence: plain/start-stop fingerprints `59d49206`/`79556c38` reproduced under the conditional contract blocks). ±κ mirror economics symmetric (mirrored subject rows price equal within noise; both sides equally broken by D2/D3). Speed/cadence probes are answered structurally by D1: **the tuple (vx, κ, cadence) does not exist as a live command manifold** — vx and cadence are episode constants, κ exists only as the phase-slaved profile. `yaw_rate = vx×κ` holds in the data, is enforced nowhere, and is consumed by three different conventions (obs45/GPU rate; yawhold cmd×t heading; drift ψ frame). **Phase 0A gate: FAIL.**

## 3. Phase 0B — exact-card reward competition: FAIL

Rig: exact C11S15C kP construction (fingerprint asserted `09668c2e`; live attrs verified: profile_mix False, yawhold 6, drift 12, latch engaged as in training — card force-latches at iter 1), milestone policy, 64 worlds × 8 subjects, per-step `last_terms` capture (ledger reconciles after excluding the raw diagnostics and track sub-groups), closed-form fence recompute cross-checked. Subjects on fresh spawns (H=660, the training truncation) and true ref-state injections at frames 300/420. Empirical turn direction identified by per-dim finite difference (knee/hip-pitch asymmetry dominates short-horizon yaw; dim9 −12.7°/0.2s).

**Return-rate per step (undiscounted; discounted γ=0.99 preserves every ordering):**

| subject | FRESH | MID-300 | MID-420 |
|---|---|---|---|
| **exact continuation** (teleport-follow) | **−36.4** (track +10.1, r_fwd +7.0, r_zt +2.0 vs yawhold −42.5, p_cap −13.1, drift −4.0) | — (same mechanism, fence −45..−92 through window) | — |
| milestone straight | **−9.4** (survives 660, yawhold −23.3) | **−56.0** (yawhold −64.7) | −47.7 (drift −10.2) |
| hip-yaw-zeroed straight | −9.0 | −57.1 | −47.2 |
| +0.10 correct turn | −10.9 | **−57.3 (< straight)** | −14.0 (dies early) |
| +0.20 correct turn | **+7.6 — best subject: falls, respawns into the benign pad, loops** | −38.3 (dies) | −10.5 (dies) |
| −0.10 wrong sign | −9.5 | −56.6 (> correct +0.10) | −47.5 |
| −0.20 wrong sign | −11.5 | −57.4 | −12.6 (dies) |
| stand (a=0) / spin | −55/−56 (p_cap −57.4) | −44.4/−44.8 (dying beats surviving) | −37.7/−38.7 |

**Gate clauses, measured:** required `exact > partial > straight > wrong-sign/exploits` — actual `fall-loop (+7.6) > straight (−9.4) > partial-correct (−10.9) > exact (−36.4) > stand/spin (−55)`; at injected mid-arc states **dying (−38..−44) strictly beats every form of surviving (−56..−57)**, and correct-sign turning prices BELOW straight and below wrong-sign. **Local gradient:** d(return)/d(correct-sign ε) at mid-arc = −1.3/step (wrong direction); at 420 apparent "improvement" at |ε|≥0.1 is earlier death, not turning. The only usable gradients point toward falling. **Dominant term:** yawhold — an unbounded hinge at 2–6× the whole book. **Phase 0B gate: FAIL.**

**This retro-explains every C11S15C observation as correct optimization of a broken book:** the from-spawn start wall (command channels incoherent through the pads: obs says walk, ref says stand, p_cap taxes the stand); falls emerging ≥5.2M (the fall-loop is the book's best return); the 8.4M mid-arc survival collapse to 14% with wrong-sign drift (dying at injected states IS optimal; fall_cost 50 once ≪ −65/step sustained); response ≈0 at every checkpoint (no channel ever paid turning).

## 4. Decision

**REWARD OR MANIFOLD BLOCKER** — both pre-PPO audits failed; per the ticket no PPO was run, kM stays held, no curriculum, nothing promoted. The era-bisection is additionally moot as specified: its premise ("July's successful kP walk-loop" for THIS mechanism) does not exist — Lane A failed in July with these exact channels, and the fences it relies on are provably frame-incoherent for any time-varying profile.

## 5. Ranked repair options (C7 ordering; all Joe-gated, none implemented)

1. **R1 — fence-frame repair (root cause, smallest diff):** expected heading and cross-track from the **path tables** (ψ(phase), normal-projected error vs the member path — exactly what the `profile_mix` branch already computes at `:2354/:1888` for manifold lanes), wired for single-member `cmd_profile` lanes; plus D5's injection fixes (inject ω from the profile, reset `_prev_y`/`_xtrack`, one anchor convention). **Mandatory re-run of this 0B audit after repair, before any PPO.**
2. **R2 — Lane-B-class references:** constant-κ walk-loop members without stand pads (the `ccref8_tightturn` pattern, rebuilt on today's plant if reused). Makes cmd×t coherent *by data*, removes the pad/start wall and the D7 pad tax, and matches the only construction class that ever produced sustained signed turning. Natural companion to R1.
3. **R3 — full Lane-B recipe reconstruction on today's stack:** manifold (straight + constant-κ members) + `yaw_dominant_w` income + imitation anchor from a certified straight parent. Largest change; justified if R1+R2 specialist lanes still stall. Note: C11S15's manifold negative tested a *different* manifold (padded members, yaw-avg income, no anchor) — Lane B proper is untested on today's stack.
4. Not implicated by evidence: plant, control law, action interface, obs widths — the book was never coherent on today's stack, so nothing yet incriminates the physics layer.

## 6. Instrument fidelity + limitations

Rig deviations, disclosed: obs52 read 0.31 (adapter default) vs the card's 0.2197 — affects only policy input, no reward term reads it; conclusions at these magnitudes are unaffected. Teleport-follow scores the live reward path from per-frame injected states (one dynamics step per frame); per-step means include post-respawn segments (that is the point for return-rate comparison; `mean_alive_steps` reported per row). Deterministic policy, 64 worlds, single realization — margins here are 5–60/step, far above the eval noise floor. Scripted exploit subjects (spin) are coarse; their placement (dominated while alive, better when dying) is robust to realization detail. Closed-form fence recompute is a verbatim port of `:1879-1891`/`:2344-2371` and reconciles against the captured aggregate.

## 7. Artifacts + corrections

`rgl/artifacts/c11s15d_audit_20260805T034500Z/` — rig (`c15d_rig.py`), turn-direction probe, four pricing passes (full per-term JSON). Provenance + lineage scout reports summarized in §1–§2 (sha-pinned paths inline). Memory corrected: the specialist-then-distill doctrine's mechanism attribution (Lane A ≠ the success), the f8t3 "manifold baseline" (straight-only run, void as evidence), and the C11S15C-KP verdict's "both July mechanisms" claim (only Lane A was tested; Lane B remains untested on today's stack).
