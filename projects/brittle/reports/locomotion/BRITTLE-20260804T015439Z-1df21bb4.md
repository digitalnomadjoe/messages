---
id: BRITTLE-20260804T015439Z-1df21bb4
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S8
created_at: "2026-08-04T01:54:39.542097Z"
source_commit: ca38ba2
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S8_STEP_ECONOMY_CALIBRATION_20260804T015332Z.md
local_source_sha256: cc6bdfb25d9a704c66899087cc4faf0c68a7b1f6bae504df6df78e452ca8a486
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S8_STEP_ECONOMY_CALIBRATION_20260804T015332Z
mirror_bytes: 6250
---
# UNIFIED_LOCOMOTION_PHASE12UC11S8_STEP_ECONOMY_CALIBRATION_20260804T015332Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S8_STEP_ECONOMY_CALIBRATION_20260804T015332Z.md` |
| sha256 | `cc6bdfb25d9a704c66899087cc4faf0c68a7b1f6bae504df6df78e452ca8a486` |
| brittle commit | `ca38ba2` |
| lane | locomotion |
| unit | 12U-C11S8 |
| mirrored at | 2026-08-04T01:54:39.542097Z |

<!-- brittle:mirrored-content -->

# 12U-C11S8 — Alternation-Safe Step Economy Calibration

**date:** 2026-08-04 · **unit:** 12U-C11S8 · **lane:** locomotion (brittle-telemetry) · no PPO, no Guard re-registration
**constitution:** C1, C2, C3, C6, C7, C12 · **potential violations:** none · **amendment proposed:** no
**status:** ALL FOUR SECTIONS COMPLETE AND LANDED — residual gate closed, alternation latch wired and proven, global gate audit clean, **unique least-aggressive weight selected and landed: `rp_cadence_rew_w` 1.5 → 3.0 (the only weight moved)**.
**recommend:** the straight-walking step economy is now live, alternation-safe, and calibrated — next natural step is the R5/R6 adjudication (pelvis term + anneal/track) and then the PPO ticket with construction re-registration.
**decision:** Joe — accept the calibration; rule on what enters the next PPO unit
**next:** await ruling

---

## 1. Residual gate closed (`r_single_support`)

Kernel condition `in_ss and (abs_c >= 0.10)` → `in_ss and ((abs_c >= 0.10) or (rp_dirfactor_off > 0.5)) and (alt_state == 1)` — straight-aware via the same one-book flag, and alternation-latched so a one-legged balancer/hopper cannot harvest SS time income. Measured: walking **+0.00116/step** (was +0.0000), standing/slide/chatter **0** (no SS / no qualified events). Turn-only behavior unchanged (flag scoped as before).

## 2. Alternation latch (same-foot hopping cannot earn gait-cycle income)

New per-world state (reset −1 / 1): `last_real_td_foot` (last QUALIFIED touchdown foot) + `alt_state` (1 = alternation holds). Release rules landed in the kernel:

- **airtime**: pays only on a qualified touchdown with `alt_ok` (foot ≠ previous qualified foot);
- **cadence** (normalized branch): release additionally requires `step_ok and alt_ok` — periods are still MEASURED per foot (`ft_cad_valid`, `ft_last_td_t` untouched);
- **ss_hold**: SS-exit payout gated on `alt_state == 1`;
- **single_support**: accrues only while `alt_state == 1`;
- `alt_step` was already inherently alternating; the real-step ET clock stays UNGATED (a hopper is stepping — it survives the clock but earns no gait-cycle income; measurement vs payout separation).

**Tests (measured + kernel-logic mirror):**

| trajectory | alternating economy |
|---|---|
| correct alternating walking (s2-final, 64 worlds, live kernel) | UNCHANGED by the latch (alt +0.00181, ss_hold +0.00176, airtime +0.00036 — byte-similar pre/post) |
| same-foot hopping | first event only, then `alt_state=0` → 1/20 events pay, cadence 1/20 |
| repeated same-foot touchdown | blocked while repeating; recovers on re-alternation (4/6) |
| toe-tap hop (unqualified taps) | taps cannot flip the QUALIFIED register → still 1/20 |
| standing / sliding / no-lift chatter | 0 events → 0 (measured standing; structural slide/chatter at the 0.30/0.15 s/21 mm gates) |

## 3. Global `rp_dirfactor_off` gate audit

Channels affected by the flag: exactly the step-shaping set {r_alt_step, r_airtime, r_ss_hold, r_cadence-legacy, r_single_support (via this unit's condition), ss_inplace anchor semantics}. Before/after at zero yaw command (64-world means, per-step):

| channel | before | after | | turn/yaw channel | before | after |
|---|---|---|---|---|---|---|
| r_alt_step | 0 | +0.0018 | | r_wrongway, r_yaw_integ, r_yaw_rate | 0 | **0** |
| r_airtime | 0 | +0.0004 | | r_step_yaw (s_cmd=0) | 0 | **0** |
| r_single_support | 0 | +0.0012 | | p_wrongdir, p_pivot | 0 | **0** |
| r_ss_hold | 0 | +0.0018 | | r_shift_step/acq, r_ds, r_under_com, r_com_* | 0 | **0** |
| r_cadence | 0 | +0.000025 (at the 0.64 s gait; ×30 at target) | | r_yaw (yaw-HOLD, never dir-gated) | +0.0034 | +0.0034 unchanged |

**No turn-only, wrong-direction or yaw channel became active.**

## 4. Weight calibration — unique selection

Sweep: common scale w_c × w_event over {1.5,3,6,12,24,48} × {1.5,0.75,0.375}, income models CALIBRATED to the measured per-channel values (event-channel calibration factor 0.023 anchors the model to the live kernel's DT/qualification accounting). Subjects: exact c344 (analytic on its 17 events, SS-frac 0.703), s2-final (measured), crown-period row, hopping (latched ≈ 0), standing/sliding (0), early fall (c344-like ×50).

Selection criteria → results:

- prefers c344 cadence over C11S5: every w_c (channel ratio ~30×) ✓
- **usable gradient toward slower cadence** (declared threshold: total step-channel income gain 0.64 s → 1.33 s ≥ 0.05/step): w_c=1.5 gives +0.027 **FAIL**; **w_c=3.0 gives +0.056 PASS**; higher w_c also pass
- alternating walking outranks hopping: any w under the latch (hop earns ≤1 event total) ✓
- step-shaping ≤ 10% of intended book: 0.3% at w_c=3 (cap reached only far above 48) ✓
- no touchdown payout spikes: max single-event ledger payout 0.04 at w_c=3 (bound 0.5; w_c=48 fails at 0.638) ✓
- C3 preserved on all four bases: slide/stand/chatter gain exactly 0 → S1−S2 per-step margin 3.574 → 3.637 (widens); S5-aggressive gains as S1 (same reference events) → discounted S1−S5 thin margin preserved (+34 → +34.7); full-episode widens ✓

**Unique least-aggressive pass: `rp_cadence_rew_w` = 3.0 with ALL event weights unchanged** (the scan in least-aggression order — w_c ascending, w_event at its current value first — has exactly one first-pass). Landed as the C11S4J-pattern trainer append; full-chain dry-run: resolved diff vs C11S7 = exactly {`rp_cadence_rew_w`: 1.5 → 3.0}, fingerprint `9e1902d0895867f5`.

**Final regression at the landed configuration** (64 worlds): cadence +0.000025/step (doubled with the weight, still priced 30× down at the wrong cadence — the gradient, not a windfall), alternation channels byte-similar, 0 stalls / 0 falls.

## 5. Provenance

Trainer `fddcd358d453105f`, kernels_tip `7d8b237a27925989`, batch_sim_tip `42581f723cea7a24` (kernels/batch_sim are canonical instruments, not bound members; trainer supersedes the bound pin — re-registration remains pending, deny-by-default correct; no PPO). Artifacts: `rgl/artifacts/c11s8_step_economy_20260804T015332Z/` (dry-run resolved config, pre/post/landed recordings, measurement json). Plant, law (`7c05558a` effective), reference, observations, action contract: untouched.
