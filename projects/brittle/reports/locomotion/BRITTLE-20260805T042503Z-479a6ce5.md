---
id: BRITTLE-20260805T042503Z-479a6ce5
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: 12U-C11S15E
created_at: "2026-08-05T04:25:03.887624Z"
source_commit: 2af6eaf
local_source_path: /home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15E_MANIFOLD_REPAIR_20260805T041500Z.md
local_source_sha256: 86fc30fe15cdf70c1566520db16c4c0786b3f5e2e32e703a3b8c1d56a1df0d95
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: UNIFIED_LOCOMOTION_PHASE12UC11S15E_MANIFOLD_REPAIR_20260805T041500Z
mirror_bytes: 10821
---
# UNIFIED_LOCOMOTION_PHASE12UC11S15E_MANIFOLD_REPAIR_20260805T041500Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/UNIFIED_LOCOMOTION_PHASE12UC11S15E_MANIFOLD_REPAIR_20260805T041500Z.md` |
| sha256 | `86fc30fe15cdf70c1566520db16c4c0786b3f5e2e32e703a3b8c1d56a1df0d95` |
| brittle commit | `2af6eaf` |
| lane | locomotion |
| unit | 12U-C11S15E |
| mirrored at | 2026-08-05T04:25:03.887624Z |

<!-- brittle:mirrored-content -->

# 12U-C11S15E — Command-Manifold Integrity Repair: all 8 repairs LANDED + verified; 0B verdict FAIL on one named gap (missing yaw-following income channel)

**date:** 2026-08-05 · **unit:** 12U-C11S15E · **lane:** locomotion · out-of-tree candidate only; NO PPO, no reference replacement, no adoption; canonical trainer untouched (pin `5c17a4338e21913f` verified before every build)
**constitution:** C1, C2, C3, C6, C7, C12 · **potential violations:** none · **amendment proposed:** no
**status:** candidate trainer built (17 anchored edits, every anchor unique-matched against the pinned source) implementing all eight required repairs; the full C11S15D battery was rerun on the current C10Q kP member. **Every C11S15D catastrophe is eliminated** — exact commanded-turn execution now prices TOP (+19.0/step vs −36.4 before), fences read 0.00 on correct behavior through ramps/turn/post-turn, pads are truthful and untaxed, falling prices BOTTOM. One required clause fails: **no measurable local return-gradient toward correct turning on the kP side** — not a conflicting channel (none remains), a **missing income channel**: nothing in the book PAYS yaw-following, so the economy is flat in the turning direction outside the tracking tube.
**decision (exactly one):** **FAIL** — remaining gap identified precisely (§5): the book needs one bounded yaw-following INCOME term with mid-range reach; two dormant candidates already exist in code (`yaw_dominant_w` — the Lane-B channel; GPU `r_yaw` with widened σ). Enabling either is a Joe-gated book change.
**recommend:** next unit = enable exactly ONE income channel on the candidate (recommend `yaw_dominant_w`, the only channel with a measured historical success — Lane B) → re-run this exact battery → if the gradient clause passes, freeze candidate pins for the kP probe.
**next:** await ruling
**changelog:** row 40 `12U-C11S15E, 20260805T041500Z` · csv sha in digest (13-col verified, trees identical)

---

## 1. Candidate construction (out-of-tree, immutable pins)

`rgl/artifacts/c11s15e_repair_20260805T041500Z/`:
**candidate** `train_tip_reftrack_CANDIDATE.py` sha256_16 **`80c4d982d0219290`** · **diff vs pinned trainer** `CANDIDATE.diff` (311 lines) `dd6832c8aec4809d` · **builder** (anchored, uniqueness-asserted, re-runnable) `c15e_build_candidate.py` `1e85c21efd0d789b` · **validation rig** `c15e_rig2.py` `35ef2e42fabf48a9` · 8 validation JSONs. All edits are gated on the new `_cmd_*_tab` state — **lanes without live tables (plain, f8, manifold, obs-v3/v4) take the original code paths bit-unchanged**.

| # | required repair | implementation (candidate lines ≈ pinned+Δ) | verified by |
|---|---|---|---|
| 1 | live command obs | one per-step refresh (`_c15e_refresh_live_cmds`) drives `vx_cmd`/`cadence_cmd` from the member's `cmd_vx`/`cmd_cadence` tables (× retime); called from the phase advance, reset, and end-of-step (post-respawn freshness) | obs52/53 == table[phase] exact (`table_absmax_err 0.0`); **pads read 0/0**; widths 54/60 unchanged |
| 2 | single manifold | one source: the member's command block — profile (yaw), its integral `_cmd_psi_tab` (heading), `tpos` (path), `cmd_vx`/`cmd_cadence`, contact-intent (DS state); identity `yaw_rate = vx×κ` wrap-verified in data; competing conventions removed (cmd×t frames, raw world-X/Y, fixed-scalar targets, dead tables) | §2 tables + code |
| 3 | heading reward | `yawhold` expected heading = `_cmd_psi_tab[phase] (+ loop θ)`; hinge **capped at 2.0/step** (fences are tie-breakers — the C11S15D death-dominance came from unbounded hinges) | exact-follow yh = **0.00** through the entire ±120° schedule; near-ref −0.3; divergent capped −2 |
| 4 | drift reward | **stateless** cross-track along the commanded path normal, same anchor+`tpos` frame as track/pe; world-Y fallback and cross-world `np.any` branch gone; nothing to reset at RSI; capped 1.5/step | exact-follow ≈ 0; no −10/step world-Y blowups (D's MID-420) |
| 5 | forward income | `altwin_ref` progress projected on the commanded path TANGENT, priced vs the ref's own chord rate; reduces **exactly** to the legacy world-x form on straight members (ψ≡0) | fwd income persists through the full turn (teleport +5.6..+7.2/step); straight-member reduction run |
| 6 | RSI init | `_bent_state` injects the commanded yaw rate (ω was 0); one anchor convention (`_path_anchor=q−tpos[f]≈0`, θ/aw state reset); lead-in modulus unified (native-cadence-derived, division-safe); tick=phase is now benign (fences are phase-keyed, the tick-detonation is structurally gone) | injection probe: reward first-3-steps ≈ steady (no spike); τ first-3 ≤ 11.5 one-step transient vs steady P99 6.3, under the 12.5 clip |
| 7 | DS/termination scoping | every `cadence≤1e-6` scope now LIVE (obs, altwin stand-scope, ET stall-clock hold, cadence-income sp_w on transitions); `p_cap` DS exemption keyed on live command state **including the member's contact-intent tables** (the C10Q walk-commanded DS preparation frames 58–169 are commanded behavior, not perch) | pads pay no tax on correct behavior (exact-follow p_cap −13.1 → −2.1 residual SS-dwell); perch-at-commanded-SS still taxed (−33.5/step, floored) |
| 8 | critic coherence | priv (critic-only, widths kept): 57 raw-world-x → **along-track error**; 58/59 (duplicates of 50/51) → **cross-track error + heading error (rad)**; candidate semantics documented in-code | by construction; actor contract untouched |

## 2. Battery results (same rig protocol as C11S15D; episode-truncated accounting primary — it is what PPO optimizes; the D-report's respawn-mixed rates kept as secondary)

**Return-rate per step, episode-truncated (kP200, milestone policy, 64 worlds):**

| subject | D (old book) | **E candidate** | note |
|---|---|---|---|
| exact continuation (teleport-follow) | **−36.4 (worst walking)** | **+19.0 (best)** | fences 0.00; net yaw +118.8° ≈ schedule; τ12.5 rows are a teleport-instrument artifact |
| walking straight through the command (fresh, kicked) | −9.4 | **+12–13, 660 steps fall-free** | positive but **−6/step below exact** — the acquisition incentive |
| partial correct turn (mid-arc ±ε probes) | < straight, < wrong-sign | ≈ straight (kP: −0.1..−0.25; **kM: +1.0, fully ordered**) | §5 |
| wrong-sign turn | ≈ straight | ≤ straight (kM monotone worse; kP at ε=0.10 worse than correct) | |
| stand/spin exploits at commanded walk | stand +alive, lightly taxed | **−37..−38/step, ET-terminated** | perch cap correct |
| fall / fall-respawn | **best available (+7.6 mixed; dying beat surviving at mid-arc by 8–12/step)** | **worst** (mid-arc: die ≈ −50+small vs survive +1400..+2100/episode) | death-dominance eliminated |

**Mid-arc injected states (the arc-RSI training distribution):** D-book survival was −56/step (dying optimal); candidate: milestone survives 200/200 steps at **+7.7 to +10.5/step**, fences −1.8 to −1.9 (capped, correct-frame), p_cap ≈ 0, no injection discontinuity. The C11S15C "trained-to-die-at-injection" economics cannot recur on this book.

**Manifold probes:** obs52/53 table-exact incl. zeros on pads (repair 1); κ=0 straight member: fences reduce to the legacy straight semantics (yh* −0.01, drift = y-fence by construction); **speed ×1.25** member: yaw-rate command scales ×1.25, κ table-delta 0.0 (curvature preserved); **retime ×1.1**: (vx, cadence, yaw-rate) scale together, implied κ unchanged (obs-verified 0.2416/0.4136); no fixed world-axis term remains in any command-dependent channel (all path-frame; evidenced by zero fences through a full 120° world-frame rotation). Mirror: kM tables are exact mirrors (±0.1194); fence recomputes symmetric (kP −1.77/−1.78 vs kM equivalents); the kP↔kM subject differences trace to the milestone gait's own chirality, not the book.

## 3. What the fresh-spawn table shows about the start wall

With truthful pads the fresh episode is now: commanded stand (0–57, exempt, ET-held) → walk-commanded DS preparation (58–169, exempt under intent-scoping) → commanded walk. The milestone still cannot START on this member's geometry (62% topple at onset — the C11S15C wall is a start-skill/geometry gap, not a book artifact), while a minimal start-kick (any small asymmetric offset) walks the full 660 steps fall-free at +12–13/step. The book now rewards starting and walking; the start skill itself remains a policy/reference-geometry matter (C11S14 machinery or member-side DS-exit blending — out of this unit's scope).

## 4. Known caveats (documented, none blocking)

Teleport-follow forces per-frame velocity discontinuities → τ at the 12.5 clip in that instrument only. obs45 keeps the trainer's pre-existing one-step command lag (kernel reads the cmd written at the previous step's end — unchanged semantics, now documented). Pre-C15E members can carry non-truthful `cmd_vx` tables (the C14 straight member's is ~0 — its era fed vx from the f8 schedule): **candidate adoption needs a member-table audit or a declared fallback** for legacy members. Respawn-mixed accounting still makes post-death farming look mild — the primary episode-truncated metric (what PPO optimizes) prices it correctly; noted for future 0B protocols.

## 5. The FAIL clause, precisely

Required: "correct-sign local perturbations increase total return." Measured with two probe families (measured turn-direction joint offsets ±ε; action-blend λ toward the reference intent) at two mid-arc frames on both members: **kM PASSES** (+1.0/step at ε=0.05–0.10, monotone both directions; blends +5–8 return at λ≤0.10); **kP FAILS** (flat-to-slightly-inverted: every probe's tracking cost exceeds its yaw gain). Root cause is structural, not a conflict: after repair, the only turning signals are the two **capped fences** (silent beyond ~38° error — caps are what killed death-dominance) and **track/fwd** (gated to a tube all non-exact subjects exit within ~1.2 s at κ=2.0). **Nothing pays yaw-following as bounded positive income with mid-range reach.** The gap between straight (+12) and exact (+19) exists but has no measured climbable slope on kP from out-of-tube states. Dormant channels already in code: `yaw_dominant_w` (Lane B's certified-success channel, weight 0 here) and GPU `r_yaw` sharp_norm (σ too narrow to reach rate-0 from cmd 0.645). Enabling one is the recommended next unit — a book change, Joe-gated, followed by a mandatory re-run of this battery.

## 6. Artifacts

Unit dir `rgl/artifacts/c11s15e_repair_20260805T041500Z/` (candidate + diff + builder + rig + 8 pricing/blend JSONs + speed-probe member). Canonical stacks untouched; no Guard mutation; no PPO. Prior-unit evidence: `c11s15d_audit_20260805T034500Z/`.
