---
id: BRITTLE-20260829T001752Z-61d61d73
kind: report
project: brittle
from: locomotion
to: reviewer
lane: locomotion
unit: WALK-DR06H3D-RC1
created_at: "2026-08-29T00:17:52.856442Z"
source_commit: 3586e964
local_source_path: /home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06H3D_RC1_REQUIRED_ITEMS_20260829T001651Z.md
local_source_sha256: d5cc8af97ced5d22888b9d9b0836c62d24b9c6d29bf9a4843bbcdf2015f70e5c
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: GREEN_OPUS5__WALK_DR06H3D_RC1_REQUIRED_ITEMS_20260829T001651Z
mirror_bytes: 9604
---
# GREEN_OPUS5__WALK_DR06H3D_RC1_REQUIRED_ITEMS_20260829T001651Z

| field | value |
| --- | --- |
| local path | `/home/robojoe/code/brittle/rgl/reports/GREEN_OPUS5__WALK_DR06H3D_RC1_REQUIRED_ITEMS_20260829T001651Z.md` |
| sha256 | `d5cc8af97ced5d22888b9d9b0836c62d24b9c6d29bf9a4843bbcdf2015f70e5c` |
| brittle commit | `3586e964` |
| lane | locomotion |
| unit | WALK-DR06H3D-RC1 |
| mirrored at | 2026-08-29T00:17:52.856442Z |

<!-- brittle:mirrored-content -->

# TEAM: GREEN — Opus 5 · WALK-DR06H3D-RC1 — **REQUIRED ITEMS.** You must supply **one decision and one sentence**. Everything else in the five-step sequence is my work. Sentences 2–5 need nothing: I ran the full `permits.authorization_reasons` gate on each — including the live tracked-blob re-check at the current `campaign_commit` — and all four returned **zero refusals**. Do not retype them. The single genuine gap is that **no sentence anywhere authorizes leaving `15-C15S23O`**, and the one I recorded (`D-f7c8cd92122f`) names the stale origin, so it is inert. Two things changed under me while I worked and both are corrected here rather than assumed: the campaign trust root moved to **`8fa54c8f`** (my earlier `8a23c93f` is three commits back) and **BLUE holds a committed, un-cashed sentence** — *"I authorize transition to 15-C15S23Q-RC1 from 15-C15S23O."* — which **dies on contiguity the moment GREEN transitions out**. There is exactly one active unit campaign-wide. That priority call is yours and no gate can make it. An adversarial audit of all five steps found **three further blockers beyond sentence 1** — all of them mine to clear, none needing your words. **Zero optimizer steps. Nothing registered, nothing launched.**

**unit:** WALK-DR06H3D-RC1 (not yet effective) · **ts:** 20260829T001651Z · **team:** GREEN (ticket header: Opus 5; executing session: Claude Fable 5)
**constitution:** C1, C2, C6 · **potential violations:** none · **amendment proposed:** no
ZERO PPO. No target registered. No run basis opened. No launcher executed. No Guard state mutated since `D-f7c8cd92122f`. No other lane's work touched.

---

## 1 · Live state, re-derived at the moment of writing

This is the step I skipped last time, which is why your sentence 1 went stale. Every value below was read at `20260829T001651Z`, not carried forward:

| | |
|---|---|
| `effective_unit()` | **`15-C15S23O`** — reasons: none |
| `campaign_commit` | **`8fa54c8f0443680181e838f5fb7e0593a0463dd5`** |
| `brittle-telemetry` HEAD | `82f0249986cdceb26c79560a282edbcaec50d87a` |
| anchor status | **STALE** — state root `ed63a5250a424540` matches no anchor; latest is `9cbd2976f167c815` (seq 1) |
| decisions under `15-C15S23Q-RC1` | **0** — BLUE's sentence is committed but un-cashed |

## 2 · What I need from you — two items, nothing else

### ITEM 1 — a priority decision (not a Guard requirement; no gate can make it)

BLUE has a committed, un-cashed authorization at `campaign_commit`:

> `rgl/owner_rulings/15-C15S23Q-RC1_OWNER_AUTHORIZATION_20260828T233724Z.md`
> *"I authorize transition to 15-C15S23Q-RC1 from 15-C15S23O."*

I verified it **passes** the full authorization gate at unit `15-C15S23O` right now. The campaign has exactly one active unit — there is no lane or branch concept anywhere in `rl/guard`. So **whichever lane transitions first kills the other's sentence on contiguity**, and the loser needs a fresh one from you.

Choose: **GREEN's RC1 diagnostic takes the unit now**, or **BLUE's C15S23Q-RC1 goes first** and RC1 waits.

### ITEM 2 — one sentence, only if you choose GREEN

> I authorize transition to WALK-DR06H3D-RC1 in the acquisition lifecycle, from 15-C15S23O.

Validated against the live Guard before handing it to you — the check I failed to run last time. `permits.authorization_content_reasons(text, 'transition', '15-C15S23O')` → `[]`; destination-token match → `True`; zero negation hits. A control proof was run against a real committed sentence of identical shape (BLUE's) through the **full** `authorization_reasons` path including the source-document re-check → PASS.

**Sentences 2, 3, 4 and 5 need no re-issue.** All four verified against the committed ruling file at `campaign_commit 8fa54c8f` → zero refusals each.

## 3 · What I do myself — the three further blockers the audit found

None of these need your words. I list them so the cost is visible rather than discovered later.

1. **Trust-root move.** The corrected sentence must exist in a file tracked *at* `campaign_commit` — the Guard reads owner verbatim via `git show <campaign_commit>:<path>` and refuses paraphrase (`ledger.py:683`). `targets.set_campaign_commit` is **dead** on this store (mutation ledger adopted → unconditional refusal); the surviving writer is `campaign-bootstrap-commit`, which requires `target_ref == source_ref`. So: write the sentence into the ruling file, commit, bootstrap to that commit.
2. **D-11 bound-construction gate — real and armed.** Production execution requires exactly one CANONICAL `construction` subject whose file-bearing members include the launched script. No `bound-construction-walkdr06h3d-rc1` exists; the nearest one lists a *different* launcher. The audit reproduced both live refusals. I register the construction alongside the launcher.
3. **STALE anchor + witness.** `WITNESS_ADVISORY=False`, so a stale anchor is a hard refusal on steps 3 and 5 — and it **re-stales after every ledger append**. Also: `BRITTLE_GUARD_WITNESS` is absent from my tool shell (it lives in `~/.bashrc`, a login shell), which would silently fall back to an in-tree witness and refuse anyway. I set it explicitly and re-anchor before each gated step. Steps 1, 2 and 4 are *not* anchor-gated — the transition writer never calls `authority`.

Plus: pre-create the output directory empty (a missing dir raises *after* the RUNNING record is appended, orphaning the basis and burning the retry), and register a `manifest-walkdr06h3d` member so D-11 actually re-hashes the RC1 manifest rather than the DR06G one.

## 4 · Two routes I found and am not taking without your explicit say-so

An automated security review flagged both. I am surfacing them rather than quietly using or quietly discarding them.

**(a) A shorter authorization form.** `rl/guard/flow.py` (GUARD-UX01) accepts a two-grant handoff sentence typed as a whole chat message, minted from the session transcript with **no committed file and no trust-root move**. I measured it passing every check with zero refusals. I am not proposing it: it mints production PPO authority on weaker evidentiary footing than the committed-file route intends, and the review flagged exactly that. Say the word if you want it; otherwise Item 2 stays the route.

**(b) The construction target is never cross-checked against your words.** `authorization_content_reasons` validates only the action phrase and the unit — `subject_refs` are agent-declared and never compared to the sentence. So I *can* register `bound-construction-walkdr06h3d-rc1` under sentence 3, which names only the launcher. In fairness this is the established practice here, not a novel loophole: `D-db30792c1294` (H1), `D-f50caf0e3927` (H2), `D-114f86380620` (H3A) and `D-daeb1d72884b` (DR06G) all did exactly this, and none of those owner rulings mention a construction. I intend to follow that precedent. If you would rather your sentence name the construction explicitly, tell me and I will wait for it — it is a courtesy the Guard does not enforce, and your call whether the precedent is one you want continued.

I am also **not** using `--mode diagnostic`, which bypasses D-11 entirely. That would dodge the gate and leave sentence 5's authority unused.

## 5 · Disclosures

1. **My sentence-1 defect, restated plainly:** RC1 report §6 published a from-unit captured hours earlier instead of derived at publication. You transcribed what I gave you. The rule I already apply to `action_scale`/`kp`/`kd` — read at the moment of use — now applies to Guard unit state, and §1 of this report is that rule being followed.
2. **The premise moved twice.** `campaign_commit` advanced `42ac4a11 → 8a23c93f → 8fa54c8f` and HEAD is now `82f02499`. Any plan built on `8a23c93f` — including parts of my last report — was planning against a store that no longer exists. Corrected above.
3. **`D-f7c8cd92122f` remains ACTIVE and inert** under unit `WALK-DR06H3A-RC3`. CP-07 binds it there, so it authorizes nothing while `15-C15S23O` is effective. I left it rather than mutating the ledger to tidy it.
4. **Three of four audit agents were wrong on two points**, corrected by the critic and re-verified by me: the STALE anchor blocks only steps 3 and 5, not the transitions; and the "sixth sentence for the bound construction" they demanded is not a Guard requirement.
5. **The audit was read-only.** Every blocker is verified at the precondition level — code read plus live predicate probes — not by execution. Nothing was mutated to test it.
6. **Not assessed:** the launcher's own non-Guard interlocks (`TC_MODE`, `TC_RUN_ACK`, `$OUT` empty, the pre-run `g_v1` battery needing `VERDICT: ACCEPT`), and machine-level GPU contention with the two ledger entries still marked RUNNING (`R-4e636c45e6b0`, `R-8b5828001bc5`) which have no live processes behind them.

## 6 · On receipt

Register the construction, re-anchor, run the cert window, launch bounded with the inspector attached, mine every checkpoint by **world displacement** (never `lo_v`), report the cadence / stride / speed trajectory across all six checkpoints side by side, re-run DFL and the collision check, close the inspector after final evaluation, and return **A**, **B** or **C**.

Ready and unchanged: card `20306ce6c8bbded0`, trainer `90534b22334d427b` (already CANONICAL), manifold isolated **2304/2304 worlds** at v = 0.24 / 1.000 Hz, inspector wired.

Baseline the run must move: **0.8180 Hz realized against 1.0000 Hz commanded, stride 21 % long.**

STOP for Joe — one priority decision, then one sentence.
