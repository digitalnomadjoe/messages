---
id: BRITTLE-20260825T215149Z-607619ee
kind: escalation
project: brittle
from: locomotion
to: joe
lane: locomotion
unit: WALK-DR06G
created_at: "2026-08-25T21:51:49.005823Z"
source_commit: 3586e964
local_source_path: null
local_source_sha256: null
in_reply_to: null
supersedes: null
requires_owner: true
confidence: null
status: open
title: "WALK-DR06G cannot start: Guard holds no DR06G authorization (0 decisions, launcher-walkdr06g-ratchet unregistered, evalu"
---
# Owner decision required

**Question for Joe:** WALK-DR06G cannot start: Guard holds no DR06G authorization (0 decisions, launcher-walkdr06g-ratchet unregistered, evaluate_authority ppo allowed=False, active unit still WALK-DR05E). Will you issue the four canonical authorizing sentences?

- lane: `locomotion`
- unit: `WALK-DR06G`
- brittle commit: `3586e964`
- raised at: 2026-08-25T21:51:49.005823Z

## Detail

# WALK-DR06G cannot start — Guard holds no DR06G authorization (measured)

The ticket directs me to "proceed under existing Guard authorization". Against LIVE Guard
state that authorization does not exist. Measured just now, not inferred:

* `campaign status` → campaign `brittle-unified-locomotion`, ACQUISITION, **active unit
  WALK-DR05E** (not WALK-DR06G).
* `authority.evaluate_authority(live, 'ppo', active_unit='WALK-DR06G')` → **allowed=False**,
  reason: *"caller asserted active unit 'WALK-DR06G' but the authenticated derivation gives
  'WALK-DR05E' — a caller may not name its own unit"*.
* `decisions.jsonl` (697 records): **0** mention WALK-DR06G.
* `targets.json` (143 targets): **launcher-walkdr06g-ratchet is NOT registered.**
* The only ACTIVE ppo grant is `D-d2c6f5a0d6e5`, scoped to unit **WALK-DR05E** on target
  **launcher-walkdr05e-anchor** — a different unit AND a different launcher. Guard states
  explicitly that authority does not carry across units.

I am not able to close this gap myself. Running the card under the DR05E grant would launder
authority across units; running it via `TC_MODE=run` + `TC_RUN_ACK` would bypass
`brittlectl run guarded`, which is the only sanctioned entry. Authoring the authorizing
sentences would fabricate an owner authorization — the exact thing Guard exists to prevent.
Guard's refusal is recorded here as a finding; it has not been worked around.

## The decidable question

Will you issue the four authorizing sentences for WALK-DR06G in the form Guard accepts?

Guard requires literal canonical phrases (`permits.CANONICAL_PHRASES`) and rejects any
sentence containing a negation/conditional token (`permits.NEGATION` — this includes the
word "no"). Your own prior DR05E sentences, which Guard accepted verbatim, are the pattern:

    "I authorize transition to WALK-DR05E in the acquisition lifecycle, from WALK-DR05D."
    "I authorize ppo for WALK-DR05E on launcher-walkdr05e-anchor, 1 seed of 884736 steps
     with a 884736 step ceiling."

The DR06G unit additionally needs a certification-lifecycle window (canonical mutation is
denied inside ACQUISITION) and a canonical-mutation sentence to register the new launcher
target, then a transition back. Four sentences total: transition to WALK-DR06G · transition
to the certification lifecycle · canonical mutation registering the bound construction
(trainer 3e021545f77ce0f9, card c64966828ef93d2c / launcher-walkdr06g-ratchet, member
8b812eb26af56c9d, manifest 28542bf607aa22…) · ppo on launcher-walkdr06g-ratchet.

IMPORTANT: the foundation-criterion qualifier ("no >0.274 m/s improvement is required")
must NOT appear inside the ppo sentence — the word "no" trips NEGATION and Guard will refuse
the whole grant. That qualifier is already recorded in the staged report §4a and in the
changelog, so the authorizing sentence does not need to carry it.

## State of the staged construction

Unchanged and still dry-proven end-to-end (card exit 0, parity receipt OK, NO optimizer
step): member `8b812eb26af56c9d`, trainer `3e021545f77ce0f9`, card `c64966828ef93d2c`,
manifest `28542bf607aa22…`. Nothing has been trained, promoted, banked or appended.
GPU is clear (881 MiB / 16303 MiB). Ready to launch the moment authority exists.


## How to answer

```bash
messagesctl resolve-escalation \
  --id BRITTLE-20260825T215149Z-607619ee \
  --decision-file /path/to/decision.md
```
