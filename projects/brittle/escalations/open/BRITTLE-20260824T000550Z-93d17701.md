---
id: BRITTLE-20260824T000550Z-93d17701
kind: escalation
project: brittle
from: locomotion
to: joe
lane: locomotion
unit: WALK-DR02
created_at: "2026-08-24T00:05:50.681675Z"
source_commit: 3586e964
local_source_path: null
local_source_sha256: null
in_reply_to: null
supersedes: null
requires_owner: true
confidence: null
status: open
title: "WALK-DR02 pilot is staged and dry-proven; Guard needs your two sentences: 'I authorize transition to WALK-DR02 in the ac"
---
# Owner decision required

**Question for Joe:** WALK-DR02 pilot is staged and dry-proven; Guard needs your two sentences: 'I authorize transition to WALK-DR02 in the acquisition lifecycle, from 12U-C11S65A.' and 'I authorize ppo for WALK-DR02 on launcher-walkdr02-kpshell, 1 seed of 884736 steps with a 884736 step ceiling.' (report sec 6)

- lane: `locomotion`
- unit: `WALK-DR02`
- brittle commit: `3586e964`
- raised at: 2026-08-24T00:05:50.681675Z

## Detail

TEAM: GREEN — Opus 5

# WALK-DR02 — Kp High-Shell DR Pilot: fully staged and dry-proven; STOPPED at the Guard authorization boundary

**status:** REVIEW · The pilot is built, integrity-proven and dry-run end-to-end with zero optimizer steps; Guard denies ppo for WALK-DR02 (campaign bound to 12U-C11S65A) — the exact owner sentences are in §6. No training was run.
**recommend:** Issue the two §6 sentences; the launch then proceeds through `run guarded` on the pre-staged card with no further build work.
**decision:** Everything up to the authorization boundary is done: kp-shell sampler (measured 15.19% exact-nominal / U(1.00,1.20) shell), Crown anchor, card, inspector chain, deterministic monitor + frozen Crown baseline.
**next:** Joe's Guard grant → `run guarded` → 884,736-step pilot → per-checkpoint monitor → Isaac confirmation on candidates → PASS/FAIL report.

**constitution:** C1, C2, C4, C6, C8, C11, C12
**potential violations:** none
**amendment proposed:** no

Ticket: WALK-DR02 (bus `BRITTLE-20260823T234323Z-ec6d7c8c`, owner-direct 2026-08-23, in-reply-to escalation `BRITTLE-20260823T232404Z-d2bf0970`). Provenance note: the ticket header names Opus 5; the executing session runs Claude Fable 5 — recorded for the audit trail.

---

## 0 · What stands ready

ONE bounded pilot: 1 seed × 884,736 steps (one Crown-sized budget), kp-shell DR replacing additive-DR, nominal-anchored to the frozen Crown, from the frozen Crown. Everything the ticket's §2–§8 requires is implemented, receipt-proven in dry mode (zero optimizer steps), and blocked ONLY by Guard — exactly the ticket's §1 stop rule. Guard was not weakened or bypassed; `evaluate_authority(live,'ppo',unit='WALK-DR02')` → **allowed=False**, campaign bound to `12U-C11S65A`, no ppo decision names WALK-DR02.

## 1 · The ONE training change (ticket §2) — implemented and MEASURED

`--dr-shell "1.0,1.2,0.15"` in the qualified stack (`batch_sim_tip.py` reset kernel + `build_sim` + `train_tip_reftrack.py` argparse/resolved-config; flag-gated, default-off = byte-identical: the off path executes the unchanged legacy assignments and consumes zero RNG draws). When on it REPLACES additive_dr (build_sim forces additive_dr off; the card also passes `--no-additive-dr`).

**Live-sim measurement (2048 worlds, real reset kernel):** 311 worlds EXACT nominal = **15.19%** (kp=kd=nominal, delay 0 — `kd unique [1.0]`, `delay unique [0]`); 1737 shell worlds kp-scale **min 1.0003 / max 1.1998 / mean 1.1019**, histogram flat across [1.00–1.05–1.10–1.15–1.20] = [425, 423, 436, 453]. No torque/mass/contact/terrain/observation channel is drawn — the parent card's additive_dr (kp×U(0.7,1.3) + kd×U(0.8,1.2) + delay∈{0,1}) is fully replaced.

## 2 · The card (C2: parent copy + marked deltas)

`rgl/artifacts/walkdr02_kp_shell_pilot_20260823T234323Z/launch_walkdr02_kpshell.sh` (sha256_16 `8c3e5e4d60776849`) derives from **the card that trained the Crown**: `launch_ident_probe.sh` @ git `63d1b13c` (sha `38a299f94e017e2c` — matched to the Guard run record of `cy_stride_polish_r_s0`; the P2 revision is preserved beside the new card). Complete delta list (in the card header; everything else byte-identical): OUT dir · trainer pin `5e7ea6d5589f2d38` · dep pins train_tip_gpu `2bcc02bdc0f18621` + isaac_spectator `2a7da463fe5bc46d` (the CROWN-bound runtime) · INIT_CKPT = frozen Crown `876025a9cb883f37` · `--anchor-ckpt $INIT_CKPT` (§3: anchor = frozen Crown; w0 0.15 + decay 8,388,608 inherited unchanged; anchor gradient exactly 0 at the seed) · `--dr-shell` + `--no-additive-dr` · `--chunk 147456` (6 checkpoints, save cadence only — trainer consumes chunk solely as `next_ckpt`) · v2 manifest invocation (§3 of card header, forced by the crown-bound spectator's C11S20/C11S47 guards).

Two bounded trainer repairs were required and are disclosed: (a) the anchor constructor now honors `--actor-hidden` (the P2 card anchored a 256-wide bank zip; anchoring the 512-wide Crown hit a strict-load size mismatch; None → legacy behavior byte-identical); (b) none other — the dr_shell flag is the only other trainer delta.

## 3 · Pre-PPO integrity (ticket §4) — all asserts evidenced, zero optimizer steps

Dry mode (`TC_MODE=dry`, run dir `rl/runs/walking/walkdr02_dry1`) ran the FULL chain: dependency pins (7 modules + gate producer + init ckpt) → g_v1 real-state battery ACCEPT (22/22 rows) → inspector preflight on a real checkpoint load (`episode start` + matrices + manifest parity receipts) → live inspector → argv assertions on the executed array → trainer construction + BC-init from the Crown + Crown anchor load → one rollout batch → **`no optimizer step taken` receipt, no ppo_*.zip written**.

- **Crown hash / plant / law / contract:** PARITY RECEIPT **OK** — 30 measured fields: plant `81d4e6d8226ed12b` + complete/as-built `c44ad73fbd8b6f45`, kp 20.0×12, kd [1,1,2,2,1,0.2]×2, action scale ASO v3 ×12, τ 12.5×12, ACD 10 Hz, 50/500/500, nconmax 64, legacy RSI 0, yaw_cmd_range [0,0], init-zone active, arc-RSI 1.0/zone.
- **Reward book vs Crown:** **32 shared keys, ZERO value diffs.** Two structural keys exist only in the current trainer's book (`yawrate_tight_w: 0.0`, `yawrate_tight_sigma: 0.05` — the C11S48 term): weight exactly 0.0 and the term's code path is guarded by `if yawrate_tight_w > 0.0` — it never executes. The book hash moves (`359915e1` → `e583e09d`) purely from these keys, which is C12's by-design behavior for trainer evolution; no reward VALUE differs. Resolved-config check: `cadence_track_w=19.5`, `ss_tempo_w=0.0` OK.
- **Kp sampling distribution + nominal fraction:** measured (§1) + bound into `resolved_config.dr_shell` and `settings_summary` (`additive_dr: False`, `dr_shell: True 1.0 1.2 0.15`).
- **Anchor configuration:** trainer log `IMITATION ANCHOR from …crown_longstride_cy884736/ppo_884736.zip w0=0.15 decay_steps=8388608`.
- **Command semantics:** SPEED+CURVATURE v2 manifest (schedule c(v) bound; vx 0.153 / κ 0), yaw_cmd_range [0,0] measured.

**One receipt recalibration, control-backed and pre-run:** the P2-era parity check expected dry-batch `cyclic_fraction == 1.0`; on the evolved trainer it reads 0.99857 — and a CONTROL dry with the crown card's own additive_dr and NO shell reads **0.99858** (`launch_walkdr02_CONTROL_diagnostic.sh`, run dir `walkdr02_dryctl`; shell-vs-control Δ = 7e-6; the ~0.14% out-of-loop samples spread uniformly over all 22 members). The deviation is a property of the evolved trainer's telemetry, not of the shell. The card's check now holds the intent at tol 2e-3 with the control cited in-line. Residual question (cosmetic, non-blocking): which trainer change between `94a86303` and `5e7ea6d5` moved it — suspected respawn-transition sampling (C11S37 arc-injection ordering fix).

## 4 · Deterministic checkpoint monitor (ticket §6–§8) — built, baselined

`rl/isaac_parity/walkdr02_monitor.py`: per checkpoint (parsed-step order) exports the actor bundle (actor_port math, C11S51 canonical phase-mirror bank — the crown FUNCTION's deployment bank), runs the NOMINAL battery (stand/crown/**vlo 0.10**/**vhi 0.24**/κ±0.35/κ±1.0 × seeds 0,1) and the DR battery (kp×{1.05,1.10,1.15,1.20} + {1.25,1.30} diagnostic × 7 cells) through the bit-proven MuJoCo arm, and scores **NOMINAL and DR SHELL separately** (never one scalar): materiality thresholds 5%/10%/2.5 pts vs the frozen Crown baseline, falls, τ99 (kill >10), saturation, tilt, and the 3-way foot separation with the invariant scored on **real colliding geometry (sole sphere-sphere)** — proxy-envelope (plate-AABB) reported but never scored as collision, per ticket §7. Isaac confirmation on candidates: `isaac_arm.py --actor-bundle` (default None = byte-identical to every prior receipt).

**Frozen Crown baseline (`monitor/crown/metrics.json`, 58 cells):** NOMINAL 16/16 no falls, zero real collisions, τ99 ≤ 4.42, saturation 0; crown stride 0.2400 / cadence 0.962 / speed 0.2260; vlo speed 0.1328, vhi speed 0.2499. DR baseline falls: ×1.05 none · ×1.10 {km10_s0} · ×1.15 {km10_s0, kp10_s0, kp10_s1} · ×1.20 all four κ±1.0 · ×1.25/×1.30 all four. **C11 parent reproduction PASS:** the baseline `crown_s0` trace is bit-identical to the WALK-DR01 trace of the same cell. Known instrument note: at vhi (0.24 m/s) the c62 event-chain metrics return None inside 700 steps (speed channel measures fine); candidate scoring treats None-vs-None as comparable and a became-None transition as a violation.

Qualification logic per ticket §8: a checkpoint qualifies only if DR improves (fewer shell falls than baseline, no new level regressing) AND nominal shows zero envelope violations; **two consecutive** saved checkpoints required; ≥1 further checkpoint after first qualification inside the 884,736 ceiling; no early-checkpoint rescue.

## 5 · Disclosures

- **CACHE_WORKER_ENV: N/A on this stack** (ticket §5 contingency): the flag exists only in the SE3 CPU worker-pool collector (`rl/se3/se3_recover39a.py`); the walking stack is MJWarp with no worker pool, and the monitor's arm runs one env per process. No plumbing invented.
- **Inspector:** the card's own chain preflights the spectator on a real checkpoint load, keeps a live read-only auto-reloading viewer through training (watch mode, `RELOADED_CHECKPOINT` receipts), and its supervision KILLS THE TRAINER if the inspector dies — stronger than the ticket's "restart independently" (a training run must never continue unwatched, per the standing rule). The EXIT trap closes the exact PID; the viewer stays up only until the final report.
- The training-side exact-symmetry matrices are the P2/crown TRAINING bank (0807), byte-faithful to the lineage construction; deployment evaluation (monitor + Isaac arm) uses the C11S51 canonical phase-mirror bank as the crown FUNCTION defines. This split is the crown's own qualified state (it was trained pre-C11S51 and promoted with the canonical bank).
- Dirs `walkdr02_dry1` (shell dry) and `walkdr02_dryctl` (control dry) under `rl/runs/walking/` are evidence, not training runs; neither contains a checkpoint.

## 6 · THE AUTHORIZATION BOUNDARY (ticket §1 stop)

Guard live state: ppo **allowed=False**; active unit `12U-C11S65A`; reasons: no decision authorizes ppo for WALK-DR02 (authority does not carry across units). The exact owner sentences (plain, no conditional words — Guard's NEGATION list voids grants):

1. `I authorize transition to WALK-DR02 in the acquisition lifecycle, from 12U-C11S65A.`
2. `I authorize ppo for WALK-DR02 on launcher-walkdr02-kpshell, 1 seed of 884736 steps with a 884736 step ceiling.`

On receipt of these (as a signed owner_decision / ruling artifact — never authored by an agent), the staged sequence is: ruling committed → `campaign-bootstrap-commit` → `decision request` (subjects: WALK-DR02 + `launcher-walkdr02-kpshell`) → `decision resolve` → `transition --authorized-by D-…` → `target register` for the card (sha `8c3e5e4d60776849`) → the ppo decision → `brittlectl run guarded … -- bash launch_walkdr02_kpshell.sh <out>` → monitor every checkpoint → Isaac confirmation on candidates → the §9 PASS/FAIL verdict → STOP for Joe.

## 7 · Scope compliance

No training run (zero optimizer steps anywhere). No promotion, no canonical/latest mutation, no obs/action/interface change, no reward change (book value-diff empty), no terrain, no TIP, no recovery work, no other DR family. New/changed files: `--dr-shell` hook (batch_sim_tip.py, train_tip_gpu.py, train_tip_reftrack.py — all default-off), anchor split-width repair (train_tip_reftrack.py), `--bundle`/`--actor-bundle` eval overrides (walkdr01_mj_arm.py, isaac_arm.py — default byte-identical), `walkdr02_monitor.py`, the two cards. Banked artifacts untouched.

## 8 · Artifacts

`rgl/artifacts/walkdr02_kp_shell_pilot_20260823T234323Z/` — pilot card + preserved P2 parent card + control card + `monitor/crown/` baseline (58 traces + metrics). Dry evidence: `rl/runs/walking/walkdr02_dry1/` (parity_receipt.json OK · tc_dryproof.json · resolved_config.json fingerprint `74b5a097e510c2ee` · settings_summary.json) and `walkdr02_dryctl/`. Prior map: `rgl/reports/WALK_DR01_ISAAC_CROWN_DR_BASIN_STAGE1_MAP_20260823T230443Z.md`.

**changelog row:** `WALK-DR02, 20260823T234323Z` — post-append `rgl/LOCOMOTION_CHANGELOG.csv` sha256_16 `a07c811bfe193cad` (462 rows × 13 cols, both trees byte-identical)


## How to answer

```bash
messagesctl resolve-escalation \
  --id BRITTLE-20260824T000550Z-93d17701 \
  --decision-file /path/to/decision.md
```
