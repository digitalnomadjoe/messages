---
id: BRITTLE-20260826T135637Z-8b376cf3
kind: ticket
project: brittle
from: reviewer
to: locomotion
lane: locomotion
unit: WALK-DR06H2
created_at: "2026-08-26T13:56:37.324741Z"
source_commit: 3586e964
local_source_path: null
local_source_sha256: null
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: "WALK-DR06H2 — Per-Member Stride Acquisition (TEAM: GREEN)"
---
# WALK-DR06H2 — Per-Member Stride Acquisition (TEAM: GREEN)

Use the EXISTING smooth stride_track reward as the ACQUISITION mechanism but source its target from each member's own stride geometry instead of vx_cmd/cadence_cmd; the per-member corridor remains the HOLD/containment mechanism. Do not change cadence. (1) MINIMAL EXISTING-TERM CHANGE: add an opt-in target mode to stride_track — DEFAULT stride_target = vx_cmd/cadence_cmd; NEW LOCAL MODE stride_target = member_stride_target, using exactly the per-member binding proven in DR06H1. Do NOT change the stride_track reward equation. Default mode must remain byte-identical. (2) TARGET PROOF before PPO: ratchet target ~0.3793; vx0.153 retains ~0.153 geometry; vx0.204/0.240 retain their own; member indexing matches the same round-robin used for commands; deliberate cross-member shuffle fails; cadence branch absent; legacy cadence law active. ALSO FIX THE DR06H1 RECEIPT GAP: resolved_config must serialize the ACTUAL per-member stride targets / corridor bounds used by training — no more hidden runtime-only targets. (3) COMPOSITION: stride_track = smooth acquisition toward member stride; stride_corridor = hold once near it; no additional stride reward; use existing calibrated weights/parameters where available and do not invent new values without showing why precedent is insufficient. (4) REQUIRED MECHANISM at the ratchet command: stride ~0.327 -> toward ~0.379; cadence remains approximately in the existing stable basin; TRUE speed rises BECAUSE stride rises; checkpoint success must show delta-speed explained primarily by delta-stride, not delta-cadence. (5) MONOTONICITY: evaluate every checkpoint; require member-stride error DOWN, realized stride UP, TRUE speed UP, cadence not running away, clearance preserved; if reward improves but stride does not, STOP; do not cherry-pick. (6) PRESERVATION (DR06H1 is the reference, currently the cleanest run): stand, 0.153, 0.240, curved/F8, DS bookends; zero falls; ZERO physical L<->R foot collision; no material clearance collapse; bounded torque; lower-speed gait geometry preserved. (7) DECISION: PASS only if a new realized-speed basin materially above 0.257 is established THROUGH increased stride; desired first rung ~0.29-0.30 TRUE. If the smooth per-member stride target still cannot pull stride upward, STOP for Joe and do not add another reward term automatically — the evidence would then indicate a genuine policy/gait-geometry barrier rather than missing reward gradient. Independent training visualization with checkpoint auto-follow; final gait review in Isaac; no changelog append until Guard closes. REPORT under rgl/reports/: stride_track target semantics BEFORE->AFTER, default non-perturbation proof, serialized per-member target receipt, reward/config delta, stride / cadence / TRUE-speed / stride-error progression, clearance / F8 / DS preservation, torque, falls / physical foot-foot collisions, and whether a new speed basin was established. STOP for Joe.
