---
id: BRITTLE-20260826T030455Z-8071f290
kind: ticket
project: brittle
from: reviewer
to: locomotion
lane: locomotion
unit: WALK-DR06G3
created_at: "2026-08-26T03:04:55.529936Z"
source_commit: 3586e964
local_source_path: null
local_source_sha256: null
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: "WALK-DR06G3 — Cadence-Branch Runtime Parity (TEAM: GREEN)"
---
# WALK-DR06G3 — Cadence-Branch Runtime Parity (TEAM: GREEN)

ZERO TRAINING. Make the experimental >0.24 cadence branch reproducible by the evaluation/deployment runtime, then re-grade DR06G under exact train/runtime parity. (1) RUNTIME PARITY REPAIR: extend the runtime path to understand the existing experimental cadence-branch configuration; branch must come from the same manifest/config semantics as training; NO hard-coded DR06G special case; when branch absent, canonical/default runtime behavior must remain BYTE-IDENTICAL; branch stays EXPERIMENTAL/local to this walking arc and is NOT promoted as the global cadence architecture. At vx=0.272219 prove the runtime produces exactly cadence 0.717721 Hz, obs[53] 0.35886, and cyclic phase advance matching trainer semantics; verify representative commands below the branch threshold are unchanged. (2) FAIL-CLOSED CONTRACT: add explicit parity checks so this cannot recur; the run/eval manifest must expose enough cadence configuration to assert TRAIN cadence law == EVAL == DEPLOY; fail closed if the branch exists in the trainer but the runtime cannot represent it, if resolved cadence differs at probe commands, if obs[50]/[51]/[53] cadence semantics differ, or if the cyclic clock update differs; do not rely on dead state such as cp54_acc. (3) RE-GRADE DR06G with ZERO optimizer steps: re-evaluate DR05D-737280 and DR06G-884736 at command 0.272219 with the repaired runtime, measuring from WORLD DISPLACEMENT: TRUE forward velocity, cadence, stride, speed/command ratio, inter-foot separation, falls, physical foot-foot collision. This is a re-grade of existing checkpoints, not a new training unit. (4) DECISION: do NOT launch DR06G2 after the repair; if DR06G remains approximately the already-measured ~0.202 m/s under exact runtime parity then DR06G foundation acquisition is FALSIFIED -- STOP and report; do not spend another PPO turn or increase member share automatically; if the repaired runtime materially disagrees with the DR06G2 consistent-harness measurement, STOP and explain the discrepancy before any training. GUARD/SCOPE: this changes a banked runtime path, so obtain the required owner/Guard authorization and register the exact runtime mutation before execution. NO PPO, reward change, reference/member change, cadence-branch redesign, stride reward, command-manifold expansion, promotion, or canonical gait-bank mutation. REPORT under rgl/reports/: runtime path + hash BEFORE->AFTER, exact cadence-branch semantics, default-runtime non-perturbation proof, train/eval/deploy parity probes, DR05D re-grade, DR06G re-grade, TRUE speed/cadence/stride, clearance/falls/collision, and whether DR06G acquisition is now supported or falsified. STOP for Joe.
