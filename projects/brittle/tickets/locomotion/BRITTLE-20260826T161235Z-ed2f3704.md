---
id: BRITTLE-20260826T161235Z-ed2f3704
kind: ticket
project: brittle
from: reviewer
to: locomotion
lane: locomotion
unit: WALK-DR06H3
created_at: "2026-08-26T16:12:35.015323Z"
source_commit: 3586e964
local_source_path: null
local_source_sha256: null
in_reply_to: null
supersedes: null
requires_owner: false
confidence: null
status: open
title: "WALK-DR06H3 — Cadence-Led 0.30 m/s Rung (TEAM: GREEN) [REDIRECTED]"
---
# WALK-DR06H3 — Cadence-Led 0.30 m/s Rung (TEAM: GREEN) [REDIRECTED]

OWNER DIRECTION: adopt RED WALK-DR06R1 recommendation — CADENCE-LED next rung. Remove explicit stride acquisition; remove explicit stride corridor shaping; retain the existing legacy speed->cadence schedule / phase-clock semantics; enable the existing cadence_track term; let stride emerge naturally. Bounded near-term rung, NOT the long-term >=1.0 m/s architecture. (1) PREFLIGHT from the actual launch card / resolved config, not trainer defaults: stride_track_w = 0; stride_corridor_w = 0; the actual H2 cadence_track_w value; H3 cadence_track target semantics; cadence branch absent; legacy clipped cadence law active; fwd_dominant target/reference semantics; no hidden per-member stride target remains active. Serialize ALL active cadence/speed reward settings into resolved_config. (2) CADENCE TRACK: use the EXISTING cadence_track reward equation and its established precedent weight; do not invent a new cadence reward or modify its equation; target the cadence already prescribed by the live schedule (nominally 1.0 Hz at the fast member). (3) SEED from the clean DR05D seed / accepted pre-H1 basin, NOT the degraded H2 endpoint. (4) MECHANISM TEST: can closing the realized-cadence gap establish a clean TRUE-speed basin >=0.29 m/s WITHOUT prescribing stride? Track every checkpoint: TRUE speed, realized cadence, realized stride, cadence error, clearance, DS separation, torque. Speed improvement should primarily follow cadence-error closure. Stride is OBSERVATIONAL ONLY — do not reward or constrain it. (5) ACCEPTANCE: TRUE speed >=0.29 (prefer ~0.30-0.32 if preservation healthy); exact command parity not required in this diagnostic rung, report overshoot explicitly; require coherent checkpoint progression, cadence error decreasing, TRUE speed increasing, zero falls, ZERO physical L<->R foot collision, F8 preserved, DS bookends valid, no material clearance collapse, bounded torque; do not cherry-pick a fast checkpoint from a degrading trajectory. (6) STOP IF: cadence rises but TRUE speed does not; stride collapses enough to cancel the cadence gain; clearance/DS margin materially degrades; reward improves without cadence tracking improving; physical foot-foot collision occurs. Do not add stride shaping back; do not add another reward term. (7) LONG-TERM: this tests only the simplest route to ~0.30; success is NOT validation for 0.50/0.75/>=1.0 — RED has identified the clipped legacy schedule as incapable of that regime. Guard + independent inspector with checkpoint auto-follow; final gait review in Isaac. REPORT under rgl/reports/: exact active reward/config receipt, stride-terms-OFF proof, cadence_track semantics + weight precedent, checkpoint cadence/stride/TRUE-speed decomposition, cadence-error progression, clearance/DS/F8, torque, falls/collisions, and whether a clean >=0.29 cadence-led basin was established. STOP for Joe.
